#!/usr/bin/env python3
"""Self-tests for stewardship gates (positive live-tree + negative fixtures).

Runs in CI after the live-tree gates so regressions in checkers fail closed.
Does not invent product surface — only validates gate behavior.
TOKENMAXX coverage: badge / wiki / schema / relative / workflow / secrets /
lycheeignore shields / actionlint-style / markdown-link edges.
Deepened after #100: docs-lint (.lycheeignore + .markdownlint.json) exact
host/object pins + gate contract (not wiki / relative / CI workflow spam).
Deepened after #104: badge-standard third-pass — BADGE_LINE_RE+REPO_FROM_* exact /
REQUIRED_WORKFLOWS exact / group(label|img|link) / sys.exit / stewardship_common /
BADGE_GATE / utf-8 / Strict row / H1 startswith / FAIL README / https needles /
absolute workflow URL / License point / Unexpected label / extract+check_badges /
contract(errors) / IGNORECASE / blob.lower / EXPECTED_REPO.lower (not docs-lint /
wiki / relative / actionlint / schema / common / CI workflow pin spam).
Deepened after #29: cancel-in-progress:true / agents exclude / md **/*.md /
lycheeignore http://* / Repo-Stewardship actionlint.
Deepened after #30: Python 3.12 pin / Home invent+secrets / existing-path fixtures.
Deepened after #31: lychee --verbose/--no-progress + stewardship actions/checkout
+ commerce/coveralls/x.com badge hints + wiki L0/secret/surface/governance.
Deepened after #32: link/markdown actions/checkout + lychee-action + MD013
+ twitter/codecov/downloads/github_pat + wiki discord/js + schema edit_policy.
Deepened after #33: lycheeverse/lychee-action / markdownlint-cli2-action /
MD013 line_length / pip install PyYAML / deepen edges.
Deepened after #34: DavidAnson/markdownlint-cli2-action / --github-token /
MD024 / download-actionlint.bash / deepen edges.
Deepened after #35: MD024 siblings_only / rhysd/actionlint / curl download /
lychee-action with: token: / deepen edges.
Deepened after #36: MD013 line_length 200 / MD024 siblings_only:true /
raw.githubusercontent.com + curl -fsSL / deepen edges.
Deepened after #37: markdownlint default:true / get_actionlint.outputs /
actionlint /v1.7.7/ path / lychee --max-concurrency 8 --timeout 20
--max-retries 3 / deepen edges.
Deepened after #38: MD033/MD041/MD060 false / markdownlint-cli2-action@v24 /
setup-python@v5 / id: get_actionlint / deepen edges.
Deepened after #39: actions/checkout@v7 / lychee-action@v2 / job timeouts
20/10/15 / weekly crons / ubuntu-latest / pip --quiet / shell: bash /
actionlint -color / CI workflow pin edges (not fixture-reject spam).
Deepened after #41: relative-link gate contract pins — OWASP/agents/node_modules
skip, fully_unquote, empty path# fragments, query-string reject, run_stewardship
gate order badge→wiki→schema→relative (not CI workflow pin / fixture-reject spam).
Deepened after #43: wiki-outline gate contract pins — PUBLISHABLE_PAGES L0–L3 /
credential / copilot topic hints, strip_fenced_code, protocol-relative // reject,
Home kill-switch callout (not relative-link / CI workflow pin spam).
Deepened after #45: stewardship-schema gate contract pins — DOC_SCHEMAS /
EXPECTED_VALUES / SEMVER / ISO-8601 / closes #N / invent edit_policy /
issue-backlog owner copilot / scan_secrets (not wiki-outline / relative-link /
CI workflow pin spam).
Deepened after #46: stewardship_common contract pins — SECRET_PATTERNS /
SECRET_URL_HINTS / FORBIDDEN_BADGE_HINTS / DANGEROUS_LINK_SCHEMES /
strip_fenced_code / scan_secrets / invent-product (not schema / wiki /
relative-link / CI workflow pin spam).
Deepened after #48: badge-standard gate contract pins — REQUIRED_ORDER /
MAX_BADGES=3 / EXPECTED_REPO / REQUIRED_WORKFLOWS / badge.svg + shields
license / contiguous row / invent-product / fourth-badge refusal /
Stewardship product badge reject (not common / schema / wiki / relative /
CI workflow pin spam).
Deepened after #53: stewardship-schema scalar / STRING_KEYS / empty-yaml /
bool-int rejects + live badge scope / PUBLISH purpose / closes #16 pins
(not badge-standard / common / wiki / relative / CI workflow pin spam).
Deepened after #55: relative-link second-pass — MD_LINK_RE / SKIP_* /
_MAX_UNQUOTE_PASSES=4 / should_skip / iter_markdown / headings_in /
check_file / mailto+tel allow / NUL / angle brackets / image links /
escapes+broken+missing needles / rglob fail-closed (not schema-scalar /
badge / wiki / common / CI workflow pin spam).
Deepened after #59: wiki-outline second-pass — OPERATOR_ONLY / docs/wiki /
_reject_invent_badge_chrome / autonomy+governance+public+kill+secret+
surface+routing+run_stewardship_checks.sh+badge topic pins / ](Home.md)
backlink / Unexpected+Missing+FAILED needles / angle+image RE /
Do-not-push / README blob / badge-standard hint / stars+forks+followers /
invent-chrome needle / Home.md table row (not relative / schema / badge /
common / CI workflow pin spam).
Deepened after #61: badge-standard second-pass — REQUIRED_ORDER+EXPECTED_REPO
exact assigns / README+LICENSE+badge-standard+CONTRIBUTING+AGENTS+
lycheeignore+markdownlint paths / contract+main fns /
actions/workflows/*.yml/badge.svg / exactly+order+contiguous+H1+
unexpected+forbidden+secret+FAILED+OK needles / endswith+/./LICENSE /
load_workflow_text / invent-product doc / intentionally / quiet stewardship /
selftest+relative AGENTS pins / img https startswith / http link reject
(not wiki / relative / schema / common / CI workflow pin spam).
Deepened after #65: stewardship_common second-pass helper/constant pins
(not badge / wiki / relative / schema / CI workflow pin spam).
Deepened after #72: CI workflow second-pass — workflow names / branches ["**"] /
concurrency prefixes+github.workflow+github.ref / job ids /
self path filters / scripts+docs+README paths / secrets.GITHUB_TOKEN /
python-version "3.12" / pip --quiet pyyaml / bash run + python3 selftest /
markdownlint config+bang excludes / actionlint three-path list + 1.7.7
download arg (not badge / wiki / relative / schema / common spam).
Deepened after #75: actionlint-style gate contract pins — top-level
name: / jobs.*.runs-on / jobs.*.steps / timeout-minutes / no
pull_request_target / no write-all / no contents: write / no id-token: write /
@-pin float set main|master|latest / docker:// skip / unpinned reject
(not CI workflow / common / relative-link / schema-scalar / badge / wiki spam).
Deepened after #72/#75: stewardship-schema second-pass — FENCED_YAML_RE /
ISO_DATE_RE / SEMVER_RE / ISSUE_REF_RE / DATE_KEYS / Tiny YAML /
scalar+non-empty+string / ACTIVE / tier / 0..3 / ISO-8601 / invent /
semver / closes #N / FAILED+OK / stdlib-subset+PyYAML / bool subclass /
match.group(1) / missing keys / utf-8 / STRING_KEYS members (not badge / wiki /
relative / common / CI workflow / actionlint pin spam).
Deepened after #83/#86: actionlint-style second-pass — exact name/uses
regexes / write-all+contents+id-token regexes / docker startswith /
@ not in uses / rsplit / group(1).strip() / fail needles /
least-privilege+OIDC+majors comments / REQUIRED_WORKFLOWS loop
(not schema / badge / wiki / relative / common / CI workflow pin spam).
Deepened after #90: relative-link third-pass — MD_LINK_RE+ATX exact /
SKIP_* exact / OK+FAILED banners / empty+http+proto+dangerous needles /
utf-8 / as_posix / .md suffix / ValueError / sorted / UNICODE /
space-dash / Percent-decode+Cap / empty () / sys.exit / urllib.unquote /
group(2) / startswith# / split# / files scanned / stewardship_common /
title / #{1,6} / slug punct / Offline+lychee / path.parent (not schema /
badge / wiki / common / CI workflow / actionlint pin spam).
Deepened after #108: actionlint-style third-pass — concurrency/cancel/permissions
+ actions|packages|pull-requests write reject + finditer/docker-continue/rsplit[-1]
(not badge/docs-lint/wiki/relative spam).
Deepened after #111: run_stewardship_checks.sh runner pins — shebang /
set -euo pipefail / dirname "$0" / cd "$ROOT" / pwd / same set as CI /
Run all stewardship doc gates / python3 scripts/* gate order
badge→wiki→schema→relative / gate contract (lands closed #96; not
lycheeignore / markdownlint / actionlint / schema / CI workflow spam).
"""

from __future__ import annotations

import shutil
import subprocess
import sys
import tempfile
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
SCRIPTS = ROOT / "scripts"

GATE_SCRIPTS = (
    "check_badge_standard.py",
    "check_wiki_outline.py",
    "check_stewardship_schema.py",
    "check_relative_links.py",
)

SHARED_SCRIPTS = ("stewardship_common.py",)


def run(script: Path, cwd: Path) -> subprocess.CompletedProcess[str]:
    return subprocess.run(
        [sys.executable, str(script)],
        cwd=cwd,
        capture_output=True,
        text=True,
        check=False,
    )


def assert_pass_live(script: str) -> None:
    proc = run(SCRIPTS / script, ROOT)
    if proc.returncode != 0:
        raise AssertionError(f"{script} failed on live tree\n{proc.stdout}{proc.stderr}")


def _seed_scripts(tmp: Path, *names: str) -> Path:
    scripts_dir = tmp / "scripts"
    scripts_dir.mkdir(parents=True, exist_ok=True)
    for name in (*SHARED_SCRIPTS, *names):
        shutil.copy2(SCRIPTS / name, scripts_dir / name)
    return scripts_dir


def _write(path: Path, text: str) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(text, encoding="utf-8")


def _minimal_workflows(tmp: Path) -> None:
    wf = tmp / ".github" / "workflows"
    wf.mkdir(parents=True, exist_ok=True)
    # Keep aligned with live CI second-pass pins after #72 (branches / concurrency /
    # job ids / path filters / action pins). Not invent-product workflows.
    link = """name: Link Check
on:
  push:
    branches: ["**"]
    paths:
      - "**/*.md"
      - ".lycheeignore"
      - ".github/workflows/link-check.yml"
  pull_request:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: link-check-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  link-check:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: lycheeverse/lychee-action@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          args: >-
            --verbose --no-progress --max-concurrency 8 --timeout 20
            --max-retries 3 --exclude-loopback --exclude-path .github/agents
            --github-token ${{ secrets.GITHUB_TOKEN }}
            "**/*.md"
          fail: true
"""
    lint = """name: Markdown Lint
on:
  push:
    branches: ["**"]
    paths:
      - "**/*.md"
      - ".markdownlint.json"
      - ".github/workflows/markdown-lint.yml"
  pull_request:
  schedule:
    - cron: "30 6 * * 1"
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: markdown-lint-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: DavidAnson/markdownlint-cli2-action@v24
        with:
          globs: |
            **/*.md
            !.github/agents/**
            !OWASP-AGENTIC.md
          config: ".markdownlint.json"
"""
    stew = """name: Stewardship Checks
on:
  push:
    branches: ["**"]
    paths:
      - "README.md"
      - "AGENTS.md"
      - "CLAUDE.md"
      - "LICENSE"
      - "CONTRIBUTING.md"
      - "docs/**"
      - "scripts/**"
      - ".github/workflows/**"
      - ".lycheeignore"
      - ".markdownlint.json"
  pull_request:
  schedule:
    - cron: "15 6 * * 1"
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: stewardship-checks-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  stewardship:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install --quiet pyyaml
      - run: bash scripts/run_stewardship_checks.sh
      - run: python3 scripts/test_stewardship_gates.py
      - name: Download actionlint
        id: get_actionlint
        run: bash <(curl -fsSL https://raw.githubusercontent.com/rhysd/actionlint/v1.7.7/scripts/download-actionlint.bash) 1.7.7
        shell: bash
      - name: actionlint existing workflow paths
        run: ${{ steps.get_actionlint.outputs.executable }} -color .github/workflows/link-check.yml .github/workflows/markdown-lint.yml .github/workflows/stewardship-checks.yml
"""
    (wf / "link-check.yml").write_text(link, encoding="utf-8")
    (wf / "markdown-lint.yml").write_text(lint, encoding="utf-8")
    (wf / "stewardship-checks.yml").write_text(stew, encoding="utf-8")
    _write(
        tmp / ".lycheeignore",
        "# modelcontextprotocol.io returns 308 redirect — valid site, lychee false-positive\n"
        "https://modelcontextprotocol.io/\n"
        "# linuxfoundation.org returns 103 early hints — valid site\n"
        "https://www.linuxfoundation.org/\n"
        "# img.shields.io badge CDN is flaky — not a broken URL.\n"
        "# License badge presence remains enforced by stewardship "
        "(check_badge_standard.py).\n"
        r"https://img\.shields\.io\n",
    )
    _write(
        tmp / ".markdownlint.json",
        '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n'
        '  "MD024": { "siblings_only": true },\n'
        '  "MD033": false,\n  "MD041": false,\n  "MD060": false\n}\n',
    )


def _badge_standard_doc() -> str:
    return (
        "# Badge\n\n"
        "```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
        "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
        "closes: \"#16\"\n```\n\n"
        "| Link Check | x | y |\n| Markdown Lint | x | y |\n| License | x | y |\n\n"
        "link-check.yml/badge.svg\nmarkdown-lint.yml/badge.svg\n"
        "img.shields.io/github/license/\ndo not invent product badges\n"
        "Three badges max. A fourth Stewardship Checks badge is intentionally not added.\n"
        "Executable enforcement: stewardship-checks.yml badge gate (quiet stewardship).\n"
    )


def _good_readme(order: tuple[str, str, str] | None = None) -> str:
    labels = order or ("Link Check", "Markdown Lint", "License")
    lines = ["# agents-governance", ""]
    for label in labels:
        if label == "Link Check":
            lines.append(
                "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
                "actions/workflows/link-check.yml/badge.svg)]"
                "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)"
            )
        elif label == "Markdown Lint":
            lines.append(
                "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
                "actions/workflows/markdown-lint.yml/badge.svg)]"
                "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)"
            )
        elif label == "License":
            lines.append(
                "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
                "(LICENSE)"
            )
        else:
            lines.append(
                f"[![{label}](https://img.shields.io/badge/{label}-x-red)]"
                f"(https://example.com/{label})"
            )
    lines.extend(
        [
            "",
            "See [docs/badge-standard.md](docs/badge-standard.md).",
            "Run `bash scripts/run_stewardship_checks.sh`.",
            "",
        ]
    )
    return "\n".join(lines) + "\n"


def _seed_badge_tree(tmp: Path, readme: str) -> Path:
    scripts = _seed_scripts(
        tmp,
        "check_badge_standard.py",
        "check_relative_links.py",
        "check_wiki_outline.py",
        "check_stewardship_schema.py",
    )
    _minimal_workflows(tmp)
    _write(tmp / "LICENSE", "MIT\n")
    _write(tmp / "docs" / "badge-standard.md", _badge_standard_doc())
    _write(
        tmp / "scripts" / "run_stewardship_checks.sh",
        "#!/usr/bin/env bash\n"
        "# Run all stewardship doc gates locally (same set as CI).\n"
        "set -euo pipefail\n"
        'ROOT="$(cd "$(dirname "$0")/.." && pwd)"\n'
        'cd "$ROOT"\n'
        "\n"
        "python3 scripts/check_badge_standard.py\n"
        "python3 scripts/check_wiki_outline.py\n"
        "python3 scripts/check_stewardship_schema.py\n"
        "python3 scripts/check_relative_links.py\n",
    )
    _write(
        tmp / "CONTRIBUTING.md",
        "# Contributing\n\nRun `bash scripts/run_stewardship_checks.sh`.\n"
        "Do not invent product badges.\n",
    )
    _write(
        tmp / "AGENTS.md",
        "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
        "maintainer: smtp.eth\nscope: repository-specific\n"
        "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n\n"
        "## 3. Testing Requirements\n\n"
        "bash scripts/run_stewardship_checks.sh\n"
        "python3 scripts/test_stewardship_gates.py\n"
        "CI: markdown-lint.yml, link-check.yml, stewardship-checks.yml\n"
        "Stewardship standards (badge / wiki / schema / relative links).\n",
    )
    _write(tmp / "README.md", readme)
    return scripts


def _seed_schema_tree(
    tmp: Path,
    *,
    agents: str | None = None,
    claude: str | None = None,
    badge: str | None = None,
    publish: str | None = None,
    backlog: str | None = None,
) -> Path:
    scripts = _seed_scripts(tmp, "check_stewardship_schema.py")
    _write(
        tmp / "AGENTS.md",
        agents
        or (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
            "maintainer: smtp.eth\nscope: repository-specific\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
        ),
    )
    _write(
        tmp / "CLAUDE.md",
        claude
        or (
            "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"fuzzywigg (smtp.eth)\"\n"
            "surface: copilot\nautonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
            "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
        ),
    )
    _write(
        tmp / "docs" / "badge-standard.md",
        badge
        or (
            "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
            "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
            "closes: \"#16\"\n```\n"
        ),
    )
    _write(
        tmp / "docs" / "wiki" / "PUBLISH.md",
        publish
        or (
            "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\npurpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\n"
            "closes: \"#16\"\n```\n"
        ),
    )
    _write(
        tmp / "docs" / "issue-backlog.md",
        backlog
        or (
            "# I\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
            "owner: copilot\nedit_policy: x\n```\n"
        ),
    )
    return scripts


def _seed_wiki_tree(tmp: Path, *, extra_pages: tuple[str, ...] = (), mutate=None) -> Path:
    scripts = _seed_scripts(tmp, "check_wiki_outline.py")
    wiki = tmp / "docs" / "wiki"
    pages = {
        "Home.md": (
            "# Home\n\n"
            "[README](https://github.com/fuzzywigg/agents-governance/blob/main/README.md)\n"
            "[Badge](../badge-standard.md)\n"
            "[Overview](Overview.md)\n"
            "[Autonomy-Levels](Autonomy-Levels.md)\n"
            "[Repo-Stewardship](Repo-Stewardship.md)\n"
            "[Agent-Routing](Agent-Routing.md)\n"
            "[Security-Boundaries](Security-Boundaries.md)\n\n"
            "## Out of scope\n\nSecrets and invent product frameworks.\n"
            "Kill switch blocks agent writes.\n"
        ),
        "PUBLISH.md": (
            "# PUBLISH\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\n"
            "purpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\ncloses: \"#16\"\n```\n\n"
            "| `Home.md` | Home |\n| `Overview.md` | Overview |\n"
            "| `Autonomy-Levels.md` | Autonomy-Levels |\n"
            "| `Repo-Stewardship.md` | Repo-Stewardship |\n"
            "| `Agent-Routing.md` | Agent-Routing |\n"
            "| `Security-Boundaries.md` | Security-Boundaries |\n\n"
            "Do **not** push `PUBLISH.md`.\n"
            "Link Check and Markdown Lint. No secrets.\n"
        ),
        "Repo-Stewardship.md": (
            "# Repo\n\n[← Home](Home.md)\n\n"
            "markdown-lint link-check stewardship-checks\n"
            "run_stewardship_checks.sh relative links badge\n"
            "actionlint on existing workflow paths\n"
            "no invent product\n"
        ),
        "Autonomy-Levels.md": "# A\n\n[Home](Home.md)\n\nL0 L1 L2 L3 autonomy\n",
        "Security-Boundaries.md": "# S\n\n[Home](Home.md)\n\nkill switch secret credential\n",
        "Agent-Routing.md": "# R\n\n[Home](Home.md)\n\nsurface routing copilot\n",
        "Overview.md": "# O\n\n[Home](Home.md)\n\ngovernance public\n",
    }
    for name in extra_pages:
        pages[name] = f"# {name}\n\n[Home](Home.md)\n"
    if mutate:
        mutate(pages)
    for name, body in pages.items():
        _write(wiki / name, body)
    return scripts


def assert_fail_script(script_path: Path, cwd: Path, needle: str) -> None:
    proc = run(script_path, cwd)
    blob = proc.stdout + proc.stderr
    if proc.returncode == 0:
        raise AssertionError(f"{script_path.name} unexpectedly passed\n{blob}")
    if needle not in blob:
        raise AssertionError(f"{script_path.name} missing {needle!r}\n{blob}")


def assert_pass_script(script_path: Path, cwd: Path) -> None:
    proc = run(script_path, cwd)
    if proc.returncode != 0:
        raise AssertionError(
            f"{script_path.name} unexpectedly failed\n{proc.stdout}{proc.stderr}"
        )


# --- Badge fixtures ---------------------------------------------------------


def test_badge_rejects_wrong_order() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(
            tmp_path,
            _good_readme(("Markdown Lint", "Link Check", "License")),
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Badge labels must be in order",
        )


def test_badge_rejects_invent_product() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n"
            "[![coverage](https://codecov.io/gh/fuzzywigg/agents-governance/branch/main/graph/badge.svg)]"
            "(https://codecov.io)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "exactly 3 badges",
        )


def test_badge_rejects_wrong_repo_slug() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/other/repo/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/other/repo/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Badge URL repo slug must be",
        )


def test_badge_rejects_noncontiguous_row() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "contiguous",
        )


def test_badge_rejects_secret_url() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?token=abc)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Secret-like token",
        )


def test_badge_rejects_http_image() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](http://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must be https://",
        )


def test_badge_rejects_stewardship_product_badge() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme() + (
            "\n[![Stewardship](https://img.shields.io/badge/stewardship-x-green)]"
            "(https://example.com)\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        # Stewardship marketed as a badge anywhere in README fails closed.
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must not add a Stewardship",
        )


def test_badge_rejects_wrong_license_link() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(https://example.com/not-license)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "License badge link must point at LICENSE",
        )


def test_badge_rejects_missing_workflow_dispatch() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("workflow_dispatch:", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "workflow_dispatch",
        )


def test_badge_rejects_missing_fail_true() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("fail: true", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "fail: true",
        )


def test_badge_rejects_missing_markdownlint_config() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(".markdownlint.json", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            ".markdownlint.json",
        )


def test_badge_rejects_missing_contributing_invent_warning() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(
            tmp_path / "CONTRIBUTING.md",
            "# Contributing\n\nRun `bash scripts/run_stewardship_checks.sh`.\n",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "invent-product",
        )


def test_badge_passes_good_fixture() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)


# --- Relative link fixtures -------------------------------------------------


def test_relative_links_reject_missing() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\nSee [missing](./nope.md).\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "broken relative link",
        )


def test_relative_links_reject_escape() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\nSee [out](../../etc/passwd).\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "escapes repo",
        )


def test_relative_links_reject_missing_fragment() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\nSee [x](#does-not-exist).\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "missing heading",
        )


def test_relative_links_ignore_fenced_examples() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(
            tmp_path / "README.md",
            "# Title\n\n```markdown\n[broken](./missing-in-fence.md)\n```\n",
        )
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_ignore_tilde_fences() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(
            tmp_path / "README.md",
            "# Title\n\n~~~\n[broken](./missing-tilde.md)\n~~~\n",
        )
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_javascript_scheme() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](javascript:alert(1))\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_data_scheme() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](data:text/html,hi)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_http_insecure() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](http://example.com)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "insecure http://",
        )


def test_relative_links_reject_protocol_relative() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](//evil.example/x)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "protocol-relative",
        )


def test_relative_links_reject_encoded_escape() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        # %2e%2e == ..
        _write(tmp_path / "README.md", "# T\n\n[x](%2e%2e/%2e%2e/etc/passwd)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "escapes repo",
        )


def test_relative_links_reject_cross_file_missing_fragment() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "a.md", "# A\n\nSee [b](b.md#nope).\n")
        _write(tmp_path / "b.md", "# B\n\nBody.\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "missing heading",
        )


def test_relative_links_accept_valid_fragment() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\n## Section One\n\nSee [x](#section-one).\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_skip_owasp_file() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\nOk.\n")
        _write(tmp_path / "OWASP-AGENTIC.md", "# O\n\n[broken](./missing.md)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


# --- Wiki fixtures ----------------------------------------------------------


def test_wiki_rejects_unexpected_page() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_wiki_tree(tmp_path, extra_pages=("Extra.md",))
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Unexpected markdown",
        )


def test_wiki_rejects_missing_home_backlink() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = "# O\n\ngovernance public\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "must link back to Home.md",
        )


def test_wiki_rejects_invent_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[![coverage](https://img.shields.io/badge/coverage-99-green)]"
                "(https://codecov.io)\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_missing_topic_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Autonomy-Levels.md"] = "# A\n\n[Home](Home.md)\n\nlevels only\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "topic hint",
        )


def test_wiki_rejects_http_link() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[site](http://example.com)\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "insecure http://",
        )


def test_wiki_rejects_secret_pattern() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "token: ghp_" + ("a" * 36) + "\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "secret-like pattern",
        )


def test_wiki_rejects_missing_out_of_scope() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = (
                "# Home\n\n"
                "[README](https://github.com/fuzzywigg/agents-governance/blob/main/README.md)\n"
                "[Badge](../badge-standard.md)\n"
                "[Overview](Overview.md)\n"
                "[Autonomy-Levels](Autonomy-Levels.md)\n"
                "[Repo-Stewardship](Repo-Stewardship.md)\n"
                "[Agent-Routing](Agent-Routing.md)\n"
                "[Security-Boundaries](Security-Boundaries.md)\n\n"
                "Secrets and invent product frameworks mentioned without section.\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Out of scope",
        )


# --- Schema fixtures --------------------------------------------------------


def test_schema_rejects_wrong_value() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/wrong/repo\nautonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "parent_governance",
        )


def test_schema_rejects_inactive_status() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: DRAFT\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "status must be ACTIVE",
        )


def test_schema_rejects_missing_key() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "autonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing metadata keys",
        )


def test_schema_rejects_bad_autonomy_level() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 9\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "autonomy_level must be int in 0..3",
        )


def test_schema_rejects_bad_tier() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 0\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "tier must be a positive int",
        )


def test_schema_rejects_bad_iso_date() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"13-04-2026\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "ISO-8601",
        )


def test_schema_rejects_edit_policy_without_invent() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"agents may edit freely\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "no-invent-product",
        )


def test_schema_rejects_wrong_surface() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"fuzzywigg (smtp.eth)\"\n"
                "surface: geryon\nautonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
                "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "surface",
        )


def test_schema_rejects_bad_semver() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"v1\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "semver",
        )


def test_schema_rejects_closes_without_issue() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
                "closes: \"soon\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "closes must reference an issue",
        )


def test_schema_rejects_empty_required_value() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            publish=(
                "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\npurpose: \"\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "must be non-empty",
        )


# --- Common / workflow fixtures ---------------------------------------------


def test_common_secret_patterns() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    samples = (
        ("ghp_" + ("a" * 36), "ghp_"),
        ("-----BEGIN RSA PRIVATE KEY-----\nMIIE\n", "PRIVATE KEY"),
        ("aws_secret_access_key = '" + ("A" * 24) + "'", "aws_secret"),
        ("xoxb-" + ("1" * 12), "xoxb"),
        ('password = "hunter2xx"', "password"),
        ("npm_" + ("b" * 24), "npm_"),
        ("AIza" + ("c" * 24), "AIza"),
    )
    for sample, label in samples:
        if not any(p.search(sample) for p in SECRET_PATTERNS):
            raise AssertionError(f"{label} sample should match SECRET_PATTERNS: {sample!r}")


def test_common_dangerous_schemes() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402

    for target, want in (
        ("javascript:alert(1)", "javascript:"),
        ("DATA:text/html,x", "data:"),
        ("vbscript:msgbox", "vbscript:"),
        ("file:///etc/passwd", "file:"),
        ("https://example.com", None),
        ("./docs/x.md", None),
    ):
        got = has_dangerous_scheme(target)
        if got != want:
            raise AssertionError(f"has_dangerous_scheme({target!r})={got!r}, want {want!r}")


def test_workflow_hardening_requires_timeout() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("timeout-minutes: 20", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "timeout-minutes",
        )


def test_workflow_hardening_requires_schedule() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("schedule:", "sched_disabled:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "weekly schedule",
        )


def test_workflow_hardening_requires_concurrency() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace("concurrency:", "group_disabled:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "concurrency",
        )


def test_workflow_hardening_requires_pyyaml_install() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("pyyaml", "notinstalled")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "PyYAML",
        )


def test_lycheeignore_requires_shields_exclude() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".lycheeignore", "# nothing about shields\nhttps://example.com/\n")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "img.shields.io",
        )


def test_lycheeignore_rejects_star_exclude() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".lycheeignore", "https://img\\.shields\\.io\nhttps://*\n")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must not exclude all http",
        )


def test_missing_lycheeignore_fails() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        (tmp_path / ".lycheeignore").unlink()
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Missing .lycheeignore",
        )


def test_missing_markdownlint_json_fails() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        (tmp_path / ".markdownlint.json").unlink()
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Missing .markdownlint.json",
        )


def test_actionlint_rejects_pull_request_target() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8")
        # Keep pull_request: for hardening; add forbidden pull_request_target.
        text = text.replace(
            "pull_request:\n",
            "pull_request:\n  pull_request_target:\n",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pull_request_target",
        )


def test_actionlint_rejects_contents_write() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "contents: read", "contents: write"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "contents: write",
        )


def test_actionlint_rejects_unpinned_action() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "actions/checkout@v7", "actions/checkout"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "unpinned action",
        )


def test_actionlint_rejects_float_main_ref() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "actions/checkout@v7", "actions/checkout@main"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must not float on @main",
        )


def test_actionlint_requires_runs_on() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("runs-on:", "runner:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "runs-on",
        )


def test_stewardship_requires_actionlint_needle() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("actionlint", "notlint")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "actionlint",
        )


def test_link_check_requires_lycheeignore_reference() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(".lycheeignore", ".ignore-links")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            ".lycheeignore",
        )


def test_badge_rejects_missing_license_file() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        (tmp_path / "LICENSE").unlink()
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Missing LICENSE",
        )


def test_badge_rejects_forbidden_hint_in_row() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=codecov)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_wrong_link_check_image() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/other.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "link-check.yml/badge.svg",
        )


def test_badge_rejects_missing_readme_badge_doc_link() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme().replace("docs/badge-standard.md", "docs/other.md")
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "docs/badge-standard.md",
        )


def test_badge_rejects_missing_agents_selftest_needle() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(
            tmp_path / "AGENTS.md",
            "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
            "maintainer: smtp.eth\nscope: repository-specific\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n\n"
            "## 3. Testing Requirements\n\n"
            "bash scripts/run_stewardship_checks.sh\n"
            "CI: markdown-lint.yml, link-check.yml, stewardship-checks.yml\n",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "test_stewardship_gates.py",
        )


def test_workflow_rejects_missing_permissions_read() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("contents: read", "contents: none")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "contents: read",
        )


def test_workflow_rejects_missing_exclude_loopback() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("--exclude-loopback", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "exclude loopback",
        )


def test_workflow_rejects_missing_max_retries() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("--max-retries 3", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "--max-retries",
        )


# --- Extra markdown-link / relative fixtures --------------------------------


def test_relative_links_reject_file_scheme() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](file:///etc/passwd)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_vbscript_scheme() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](vbscript:msgbox(1))\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_empty_target() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x]()\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "empty relative link",
        )


def test_relative_links_accept_mailto() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[mail](mailto:ops@example.com)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_accept_https() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[ok](https://example.com/docs)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_skip_github_agents() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\nOk.\n")
        _write(
            tmp_path / ".github" / "agents" / "notes.md",
            "# Agents\n\n[broken](./missing.md)\n",
        )
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_broken_image() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n![alt](./missing.png)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "broken relative link",
        )


def test_relative_links_accept_cross_file_fragment() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "a.md", "# A\n\nSee [b](b.md#section-two).\n")
        _write(tmp_path / "b.md", "# B\n\n## Section Two\n\nBody.\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_github_slug_punctuation() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(
            tmp_path / "README.md",
            "# Title\n\n## A & B\n\nSee [x](#a--b).\n",
        )
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


# --- Extra wiki / schema fixtures -------------------------------------------


def test_wiki_rejects_missing_page() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_wiki_tree(tmp_path)
        (tmp_path / "docs" / "wiki" / "Overview.md").unlink()
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Missing required wiki source page",
        )


def test_wiki_rejects_missing_readme_link() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = (
                "# Home\n\n"
                "[Badge](../badge-standard.md)\n"
                "[Overview](Overview.md)\n"
                "[Autonomy-Levels](Autonomy-Levels.md)\n"
                "[Repo-Stewardship](Repo-Stewardship.md)\n"
                "[Agent-Routing](Agent-Routing.md)\n"
                "[Security-Boundaries](Security-Boundaries.md)\n\n"
                "## Out of scope\n\nSecrets and invent product frameworks.\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "must link back to the repository README",
        )


def test_wiki_rejects_missing_ci_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = (
                "# Repo\n\n[← Home](Home.md)\n\n"
                "run_stewardship_checks.sh relative links badge\n"
                "no invent product\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "stewardship CI workflows",
        )


def test_wiki_rejects_dangerous_scheme() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[x](javascript:alert(1))\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_wiki_passes_good_fixture() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_wiki_tree(tmp_path)
        assert_pass_script(scripts / "check_wiki_outline.py", tmp_path)


def test_schema_rejects_missing_yaml_block() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents="# AGENTS\n\nNo metadata fence.\n",
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "no fenced",
        )


def test_schema_rejects_missing_file() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(tmp_path)
        (tmp_path / "CLAUDE.md").unlink()
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing file",
        )


def test_schema_passes_good_fixture() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(tmp_path)
        assert_pass_script(scripts / "check_stewardship_schema.py", tmp_path)


def test_common_secret_url_hints() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_URL_HINTS  # noqa: E402

    required = ("token=", "access_token=", "api_key=", "ghp_", "github_pat_")
    for hint in required:
        if hint not in SECRET_URL_HINTS:
            raise AssertionError(f"SECRET_URL_HINTS missing {hint!r}")


def test_common_forbidden_badge_hints() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import FORBIDDEN_BADGE_HINTS  # noqa: E402

    required = ("coverage", "codecov", "discord", "stars", "producthunt")
    for hint in required:
        if hint not in FORBIDDEN_BADGE_HINTS:
            raise AssertionError(f"FORBIDDEN_BADGE_HINTS missing {hint!r}")


def test_github_slug_helper() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from check_relative_links import github_slug  # noqa: E402

    if github_slug("Section One") != "section-one":
        raise AssertionError("github_slug Section One")
    if github_slug("A & B") != "a--b":
        raise AssertionError(f"github_slug A & B -> {github_slug('A & B')!r}")


# --- TOKENMAXX deepen after #27: actionlint / link-check / wiki / schema -----


def test_actionlint_rejects_write_all() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "permissions:\n  contents: read",
            "permissions: write-all",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "write-all",
        )


def test_actionlint_rejects_float_master_ref() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "actions/checkout@v7", "actions/checkout@master"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must not float on @master",
        )


def test_actionlint_rejects_float_latest_ref() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "actions/checkout@v7", "actions/checkout@latest"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must not float on @latest",
        )


def test_actionlint_requires_name() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("name: Link Check\n", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "top-level name:",
        )


def test_actionlint_requires_steps() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace("steps:", "phase:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "steps",
        )


def test_link_check_requires_github_token() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("GITHUB_TOKEN", "NO_TOKEN")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "GITHUB_TOKEN",
        )


def test_link_check_requires_lychee_needle() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("lychee", "linktool")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "lychee",
        )


def test_link_check_requires_exclude_path() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("--exclude-path .github/agents", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "exclude .github/agents",
        )


def test_link_check_requires_max_concurrency() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("--max-concurrency 8", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "--max-concurrency",
        )


def test_link_check_requires_lychee_timeout() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("--timeout 20", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "--timeout",
        )


def test_markdown_lint_requires_owasp_exclude() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace("OWASP-AGENTIC.md", "OTHER.md")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "OWASP-AGENTIC.md",
        )


def test_markdown_lint_requires_agents_exclude() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(".github/agents", ".github/other")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            ".github/agents",
        )


def test_markdown_lint_requires_markdownlint_needle() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace("markdownlint", "mdl")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "markdownlint",
        )


def test_stewardship_requires_selftest_needle() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "test_stewardship_gates.py", "test_other.py"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "test_stewardship_gates.py",
        )


def test_stewardship_requires_setup_python() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("setup-python", "setup-node")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Python",
        )


def test_stewardship_requires_run_script() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "run_stewardship_checks.sh", "run_other.sh"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "run_stewardship_checks.sh",
        )


def test_stewardship_requires_actionlint_version_pin() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("1.7.7", "9.9.9")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "1.7.7",
        )


def test_stewardship_requires_actionlint_all_workflows() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            ".github/workflows/markdown-lint.yml",
            ".github/workflows/other-lint.yml",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "markdown-lint.yml",
        )


def test_workflow_rejects_missing_pull_request() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("pull_request:", "push_request:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pull_request",
        )


def test_badge_rejects_missing_h1() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme().replace("# agents-governance", "agents-governance")
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "missing H1",
        )


def test_badge_rejects_two_badges_only() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "exactly 3 badges",
        )


def test_badge_rejects_http_badge_link() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(http://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must use https://",
        )


def test_badge_rejects_wrong_markdown_lint_image() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/wrong-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "markdown-lint.yml/badge.svg",
        )


def test_badge_rejects_missing_readme_stewardship_script() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme().replace("run_stewardship_checks.sh", "run_other.sh")
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "run_stewardship_checks.sh",
        )


def test_badge_rejects_missing_contributing_script() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(
            tmp_path / "CONTRIBUTING.md",
            "# Contributing\n\nDo not invent product badges.\n",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "run_stewardship_checks.sh",
        )


def test_badge_rejects_missing_workflow_file() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        (tmp_path / ".github" / "workflows" / "markdown-lint.yml").unlink()
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Missing workflow",
        )


def test_lycheeignore_accepts_regex_escaped_shields() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        # After #100 docs-lint deepen: full live fixture (escaped shields + MCP/LF + note).
        _write(
            tmp_path / ".lycheeignore",
            "# modelcontextprotocol.io 308 redirect\n"
            "https://modelcontextprotocol.io/\n"
            "# linuxfoundation.org 103 early hints\n"
            "https://www.linuxfoundation.org/\n"
            "# stewardship/license-badge enforcement\n"
            "https://img\\.shields\\.io\n",
        )
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)


def test_lycheeignore_accepts_literal_shields() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        # After #100: escaped shields form is required (live docs-lint path).
        # Literal-only is no longer sufficient.
        _write(
            tmp_path / ".lycheeignore",
            "# modelcontextprotocol.io 308 redirect\n"
            "https://modelcontextprotocol.io/\n"
            "# linuxfoundation.org 103 early hints\n"
            "https://www.linuxfoundation.org/\n"
            "# stewardship/license-badge enforcement\n"
            "https://img.shields.io/\n",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            r"img\.shields\.io",
        )


def test_relative_links_accept_tel() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[call](tel:+15551212)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_nul() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](./ok%00.md)\n")
        _write(tmp_path / "ok.md", "# Ok\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "NUL",
        )


def test_relative_links_reject_bare_hash() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](#)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "empty relative link",
        )


def test_relative_links_accept_title_attr() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "a.md", "# A\n")
        _write(tmp_path / "README.md", '# T\n\n[x](a.md "title here")\n')
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_accept_existing_image() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "pic.png", "x")
        _write(tmp_path / "README.md", "# T\n\n![alt](./pic.png)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_wiki_rejects_missing_publish() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_wiki_tree(tmp_path)
        (tmp_path / "docs" / "wiki" / "PUBLISH.md").unlink()
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Missing operator page",
        )


def test_wiki_rejects_missing_badge_link() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = (
                "# Home\n\n"
                "[README](https://github.com/fuzzywigg/agents-governance/blob/main/README.md)\n"
                "[Overview](Overview.md)\n"
                "[Autonomy-Levels](Autonomy-Levels.md)\n"
                "[Repo-Stewardship](Repo-Stewardship.md)\n"
                "[Agent-Routing](Agent-Routing.md)\n"
                "[Security-Boundaries](Security-Boundaries.md)\n\n"
                "## Out of scope\n\nSecrets and invent product frameworks.\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "badge standard",
        )


def test_wiki_rejects_missing_home_page_link() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = (
                "# Home\n\n"
                "[README](https://github.com/fuzzywigg/agents-governance/blob/main/README.md)\n"
                "[Badge](../badge-standard.md)\n"
                "[Autonomy-Levels](Autonomy-Levels.md)\n"
                "[Repo-Stewardship](Repo-Stewardship.md)\n"
                "[Agent-Routing](Agent-Routing.md)\n"
                "[Security-Boundaries](Security-Boundaries.md)\n\n"
                "## Out of scope\n\nSecrets and invent product frameworks.\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Overview.md",
        )


def test_wiki_rejects_missing_publish_do_not_push() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = (
                "# PUBLISH\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\n"
                "purpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\ncloses: \"#16\"\n```\n\n"
                "| `Home.md` | Home |\n| `Overview.md` | Overview |\n"
                "| `Autonomy-Levels.md` | Autonomy-Levels |\n"
                "| `Repo-Stewardship.md` | Repo-Stewardship |\n"
                "| `Agent-Routing.md` | Agent-Routing |\n"
                "| `Security-Boundaries.md` | Security-Boundaries |\n\n"
                "Link Check and Markdown Lint. No secrets.\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "not pushed",
        )


def test_wiki_rejects_missing_relative_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = (
                "# Repo\n\n[← Home](Home.md)\n\n"
                "markdown-lint link-check stewardship-checks\n"
                "run_stewardship_checks.sh badge\n"
                "no invent product\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "relative-link",
        )


def test_wiki_rejects_missing_invent_on_stewardship() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = (
                "# Repo\n\n[← Home](Home.md)\n\n"
                "markdown-lint link-check stewardship-checks\n"
                "run_stewardship_checks.sh relative links badge\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent",
        )


def test_schema_rejects_wrong_maintainer() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: other\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "maintainer",
        )


def test_schema_rejects_wrong_claude_parent() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"fuzzywigg (smtp.eth)\"\n"
                "surface: copilot\nautonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
                "parent_governance: github.com/other/repo\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "parent_governance",
        )


def test_schema_rejects_wrong_badge_owner() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: geryon\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "owner",
        )


def test_schema_rejects_secret_in_doc() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n\n"
                "token: ghp_" + ("a" * 36) + "\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "secret-like",
        )


def test_common_strip_fenced() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import strip_fenced_code  # noqa: E402

    text = "# T\n\n```md\n[x](./missing.md)\n```\n\nok\n"
    out = strip_fenced_code(text)
    if "[x](./missing.md)" in out:
        raise AssertionError("fenced link should be stripped")
    if "ok" not in out:
        raise AssertionError("non-fenced text should remain")


def test_common_dangerous_schemes_complete() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import DANGEROUS_LINK_SCHEMES  # noqa: E402

    required = ("javascript:", "data:", "vbscript:", "file:")
    for scheme in required:
        if scheme not in DANGEROUS_LINK_SCHEMES:
            raise AssertionError(f"DANGEROUS_LINK_SCHEMES missing {scheme!r}")


# --- TOKENMAXX deepen after #28: existing-path fixtures only ----------------


def test_workflow_requires_cancel_in_progress() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "cancel-in-progress: true", "cancel-in-progress-disabled: true"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "cancel-in-progress",
        )


def test_link_check_requires_markdown_glob() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("**/*.md", "**/*.txt")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "**/*.md",
        )


def test_actionlint_requires_timeout_minutes_local() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace("timeout-minutes:", "timebox-minutes:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "timeout-minutes",
        )


def test_actionlint_allows_docker_uses_without_pin() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8")
        text = text.replace(
            "- uses: actions/checkout@v7",
            "- uses: docker://alpine:3.20\n      - uses: actions/checkout@v7",
        )
        path.write_text(text, encoding="utf-8")
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)


def test_badge_rejects_missing_badge_standard_file() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        (tmp_path / "docs" / "badge-standard.md").unlink()
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "docs/badge-standard.md",
        )


def test_badge_rejects_missing_contributing_file() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        (tmp_path / "CONTRIBUTING.md").unlink()
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "CONTRIBUTING.md",
        )


def test_badge_rejects_license_image_wrong_path() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/badge/license-MIT-blue)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "img.shields.io/github/license/",
        )


def test_badge_rejects_license_image_missing_repo_slug() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/other/repo)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "fuzzywigg/agents-governance",
        )


def test_badge_rejects_secret_in_readme_body() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme() + "\napi_key: " + ("k" * 20) + "\n"
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "secret-like",
        )


def test_badge_rejects_agents_missing_link_check_needle() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        agents = (tmp_path / "AGENTS.md").read_text(encoding="utf-8").replace(
            "link-check.yml", "linkcheck.yml"
        )
        (tmp_path / "AGENTS.md").write_text(agents, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "link-check.yml",
        )


def test_badge_rejects_agents_missing_markdown_lint_needle() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        agents = (tmp_path / "AGENTS.md").read_text(encoding="utf-8").replace(
            "markdown-lint.yml", "md-lint.yml"
        )
        (tmp_path / "AGENTS.md").write_text(agents, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "markdown-lint.yml",
        )


def test_badge_rejects_agents_missing_stewardship_needle() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        agents = (tmp_path / "AGENTS.md").read_text(encoding="utf-8").replace(
            "stewardship-checks.yml", "stew-checks.yml"
        )
        (tmp_path / "AGENTS.md").write_text(agents, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "stewardship-checks.yml",
        )


def test_badge_rejects_agents_missing_run_script_needle() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        agents = (tmp_path / "AGENTS.md").read_text(encoding="utf-8").replace(
            "run_stewardship_checks.sh", "run_checks.sh"
        )
        (tmp_path / "AGENTS.md").write_text(agents, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "run_stewardship_checks.sh",
        )


def test_badge_doc_rejects_missing_invent_wording() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        doc = (tmp_path / "docs" / "badge-standard.md").read_text(encoding="utf-8")
        doc = doc.replace("do not invent product badges", "keep badges minimal")
        doc = doc.replace("invent product", "product chrome")
        (tmp_path / "docs" / "badge-standard.md").write_text(doc, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "invent",
        )


def test_badge_doc_rejects_missing_three_badge_max() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        doc = (tmp_path / "docs" / "badge-standard.md").read_text(encoding="utf-8")
        doc = doc.replace("Three badges max", "Keep badges thin")
        doc = doc.replace("three badges", "badges")
        doc = doc.replace("3 badges", "badges")
        doc = doc.replace("badges max", "badges thin")
        (tmp_path / "docs" / "badge-standard.md").write_text(doc, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "three-badge",
        )


def test_badge_doc_rejects_stewardship_without_fourth_refusal() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        doc = (tmp_path / "docs" / "badge-standard.md").read_text(encoding="utf-8")
        doc = doc.replace("fourth", "extra")
        doc = doc.replace("intentionally", "usually")
        doc = doc.replace("not** added", "deferred")
        doc = doc.replace("not added", "deferred")
        (tmp_path / "docs" / "badge-standard.md").write_text(doc, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "fourth badge",
        )


def test_badge_accepts_absolute_license_blob_link() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(https://github.com/fuzzywigg/agents-governance/blob/main/LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)


def test_lycheeignore_rejects_https_star() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".lycheeignore", "https://*\n")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must not exclude all http",
        )


def test_relative_links_accept_angle_bracket_https() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "a.md", "# A\n\n[x](<https://example.com/path>)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_accept_license_target() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "LICENSE", "MIT\n")
        _write(tmp_path / "a.md", "# A\n\n[License](LICENSE)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_github_slug_ampersand() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from check_relative_links import github_slug  # noqa: E402

    # GitHub: strip '&' then map spaces → '-' yielding double hyphen.
    if github_slug("A & B") != "a--b":
        raise AssertionError(f"unexpected slug: {github_slug('A & B')!r}")


def test_relative_links_accept_ampersand_heading_fragment() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "a.md", "# A & B\n\n[jump](#a--b)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_whitespace_only_target() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        # Spaces inside () are stripped → empty → fail closed.
        _write(tmp_path / "a.md", "# A\n\n[x](   )\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "empty relative link",
        )


def test_wiki_rejects_missing_publish_link_check() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = (
                pages["PUBLISH.md"]
                .replace("Link Check", "CI Check")
                .replace("link-check", "ci-check")
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Link Check",
        )


def test_wiki_rejects_missing_publish_markdown_lint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = (
                pages["PUBLISH.md"]
                .replace("Markdown Lint", "Doc Lint")
                .replace("markdown", "docsfmt")
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Markdown",
        )


def test_wiki_rejects_missing_publish_secrets() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = (
                pages["PUBLISH.md"]
                .replace("No secrets.", "No tokens.")
                .replace("secrets", "tokens")
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "secrets",
        )


def test_wiki_rejects_missing_overview_topic() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = "# O\n\n[Home](Home.md)\n\nsummary only\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "governance",
        )


def test_wiki_rejects_missing_security_topic() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Security-Boundaries.md"] = "# S\n\n[Home](Home.md)\n\nboundaries\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "kill",
        )


def test_wiki_rejects_stars_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge stars chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "stars",
        )


def test_wiki_rejects_missing_wiki_dir() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_wiki_outline.py")
        # No docs/wiki created → fail closed.
        proc = run(scripts / "check_wiki_outline.py", tmp_path)
        blob = proc.stdout + proc.stderr
        if proc.returncode == 0:
            raise AssertionError(f"wiki check unexpectedly passed\n{blob}")
        if "docs/wiki/" not in blob:
            raise AssertionError(f"missing docs/wiki/ failure\n{blob}")


def test_schema_rejects_wrong_agents_scope() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: ecosystem-wide\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "scope",
        )


def test_schema_rejects_wrong_agents_parent() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/other/governance\nautonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "parent_governance",
        )


def test_schema_rejects_wrong_claude_repo() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo: other-repo\nowner: \"fuzzywigg (smtp.eth)\"\n"
                "surface: copilot\nautonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
                "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "repo",
        )


def test_schema_rejects_inactive_publish_status() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            publish=(
                "# P\n\n```yaml\nstatus: DRAFT\ncreated: \"2026-09-13\"\npurpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "ACTIVE",
        )


def test_schema_rejects_backlog_wrong_tier() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            backlog=(
                "# I\n\n```yaml\nstatus: ACTIVE\ntier: 9\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nedit_policy: x\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "tier",
        )


def test_schema_rejects_autonomy_as_string() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\n"
                "autonomy_level: \"1\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "autonomy_level",
        )


def test_schema_rejects_bad_claude_date() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"fuzzywigg (smtp.eth)\"\n"
                "surface: copilot\nautonomy_level: 1\nlast_updated: \"13-04-2026\"\n"
                "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "ISO-8601",
        )


def test_schema_rejects_unparseable_yaml() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\n: bad\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "AGENTS.md",
        )


def test_common_secret_patterns_extended() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    samples = (
        "npm_" + ("n" * 24),
        "AIza" + ("A" * 35),
        "xoxb-1234567890-abcdefghij",
        'aws_secret_access_key: "' + ("A" * 24) + '"',
        "github_pat_" + ("p" * 22),
        "sk-" + ("s" * 24),
    )
    for sample in samples:
        if not any(p.search(sample) for p in SECRET_PATTERNS):
            raise AssertionError(f"SECRET_PATTERNS missed {sample[:20]!r}...")


def test_common_forbidden_badge_hints_extended() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import FORBIDDEN_BADGE_HINTS  # noqa: E402

    required = (
        "producthunt",
        "buymeacoffee",
        "opencollective",
        "coveralls",
        "pypi/",
        "npm/",
    )
    for hint in required:
        if hint not in FORBIDDEN_BADGE_HINTS:
            raise AssertionError(f"FORBIDDEN_BADGE_HINTS missing {hint!r}")


def test_common_has_dangerous_scheme_helper() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402

    if has_dangerous_scheme("javascript:alert(1)") != "javascript:":
        raise AssertionError("expected javascript: match")
    if has_dangerous_scheme("https://example.com") is not None:
        raise AssertionError("https should be clean")


def test_common_load_workflow_text_helper() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import load_workflow_text  # noqa: E402

    text = load_workflow_text("link-check.yml")
    if text is None or "lychee" not in text.lower():
        raise AssertionError("load_workflow_text failed for link-check.yml")
    if load_workflow_text("does-not-exist.yml") is not None:
        raise AssertionError("missing workflow should return None")


def test_common_fail_helper_appends() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import fail  # noqa: E402

    errors: list[str] = []
    fail("one", errors)
    fail("two", errors)
    if errors != ["one", "two"]:
        raise AssertionError(f"fail() append broken: {errors!r}")


def test_common_markdown_files_helper() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import markdown_files  # noqa: E402

    found = markdown_files("README.md", "docs/badge-standard.md")
    names = {p.name for p in found}
    if "README.md" not in names or "badge-standard.md" not in names:
        raise AssertionError(f"markdown_files missed expected paths: {names}")


def test_markdown_lint_requires_config_needle() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            ".markdownlint.json", ".markdownlint.yaml"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            ".markdownlint.json",
        )


def test_stewardship_requires_actionlint_link_check_path() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            ".github/workflows/link-check.yml",
            ".github/workflows/links.yml",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "link-check.yml",
        )


def test_stewardship_requires_actionlint_stewardship_path() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            ".github/workflows/stewardship-checks.yml",
            ".github/workflows/stew.yml",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "stewardship-checks.yml",
        )


def test_workflow_rejects_missing_dispatch_on_stewardship() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "workflow_dispatch:", "workflow_call:"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "workflow_dispatch",
        )


# --- TOKENMAXX deepen after #29: existing-path fixtures only ----------------


def test_workflow_requires_cancel_in_progress_true() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "cancel-in-progress: true", "cancel-in-progress: false"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "cancel-in-progress: true",
        )


def test_workflow_requires_cancel_in_progress_true_on_stewardship() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "cancel-in-progress: true", "cancel-in-progress: false"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "cancel-in-progress: true",
        )


def test_link_check_requires_agents_exclude_path() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "--exclude-path .github/agents", "--exclude-path vendor/agents"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            ".github/agents",
        )


def test_markdown_lint_requires_markdown_glob() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace("**/*.md", "**/*.txt")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "**/*.md",
        )


def test_lycheeignore_rejects_http_star() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".lycheeignore", "http://*\n")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must not exclude all http",
        )


def test_stewardship_requires_actionlint_markdown_lint_path() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            ".github/workflows/markdown-lint.yml",
            ".github/workflows/md-lint.yml",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "markdown-lint.yml",
        )


def test_actionlint_rejects_unpinned_second_action() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "- uses: actions/setup-python@v5",
            "- uses: actions/setup-python",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "unpinned action",
        )


def test_actionlint_rejects_float_main_on_setup_python() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "- uses: actions/setup-python@v5",
            "- uses: actions/setup-python@main",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "@main",
        )


def test_workflow_rejects_missing_schedule_on_link_check() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("schedule:", "scheduled:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "schedule",
        )


def test_workflow_rejects_missing_concurrency_on_markdown_lint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace("concurrency:", "serial:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "concurrency",
        )


def test_badge_rejects_link_check_relative_workflow_link() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(.github/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "absolute https://",
        )


def test_badge_rejects_markdown_lint_relative_workflow_link() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(.github/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "absolute https://",
        )


def test_badge_accepts_dot_slash_license_link() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(./LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)


def test_badge_rejects_forbidden_coverage_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(https://example.com/coverage)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "coverage",
        )


def test_badge_rejects_secret_url_token_query() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?token=abc)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Secret-like token",
        )


def test_badge_doc_rejects_missing_link_check_label() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        doc = (tmp_path / "docs" / "badge-standard.md").read_text(encoding="utf-8")
        doc = doc.replace("Link Check", "Link Gate").replace("link-check", "linkgate")
        (tmp_path / "docs" / "badge-standard.md").write_text(doc, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Link Check",
        )


def test_badge_doc_rejects_missing_shields_license_snippet() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        doc = (tmp_path / "docs" / "badge-standard.md").read_text(encoding="utf-8")
        doc = doc.replace("img.shields.io/github/license/", "shields.io/license/")
        (tmp_path / "docs" / "badge-standard.md").write_text(doc, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "shields license image",
        )


def test_badge_rejects_four_badges() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n"
            "[![Extra](https://img.shields.io/badge/extra-x-red)]"
            "(https://example.com/extra)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "exactly 3 badges",
        )


def test_relative_links_accept_angle_bracket_relative() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "b.md", "# B\n")
        _write(tmp_path / "a.md", "# A\n\n[x](<b.md>)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_github_slug_backticks() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from check_relative_links import github_slug  # noqa: E402

    if github_slug("`Code` Title") != "code-title":
        raise AssertionError(f"unexpected slug: {github_slug('`Code` Title')!r}")


def test_relative_links_github_slug_markdown_link_heading() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from check_relative_links import github_slug  # noqa: E402

    if github_slug("[Linked](https://example.com) Heading") != "linked-heading":
        raise AssertionError(
            f"unexpected slug: {github_slug('[Linked](https://example.com) Heading')!r}"
        )


def test_relative_links_accept_nested_path() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "x.md", "# X\n")
        _write(tmp_path / "a.md", "# A\n\n[x](docs/x.md)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_missing_nested() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "a.md", "# A\n\n[x](docs/missing.md)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "broken relative link",
        )


def test_relative_links_accept_image_with_title() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "img.png", "x")
        _write(tmp_path / "a.md", '# A\n\n![alt](img.png "title")\n')
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_percent_encoded_escape() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        # %2e%2e is ".." — must fail closed as repo escape.
        _write(tmp_path / "a.md", "# A\n\n[x](%2e%2e/%2e%2e/etc/passwd)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "escapes repo",
        )


def test_wiki_rejects_missing_actionlint_on_stewardship() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = (
                pages["Repo-Stewardship.md"]
                .replace("actionlint", "workflow-lint")
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "actionlint",
        )


def test_wiki_rejects_missing_autonomy_topic() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Autonomy-Levels.md"] = "# A\n\n[Home](Home.md)\n\nlevels only\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "L0",
        )


def test_wiki_rejects_missing_routing_topic() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Agent-Routing.md"] = "# R\n\n[Home](Home.md)\n\nagents only\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "surface",
        )


def test_wiki_rejects_missing_home_out_of_scope() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = (
                pages["Home.md"]
                .replace("## Out of scope", "## Limits")
                .replace("Out of scope", "Limits")
                .replace("out of scope", "limits")
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Out of scope",
        )


def test_wiki_rejects_forks_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge forks chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "forks",
        )


def test_wiki_rejects_codecov_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[![cov](https://codecov.io/gh/x/badge.svg)](https://codecov.io)\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "codecov",
        )


def test_wiki_rejects_missing_home_to_routing() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace(
                "[Agent-Routing](Agent-Routing.md)\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Agent-Routing.md",
        )


def test_wiki_rejects_secret_in_publish() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = pages["PUBLISH.md"] + "\napi_key: " + ("z" * 20) + "\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "secret-like",
        )


def test_wiki_rejects_missing_publish_page_table_row() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = pages["PUBLISH.md"].replace(
                "| `Security-Boundaries.md` | Security-Boundaries |\n", ""
            ).replace("Security-Boundaries", "SecBounds")

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Security-Boundaries",
        )


def test_schema_rejects_wrong_badge_status() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: DRAFT\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "ACTIVE",
        )


def test_schema_rejects_wrong_badge_tier() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 2\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "tier",
        )


def test_schema_rejects_publish_closes_without_issue() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            publish=(
                "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\npurpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\n"
                "closes: \"soon\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "closes",
        )


def test_schema_rejects_empty_agents_maintainer() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: \"\"\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_tier_as_string() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            backlog=(
                "# I\n\n```yaml\nstatus: ACTIVE\ntier: \"1\"\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nedit_policy: x\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "tier",
        )


def test_schema_rejects_autonomy_out_of_range() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\n"
                "autonomy_level: 9\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "autonomy_level",
        )


def test_schema_rejects_non_mapping_yaml() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\n- version: \"1.0.0\"\n- autonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "mapping",
        )


def test_schema_rejects_bad_badge_created_date() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"09/13/2026\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "ISO-8601",
        )


def test_common_secret_url_hints_complete() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_URL_HINTS  # noqa: E402

    required = (
        "token=",
        "access_token=",
        "api_key=",
        "apikey=",
        "client_secret=",
        "ghp_",
        "gho_",
        "github_pat_",
    )
    for hint in required:
        if hint not in SECRET_URL_HINTS:
            raise AssertionError(f"SECRET_URL_HINTS missing {hint!r}")


def test_common_forbidden_badge_hints_social() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import FORBIDDEN_BADGE_HINTS  # noqa: E402

    required = ("coverage", "codecov", "discord", "twitter", "forks", "followers")
    for hint in required:
        if hint not in FORBIDDEN_BADGE_HINTS:
            raise AssertionError(f"FORBIDDEN_BADGE_HINTS missing {hint!r}")


def test_common_secret_patterns_private_key() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "-----BEGIN RSA PRIVATE KEY-----\nMIIE\n"
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missed RSA private key block")


def test_common_strip_fenced_tilde() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import strip_fenced_code  # noqa: E402

    text = "before\n~~~md\n[bad](missing.md)\n~~~\nafter\n"
    stripped = strip_fenced_code(text)
    if "missing.md" in stripped:
        raise AssertionError("tilde fence not stripped")
    if "before" not in stripped or "after" not in stripped:
        raise AssertionError("strip_fenced_code removed surrounding prose")


def test_common_scan_secrets_ghp() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import scan_secrets  # noqa: E402

    with tempfile.TemporaryDirectory() as tmp:
        path = Path(tmp) / "doc.md"
        path.write_text("token ghp_" + ("x" * 36) + "\n", encoding="utf-8")
        # scan_secrets uses ROOT-relative labels; call with label override.
        errors: list[str] = []
        # Monkey via label to avoid ROOT.relative_to issues outside repo.
        text = path.read_text(encoding="utf-8")
        from stewardship_common import SECRET_PATTERNS, fail  # noqa: E402

        for pattern in SECRET_PATTERNS:
            if pattern.search(text):
                fail("doc.md matches forbidden secret-like pattern", errors)
        if not errors:
            raise AssertionError("expected ghp_ secret detection")


def test_markdown_lint_requires_agents_exclude_path() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            ".github/agents", ".github/prompts"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            ".github/agents",
        )


def test_link_check_requires_fail_true_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("fail: true", "fail: false")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "fail: true",
        )


def test_stewardship_requires_pyyaml_install_needle() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("pyyaml", "ruamel")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "PyYAML",
        )


def test_workflow_rejects_missing_permissions_on_link_check() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        # Remove every permissions.contents: read block (top-level + job-level).
        text = path.read_text(encoding="utf-8").replace(
            "permissions:\n  contents: read\n", ""
        ).replace(
            "    permissions:\n      contents: read\n", ""
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "contents: read",
        )


# --- TOKENMAXX deepen after #30: existing-path fixtures only ----------------


def test_stewardship_requires_python_version_needle() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("python-version", "py-version")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "python-version",
        )


def test_stewardship_requires_python_312_pin() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("3.12", "3.11")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "3.12",
        )


def test_workflow_requires_cancel_in_progress_true_on_link_check() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "cancel-in-progress: true", "cancel-in-progress: false"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "cancel-in-progress: true",
        )


def test_actionlint_rejects_float_master_on_setup_python() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "actions/setup-python@v5", "actions/setup-python@master"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "@master",
        )


def test_actionlint_rejects_float_latest_on_setup_python() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "actions/setup-python@v5", "actions/setup-python@latest"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "@latest",
        )


def test_actionlint_rejects_contents_write_on_stewardship() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "contents: read", "contents: write"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "contents: write",
        )


def test_workflow_rejects_missing_timeout_on_stewardship() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("timeout-minutes: 15", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "timeout-minutes",
        )


def test_workflow_rejects_missing_dispatch_on_markdown_lint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace("workflow_dispatch:", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "workflow_dispatch",
        )


def test_lycheeignore_rejects_bare_star() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".lycheeignore", "*\n")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must not exclude all http",
        )


def test_link_check_requires_markdown_glob_not_txt() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("**/*.md", "**/*.txt")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "**/*.md",
        )


def test_badge_rejects_discord_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(https://discord.gg/example)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "discord",
        )


def test_badge_rejects_producthunt_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(https://www.producthunt.com/posts/x)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "producthunt",
        )


def test_badge_rejects_api_key_query() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?api_key=abc)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Secret-like token",
        )


def test_badge_rejects_access_token_query() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?access_token=abc)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Secret-like token",
        )


def test_badge_rejects_http_license_image() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](http://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must be https://",
        )


def test_badge_rejects_license_first_order() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(
            tmp_path,
            _good_readme(("License", "Link Check", "Markdown Lint")),
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Badge labels must be in order",
        )


def test_badge_doc_rejects_missing_markdown_lint_snippet() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        doc = (tmp_path / "docs" / "badge-standard.md").read_text(encoding="utf-8")
        doc = doc.replace("markdown-lint.yml/badge.svg", "md-lint.yml/badge.svg")
        (tmp_path / "docs" / "badge-standard.md").write_text(doc, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "markdown-lint",
        )


def test_badge_doc_rejects_missing_link_check_snippet() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        doc = (tmp_path / "docs" / "badge-standard.md").read_text(encoding="utf-8")
        doc = doc.replace("link-check.yml/badge.svg", "linkgate.yml/badge.svg")
        (tmp_path / "docs" / "badge-standard.md").write_text(doc, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "link-check",
        )


def test_badge_rejects_missing_workflow_link_check_file() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        (tmp_path / ".github" / "workflows" / "link-check.yml").unlink()
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Missing workflow",
        )


def test_relative_links_skip_node_modules() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\nOk.\n")
        _write(
            tmp_path / "node_modules" / "pkg" / "README.md",
            "# Pkg\n\n[broken](./missing.md)\n",
        )
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_javascript_uppercase() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](JAVASCRIPT:alert(1))\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_data_uppercase() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](DATA:text/html,hi)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_nested_escape() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "a.md", "# A\n\n[x](../../etc/passwd)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "escapes repo",
        )


def test_relative_links_github_slug_numbers() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from check_relative_links import github_slug  # noqa: E402

    if github_slug("Section 2.1") != "section-21":
        raise AssertionError(f"unexpected slug: {github_slug('Section 2.1')!r}")


def test_relative_links_accept_numbered_heading_fragment() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(
            tmp_path / "README.md",
            "# Title\n\n## Section 2.1\n\nSee [x](#section-21).\n",
        )
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_accept_mailto_and_https() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(
            tmp_path / "README.md",
            "# T\n\n[mail](mailto:ops@example.com)\n[web](https://example.com)\n",
        )
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_file_uppercase() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](FILE:///etc/passwd)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_wiki_rejects_missing_invent_on_home() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace(
                "Secrets and invent product frameworks.",
                "Secrets and private frameworks.",
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_missing_secrets_on_home() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace(
                "Secrets and invent product frameworks.",
                "Invent product frameworks only.",
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "secrets",
        )


def test_wiki_rejects_missing_l1_on_autonomy() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Autonomy-Levels.md"] = "# A\n\n[Home](Home.md)\n\nL0 autonomy only\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "L1",
        )


def test_wiki_rejects_missing_kill_on_security() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Security-Boundaries.md"] = (
                "# S\n\n[Home](Home.md)\n\nsecret boundary only\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "kill",
        )


def test_wiki_rejects_downloads_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge downloads chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "downloads",
        )


def test_wiki_rejects_missing_home_to_security() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace(
                "[Security-Boundaries](Security-Boundaries.md)\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Security-Boundaries.md",
        )


def test_wiki_rejects_missing_publish_overview_row() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = (
                pages["PUBLISH.md"]
                .replace("| `Overview.md` | Overview |\n", "")
                .replace("Overview", "Summ")
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Overview",
        )


def test_wiki_rejects_data_scheme() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[x](data:text/html,hi)\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_wiki_rejects_missing_badge_topic_on_stewardship() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = (
                "# Repo\n\n[← Home](Home.md)\n\n"
                "markdown-lint link-check stewardship-checks\n"
                "run_stewardship_checks.sh relative links\n"
                "actionlint on existing workflow paths\n"
                "no invent product\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "badge",
        )


def test_schema_rejects_wrong_claude_owner() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"other\"\n"
                "surface: copilot\nautonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
                "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "owner",
        )


def test_schema_rejects_wrong_claude_autonomy() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"fuzzywigg (smtp.eth)\"\n"
                "surface: copilot\nautonomy_level: 2\nlast_updated: \"2026-04-13\"\n"
                "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "autonomy_level",
        )


def test_schema_rejects_inactive_backlog_status() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            backlog=(
                "# I\n\n```yaml\nstatus: DRAFT\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nedit_policy: x\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "ACTIVE",
        )


def test_schema_rejects_empty_badge_scope() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"\"\nedit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_missing_badge_closes_key() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing metadata keys",
        )


def test_schema_rejects_agents_autonomy_drift() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 0\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "autonomy_level",
        )


def test_schema_rejects_publish_missing_purpose_key() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            publish=(
                "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing metadata keys",
        )


def test_schema_rejects_float_autonomy() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\n"
                "autonomy_level: 1.5\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "autonomy_level",
        )


def test_common_forbidden_badge_hints_commerce() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import FORBIDDEN_BADGE_HINTS  # noqa: E402

    required = ("producthunt", "buymeacoffee", "opencollective", "npm/", "pypi/")
    for hint in required:
        if hint not in FORBIDDEN_BADGE_HINTS:
            raise AssertionError(f"FORBIDDEN_BADGE_HINTS missing {hint!r}")


def test_common_secret_patterns_sk_token() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "sk-" + ("A" * 24)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missed sk- token")


def test_common_has_dangerous_scheme_casefold() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402

    if has_dangerous_scheme("JaVaScRiPt:alert(1)") != "javascript:":
        raise AssertionError("has_dangerous_scheme should casefold javascript:")


def test_common_scan_secrets_url_token_hint() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_URL_HINTS  # noqa: E402

    if "token=" not in SECRET_URL_HINTS:
        raise AssertionError("SECRET_URL_HINTS missing token=")
    sample = "https://example.com/x?token=abc"
    if "token=" not in sample:
        raise AssertionError("fixture sample broken")


def test_markdown_lint_requires_owasp_exclude_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "OWASP-AGENTIC.md", "OWASP-OTHER.md"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "OWASP-AGENTIC.md",
        )


def test_stewardship_requires_timeout_minutes_needle() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("timeout-minutes", "timeout_mins")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "timeout-minutes",
        )


def test_actionlint_rejects_write_all_on_markdown_lint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "permissions:\n  contents: read",
            "permissions: write-all",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "write-all",
        )


def test_badge_rejects_stars_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=stars)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "stars",
        )


def test_relative_links_accept_parent_relative_existing() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Root\n")
        _write(tmp_path / "docs" / "a.md", "# A\n\n[up](../README.md)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_wiki_rejects_twitter_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge twitter chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "twitter",
        )


def test_schema_rejects_wrong_agents_version_semver_prerelease() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0-beta\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "semver",
        )


def test_common_dangerous_schemes_file() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402

    if has_dangerous_scheme("file:///tmp/x") != "file:":
        raise AssertionError("file: scheme should be dangerous")


# --- TOKENMAXX deepen after #31: existing-path fixtures only ----------------


def test_link_check_requires_no_progress() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("--no-progress", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "--no-progress",
        )


def test_link_check_requires_verbose() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("--verbose", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "--verbose",
        )


def test_stewardship_requires_actions_checkout() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "actions/checkout@v7", "local/repo-checkout@v4"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "actions/checkout",
        )


def test_workflow_requires_cancel_in_progress_true_on_markdown_lint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "cancel-in-progress: true", "cancel-in-progress: false"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "cancel-in-progress: true",
        )


def test_actionlint_rejects_pull_request_target_on_stewardship() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8")
        text = text.replace(
            "pull_request:\n",
            "pull_request:\n  pull_request_target:\n",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pull_request_target",
        )


def test_actionlint_rejects_unpinned_setup_python() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "actions/setup-python@v5", "actions/setup-python"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "unpinned action",
        )


def test_badge_rejects_coveralls_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=coveralls)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_buymeacoffee_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=buymeacoffee)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_opencollective_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=opencollective)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_npm_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/npm/v/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_pypi_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/pypi/v/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_followers_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=followers)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_x_com_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=x.com)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_apikey_query() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?apikey=abc)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Secret-like token",
        )


def test_badge_rejects_client_secret_query() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?client_secret=abc)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Secret-like token",
        )


def test_badge_rejects_gho_token_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?x=gho_abcdefghij)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Secret-like token",
        )


def test_badge_rejects_license_wrong_shields_slug() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/other/repo)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "repo slug",
        )


def test_badge_rejects_missing_stewardship_workflow_file() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        (tmp_path / ".github" / "workflows" / "stewardship-checks.yml").unlink()
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Missing workflow",
        )


def test_badge_doc_rejects_missing_license_label() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "docs" / "badge-standard.md"
        text = path.read_text(encoding="utf-8").replace("| License |", "| SPDX |")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "License",
        )


def test_relative_links_skip_git_dir() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\nOk.\n")
        _write(tmp_path / ".git" / "notes.md", "# Git\n\n[broken](./missing.md)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_vbscript_uppercase() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](VBSCRIPT:msgbox(1))\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_encoded_nested_escape() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "a.md", "# A\n\n[x](%2e%2e/%2e%2e/%2e%2e/etc/passwd)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "escapes repo",
        )


def test_relative_links_accept_same_dir_existing() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "a.md", "# A\n")
        _write(tmp_path / "README.md", "# T\n\n[x](a.md)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_github_slug_underscore() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from check_relative_links import github_slug  # noqa: E402

    # GitHub-ish: underscore emphasis markers are stripped (foo_bar → foobar).
    if github_slug("foo_bar") != "foobar":
        raise AssertionError(f"github_slug foo_bar -> {github_slug('foo_bar')!r}")


def test_relative_links_accept_underscore_heading_fragment() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\n## foo_bar\n\nSee [x](#foobar).\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_wiki_rejects_missing_l0_on_autonomy() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Autonomy-Levels.md"] = "# A\n\n[Home](Home.md)\n\nL1 autonomy\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "topic hint",
        )


def test_wiki_rejects_missing_secret_on_security() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Security-Boundaries.md"] = "# S\n\n[Home](Home.md)\n\nkill switch only\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "topic hint",
        )


def test_wiki_rejects_missing_surface_on_routing() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Agent-Routing.md"] = "# R\n\n[Home](Home.md)\n\nrouting only\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "topic hint",
        )


def test_wiki_rejects_missing_governance_on_overview() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = "# O\n\n[Home](Home.md)\n\npublic only\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "topic hint",
        )


def test_wiki_rejects_missing_public_on_overview() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = "# O\n\n[Home](Home.md)\n\ngovernance only\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "topic hint",
        )


def test_wiki_rejects_missing_home_to_autonomy() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = (
                "# Home\n\n"
                "[README](https://github.com/fuzzywigg/agents-governance/blob/main/README.md)\n"
                "[Badge](../badge-standard.md)\n"
                "[Overview](Overview.md)\n"
                "[Repo-Stewardship](Repo-Stewardship.md)\n"
                "[Agent-Routing](Agent-Routing.md)\n"
                "[Security-Boundaries](Security-Boundaries.md)\n\n"
                "## Out of scope\n\nSecrets and invent product frameworks.\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Autonomy-Levels.md",
        )


def test_wiki_rejects_followers_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge followers chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_x_com_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge x.com chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_missing_publish_home_row() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = (
                "# PUBLISH\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\n"
                "purpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\ncloses: \"#16\"\n```\n\n"
                "| `Overview.md` | Overview |\n"
                "| `Autonomy-Levels.md` | Autonomy-Levels |\n"
                "| `Repo-Stewardship.md` | Repo-Stewardship |\n"
                "| `Agent-Routing.md` | Agent-Routing |\n"
                "| `Security-Boundaries.md` | Security-Boundaries |\n\n"
                "Do **not** push `PUBLISH.md`.\n"
                "Link Check and Markdown Lint. No secrets.\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Home.md",
        )


def test_wiki_rejects_file_scheme() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[x](file:///etc/passwd)\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_schema_rejects_missing_claude_surface_key() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"fuzzywigg (smtp.eth)\"\n"
                "autonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
                "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing metadata keys",
        )


def test_schema_rejects_missing_backlog_owner_key() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            backlog=(
                "# I\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "edit_policy: x\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing metadata keys",
        )


def test_schema_rejects_empty_publish_purpose() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            publish=(
                "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\npurpose: \"   \"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "must be non-empty",
        )


def test_schema_rejects_empty_agents_scope() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: \"\"\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "must be non-empty",
        )


def test_schema_rejects_wrong_agents_autonomy_zero() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 0\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "autonomy_level",
        )


def test_schema_rejects_missing_publish_closes_key() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            publish=(
                "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\npurpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing metadata keys",
        )


def test_schema_rejects_missing_claude_repo_key() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nowner: \"fuzzywigg (smtp.eth)\"\n"
                "surface: copilot\nautonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
                "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing metadata keys",
        )


def test_schema_rejects_badge_closes_without_hash() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
                "closes: \"16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "closes must reference an issue",
        )


def test_common_forbidden_badge_hints_registry() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import FORBIDDEN_BADGE_HINTS  # noqa: E402

    required = (
        "coveralls",
        "buymeacoffee",
        "opencollective",
        "npm/",
        "pypi/",
        "followers",
        "x.com",
    )
    for hint in required:
        if hint not in FORBIDDEN_BADGE_HINTS:
            raise AssertionError(f"FORBIDDEN_BADGE_HINTS missing {hint!r}")


def test_common_secret_url_hints_apikey_client() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_URL_HINTS  # noqa: E402

    for hint in ("apikey=", "client_secret=", "gho_"):
        if hint not in SECRET_URL_HINTS:
            raise AssertionError(f"SECRET_URL_HINTS missing {hint!r}")


def test_common_secret_patterns_gho() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "gho_" + ("a" * 36)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("gho_ sample should match SECRET_PATTERNS")


def test_common_scan_secrets_url_apikey() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import scan_secrets  # noqa: E402

    with tempfile.TemporaryDirectory() as tmp:
        path = Path(tmp) / "doc.md"
        path.write_text(
            "See https://example.com/x?apikey=supersecretvalue\n",
            encoding="utf-8",
        )
        errors: list[str] = []
        scan_secrets(path, errors, label="fixture.md")
        if not errors:
            raise AssertionError("apikey= URL hint should fail scan_secrets")


def test_markdown_lint_requires_markdown_glob_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace("**/*.md", "*.markdown")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "**/*.md",
        )


def test_link_check_requires_exclude_loopback_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("--exclude-loopback", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "exclude loopback",
        )


def test_stewardship_requires_python_312_pin_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("3.12", "3.11")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "3.12",
        )


def test_workflow_rejects_missing_schedule_on_stewardship() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("schedule:", "sched_disabled:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "weekly schedule",
        )


def test_badge_rejects_forks_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=forks)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_relative_links_accept_docs_nested_fragment() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "a.md", "# A\n\nSee [b](b.md#section-two).\n")
        _write(tmp_path / "docs" / "b.md", "# B\n\n## Section Two\n\nBody.\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_wiki_rejects_missing_run_script_on_stewardship() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = (
                "# Repo\n\n[← Home](Home.md)\n\n"
                "markdown-lint link-check stewardship-checks\n"
                "relative links badge\n"
                "no invent product\n"
                "actionlint on existing workflow paths\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "run_stewardship_checks.sh",
        )


def test_schema_rejects_wrong_claude_surface_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"fuzzywigg (smtp.eth)\"\n"
                "surface: browser-claude\nautonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
                "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "surface",
        )


def test_common_dangerous_schemes_vbscript() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402

    if has_dangerous_scheme("vbscript:msgbox") != "vbscript:":
        raise AssertionError("vbscript: scheme should be dangerous")


# --- TOKENMAXX deepen after #32: checkout / lychee-action / MD013 / edges -----


def test_link_check_requires_lychee_action() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("lychee-action", "lychee-cli")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "lychee-action",
        )


def test_link_check_requires_actions_checkout() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("actions/checkout", "actions/noop")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "actions/checkout",
        )


def test_markdown_lint_requires_actions_checkout() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace("actions/checkout", "actions/noop")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "actions/checkout",
        )


def test_markdownlint_json_requires_md013() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text('{\n  "default": true\n}\n', encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "MD013",
        )


def test_actionlint_rejects_pull_request_target_on_link_check() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "pull_request:",
            "pull_request_target:",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pull_request_target",
        )


def test_actionlint_rejects_contents_write_on_markdown_lint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "contents: read",
            "contents: write",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "contents: write",
        )


def test_actionlint_rejects_write_all_on_stewardship() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "contents: read",
            "write-all\npermissions:\n  contents: read",
        )
        # Force a top-level permissions: write-all line.
        text = "permissions: write-all\n" + text
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "write-all",
        )


def test_workflow_requires_cancel_in_progress_true_not_false_on_link() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "cancel-in-progress: true",
            "cancel-in-progress: false",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "cancel-in-progress: true",
        )


def test_badge_rejects_twitter_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=twitter)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_codecov_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=codecov)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_downloads_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=downloads)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_github_pat_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?github_pat_ABC)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Secret-like token",
        )


def test_badge_rejects_ghp_token_hint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?x=ghp_abcdefghijklmnopqrst)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Secret-like token",
        )


def test_badge_rejects_markdown_lint_first_order() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(
            tmp_path,
            _good_readme(("Markdown Lint", "Link Check", "License")),
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Badge labels must be in order",
        )


def test_badge_doc_rejects_missing_three_badges_max_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "docs" / "badge-standard.md"
        text = path.read_text(encoding="utf-8")
        for needle in ("Three badges max", "three badges", "badges max"):
            text = text.replace(needle, "badge row")
            text = text.replace(needle.title(), "badge row")
            text = text.replace(needle.lower(), "badge row")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "three-badge",
        )


def test_badge_rejects_missing_agents_file() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        (tmp_path / "AGENTS.md").unlink()
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "AGENTS.md",
        )


def test_relative_links_reject_javascript_mixed_case() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](JavaScript:alert(1))\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_data_mixed_case() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](DATA:text/plain,hi)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_accept_tel_with_title() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", '# T\n\n[call](tel:+15551212 "phone")\n')
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_github_slug_colon_punct() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from check_relative_links import github_slug  # noqa: E402

    # Colon stripped: "Note: one" → "note-one"
    if github_slug("Note: one") != "note-one":
        raise AssertionError(f"github_slug Note: one -> {github_slug('Note: one')!r}")


def test_relative_links_accept_colon_heading_fragment() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\n## Note: one\n\nSee [x](#note-one).\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_missing_image_nested() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "a.md", "# A\n\n![x](images/missing.png)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "broken relative link",
        )


def test_relative_links_accept_license_from_docs() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "LICENSE", "MIT\n")
        _write(tmp_path / "docs" / "a.md", "# A\n\n[L](../LICENSE)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_http_uppercase() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](HTTP://example.com)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "insecure http://",
        )


def test_wiki_rejects_missing_home_to_overview() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = (
                "# Home\n\n"
                "[README](https://github.com/fuzzywigg/agents-governance/blob/main/README.md)\n"
                "[Badge](../badge-standard.md)\n"
                "[Autonomy-Levels](Autonomy-Levels.md)\n"
                "[Repo-Stewardship](Repo-Stewardship.md)\n"
                "[Agent-Routing](Agent-Routing.md)\n"
                "[Security-Boundaries](Security-Boundaries.md)\n\n"
                "## Out of scope\n\nSecrets and invent product frameworks.\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Overview.md",
        )


def test_wiki_rejects_discord_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge discord chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_buymeacoffee_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge buymeacoffee chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_javascript_scheme() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[x](javascript:alert(1))\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_wiki_rejects_missing_publish_autonomy_row() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = (
                "# PUBLISH\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\n"
                "purpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\ncloses: \"#16\"\n```\n\n"
                "| `Home.md` | Home |\n"
                "| `Overview.md` | Overview |\n"
                "| `Repo-Stewardship.md` | Repo-Stewardship |\n"
                "| `Agent-Routing.md` | Agent-Routing |\n"
                "| `Security-Boundaries.md` | Security-Boundaries |\n\n"
                "Do **not** push `PUBLISH.md`.\n"
                "Link Check and Markdown Lint. No secrets.\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Autonomy-Levels.md",
        )


def test_wiki_rejects_missing_publish_security_row() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = (
                "# PUBLISH\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\n"
                "purpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\ncloses: \"#16\"\n```\n\n"
                "| `Home.md` | Home |\n"
                "| `Overview.md` | Overview |\n"
                "| `Autonomy-Levels.md` | Autonomy-Levels |\n"
                "| `Repo-Stewardship.md` | Repo-Stewardship |\n"
                "| `Agent-Routing.md` | Agent-Routing |\n\n"
                "Do **not** push `PUBLISH.md`.\n"
                "Link Check and Markdown Lint. No secrets.\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Security-Boundaries.md",
        )


def test_wiki_rejects_missing_publish_routing_row() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = (
                "# PUBLISH\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\n"
                "purpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\ncloses: \"#16\"\n```\n\n"
                "| `Home.md` | Home |\n"
                "| `Overview.md` | Overview |\n"
                "| `Autonomy-Levels.md` | Autonomy-Levels |\n"
                "| `Repo-Stewardship.md` | Repo-Stewardship |\n"
                "| `Security-Boundaries.md` | Security-Boundaries |\n\n"
                "Do **not** push `PUBLISH.md`.\n"
                "Link Check and Markdown Lint. No secrets.\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Agent-Routing.md",
        )


def test_wiki_rejects_opencollective_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge opencollective chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_missing_actionlint_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = (
                "# Repo\n\n[← Home](Home.md)\n\n"
                "markdown-lint link-check stewardship-checks\n"
                "run_stewardship_checks.sh relative links badge\n"
                "no invent product\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "actionlint",
        )


def test_schema_rejects_empty_badge_edit_policy() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: x\nedit_policy: \"\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "must be non-empty",
        )


def test_schema_rejects_empty_claude_owner() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"\"\n"
                "surface: copilot\nautonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
                "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "must be non-empty",
        )


def test_schema_rejects_missing_agents_version_key() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing metadata keys",
        )


def test_schema_rejects_missing_backlog_edit_policy_key() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            backlog=(
                "# I\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing metadata keys",
        )


def test_schema_rejects_tier_zero() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 0\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "tier",
        )


def test_schema_rejects_empty_publish_created() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            publish=(
                "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"\"\npurpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "must be non-empty",
        )


def test_schema_rejects_wrong_badge_owner_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: geryon\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "owner",
        )


def test_schema_rejects_autonomy_three_ok_range_but_agents_expected() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 3\n```\n"
            ),
        )
        # autonomy 3 is in 0..3 range but EXPECTED_VALUES wants 1
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "autonomy_level",
        )


def test_common_secret_patterns_npm() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "npm_" + ("a" * 36)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("npm_ sample should match SECRET_PATTERNS")


def test_common_secret_patterns_aiza() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "AIza" + ("A" * 35)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("AIza sample should match SECRET_PATTERNS")


def test_common_secret_patterns_slack_xoxb() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "xoxb-" + ("a" * 12)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("xoxb- sample should match SECRET_PATTERNS")


def test_common_secret_url_hints_github_pat() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_URL_HINTS  # noqa: E402

    if "github_pat_" not in SECRET_URL_HINTS:
        raise AssertionError("SECRET_URL_HINTS missing github_pat_")


def test_common_forbidden_badge_hints_twitter_codecov() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import FORBIDDEN_BADGE_HINTS  # noqa: E402

    for hint in ("twitter", "codecov", "downloads", "discord"):
        if hint not in FORBIDDEN_BADGE_HINTS:
            raise AssertionError(f"FORBIDDEN_BADGE_HINTS missing {hint!r}")


def test_common_scan_secrets_github_pat() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import scan_secrets  # noqa: E402

    with tempfile.TemporaryDirectory() as tmp:
        path = Path(tmp) / "doc.md"
        path.write_text(
            "token github_pat_" + ("x" * 22) + "\n",
            encoding="utf-8",
        )
        errors: list[str] = []
        scan_secrets(path, errors, label="fixture.md")
        if not errors:
            raise AssertionError("github_pat_ should fail scan_secrets")


def test_common_dangerous_schemes_javascript() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402

    if has_dangerous_scheme("javascript:alert(1)") != "javascript:":
        raise AssertionError("javascript: scheme should be dangerous")


def test_stewardship_requires_actions_checkout_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("actions/checkout", "actions/noop")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "actions/checkout",
        )


def test_link_check_requires_verbose_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("--verbose", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "--verbose",
        )


def test_link_check_requires_no_progress_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("--no-progress", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "--no-progress",
        )


def test_markdown_lint_requires_config_needle_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(".markdownlint.json", ".mdlrc")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            ".markdownlint.json",
        )


def test_lycheeignore_rejects_https_star_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".lycheeignore"
        path.write_text("https://*\n", encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "https://*",
        )


def test_workflow_rejects_missing_pull_request_on_markdown_lint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace("pull_request:", "pull_req:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pull_request",
        )


def test_badge_rejects_producthunt_hint_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=producthunt)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_relative_links_accept_nested_image_existing() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        img = tmp_path / "docs" / "images" / "ok.png"
        img.parent.mkdir(parents=True, exist_ok=True)
        img.write_bytes(b"\x89PNG\r\n\x1a\n")
        _write(tmp_path / "docs" / "a.md", "# A\n\n![x](images/ok.png)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_wiki_rejects_data_scheme_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[x](data:text/plain,hi)\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_schema_rejects_missing_claude_last_updated_key() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"fuzzywigg (smtp.eth)\"\n"
                "surface: copilot\nautonomy_level: 1\n"
                "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing metadata keys",
        )


def test_common_has_dangerous_scheme_data() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402

    if has_dangerous_scheme("data:text/html,<b>") != "data:":
        raise AssertionError("data: scheme should be dangerous")



# --- TOKENMAXX deepen after #33: lycheeverse / cli2-action / line_length / edges ---


def test_link_check_requires_lycheeverse_action() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "lycheeverse/lychee-action", "other/lychee-action"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "lycheeverse/lychee-action",
        )


def test_markdown_lint_requires_cli2_action() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "markdownlint-cli2-action", "markdownlint-cli-action"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "markdownlint-cli2-action",
        )


def test_markdownlint_json_requires_md013_line_length() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text('{\n  "default": true,\n  "MD013": true\n}\n', encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "line_length",
        )


def test_stewardship_requires_pip_install_pyyaml() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("pip install", "npm install")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pip install",
        )


def test_actionlint_rejects_pull_request_target_on_markdown_lint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "pull_request:",
            "pull_request_target:",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pull_request_target",
        )


def test_actionlint_rejects_contents_write_on_link_check() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "contents: read",
            "contents: write",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "contents: write",
        )


def test_actionlint_rejects_write_all_on_link_check() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = "permissions: write-all\n" + path.read_text(encoding="utf-8")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "write-all",
        )


def test_actionlint_rejects_float_latest_on_checkout_link() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "actions/checkout@v7", "actions/checkout@latest"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must not float",
        )


def test_workflow_requires_cancel_in_progress_true_not_false_on_stewardship() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "cancel-in-progress: true",
            "cancel-in-progress: false",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "cancel-in-progress: true",
        )


def test_workflow_rejects_missing_concurrency_on_stewardship() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("concurrency:", "concurrency_off:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "concurrency",
        )


def test_workflow_rejects_missing_timeout_on_markdown_lint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace("timeout-minutes:", "timeout_off:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "timeout-minutes",
        )


def test_badge_rejects_coverage_hint_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=coverage)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_stars_hint_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=stars)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_token_query_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?token=abc)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Secret-like token",
        )


def test_badge_rejects_license_http_link() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(http://github.com/fuzzywigg/agents-governance/blob/main/LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must use https://",
        )


def test_badge_rejects_four_badges_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n"
            "[![Extra](https://img.shields.io/badge/extra-x-red)]"
            "(https://example.com)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "exactly 3 badges",
        )


def test_badge_doc_rejects_missing_fourth_refusal_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "docs" / "badge-standard.md"
        text = path.read_text(encoding="utf-8")
        text = text.replace("fourth", "extra").replace("intentionally", "maybe")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "fourth badge",
        )


def test_relative_links_accept_https_uppercase() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](HTTPS://example.com)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_accept_mailto_uppercase() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](MAILTO:a@example.com)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_file_mixed_case() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](File:///etc/passwd)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_github_slug_tilde() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from check_relative_links import github_slug  # noqa: E402

    # Tilde stripped: "Note~ one" → "note-one"
    if github_slug("Note~ one") != "note-one":
        raise AssertionError(f"github_slug Note~ one -> {github_slug('Note~ one')!r}")


def test_relative_links_accept_tilde_heading_fragment() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\n## Note~ one\n\nSee [x](#note-one).\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_missing_parent_file() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "a.md", "# A\n\n[x](../missing.md)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "broken relative link",
        )


def test_relative_links_accept_angle_bracket_mailto() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](<mailto:a@example.com>)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_protocol_relative_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](//example.com/x)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "protocol-relative",
        )


def test_wiki_rejects_missing_home_to_stewardship() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = (
                "# Home\n\n"
                "[README](https://github.com/fuzzywigg/agents-governance/blob/main/README.md)\n"
                "[Badge](../badge-standard.md)\n"
                "[Overview](Overview.md)\n"
                "[Autonomy-Levels](Autonomy-Levels.md)\n"
                "[Agent-Routing](Agent-Routing.md)\n"
                "[Security-Boundaries](Security-Boundaries.md)\n\n"
                "## Out of scope\n\nSecrets and invent product frameworks.\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Repo-Stewardship.md",
        )


def test_wiki_rejects_coveralls_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge coveralls chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_producthunt_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge producthunt chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_npm_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge npm/ chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_pypi_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge pypi/ chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_http_uppercase() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[x](HTTP://example.com)\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "insecure http://",
        )


def test_wiki_rejects_vbscript_scheme() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[x](vbscript:msgbox(1))\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_wiki_rejects_missing_publish_stewardship_row() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = (
                "# PUBLISH\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\n"
                "purpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\ncloses: \"#16\"\n```\n\n"
                "| `Home.md` | Home |\n"
                "| `Overview.md` | Overview |\n"
                "| `Autonomy-Levels.md` | Autonomy-Levels |\n"
                "| `Agent-Routing.md` | Agent-Routing |\n"
                "| `Security-Boundaries.md` | Security-Boundaries |\n\n"
                "Do **not** push `PUBLISH.md`.\n"
                "Link Check and Markdown Lint. No secrets.\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Repo-Stewardship.md",
        )


def test_wiki_rejects_missing_relative_hint_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = (
                "# Repo\n\n[← Home](Home.md)\n\n"
                "markdown-lint link-check stewardship-checks\n"
                "run_stewardship_checks.sh badge\n"
                "no invent product\n"
                "actionlint on existing workflow paths\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "relative-link",
        )


def test_schema_rejects_empty_badge_closes() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
                "closes: \"\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "must be non-empty",
        )


def test_schema_rejects_empty_agents_version() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "must be non-empty",
        )


def test_schema_rejects_missing_agents_autonomy_key() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing metadata keys",
        )


def test_schema_rejects_missing_claude_parent_key() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"fuzzywigg (smtp.eth)\"\n"
                "surface: copilot\nautonomy_level: 1\nlast_updated: \"2026-04-13\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing metadata keys",
        )


def test_schema_rejects_empty_claude_last_updated() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"fuzzywigg (smtp.eth)\"\n"
                "surface: copilot\nautonomy_level: 1\nlast_updated: \"\"\n"
                "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "must be non-empty",
        )


def test_schema_rejects_wrong_badge_scope_empty_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"   \"\nedit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "must be non-empty",
        )


def test_schema_rejects_tier_negative() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: -1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "tier",
        )


def test_schema_rejects_wrong_agents_maintainer_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: other.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "maintainer",
        )


def test_common_secret_patterns_ghs() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "ghs_" + ("a" * 36)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("ghs_ sample should match SECRET_PATTERNS")


def test_common_secret_patterns_ghu() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "ghu_" + ("a" * 36)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("ghu_ sample should match SECRET_PATTERNS")


def test_common_secret_patterns_rk_token() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "rk-" + ("a" * 36)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("rk- sample should match SECRET_PATTERNS")


def test_common_secret_patterns_aws_secret() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = 'aws_secret_access_key = "' + ("A" * 24) + '"'
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("aws_secret_access_key sample should match SECRET_PATTERNS")


def test_common_secret_patterns_openssh_key() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "-----BEGIN OPENSSH PRIVATE KEY-----\n"
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("OPENSSH PRIVATE KEY sample should match SECRET_PATTERNS")


def test_common_forbidden_badge_hints_coveralls_producthunt() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import FORBIDDEN_BADGE_HINTS  # noqa: E402

    for hint in ("coveralls", "producthunt", "npm/", "pypi/"):
        if hint not in FORBIDDEN_BADGE_HINTS:
            raise AssertionError(f"FORBIDDEN_BADGE_HINTS missing {hint!r}")


def test_common_scan_secrets_npm() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import scan_secrets  # noqa: E402

    with tempfile.TemporaryDirectory() as tmp:
        path = Path(tmp) / "doc.md"
        path.write_text("token npm_" + ("a" * 36) + "\n", encoding="utf-8")
        errors: list[str] = []
        scan_secrets(path, errors, label="fixture.md")
        if not errors:
            raise AssertionError("npm_ should fail scan_secrets")


def test_common_has_dangerous_scheme_file_still() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402

    if has_dangerous_scheme("FILE:///tmp/x") != "file:":
        raise AssertionError("FILE: scheme should be dangerous (casefold)")


def test_link_check_requires_lychee_action_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("lychee-action", "lychee-cli")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "lychee-action",
        )


def test_markdownlint_json_requires_md013_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text('{\n  "default": true\n}\n', encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "MD013",
        )


def test_markdown_lint_requires_actions_checkout_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace("actions/checkout", "actions/noop")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "actions/checkout",
        )


def test_link_check_requires_actions_checkout_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("actions/checkout", "actions/noop")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "actions/checkout",
        )


def test_stewardship_requires_pyyaml_install_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("pyyaml", "requests")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "PyYAML",
        )


def test_badge_rejects_discord_hint_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=discord)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_relative_links_reject_http_uppercase_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](HTTP://example.com)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "insecure http://",
        )


def test_wiki_rejects_javascript_scheme_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[x](javascript:alert(1))\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_schema_rejects_tier_zero_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 0\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "tier",
        )


def test_common_dangerous_schemes_vbscript_still() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402

    if has_dangerous_scheme("VBSCRIPT:msgbox") != "vbscript:":
        raise AssertionError("VBSCRIPT: scheme should be dangerous")



# --- TOKENMAXX deepen after #34: DavidAnson / --github-token / MD024 / edges ---


def test_markdown_lint_requires_davidanson_action() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "DavidAnson/markdownlint-cli2-action",
            "OtherOrg/markdownlint-cli2-action",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "DavidAnson/markdownlint-cli2-action",
        )


def test_link_check_requires_github_token_flag() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("--github-token", "--other-token")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "--github-token",
        )


def test_markdownlint_json_requires_md024() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": true,\n  "MD013": { "line_length": 200 }\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "MD024",
        )


def test_stewardship_requires_download_actionlint_bash() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "download-actionlint.bash",
            "fetch-actionlint.sh",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "download-actionlint.bash",
        )


def test_actionlint_rejects_pull_request_target_on_stewardship() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "pull_request:",
            "pull_request_target:",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pull_request_target",
        )


def test_actionlint_rejects_contents_write_on_markdown_lint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "contents: read",
            "contents: write",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "contents: write",
        )


def test_actionlint_rejects_write_all_on_markdown_lint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = "permissions: write-all\n" + path.read_text(encoding="utf-8")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "write-all",
        )


def test_actionlint_rejects_float_main_on_checkout_markdown() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "actions/checkout@v7", "actions/checkout@main"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must not float",
        )


def test_workflow_requires_cancel_in_progress_true_not_false_on_link_check() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "cancel-in-progress: true",
            "cancel-in-progress: false",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "cancel-in-progress: true",
        )


def test_workflow_rejects_missing_schedule_on_stewardship() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("schedule:", "schedule_off:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "schedule",
        )


def test_workflow_rejects_missing_workflow_dispatch_on_link_check() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "workflow_dispatch:", "workflow_dispatch_off:"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "workflow_dispatch",
        )


def test_badge_rejects_followers_hint_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=followers)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_forks_hint_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=forks)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_npm_hint_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/npm/v/fake)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_pypi_hint_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/pypi/v/fake)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_apikey_query_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?api_key=abc)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Secret-like token",
        )


def test_badge_rejects_link_check_http_image() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](http://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must be https://",
        )


def test_badge_rejects_blank_line_between_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "contiguous",
        )


def test_badge_doc_rejects_missing_three_max_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "docs" / "badge-standard.md"
        text = path.read_text(encoding="utf-8")
        text = text.replace("Three badges max", "Many badges ok").replace(
            "badges max", "badges plenty"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "three-badge",
        )


def test_relative_links_accept_tel_uppercase() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](TEL:+15551212)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_javascript_mixed_case_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](JavaScript:alert(1))\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_data_uppercase() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](DATA:text/plain,hi)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_github_slug_asterisk() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from check_relative_links import github_slug  # noqa: E402

    # Asterisk stripped: "Note* one" → "note-one"
    if github_slug("Note* one") != "note-one":
        raise AssertionError(f"github_slug Note* one -> {github_slug('Note* one')!r}")


def test_relative_links_accept_asterisk_heading_fragment() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\n## Note* one\n\nSee [x](#note-one).\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_broken_nested_docs() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "sub" / "a.md", "# A\n\n[x](../gone.md)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "broken relative link",
        )


def test_relative_links_accept_angle_bracket_https() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](<https://example.com>)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_empty_parens_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x]()\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "empty relative link",
        )


def test_relative_links_reject_percent_traversal_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "a.md", "# A\n\n[x](%2e%2e/%2e%2e/etc/passwd)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "escapes repo",
        )


def test_wiki_rejects_missing_home_to_security() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = (
                "# Home\n\n"
                "[README](https://github.com/fuzzywigg/agents-governance/blob/main/README.md)\n"
                "[Badge](../badge-standard.md)\n"
                "[Overview](Overview.md)\n"
                "[Autonomy-Levels](Autonomy-Levels.md)\n"
                "[Repo-Stewardship](Repo-Stewardship.md)\n"
                "[Agent-Routing](Agent-Routing.md)\n\n"
                "## Out of scope\n\nSecrets and invent product frameworks.\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Security-Boundaries.md",
        )


def test_wiki_rejects_buymeacoffee_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge buymeacoffee chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_opencollective_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge opencollective chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_codecov_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge codecov chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_downloads_badge_chrome() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge downloads chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_file_uppercase() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[x](FILE:///etc/passwd)\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_wiki_rejects_missing_publish_overview_row() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = pages["PUBLISH.md"].replace(
                "| `Overview.md` |", "| Overview missing |"
            ).replace("`Overview.md`", "Overview")

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Overview.md",
        )


def test_wiki_rejects_missing_actionlint_hint_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace(
                "actionlint", "workflow-lint"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "actionlint",
        )


def test_wiki_rejects_missing_invent_on_stewardship_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace(
                "invent", "productize"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent",
        )


def test_schema_rejects_empty_badge_owner() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
            "owner: \"\"\nscope: x\nedit_policy: \"do not invent product badges\"\n"
            "closes: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_empty_agents_scope() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
            "maintainer: smtp.eth\nscope: \"\"\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_missing_agents_version_key() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nlast_updated: \"2026-04-13\"\n"
            "maintainer: smtp.eth\nscope: repository-specific\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing metadata keys",
        )


def test_schema_rejects_missing_claude_surface_key() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        claude = (
            "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"fuzzywigg (smtp.eth)\"\n"
            "autonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
            "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, claude=claude)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing metadata keys",
        )


def test_schema_rejects_empty_publish_purpose() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        publish = (
            "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\npurpose: \"\"\n"
            "closes: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, publish=publish)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_wrong_badge_status_draft() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            "# B\n\n```yaml\nstatus: DRAFT\ntier: 1\ncreated: \"2026-09-13\"\n"
            "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
            "closes: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "ACTIVE",
        )


def test_schema_rejects_autonomy_four_oob() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
            "maintainer: smtp.eth\nscope: repository-specific\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 4\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "0..3",
        )


def test_schema_rejects_wrong_claude_repo_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        claude = (
            "# CLAUDE\n\n```yaml\nrepo: other-repo\nowner: \"fuzzywigg (smtp.eth)\"\n"
            "surface: copilot\nautonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
            "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, claude=claude)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "expected",
        )


def test_schema_rejects_closes_without_hash_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
            "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
            "closes: \"issue sixteen\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "closes must reference",
        )


def test_common_secret_patterns_ghr() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "ghr_" + ("a" * 36)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("ghr_ sample should match SECRET_PATTERNS")


def test_common_secret_patterns_gho() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "gho_" + ("a" * 36)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("gho_ sample should match SECRET_PATTERNS")


def test_common_secret_patterns_sk_token() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "sk-" + ("a" * 36)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("sk- sample should match SECRET_PATTERNS")


def test_common_secret_patterns_ec_key() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "-----BEGIN EC PRIVATE KEY-----\n"
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("EC PRIVATE KEY sample should match SECRET_PATTERNS")


def test_common_forbidden_badge_hints_followers_forks() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import FORBIDDEN_BADGE_HINTS  # noqa: E402

    for hint in ("followers", "forks", "discord", "stars"):
        if hint not in FORBIDDEN_BADGE_HINTS:
            raise AssertionError(f"FORBIDDEN_BADGE_HINTS missing {hint!r}")


def test_common_scan_secrets_aiza() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import scan_secrets  # noqa: E402

    with tempfile.TemporaryDirectory() as tmp:
        path = Path(tmp) / "doc.md"
        path.write_text("key AIza" + ("a" * 35) + "\n", encoding="utf-8")
        errors: list[str] = []
        scan_secrets(path, errors, label="fixture.md")
        if not errors:
            raise AssertionError("AIza should fail scan_secrets")


def test_common_has_dangerous_scheme_javascript_still() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402

    if has_dangerous_scheme("JAVASCRIPT:alert(1)") != "javascript:":
        raise AssertionError("JAVASCRIPT: scheme should be dangerous (casefold)")


def test_markdown_lint_requires_cli2_action_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "markdownlint-cli2-action", "markdownlint-cli-action"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "markdownlint-cli2-action",
        )


def test_link_check_requires_lycheeverse_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "lycheeverse/lychee-action", "other/lychee-action"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "lycheeverse/lychee-action",
        )


def test_markdownlint_json_requires_line_length_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": true,\n  "MD013": true,\n  "MD024": true\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "line_length",
        )


def test_stewardship_requires_pip_install_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("pip install", "npm install")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pip install",
        )


def test_badge_rejects_twitter_hint_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance?"
            "label=twitter)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_relative_links_reject_http_mixed_case_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](Http://example.com)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "insecure http://",
        )


def test_wiki_rejects_data_scheme_still_after_34() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[x](data:text/plain,hi)\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_schema_rejects_tier_zero_still_after_34() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            "# B\n\n```yaml\nstatus: ACTIVE\ntier: 0\ncreated: \"2026-09-13\"\n"
            "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
            "closes: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "positive int",
        )


def test_common_dangerous_schemes_data_still() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import DANGEROUS_LINK_SCHEMES  # noqa: E402

    if "data:" not in DANGEROUS_LINK_SCHEMES:
        raise AssertionError("DANGEROUS_LINK_SCHEMES missing data:")



def test_markdownlint_json_requires_siblings_only() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n'
            '  "MD024": true\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "siblings_only",
        )


def test_stewardship_requires_rhysd_actionlint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "rhysd/actionlint",
            "other/actionlint",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "rhysd/actionlint",
        )


def test_stewardship_requires_curl_download() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("curl", "wget")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "curl",
        )


def test_link_check_requires_action_token() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("token:", "auth_token:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "token:",
        )


def test_actionlint_rejects_pull_request_target_on_link_check_after_35() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "pull_request:",
            "pull_request_target:",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pull_request_target",
        )


def test_actionlint_rejects_contents_write_on_stewardship_after_35() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "contents: read",
            "contents: write",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "contents: write",
        )


def test_actionlint_rejects_write_all_on_link_check_after_35() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = "permissions: write-all\n" + path.read_text(encoding="utf-8")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "write-all",
        )


def test_actionlint_rejects_float_latest_on_checkout_stewardship() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "actions/checkout@v7", "actions/checkout@latest"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must not float",
        )


def test_workflow_requires_cancel_in_progress_true_not_false_on_markdown_lint() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "cancel-in-progress: true",
            "cancel-in-progress: false",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "cancel-in-progress: true",
        )


def test_workflow_rejects_missing_concurrency_on_link_check_after_35() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("concurrency:", "concurrency_off:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "concurrency",
        )


def test_workflow_rejects_missing_timeout_on_markdown_lint_after_35() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "timeout-minutes:", "timeout_minutes_off:"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "timeout-minutes",
        )


def test_badge_rejects_buymeacoffee_hint_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/badge/buymeacoffee-x-yellow)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_opencollective_hint_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/badge/opencollective-x-blue)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_coveralls_hint_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/coveralls/github/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_xcom_hint_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/badge/x.com-follow-black)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_client_secret_query_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?client_secret=abc)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Secret-like token",
        )


def test_badge_rejects_markdown_lint_http_image() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](http://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must be https://",
        )


def test_badge_rejects_four_badges_still_after_35() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n"
            "[![Stars](https://img.shields.io/github/stars/fuzzywigg/agents-governance)]"
            "(https://github.com/fuzzywigg/agents-governance)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "exactly 3 badges",
        )


def test_badge_doc_rejects_missing_fourth_refusal_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "docs" / "badge-standard.md"
        text = path.read_text(encoding="utf-8")
        text = text.replace("fourth", "extra").replace("intentionally", "maybe")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "fourth badge",
        )


def test_relative_links_accept_mailto_mixed_case() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](Mailto:a@b.co)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_vbscript_mixed_case_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](VbScript:msgbox(1))\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_file_uppercase_after_35() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](FILE:///etc/passwd)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_github_slug_underscore() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from check_relative_links import github_slug  # noqa: E402

    # Underscore emphasis marker stripped: "Foo_bar" → "foobar"
    if github_slug("Foo_bar") != "foobar":
        raise AssertionError(f"github_slug Foo_bar -> {github_slug('Foo_bar')!r}")


def test_relative_links_accept_underscore_heading_fragment() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\n## Foo_bar\n\nSee [x](#foobar).\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_broken_docs_sibling() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "a.md", "# A\n\n[x](missing-sibling.md)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "broken relative link",
        )


def test_relative_links_accept_angle_bracket_tel() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](<tel:+15551212>)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_bare_hash_only_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](#)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "empty relative link",
        )


def test_relative_links_reject_encoded_escape_nested_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(
            tmp_path / "docs" / "sub" / "a.md",
            "# A\n\n[x](%2e%2e/%2e%2e/%2e%2e/etc/passwd)\n",
        )
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "escapes repo",
        )


def test_wiki_rejects_missing_home_to_overview_after_35() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace(
                "[Overview](Overview.md)\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Overview.md",
        )


def test_wiki_rejects_discord_badge_chrome_after_35() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "discord badge chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_twitter_badge_chrome_after_35() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "twitter badge chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_stars_badge_chrome_after_35() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "stars badge chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_forks_badge_chrome_after_35() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "forks badge chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_javascript_uppercase() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[x](JAVASCRIPT:alert(1))\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_wiki_rejects_missing_publish_autonomy_row() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = pages["PUBLISH.md"].replace(
                "| `Autonomy-Levels.md` | Autonomy-Levels |\n", ""
            ).replace("`Autonomy-Levels.md`", "")

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Autonomy-Levels.md",
        )


def test_wiki_rejects_missing_ci_link_check_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace(
                "link-check", "link_check_off"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "link-check",
        )


def test_wiki_rejects_missing_badge_topic_on_stewardship_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace(
                "badge", "row"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "topic hint",
        )


def test_schema_rejects_empty_badge_scope() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
            "owner: copilot\nscope: \"\"\nedit_policy: \"do not invent product badges\"\n"
            "closes: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_empty_claude_owner() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        claude = (
            "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"\"\n"
            "surface: copilot\nautonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
            "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, claude=claude)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_missing_agents_maintainer_key() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
            "scope: repository-specific\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing metadata keys",
        )


def test_schema_rejects_missing_claude_repo_key() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        claude = (
            "# CLAUDE\n\n```yaml\nowner: \"fuzzywigg (smtp.eth)\"\n"
            "surface: copilot\nautonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
            "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, claude=claude)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing metadata keys",
        )


def test_schema_rejects_empty_publish_closes() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        publish = (
            "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\npurpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\n"
            "closes: \"\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, publish=publish)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_wrong_publish_status_draft() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        publish = (
            "# P\n\n```yaml\nstatus: DRAFT\ncreated: \"2026-09-13\"\npurpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\n"
            "closes: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, publish=publish)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "ACTIVE",
        )


def test_schema_rejects_autonomy_negative() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
            "maintainer: smtp.eth\nscope: repository-specific\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: -1\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "0..3",
        )


def test_schema_rejects_wrong_agents_scope_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
            "maintainer: smtp.eth\nscope: ecosystem-wide\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "repository-specific",
        )


def test_schema_rejects_semver_prerelease_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0-rc1\"\nlast_updated: \"2026-04-13\"\n"
            "maintainer: smtp.eth\nscope: repository-specific\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "semver",
        )


def test_common_secret_patterns_github_pat() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    blob = "token github_pat_" + ("A" * 22)
    if not any(p.search(blob) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missing github_pat_")


def test_common_secret_patterns_xoxb() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    blob = "xoxb-" + ("a" * 12)
    if not any(p.search(blob) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missing xoxb-")


def test_common_secret_patterns_rsa_key() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    blob = "-----BEGIN RSA PRIVATE KEY-----\nMII\n"
    if not any(p.search(blob) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missing RSA PRIVATE KEY")


def test_common_forbidden_badge_hints_buymeacoffee_opencollective() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import FORBIDDEN_BADGE_HINTS  # noqa: E402

    for hint in ("buymeacoffee", "opencollective"):
        if hint not in FORBIDDEN_BADGE_HINTS:
            raise AssertionError(f"FORBIDDEN_BADGE_HINTS missing {hint}")


def test_common_scan_secrets_xoxb() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS, fail  # noqa: E402

    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        path = tmp_path / "doc.md"
        path.write_text("# T\n\nxoxb-" + ("a" * 12) + "\n", encoding="utf-8")
        text = path.read_text(encoding="utf-8")
        errors: list[str] = []
        for pattern in SECRET_PATTERNS:
            if pattern.search(text):
                fail(
                    f"doc.md matches forbidden secret-like pattern: {pattern.pattern}",
                    errors,
                )
        if not errors:
            raise AssertionError("expected xoxb secret match")


def test_common_has_dangerous_scheme_vbscript_still() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402

    if has_dangerous_scheme("vbscript:msgbox(1)") != "vbscript:":
        raise AssertionError("has_dangerous_scheme vbscript failed")


def test_markdown_lint_requires_davidanson_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "DavidAnson/markdownlint-cli2-action",
            "Acme/markdownlint-cli2-action",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "DavidAnson/markdownlint-cli2-action",
        )


def test_link_check_requires_github_token_flag_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("--github-token", "--other-token")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "--github-token",
        )


def test_markdownlint_json_requires_md024_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": true,\n  "MD013": { "line_length": 200 }\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "MD024",
        )


def test_stewardship_requires_download_actionlint_bash_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "download-actionlint.bash",
            "fetch-actionlint.sh",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "download-actionlint.bash",
        )


def test_badge_rejects_producthunt_hint_still() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/badge/producthunt-x-red)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_relative_links_reject_http_scheme_still_after_35() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](http://example.com)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "insecure http://",
        )


def test_wiki_rejects_http_scheme_still_after_35() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[x](http://example.com)\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "insecure http://",
        )


def test_schema_rejects_tier_zero_still_after_35() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            "# B\n\n```yaml\nstatus: ACTIVE\ntier: 0\ncreated: \"2026-09-13\"\n"
            "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
            "closes: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "positive int",
        )


def test_common_dangerous_schemes_javascript_still() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import DANGEROUS_LINK_SCHEMES  # noqa: E402

    if "javascript:" not in DANGEROUS_LINK_SCHEMES:
        raise AssertionError("DANGEROUS_LINK_SCHEMES missing javascript:")


def test_lycheeignore_rejects_https_star_still_after_35() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".lycheeignore"
        path.write_text("https://*\n", encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must not exclude all http",
        )


def test_markdownlint_json_requires_line_length_200() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": true,\n  "MD013": { "line_length": 120 },\n'
            '  "MD024": { "siblings_only": true }\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "line_length: 200",
        )


def test_markdownlint_json_requires_siblings_only_true() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n'
            '  "MD024": { "siblings_only": false }\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "siblings_only: true",
        )


def test_stewardship_requires_raw_githubusercontent() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "raw.githubusercontent.com",
            "cdn.example.com",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "raw.githubusercontent.com",
        )


def test_stewardship_requires_curl_fssl() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("-fsSL", "-fSL")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "fsSL",
        )


def test_actionlint_rejects_pull_request_target_on_markdown_lint_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "pull_request:",
            "pull_request_target:",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pull_request_target",
        )


def test_actionlint_rejects_contents_write_on_link_check_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "contents: read",
            "contents: write",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "contents: write",
        )


def test_actionlint_rejects_write_all_on_stewardship_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = "permissions: write-all\n" + path.read_text(encoding="utf-8")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "write-all",
        )


def test_actionlint_rejects_float_main_on_checkout_link_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "actions/checkout@v7", "actions/checkout@main"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must not float",
        )


def test_workflow_requires_cancel_in_progress_true_not_false_on_stewardship_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "cancel-in-progress: true",
            "cancel-in-progress: false",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "cancel-in-progress: true",
        )


def test_workflow_rejects_missing_schedule_on_link_check_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("schedule:", "schedule_off:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "schedule",
        )


def test_workflow_rejects_missing_dispatch_on_stewardship_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "workflow_dispatch:", "workflow_dispatch_off:"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "workflow_dispatch",
        )


def test_badge_rejects_coverage_hint_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/badge/coverage-99-green)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_stars_hint_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/stars/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_discord_hint_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/discord/123456)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_npm_hint_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/npm/v/fake)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_token_query_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?token=abc)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Secret-like token",
        )


def test_badge_rejects_link_check_http_image_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](http://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must be https://",
        )


def test_badge_rejects_four_badges_still_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n"
            "[![Extra](https://img.shields.io/badge/extra-x-red)](https://example.com)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "exactly 3 badges",
        )


def test_badge_doc_rejects_missing_three_max_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "docs" / "badge-standard.md"
        text = path.read_text(encoding="utf-8")
        text = text.replace("Three badges max.", "Keep badges thin.")
        text = text.replace("three badges", "badges")
        text = text.replace("3 badges", "badges")
        text = text.replace("badges max", "badges")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "three-badge",
        )


def test_relative_links_accept_https_mixed_case_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](HtTpS://example.com)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_javascript_titlecase_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](Javascript:alert(1))\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_data_titlecase_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](Data:text/plain,hi)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_github_slug_hash_punct_after_36() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from check_relative_links import github_slug  # noqa: E402

    # Hash punctuation stripped: "Foo # Bar" → "foo--bar"
    got = github_slug("Foo # Bar")
    if got != "foo--bar":
        raise AssertionError(f"github_slug Foo # Bar -> {got!r}")


def test_relative_links_accept_hash_heading_fragment_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\n## Foo # Bar\n\nSee [x](#foo--bar).\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_broken_parent_docs_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "a.md", "# A\n\n[x](../missing.md)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "broken relative link",
        )


def test_relative_links_accept_angle_bracket_mailto_mixed_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](<MailTo:a@b.co>)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_empty_parens_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x]()\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "empty relative link",
        )


def test_relative_links_reject_protocol_relative_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](//evil.example/x)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "protocol-relative",
        )


def test_wiki_rejects_missing_home_to_routing_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace(
                "[Agent-Routing](Agent-Routing.md)\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Agent-Routing.md",
        )


def test_wiki_rejects_coveralls_badge_chrome_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "coveralls badge chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_producthunt_badge_chrome_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "producthunt badge chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_npm_badge_chrome_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge npm/ chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_pypi_badge_chrome_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge pypi/ chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_data_uppercase_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[x](DATA:text/plain,hi)\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_wiki_rejects_missing_publish_security_row_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = pages["PUBLISH.md"].replace(
                "| `Security-Boundaries.md` | Security-Boundaries |\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Security-Boundaries.md",
        )


def test_wiki_rejects_missing_actionlint_still_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace(
                "actionlint on existing workflow paths\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "actionlint",
        )


def test_wiki_rejects_missing_relative_hint_still_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace(
                "relative links", "offline links"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "relative",
        )


def test_schema_rejects_empty_badge_edit_policy_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
            "owner: copilot\nscope: x\nedit_policy: \"\"\ncloses: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_empty_agents_maintainer_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
            "maintainer: \"\"\nscope: repository-specific\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_missing_agents_parent_key_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
            "maintainer: smtp.eth\nscope: repository-specific\n"
            "autonomy_level: 1\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "parent_governance",
        )


def test_schema_rejects_missing_claude_autonomy_key_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        claude = (
            "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"fuzzywigg (smtp.eth)\"\n"
            "surface: copilot\nlast_updated: \"2026-04-13\"\n"
            "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, claude=claude)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "autonomy_level",
        )


def test_schema_rejects_empty_publish_created_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        publish = (
            "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"\"\npurpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\n"
            "closes: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, publish=publish)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_wrong_badge_status_draft_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            "# B\n\n```yaml\nstatus: DRAFT\ntier: 1\ncreated: \"2026-09-13\"\n"
            "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
            "closes: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "ACTIVE",
        )


def test_schema_rejects_autonomy_two_vs_expected_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
            "maintainer: smtp.eth\nscope: repository-specific\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 2\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "autonomy_level",
        )


def test_schema_rejects_wrong_claude_parent_still_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        claude = (
            "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"fuzzywigg (smtp.eth)\"\n"
            "surface: copilot\nautonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
            "parent_governance: github.com/other/repo\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, claude=claude)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "parent_governance",
        )


def test_schema_rejects_closes_without_hash_still_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
            "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
            "closes: \"issue 16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "closes",
        )


def test_common_secret_patterns_ghs_after_36() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "ghs_" + ("A" * 20)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missing ghs_")


def test_common_secret_patterns_ghu_after_36() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "ghu_" + ("B" * 20)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missing ghu_")


def test_common_secret_patterns_rk_after_36() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "rk-" + ("C" * 20)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missing rk-")


def test_common_secret_patterns_aws_after_36() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "aws_secret_access_key = '" + ("D" * 20) + "'"
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missing aws_secret_access_key")


def test_common_forbidden_badge_hints_coverage_stars_after_36() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import FORBIDDEN_BADGE_HINTS  # noqa: E402

    for hint in ("coverage", "stars"):
        if hint not in FORBIDDEN_BADGE_HINTS:
            raise AssertionError(f"FORBIDDEN_BADGE_HINTS missing {hint}")


def test_common_scan_secrets_ghs_after_36() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import scan_secrets  # noqa: E402

    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        target = tmp_path / "doc.md"
        target.write_text("# x\n\nghs_" + ("E" * 20) + "\n", encoding="utf-8")
        errors: list[str] = []
        # ROOT-relative label: scan_secrets uses path.relative_to(ROOT) by default;
        # pass label to avoid ROOT mismatch in temp trees.
        scan_secrets(target, errors, label="doc.md")
        if not errors:
            raise AssertionError("scan_secrets should flag ghs_")


def test_common_has_dangerous_scheme_file_still_after_36() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402

    if has_dangerous_scheme("FILE:///etc/passwd") != "file:":
        raise AssertionError("has_dangerous_scheme FILE: casefold failed")


def test_markdown_lint_requires_cli2_action_still_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "markdownlint-cli2-action", "markdownlint-action"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "markdownlint-cli2-action",
        )


def test_link_check_requires_lycheeverse_still_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "lycheeverse/lychee-action", "other/lychee-action"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "lycheeverse/lychee-action",
        )


def test_markdownlint_json_requires_siblings_only_still_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n'
            '  "MD024": true\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "siblings_only",
        )


def test_stewardship_requires_rhysd_still_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "rhysd/actionlint",
            "other/actionlint",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "rhysd/actionlint",
        )


def test_badge_rejects_twitter_hint_still_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/twitter/follow/x)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_relative_links_reject_http_scheme_still_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](http://example.com)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "insecure http://",
        )


def test_wiki_rejects_http_scheme_still_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[x](http://example.com)\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "insecure http://",
        )


def test_schema_rejects_tier_zero_still_after_36() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            "# B\n\n```yaml\nstatus: ACTIVE\ntier: 0\ncreated: \"2026-09-13\"\n"
            "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
            "closes: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "tier",
        )


def test_common_dangerous_schemes_data_still_after_36() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import DANGEROUS_LINK_SCHEMES  # noqa: E402

    if "data:" not in DANGEROUS_LINK_SCHEMES:
        raise AssertionError("DANGEROUS_LINK_SCHEMES missing data:")



def test_markdownlint_json_requires_default_true() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": false,\n  "MD013": { "line_length": 200 },\n'
            '  "MD024": { "siblings_only": true }\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "default: true",
        )


def test_stewardship_requires_get_actionlint_outputs() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "get_actionlint.outputs.executable",
            "actionlint",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "get_actionlint.outputs.executable",
        )


def test_stewardship_requires_actionlint_v177_path() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("/v1.7.7/", "/main/")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "/v1.7.7/",
        )


def test_link_check_requires_max_concurrency_8() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "--max-concurrency 8", "--max-concurrency 32"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "--max-concurrency 8",
        )


def test_link_check_requires_timeout_20() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("--timeout 20", "--timeout 60")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "--timeout 20",
        )


def test_link_check_requires_max_retries_3() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "--max-retries 3", "--max-retries 10"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "--max-retries 3",
        )


def test_actionlint_rejects_pull_request_target_on_stewardship_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "pull_request:",
            "pull_request_target:",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pull_request_target",
        )


def test_actionlint_rejects_contents_write_on_markdown_lint_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "contents: read",
            "contents: write",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "contents: write",
        )


def test_actionlint_rejects_write_all_on_link_check_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = "permissions: write-all\n" + path.read_text(encoding="utf-8")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "write-all",
        )


def test_actionlint_rejects_float_latest_on_checkout_stewardship_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "actions/checkout@v7", "actions/checkout@latest"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must not float",
        )


def test_workflow_requires_cancel_in_progress_true_not_false_on_link_check_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "cancel-in-progress: true",
            "cancel-in-progress: false",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "cancel-in-progress: true",
        )


def test_workflow_rejects_missing_concurrency_on_markdown_lint_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace("concurrency:", "concurrency_off:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "concurrency",
        )


def test_workflow_rejects_missing_timeout_on_stewardship_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "timeout-minutes:", "timeout_minutes_off:"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "timeout-minutes",
        )


def test_badge_rejects_codecov_hint_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/codecov/c/github/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_downloads_hint_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/downloads/fuzzywigg/agents-governance/total)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_followers_hint_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/followers/fuzzywigg)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_pypi_hint_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/pypi/v/fake)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_apikey_query_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?apikey=abc)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Secret-like token",
        )


def test_badge_rejects_markdown_lint_http_image_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](http://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must be https://",
        )


def test_badge_rejects_four_badges_still_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n"
            "[![Extra](https://img.shields.io/badge/extra-x-red)](https://example.com)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "exactly 3 badges",
        )


def test_badge_doc_rejects_missing_fourth_refusal_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "docs" / "badge-standard.md"
        text = path.read_text(encoding="utf-8")
        text = text.replace("fourth", "extra")
        text = text.replace("intentionally", "quietly")
        text = text.replace("not added", "deferred")
        text = text.replace("not** added", "deferred")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "fourth badge",
        )


def test_relative_links_accept_https_uppercase_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](HTTPS://example.com)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_vbscript_titlecase_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](VbScript:msgbox(1))\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_file_titlecase_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](File:///etc/passwd)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_github_slug_ampersand_after_37() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from check_relative_links import github_slug  # noqa: E402

    # Ampersand stripped: "Foo & Bar" → "foo--bar"
    got = github_slug("Foo & Bar")
    if got != "foo--bar":
        raise AssertionError(f"github_slug Foo & Bar -> {got!r}")


def test_relative_links_accept_ampersand_heading_fragment_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\n## Foo & Bar\n\nSee [x](#foo--bar).\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_broken_sibling_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "a.md", "# A\n\n[x](missing.md)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "broken relative link",
        )


def test_relative_links_accept_angle_bracket_tel_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](<tel:+15551212>)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_bare_hash_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](#)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "empty relative link",
        )


def test_relative_links_reject_nested_dotdot_escape_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "a.md", "# A\n\n[x](%2e%2e/missing.md)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "broken relative link",
        )


def test_wiki_rejects_missing_home_to_overview_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace(
                "[Overview](Overview.md)\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Overview.md",
        )


def test_wiki_rejects_discord_badge_chrome_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "discord badge chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_twitter_badge_chrome_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "twitter badge chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_stars_badge_chrome_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "stars badge chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_forks_badge_chrome_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "forks badge chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_javascript_uppercase_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[x](JAVASCRIPT:alert(1))\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_wiki_rejects_missing_publish_autonomy_row_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = pages["PUBLISH.md"].replace(
                "| `Autonomy-Levels.md` | Autonomy-Levels |\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Autonomy-Levels.md",
        )


def test_wiki_rejects_missing_ci_hint_still_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace(
                "markdown-lint", "md-quality"
            ).replace(
                "link-check", "url-scan"
            ).replace(
                "stewardship-checks", "quiet-gates"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "markdown-lint",
        )


def test_wiki_rejects_missing_badge_topic_still_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace(
                "badge", "status-chip"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "badge",
        )


def test_schema_rejects_empty_badge_scope_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
            "owner: copilot\nscope: \"\"\nedit_policy: \"do not invent product badges\"\n"
            "closes: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_empty_agents_scope_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
            "maintainer: smtp.eth\nscope: \"\"\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_missing_agents_maintainer_key_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
            "scope: repository-specific\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "maintainer",
        )


def test_schema_rejects_missing_claude_repo_key_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        claude = (
            "# CLAUDE\n\n```yaml\nowner: \"fuzzywigg (smtp.eth)\"\n"
            "surface: copilot\nautonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
            "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, claude=claude)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "repo",
        )


def test_schema_rejects_empty_publish_closes_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        publish = (
            "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\npurpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\n"
            "closes: \"\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, publish=publish)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_wrong_publish_status_draft_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        publish = (
            "# P\n\n```yaml\nstatus: DRAFT\ncreated: \"2026-09-13\"\npurpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\n"
            "closes: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, publish=publish)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "ACTIVE",
        )


def test_schema_rejects_autonomy_negative_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
            "maintainer: smtp.eth\nscope: repository-specific\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: -1\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "autonomy_level",
        )


def test_schema_rejects_wrong_agents_scope_still_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
            "maintainer: smtp.eth\nscope: ecosystem-wide\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "repository-specific",
        )


def test_schema_rejects_semver_prerelease_still_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0-rc1\"\nlast_updated: \"2026-04-13\"\n"
            "maintainer: smtp.eth\nscope: repository-specific\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "semver",
        )


def test_common_secret_patterns_github_pat_after_37() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "github_pat_" + ("A" * 20)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missing github_pat_")


def test_common_secret_patterns_xoxb_after_37() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "xoxb-" + ("B" * 12)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missing xoxb-")


def test_common_secret_patterns_rsa_key_after_37() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "-----BEGIN RSA PRIVATE KEY-----\nAAAA\n"
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missing RSA PRIVATE KEY")


def test_common_forbidden_badge_hints_buymeacoffee_opencollective_after_37() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import FORBIDDEN_BADGE_HINTS  # noqa: E402

    for hint in ("buymeacoffee", "opencollective"):
        if hint not in FORBIDDEN_BADGE_HINTS:
            raise AssertionError(f"FORBIDDEN_BADGE_HINTS missing {hint}")


def test_common_scan_secrets_xoxb_after_37() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import scan_secrets  # noqa: E402

    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        target = tmp_path / "doc.md"
        target.write_text("# x\n\nxoxb-" + ("C" * 12) + "\n", encoding="utf-8")
        errors: list[str] = []
        scan_secrets(target, errors, label="doc.md")
        if not errors:
            raise AssertionError("scan_secrets should flag xoxb-")


def test_common_has_dangerous_scheme_vbscript_still_after_37() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402

    if has_dangerous_scheme("VBSCRIPT:msgbox(1)") != "vbscript:":
        raise AssertionError("has_dangerous_scheme VBSCRIPT: casefold failed")


def test_markdown_lint_requires_davidanson_still_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "DavidAnson/markdownlint-cli2-action", "Other/markdownlint-cli2-action"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "DavidAnson/markdownlint-cli2-action",
        )


def test_link_check_requires_github_token_flag_still_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("--github-token", "--token")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "--github-token",
        )


def test_markdownlint_json_requires_line_length_200_still_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": true,\n  "MD013": { "line_length": 80 },\n'
            '  "MD024": { "siblings_only": true }\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "line_length: 200",
        )


def test_stewardship_requires_raw_githubusercontent_still_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "raw.githubusercontent.com",
            "cdn.example.com",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "raw.githubusercontent.com",
        )


def test_badge_rejects_coveralls_hint_still_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/coveralls/github/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_relative_links_reject_http_scheme_still_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](http://example.com)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "insecure http://",
        )


def test_wiki_rejects_http_scheme_still_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[x](http://example.com)\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "insecure http://",
        )


def test_schema_rejects_tier_zero_still_after_37() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            "# B\n\n```yaml\nstatus: ACTIVE\ntier: 0\ncreated: \"2026-09-13\"\n"
            "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
            "closes: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "positive int",
        )


def test_markdownlint_json_requires_md033_false() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n'
            '  "MD024": { "siblings_only": true },\n'
            '  "MD033": true,\n  "MD041": false,\n  "MD060": false\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "MD033: false",
        )


def test_markdownlint_json_requires_md041_false() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n'
            '  "MD024": { "siblings_only": true },\n'
            '  "MD033": false,\n  "MD041": true,\n  "MD060": false\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "MD041: false",
        )


def test_markdownlint_json_requires_md060_false() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n'
            '  "MD024": { "siblings_only": true },\n'
            '  "MD033": false,\n  "MD041": false,\n  "MD060": true\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "MD060: false",
        )


def test_markdown_lint_requires_cli2_action_v24() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "DavidAnson/markdownlint-cli2-action@v24",
            "DavidAnson/markdownlint-cli2-action@v20",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "markdownlint-cli2-action@v24",
        )


def test_stewardship_requires_setup_python_v5() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "actions/setup-python@v5",
            "actions/setup-python@v4",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "setup-python@v5",
        )


def test_stewardship_requires_get_actionlint_id() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "id: get_actionlint",
            "id: download_actionlint",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "id: get_actionlint",
        )

def test_actionlint_rejects_pull_request_target_on_markdown_lint_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "pull_request:",
            "pull_request_target:",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pull_request_target",
        )


def test_actionlint_rejects_contents_write_on_link_check_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "contents: read",
            "contents: write",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "contents: write",
        )


def test_actionlint_rejects_write_all_on_stewardship_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = "permissions: write-all\n" + path.read_text(encoding="utf-8")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "write-all",
        )


def test_actionlint_rejects_float_main_on_checkout_link_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "actions/checkout@v7", "actions/checkout@main"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must not float",
        )


def test_workflow_requires_cancel_in_progress_true_not_false_on_stewardship_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "cancel-in-progress: true",
            "cancel-in-progress: false",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "cancel-in-progress: true",
        )


def test_workflow_rejects_missing_schedule_on_link_check_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("schedule:", "schedule_off:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "schedule",
        )


def test_workflow_rejects_missing_dispatch_on_stewardship_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "workflow_dispatch:", "workflow_dispatch_off:"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "workflow_dispatch",
        )


def test_badge_rejects_coverage_hint_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/badge/coverage-99-green)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_stars_hint_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/stars/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_discord_hint_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/discord/123456)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_npm_hint_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/npm/v/fake)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_token_query_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?token=abc)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Secret-like token",
        )


def test_badge_rejects_link_check_http_image_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](http://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must be https://",
        )


def test_badge_rejects_four_badges_still_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n"
            "[![Extra](https://img.shields.io/badge/extra-x-red)](https://example.com)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "exactly 3 badges",
        )


def test_badge_doc_rejects_missing_three_max_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "docs" / "badge-standard.md"
        text = path.read_text(encoding="utf-8")
        text = text.replace("Three badges max.", "Keep badges thin.")
        text = text.replace("three badges", "badges")
        text = text.replace("3 badges", "badges")
        text = text.replace("badges max", "badges")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "three-badge",
        )


def test_relative_links_accept_https_mixed_case_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](HtTpS://example.com)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_javascript_titlecase_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](Javascript:alert(1))\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_data_titlecase_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](Data:text/plain,hi)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_github_slug_hash_punct_after_38() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from check_relative_links import github_slug  # noqa: E402

    # Hash punctuation stripped: "Foo # Bar" → "foo--bar"
    got = github_slug("Foo # Bar")
    if got != "foo--bar":
        raise AssertionError(f"github_slug Foo # Bar -> {got!r}")


def test_relative_links_accept_hash_heading_fragment_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\n## Foo # Bar\n\nSee [x](#foo--bar).\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_broken_parent_docs_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "a.md", "# A\n\n[x](../missing.md)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "broken relative link",
        )


def test_relative_links_accept_angle_bracket_mailto_mixed_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](<MailTo:a@b.co>)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_empty_parens_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x]()\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "empty relative link",
        )


def test_relative_links_reject_protocol_relative_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](//evil.example/x)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "protocol-relative",
        )


def test_wiki_rejects_missing_home_to_routing_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace(
                "[Agent-Routing](Agent-Routing.md)\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Agent-Routing.md",
        )


def test_wiki_rejects_coveralls_badge_chrome_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "coveralls badge chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_producthunt_badge_chrome_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "producthunt badge chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_npm_badge_chrome_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge npm/ chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_pypi_badge_chrome_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "badge pypi/ chrome\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent-product",
        )


def test_wiki_rejects_data_uppercase_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = (
                "# O\n\n[Home](Home.md)\n\ngovernance public\n"
                "[x](DATA:text/plain,hi)\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_wiki_rejects_missing_publish_security_row_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = pages["PUBLISH.md"].replace(
                "| `Security-Boundaries.md` | Security-Boundaries |\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Security-Boundaries.md",
        )


def test_wiki_rejects_missing_actionlint_still_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace(
                "actionlint on existing workflow paths\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "actionlint",
        )


def test_wiki_rejects_missing_relative_hint_still_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace(
                "relative links", "offline links"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "relative",
        )


def test_schema_rejects_empty_badge_edit_policy_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
            "owner: copilot\nscope: x\nedit_policy: \"\"\ncloses: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_empty_agents_maintainer_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
            "maintainer: \"\"\nscope: repository-specific\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_missing_agents_parent_key_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
            "maintainer: smtp.eth\nscope: repository-specific\n"
            "autonomy_level: 1\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "parent_governance",
        )


def test_schema_rejects_missing_claude_autonomy_key_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        claude = (
            "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"fuzzywigg (smtp.eth)\"\n"
            "surface: copilot\nlast_updated: \"2026-04-13\"\n"
            "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, claude=claude)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "autonomy_level",
        )


def test_schema_rejects_empty_publish_created_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        publish = (
            "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"\"\npurpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\n"
            "closes: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, publish=publish)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_wrong_badge_status_draft_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            "# B\n\n```yaml\nstatus: DRAFT\ntier: 1\ncreated: \"2026-09-13\"\n"
            "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
            "closes: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "ACTIVE",
        )


def test_schema_rejects_autonomy_two_vs_expected_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
            "maintainer: smtp.eth\nscope: repository-specific\n"
            "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 2\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "autonomy_level",
        )


def test_schema_rejects_wrong_claude_parent_still_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        claude = (
            "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"fuzzywigg (smtp.eth)\"\n"
            "surface: copilot\nautonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
            "parent_governance: github.com/other/repo\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, claude=claude)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "parent_governance",
        )


def test_schema_rejects_closes_without_hash_still_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
            "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
            "closes: \"issue 16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "closes",
        )


def test_common_secret_patterns_ghs_after_38() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "ghs_" + ("A" * 20)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missing ghs_")


def test_common_secret_patterns_ghu_after_38() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "ghu_" + ("B" * 20)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missing ghu_")


def test_common_secret_patterns_rk_after_38() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "rk-" + ("C" * 20)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missing rk-")


def test_common_secret_patterns_aws_after_38() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402

    sample = "aws_secret_access_key = '" + ("D" * 20) + "'"
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missing aws_secret_access_key")


def test_common_forbidden_badge_hints_coverage_stars_after_38() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import FORBIDDEN_BADGE_HINTS  # noqa: E402

    for hint in ("coverage", "stars"):
        if hint not in FORBIDDEN_BADGE_HINTS:
            raise AssertionError(f"FORBIDDEN_BADGE_HINTS missing {hint}")


def test_common_scan_secrets_ghs_after_38() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import scan_secrets  # noqa: E402

    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        target = tmp_path / "doc.md"
        target.write_text("# x\n\nghs_" + ("E" * 20) + "\n", encoding="utf-8")
        errors: list[str] = []
        # ROOT-relative label: scan_secrets uses path.relative_to(ROOT) by default;
        # pass label to avoid ROOT mismatch in temp trees.
        scan_secrets(target, errors, label="doc.md")
        if not errors:
            raise AssertionError("scan_secrets should flag ghs_")


def test_common_has_dangerous_scheme_file_still_after_38() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402

    if has_dangerous_scheme("FILE:///etc/passwd") != "file:":
        raise AssertionError("has_dangerous_scheme FILE: casefold failed")



def test_markdownlint_json_requires_default_true_still_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": false,\n  "MD013": { "line_length": 200 },\n'
            '  "MD024": { "siblings_only": true },\n'
            '  "MD033": false,\n  "MD041": false,\n  "MD060": false\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "default: true",
        )


def test_link_check_requires_max_concurrency_8_still_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "--max-concurrency 8", "--max-concurrency 16"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "--max-concurrency 8",
        )


def test_stewardship_requires_actionlint_v177_path_still_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("/v1.7.7/", "/v1.7.6/")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "/v1.7.7/",
        )


def test_stewardship_requires_get_actionlint_outputs_still_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "get_actionlint.outputs.executable",
            "actionlint",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "get_actionlint.outputs.executable",
        )


def test_badge_rejects_codecov_hint_still_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://codecov.io/gh/fuzzywigg/agents-governance/branch/main/graph/badge.svg)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_relative_links_reject_http_scheme_still_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](http://example.com)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "insecure http://",
        )


def test_schema_rejects_tier_zero_still_after_38() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            "# B\n\n```yaml\nstatus: ACTIVE\ntier: 0\ncreated: \"2026-09-13\"\n"
            "owner: copilot\nscope: \"public governance front-door repos\"\nedit_policy: \"do not invent product badges\"\n"
            "closes: \"#16\"\n```\n"
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "positive int",
        )


# --- TOKENMAXX deepen after #39: CI workflow supply-chain / job contract pins ---

def test_link_check_requires_checkout_v7() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            'actions/checkout@v7',
            'actions/checkout@v4',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'checkout@v7',
        )

def test_markdown_lint_requires_checkout_v7() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            'actions/checkout@v7',
            'actions/checkout@v4',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'checkout@v7',
        )

def test_stewardship_requires_checkout_v7() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            'actions/checkout@v7',
            'actions/checkout@v4',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'checkout@v7',
        )

def test_link_check_requires_lychee_action_v2() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            'lycheeverse/lychee-action@v2',
            'lycheeverse/lychee-action@v1',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'lychee-action@v2',
        )

def test_link_check_requires_timeout_minutes_20() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            'timeout-minutes: 20',
            'timeout-minutes: 10',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'timeout-minutes: 20',
        )

def test_markdown_lint_requires_timeout_minutes_10() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            'timeout-minutes: 10',
            'timeout-minutes: 20',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'timeout-minutes: 10',
        )

def test_stewardship_requires_timeout_minutes_15() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            'timeout-minutes: 15',
            'timeout-minutes: 10',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'timeout-minutes: 15',
        )

def test_link_check_requires_cron_0_6() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            'cron: "0 6 * * 1"',
            'cron: "0 7 * * 1"',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'cron: "0 6 * * 1"',
        )

def test_markdown_lint_requires_cron_30_6() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            'cron: "30 6 * * 1"',
            'cron: "30 7 * * 1"',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'cron: "30 6 * * 1"',
        )

def test_stewardship_requires_cron_15_6() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            'cron: "15 6 * * 1"',
            'cron: "15 7 * * 1"',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'cron: "15 6 * * 1"',
        )

def test_link_check_requires_ubuntu_latest() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            'ubuntu-latest',
            'ubuntu-22.04',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ubuntu-latest',
        )

def test_markdown_lint_requires_ubuntu_latest() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            'ubuntu-latest',
            'ubuntu-22.04',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ubuntu-latest',
        )

def test_stewardship_requires_ubuntu_latest() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            'ubuntu-latest',
            'ubuntu-22.04',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ubuntu-latest',
        )

def test_stewardship_requires_pip_quiet() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            'pip install --quiet pyyaml',
            'pip install pyyaml',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '--quiet',
        )

def test_stewardship_requires_shell_bash() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            'shell: bash',
            'shell: sh',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'shell: bash',
        )

def test_stewardship_requires_actionlint_color() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            'get_actionlint.outputs.executable }} -color ',
            'get_actionlint.outputs.executable }} ',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '-color',
        )

def test_actionlint_rejects_pull_request_target_on_link_check_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            'pull_request:',
            'pull_request_target:',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'pull_request_target',
        )

def test_actionlint_rejects_contents_write_on_markdown_lint_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            'contents: read',
            'contents: write',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'contents: write',
        )

def test_actionlint_rejects_float_main_on_checkout_stewardship_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            'actions/checkout@v7',
            'actions/checkout@main',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'must not float',
        )

def test_actionlint_rejects_float_latest_on_checkout_markdown_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            'actions/checkout@v7',
            'actions/checkout@latest',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'must not float',
        )

def test_actionlint_rejects_float_master_on_checkout_link_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            'actions/checkout@v7',
            'actions/checkout@master',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'must not float',
        )

def test_actionlint_rejects_unpinned_checkout_on_link_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            'actions/checkout@v7',
            'actions/checkout',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'unpinned action',
        )

def test_actionlint_rejects_unpinned_lychee_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            'lycheeverse/lychee-action@v2',
            'lycheeverse/lychee-action',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'unpinned action',
        )

def test_workflow_requires_cancel_in_progress_true_not_false_on_markdown_lint_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            'cancel-in-progress: true',
            'cancel-in-progress: false',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'cancel-in-progress: true',
        )

def test_workflow_rejects_missing_schedule_on_markdown_lint_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            'schedule:',
            'sched_off:',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'weekly schedule',
        )

def test_workflow_rejects_missing_dispatch_on_link_check_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            'workflow_dispatch:',
            '',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'workflow_dispatch',
        )

def test_workflow_rejects_missing_concurrency_on_stewardship_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            'concurrency:',
            'group_off:',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'concurrency',
        )

def test_workflow_rejects_missing_permissions_read_on_markdown_lint_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            'contents: read',
            'contents: none',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'contents: read',
        )

def test_actionlint_rejects_contents_write_on_stewardship_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            'contents: read',
            'contents: write',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'contents: write',
        )

def test_actionlint_rejects_pull_request_target_on_stewardship_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            'pull_request:',
            'pull_request_target:',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'pull_request_target',
        )

def test_workflow_requires_cancel_in_progress_true_not_false_on_link_check_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            'cancel-in-progress: true',
            'cancel-in-progress: false',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'cancel-in-progress: true',
        )

def test_workflow_rejects_missing_timeout_on_link_check_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            'timeout-minutes: 20',
            '',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'timeout-minutes',
        )

def test_actionlint_rejects_write_all_on_link_check_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = 'permissions: write-all\n' + path.read_text(encoding="utf-8")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'write-all',
        )

def test_actionlint_rejects_write_all_on_stewardship_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = 'permissions: write-all\n' + path.read_text(encoding="utf-8")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'write-all',
        )

def test_link_check_requires_fail_true_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            'fail: true',
            'fail: false',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'fail: true',
        )

def test_link_check_requires_exclude_loopback_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            '--exclude-loopback',
            '--exclude-mail',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'loopback',
        )

def test_link_check_requires_github_token_flag_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            '--github-token',
            '--git-token',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '--github-token',
        )

def test_link_check_requires_action_token_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            'token:',
            'auth_token:',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'token:',
        )

def test_link_check_requires_max_concurrency_8_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            '--max-concurrency 8',
            '--max-concurrency 4',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '--max-concurrency 8',
        )

def test_link_check_requires_timeout_20_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            '--timeout 20',
            '--timeout 30',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '--timeout 20',
        )

def test_link_check_requires_max_retries_3_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            '--max-retries 3',
            '--max-retries 5',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '--max-retries 3',
        )

def test_markdown_lint_requires_cli2_action_v24_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            'DavidAnson/markdownlint-cli2-action@v24',
            'DavidAnson/markdownlint-cli2-action@v20',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'markdownlint-cli2-action@v24',
        )

def test_markdown_lint_requires_davidanson_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            'DavidAnson/markdownlint-cli2-action@v24',
            'other/markdownlint-cli2-action@v24',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'DavidAnson/markdownlint-cli2-action',
        )

def test_markdown_lint_requires_config_needle_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            '.markdownlint.json',
            '.markdownlint.yml',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '.markdownlint.json',
        )

def test_markdown_lint_requires_owasp_exclude_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            'OWASP-AGENTIC.md',
            'OTHER-AGENTIC.md',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'OWASP-AGENTIC.md',
        )

def test_markdown_lint_requires_agents_exclude_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            '.github/agents',
            '.github/prompts',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '.github/agents',
        )

def test_markdownlint_json_requires_md033_false_still_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n  "MD024": { "siblings_only": true },\n  "MD033": true,\n  "MD041": false,\n  "MD060": false\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD033: false',
        )

def test_markdownlint_json_requires_md041_false_still_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n  "MD024": { "siblings_only": true },\n  "MD033": false,\n  "MD041": true,\n  "MD060": false\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD041: false',
        )

def test_markdownlint_json_requires_md060_false_still_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n  "MD024": { "siblings_only": true },\n  "MD033": false,\n  "MD041": false,\n  "MD060": true\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD060: false',
        )

def test_markdownlint_json_requires_line_length_200_still_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": true,\n  "MD013": { "line_length": 120 },\n  "MD024": { "siblings_only": true },\n  "MD033": false,\n  "MD041": false,\n  "MD060": false\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'line_length: 200',
        )

def test_markdownlint_json_requires_default_true_still_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        path.write_text(
            '{\n  "default": false,\n  "MD013": { "line_length": 200 },\n  "MD024": { "siblings_only": true },\n  "MD033": false,\n  "MD041": false,\n  "MD060": false\n}\n',
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'default: true',
        )

def test_stewardship_requires_setup_python_v5_still_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            'actions/setup-python@v5',
            'actions/setup-python@v4',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'setup-python@v5',
        )

def test_stewardship_requires_get_actionlint_id_still_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            'id: get_actionlint',
            'id: download_actionlint',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'id: get_actionlint',
        )

def test_stewardship_requires_actionlint_v177_path_still_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            '/v1.7.7/',
            '/v1.7.6/',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '/v1.7.7/',
        )

def test_stewardship_requires_get_actionlint_outputs_still_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            'get_actionlint.outputs.executable',
            'steps.actionlint.outputs.path',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'get_actionlint.outputs.executable',
        )

def test_stewardship_requires_python_312_pin_still_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            '"3.12"',
            '"3.11"',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '3.12',
        )

def test_stewardship_requires_rhysd_still_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            'rhysd/actionlint',
            'other/actionlint',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'rhysd/actionlint',
        )

def test_stewardship_requires_curl_fssl_still_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            'curl -fsSL',
            'curl -sL',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'fsSL',
        )

def test_stewardship_requires_raw_githubusercontent_still_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            'raw.githubusercontent.com',
            'cdn.example.com',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'raw.githubusercontent.com',
        )

def test_lycheeignore_rejects_https_star_still_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".lycheeignore"
        path.write_text('https://img\\.shields\\.io\nhttps://*\n', encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'https://*',
        )

def test_lycheeignore_rejects_http_star_still_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".lycheeignore"
        path.write_text('https://img\\.shields\\.io\nhttp://*\n', encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'http://*',
        )

def test_lycheeignore_requires_shields_exclude_still_after_39() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".lycheeignore"
        path.write_text('# no shields\n', encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'img.shields.io',
        )


# --- TOKENMAXX deepen after #41: relative-link gate contract / fail-closed pins ---


def test_relative_gate_requires_owasp_skip_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts/check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace('OWASP-AGENTIC.md', 'NOT_A_SKIP_FILE.md')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'OWASP-AGENTIC.md',
        )


def test_relative_gate_requires_agents_skip_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts/check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace('agents', 'XXXXXX')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'agents',
        )


def test_relative_gate_requires_node_modules_skip_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts/check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace('node_modules', 'node_mods')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'node_modules',
        )


def test_relative_gate_requires_strip_fenced_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts/check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace('strip_fenced_code', 'strip_fenced_X')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'fenced code',
        )


def test_relative_gate_requires_fully_unquote_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts/check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace('fully_unquote', 'fully_decode')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'fully_unquote',
        )


def test_relative_gate_requires_github_slug_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts/check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace('github_slug', 'heading_slug')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'github_slug',
        )


def test_relative_gate_requires_dangerous_scheme_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts/check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace('has_dangerous_scheme', 'has_risky_scheme')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'dangerous',
        )


def test_relative_gate_requires_empty_fragment_needle_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace('empty fragment', 'blank fragment')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'empty path#',
        )


def test_relative_gate_requires_query_string_needle_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts/check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace('query string', 'query param')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'query string',
        )


def test_relative_gate_requires_http_reject_needle_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts/check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace('http://', 'hxxp://')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'http://',
        )


def test_run_stewardship_requires_relative_gate_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts/run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('check_relative_links.py', 'check_rel_links.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_relative_links.py',
        )


def test_run_stewardship_requires_badge_gate_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts/run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('check_badge_standard.py', 'check_badge_std.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_badge_standard.py',
        )


def test_run_stewardship_requires_wiki_gate_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts/run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('check_wiki_outline.py', 'check_wiki_out.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_wiki_outline.py',
        )


def test_run_stewardship_requires_schema_gate_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts/run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('check_stewardship_schema.py', 'check_stew_schema.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_stewardship_schema.py',
        )


def test_agents_requires_relative_mention_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "AGENTS.md"
        text = path.read_text(encoding="utf-8").replace('relative', 'relational')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'relative',
        )


def test_run_stewardship_requires_gate_order_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        path.write_text(
            "#!/usr/bin/env bash\n"
            "set -euo pipefail\n"
            "python3 scripts/check_badge_standard.py\n"
            "python3 scripts/check_wiki_outline.py\n"
            "python3 scripts/check_relative_links.py\n"
            "python3 scripts/check_stewardship_schema.py\n",
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "badge → wiki → schema → relative",
        )


def test_relative_gate_file_must_exist_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        (tmp_path / "scripts" / "check_relative_links.py").unlink()
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "check_relative_links.py",
        )


def test_run_stewardship_file_must_exist_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        (tmp_path / "scripts" / "run_stewardship_checks.sh").unlink()
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "run_stewardship_checks.sh",
        )


def test_relative_gate_requires_atx_needle_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace('ATX_HEADING_RE', 'MARKDOWN_HEADING_RE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ATX',
        )


def test_relative_gate_requires_git_skip_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace('".git"', '".svn"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '.git',
        )


def test_relative_links_reject_empty_path_fragment_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](README.md#)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'empty fragment',
        )


def test_relative_links_reject_empty_path_fragment_nested_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](docs/a.md#)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'empty fragment',
        )


def test_relative_links_reject_query_string_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](README.md?x=1)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'query string',
        )


def test_relative_links_reject_query_before_fragment_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](README.md?x=1#t)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'query string',
        )


def test_relative_links_reject_double_encoded_escape_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](%252e%252e/%252e%252e/etc/passwd)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'escapes repo',
        )


def test_relative_links_reject_abs_unix_escape_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](/etc/passwd)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'escapes repo',
        )


def test_relative_links_accept_https_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[ok](https://example.com/x)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_accept_mailto_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[m](mailto:ops@example.com)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_accept_tel_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[t](tel:+15551212)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_protocol_relative_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](//evil.example/x)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'protocol-relative',
        )


def test_relative_links_reject_http_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](http://example.com)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'http://',
        )


def test_relative_links_reject_javascript_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](javascript:alert(1))\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'dangerous link scheme',
        )


def test_relative_links_reject_data_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](data:text/html,hi)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'dangerous link scheme',
        )


def test_relative_links_reject_file_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](file:///etc/passwd)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'dangerous link scheme',
        )


def test_relative_links_reject_vbscript_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](vbscript:msgbox(1))\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'dangerous link scheme',
        )


def test_relative_links_reject_empty_parens_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x]()\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'empty relative link',
        )


def test_relative_links_reject_bare_hash_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](#)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'empty relative link',
        )


def test_relative_links_reject_broken_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](./nope.md)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'broken relative link',
        )


def test_relative_links_reject_missing_fragment_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n## Real\n\n[x](#missing)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'missing heading',
        )


def test_relative_links_accept_valid_fragment_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n## Real Heading\n\n[x](#real-heading)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_ignore_fenced_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n```\n[x](./missing.md)\n```\n\nOk.\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_ignore_tilde_fence_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n~~~\n[x](./missing.md)\n~~~\n\nOk.\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_image_query_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n![a](README.md?raw=1)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'query string',
        )


def test_relative_links_reject_image_empty_fragment_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n![a](README.md#)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'empty fragment',
        )


def test_relative_links_accept_angle_https_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](<https://example.com>)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_encoded_escape_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](%2e%2e/%2e%2e/etc/passwd)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'escapes repo',
        )


def test_relative_links_reject_dotdot_escape_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](../../etc/passwd)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'escapes repo',
        )


def test_relative_links_github_slug_amp_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n## A & B\n\n[x](#a--b)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_nul_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](README.md%00.txt)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'NUL',
        )


def test_relative_links_accept_title_attr_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", '# T\n\n[x](https://example.com "t")\n')
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_http_mixed_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](HtTp://example.com)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'http://',
        )


def test_relative_links_accept_https_mixed_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](HtTpS://example.com)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_whitespace_target_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](   )\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'empty relative link',
        )


def test_relative_links_reject_protocol_rel_image_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n![a](//cdn.example/x.png)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'protocol-relative',
        )


def test_relative_links_reject_javascript_image_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n![a](javascript:alert(1))\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'dangerous link scheme',
        )


def test_relative_links_reject_empty_frag_angle_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](<README.md#>)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'empty fragment',
        )


def test_relative_links_skip_owasp_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\nOk.\n")
        _write(tmp_path / "OWASP-AGENTIC.md", "# O\n\n[broken](./missing.md)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_skip_agents_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\nOk.\n")
        _write(
            tmp_path / ".github" / "agents" / "notes.md",
            "# Agents\n\n[broken](./missing.md)\n",
        )
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_accept_cross_file_fragment_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "a.md", "# A\n\nSee [b](b.md#section-two).\n")
        _write(tmp_path / "b.md", "# B\n\n## Section Two\n\nBody.\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_accept_license_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "LICENSE", "MIT\n")
        _write(tmp_path / "README.md", "# T\n\n[L](LICENSE)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_accept_nested_docs_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "a.md", "# A\n\n[b](b.md)\n")
        _write(tmp_path / "docs" / "b.md", "# B\n\nOk.\n")
        _write(tmp_path / "README.md", "# T\n\n[d](docs/a.md)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_query_on_nested_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "a.md", "# A\n\nOk.\n")
        _write(tmp_path / "README.md", "# T\n\n[d](docs/a.md?x=1)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "query string",
        )


def test_relative_links_reject_empty_frag_on_nested_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "a.md", "# A\n\nOk.\n")
        _write(tmp_path / "README.md", "# T\n\n[d](docs/a.md#)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "empty fragment",
        )


def test_relative_links_skip_node_modules_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\nOk.\n")
        _write(tmp_path / "node_modules" / "pkg" / "README.md", "# N\n\n[b](./missing.md)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_accept_image_existing_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        (tmp_path / "logo.png").write_bytes(b"\x89PNG\r\n")
        _write(tmp_path / "README.md", "# T\n\n![a](logo.png)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_broken_image_still_after_41() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n![a](./missing.png)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "broken relative link",
        )


def test_relative_links_fully_unquote_helper_after_41() -> None:
    import importlib.util
    spec = importlib.util.spec_from_file_location(
        "check_relative_links",
        SCRIPTS / "check_relative_links.py",
    )
    mod = importlib.util.module_from_spec(spec)
    assert spec.loader is not None
    spec.loader.exec_module(mod)
    assert mod.fully_unquote("%252e%252e") == ".."
    assert mod.fully_unquote("%2e%2e") == ".."
    assert mod.github_slug("A & B") == "a--b"


# --- TOKENMAXX deepen after #43: wiki-outline gate contract / fail-closed pins ---


def test_wiki_gate_file_must_exist_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        (tmp_path / "scripts" / "check_wiki_outline.py").unlink()
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "check_wiki_outline.py",
        )


def test_wiki_gate_requires_publishable_home_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace("Home.md", "Index.md")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Home.md",
        )


def test_wiki_gate_requires_publishable_overview_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace("Overview.md", "About.md")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Overview.md",
        )


def test_wiki_gate_requires_publishable_autonomy_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "Autonomy-Levels.md", "Autonomy.md"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Autonomy-Levels.md",
        )


def test_wiki_gate_requires_publishable_stewardship_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "Repo-Stewardship.md", "Stewardship.md"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Repo-Stewardship.md",
        )


def test_wiki_gate_requires_publishable_routing_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "Agent-Routing.md", "Routing.md"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Agent-Routing.md",
        )


def test_wiki_gate_requires_publishable_security_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "Security-Boundaries.md", "Security.md"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Security-Boundaries.md",
        )


def test_wiki_gate_requires_operator_publish_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace("PUBLISH.md", "OPERATOR.md")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "PUBLISH.md",
        )


def test_wiki_gate_requires_publishable_pages_const_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "PUBLISHABLE_PAGES", "WIKI_PAGES"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "PUBLISHABLE_PAGES",
        )


def test_wiki_gate_requires_topic_hints_const_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "PAGE_TOPIC_HINTS", "TOPIC_HINTS"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "PAGE_TOPIC_HINTS",
        )


def test_wiki_gate_requires_l2_topic_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"L2", ', "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "L2",
        )


def test_wiki_gate_requires_l3_topic_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"L3", ', "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "L3",
        )


def test_wiki_gate_requires_credential_topic_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"credential"', '"passwd"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "credential",
        )


def test_wiki_gate_requires_copilot_topic_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"copilot"', '"cursor"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "copilot",
        )


def test_wiki_gate_requires_strip_fenced_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "strip_fenced_code", "strip_fenced_X"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "fenced code",
        )


def test_wiki_gate_requires_dangerous_scheme_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "has_dangerous_scheme", "has_risky_scheme"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "dangerous",
        )


def test_wiki_gate_requires_protocol_relative_needle_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "protocol-relative", "scheme-relative"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "protocol-relative",
        )


def test_wiki_gate_requires_slash_slash_match_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"//"', '"\\\\"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "//",
        )


def test_wiki_gate_requires_http_reject_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace("http://", "hxxp://")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "http://",
        )


def test_wiki_gate_requires_forbidden_badge_hints_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "FORBIDDEN_BADGE_HINTS", "BAD_BADGE_HINTS"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "FORBIDDEN_BADGE_HINTS",
        )


def test_wiki_gate_requires_stewardship_ci_hints_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "STEWARDSHIP_CI_HINTS", "CI_HINTS"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "STEWARDSHIP_CI_HINTS",
        )


def test_wiki_gate_requires_markdown_lint_ci_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "markdown-lint", "md-lint"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "markdown-lint",
        )


def test_wiki_gate_requires_link_check_ci_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "link-check", "lnk-check"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "link-check",
        )


def test_wiki_gate_requires_stewardship_checks_ci_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "stewardship-checks", "stew-checks"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "stewardship-checks",
        )


def test_wiki_gate_requires_actionlint_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace("actionlint", "actlint")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "actionlint",
        )


def test_wiki_gate_requires_invent_pin_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace("invent", "fabricate")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "invent",
        )


def test_wiki_gate_requires_kill_switch_needle_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "kill-switch", "kill_switch"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "kill-switch",
        )


def test_wiki_gate_requires_scan_secrets_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "scan_secrets", "scan_tokens"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "scan_secrets",
        )


def test_wiki_gate_requires_readme_link_hints_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "README_LINK_HINTS", "README_HINTS"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "README_LINK_HINTS",
        )


def test_wiki_gate_requires_badge_standard_hints_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "BADGE_STANDARD_HINTS", "BADGE_HINTS"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "BADGE_STANDARD_HINTS",
        )


def test_wiki_gate_requires_out_of_scope_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8")
        text = text.replace("out of scope", "outta scope").replace(
            "Out of scope", "Outta scope"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Out of scope",
        )


def test_wiki_rejects_protocol_relative_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n[cdn](//evil.example/x)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "protocol-relative",
        )


def test_wiki_rejects_protocol_relative_image_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n![cdn](//evil.example/x.png)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "protocol-relative",
        )


def test_wiki_ignores_fenced_http_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += (
                "\n```bash\n"
                'echo "[x](http://example.com)"\n'
                "```\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_pass_script(scripts / "check_wiki_outline.py", tmp_path)


def test_wiki_ignores_tilde_fenced_javascript_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += (
                "\n~~~\n"
                "[x](javascript:alert(1))\n"
                "~~~\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_pass_script(scripts / "check_wiki_outline.py", tmp_path)


def test_wiki_rejects_missing_l2_topic_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Autonomy-Levels.md"] = (
                "# A\n\n[Home](Home.md)\n\nL0 L1 L3 autonomy\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "L2",
        )


def test_wiki_rejects_missing_l3_topic_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Autonomy-Levels.md"] = (
                "# A\n\n[Home](Home.md)\n\nL0 L1 L2 autonomy\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "L3",
        )


def test_wiki_rejects_missing_credential_topic_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Security-Boundaries.md"] = (
                "# S\n\n[Home](Home.md)\n\nkill switch secret\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "credential",
        )


def test_wiki_rejects_missing_copilot_topic_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Agent-Routing.md"] = (
                "# R\n\n[Home](Home.md)\n\nsurface routing\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "copilot",
        )


def test_wiki_rejects_missing_home_kill_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace(
                "Kill switch blocks agent writes.\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "kill-switch",
        )


def test_wiki_rejects_http_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n[bad](http://example.com)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "http://",
        )


def test_wiki_rejects_javascript_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n[bad](javascript:alert(1))\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous",
        )


def test_wiki_rejects_data_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n[bad](data:text/html,hi)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous",
        )


def test_wiki_rejects_vbscript_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n[bad](vbscript:msgbox)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous",
        )


def test_wiki_rejects_file_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n[bad](file:///etc/passwd)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous",
        )


def test_wiki_rejects_discord_chrome_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += (
                "\nInvent badge chrome: discord community shield.\n"
                "[![discord](https://img.shields.io/discord/1)]"
                "(https://discord.gg/x)\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "discord",
        )


def test_wiki_rejects_stars_chrome_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += (
                "\nSee the stars badge on shields.io for vanity metrics.\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "stars",
        )


def test_wiki_rejects_missing_actionlint_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace(
                "actionlint on existing workflow paths\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "actionlint",
        )


def test_wiki_rejects_missing_relative_hint_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace(
                "relative links ", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "relative",
        )


def test_wiki_rejects_missing_invent_home_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace("invent product", "product")

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "invent",
        )


def test_wiki_rejects_missing_secret_home_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace("Secrets and", "Private and")

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "secret",
        )


def test_wiki_rejects_missing_ci_hint_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace(
                "link-check ", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "link-check",
        )


def test_wiki_rejects_missing_publish_home_row_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = pages["PUBLISH.md"].replace(
                "| `Home.md` | Home |\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Home.md",
        )


def test_wiki_rejects_unexpected_page_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_wiki_tree(tmp_path, extra_pages=("Extra.md",))
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Unexpected",
        )


def test_wiki_rejects_missing_home_backlink_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = "# O\n\ngovernance public\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Home.md",
        )


def test_wiki_rejects_missing_home_to_security_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace(
                "[Security-Boundaries](Security-Boundaries.md)\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Security-Boundaries.md",
        )


def test_wiki_rejects_missing_badge_topic_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace(
                " badge\n", "\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "badge",
        )


def test_wiki_rejects_missing_l0_topic_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Autonomy-Levels.md"] = (
                "# A\n\n[Home](Home.md)\n\nL1 L2 L3 autonomy\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "L0",
        )


def test_wiki_rejects_missing_l1_topic_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Autonomy-Levels.md"] = (
                "# A\n\n[Home](Home.md)\n\nL0 L2 L3 autonomy\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "L1",
        )


def test_wiki_rejects_missing_kill_topic_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Security-Boundaries.md"] = (
                "# S\n\n[Home](Home.md)\n\nsecret credential\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "kill",
        )


def test_wiki_rejects_missing_surface_topic_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Agent-Routing.md"] = (
                "# R\n\n[Home](Home.md)\n\nrouting copilot\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "surface",
        )


def test_wiki_rejects_missing_governance_topic_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = "# O\n\n[Home](Home.md)\n\npublic\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "governance",
        )


def test_wiki_passes_good_fixture_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_wiki_tree(tmp_path)
        assert_pass_script(scripts / "check_wiki_outline.py", tmp_path)


def test_wiki_rejects_http_mixed_case_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n[bad](HtTp://example.com)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "http://",
        )


def test_wiki_rejects_javascript_mixed_case_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n[bad](JavaScript:alert(1))\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "dangerous",
        )


def test_wiki_rejects_missing_do_not_push_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = pages["PUBLISH.md"].replace(
                "Do **not** push `PUBLISH.md`.\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "PUBLISH.md",
        )


def test_wiki_rejects_npm_chrome_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += (
                "\n[![npm](https://img.shields.io/npm/v/x)]"
                "(https://npmjs.com/package/x)\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "npm",
        )


def test_wiki_rejects_missing_out_of_scope_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace(
                "## Out of scope\n\n", "## Boundaries\n\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Out of scope",
        )


def test_wiki_rejects_missing_run_script_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace(
                "run_stewardship_checks.sh ", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "run_stewardship_checks.sh",
        )


def test_wiki_rejects_missing_home_badge_link_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace(
                "[Badge](../badge-standard.md)\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "badge",
        )


def test_wiki_rejects_secret_pattern_still_after_43() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\nghp_abcdefghijklmnopqrstuvwxyz0123456789\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "secret",
        )


# --- TOKENMAXX deepen after #45: stewardship-schema gate contract / fail-closed pins ---


def test_schema_gate_file_must_exist_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        (tmp_path / "scripts" / "check_stewardship_schema.py").unlink()
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_stewardship_schema.py',
        )


def test_schema_gate_requires_doc_schemas_const_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('DOC_SCHEMAS', 'SCHEMA_DOCS')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'DOC_SCHEMAS',
        )


def test_schema_gate_requires_expected_values_const_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('EXPECTED_VALUES', 'PINNED_VALUES')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'EXPECTED_VALUES',
        )


def test_schema_gate_requires_badge_standard_doc_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('docs/badge-standard.md', 'docs/badge-row.md')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'docs/badge-standard.md',
        )


def test_schema_gate_requires_publish_doc_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('docs/wiki/PUBLISH.md', 'docs/wiki/RELEASE.md')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'docs/wiki/PUBLISH.md',
        )


def test_schema_gate_requires_backlog_doc_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('docs/issue-backlog.md', 'docs/issue-list.md')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'docs/issue-backlog.md',
        )


def test_schema_gate_requires_agents_doc_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"AGENTS.md"', '"AGENTS_FILE.md"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'AGENTS.md',
        )


def test_schema_gate_requires_claude_doc_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"CLAUDE.md"', '"CLAUDE_FILE.md"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'CLAUDE.md',
        )


def test_schema_gate_requires_edit_policy_key_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"edit_policy"', '"edit_rule"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'edit_policy',
        )


def test_schema_gate_requires_autonomy_key_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"autonomy_level"', '"autonomy"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'autonomy_level',
        )


def test_schema_gate_requires_parent_governance_key_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"parent_governance"', '"parent_repo"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'parent_governance',
        )


def test_schema_gate_requires_version_key_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"version"', '"semver"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'version',
        )


def test_schema_gate_requires_surface_key_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"surface"', '"channel"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'surface',
        )


def test_schema_gate_requires_closes_key_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"closes"', '"resolves"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'closes',
        )


def test_schema_gate_requires_purpose_key_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"purpose"', '"intent"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'purpose',
        )


def test_schema_gate_requires_maintainer_key_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"maintainer"', '"steward"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'maintainer',
        )


def test_schema_gate_requires_semver_re_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('SEMVER_RE', 'VERSION_RE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'SEMVER_RE',
        )


def test_schema_gate_requires_iso_date_re_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('ISO_DATE_RE', 'DATE_RE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ISO_DATE_RE',
        )


def test_schema_gate_requires_issue_ref_re_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('ISSUE_REF_RE', 'TICKET_RE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ISSUE_REF_RE',
        )


def test_schema_gate_requires_fenced_yaml_re_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('FENCED_YAML_RE', 'YAML_BLOCK_RE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'FENCED_YAML_RE',
        )


def test_schema_gate_requires_yaml_fence_needle_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('```yaml', '```yml')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '```yaml',
        )


def test_schema_gate_requires_date_keys_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('DATE_KEYS', 'TIME_KEYS')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'DATE_KEYS',
        )


def test_schema_gate_requires_parse_simple_yaml_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('parse_simple_yaml', 'parse_basic_yaml')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'parse_simple_yaml',
        )


def test_schema_gate_requires_load_yaml_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('load_yaml', 'read_yaml')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'load_yaml',
        )


def test_schema_gate_requires_first_yaml_block_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('first_yaml_block', 'first_meta_block')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'first_yaml_block',
        )


def test_schema_gate_requires_safe_load_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('safe_load', 'full_load')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'safe_load',
        )


def test_schema_gate_requires_mapping_needle_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('must be a mapping', 'must be a dict')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'must be a mapping',
        )


def test_schema_gate_requires_scan_secrets_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('scan_secrets', 'scan_tokens')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'scan_secrets',
        )


def test_schema_gate_requires_invent_pin_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8")
        text = text.replace("invent", "fabricate").replace("Invent", "Fabricate")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'invent',
        )


def test_schema_gate_requires_smtp_eth_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('smtp.eth', 'smtp-eth')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'smtp.eth',
        )


def test_schema_gate_requires_repository_specific_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('repository-specific', 'repo-specific')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'repository-specific',
        )


def test_schema_gate_requires_copilot_pin_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('copilot', 'cursor-agent')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'copilot',
        )


def test_schema_gate_requires_active_status_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('ACTIVE', 'LIVE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ACTIVE',
        )


def test_schema_gate_requires_autonomy_range_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('0..3', '0-3')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '0..3',
        )


def test_schema_gate_requires_backlog_owner_copilot_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace(
            "live backlog owner is copilot",
            "live backlog owner is agent",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "issue-backlog owner",
        )



def test_schema_rejects_backlog_wrong_owner_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        backlog = (
            '# I\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: "2026-09-13"\nowner: geryon\nedit_policy: x\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, backlog=backlog)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'owner',
        )


def test_schema_rejects_empty_badge_edit_policy_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            '# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: "2026-09-13"\nowner: copilot\nscope: x\nedit_policy: ""\ncloses: "#16"\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'non-empty',
        )


def test_schema_rejects_empty_agents_maintainer_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            '# AGENTS\n\n```yaml\nversion: "1.0.0"\nlast_updated: "2026-04-13"\nmaintainer: ""\nscope: repository-specific\nparent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'non-empty',
        )


def test_schema_rejects_missing_agents_parent_key_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            '# AGENTS\n\n```yaml\nversion: "1.0.0"\nlast_updated: "2026-04-13"\nmaintainer: smtp.eth\nscope: repository-specific\nautonomy_level: 1\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'parent_governance',
        )


def test_schema_rejects_missing_claude_autonomy_key_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        claude = (
            '# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: "fuzzywigg (smtp.eth)"\nsurface: copilot\nlast_updated: "2026-04-13"\nparent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, claude=claude)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'autonomy_level',
        )


def test_schema_rejects_empty_publish_created_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        publish = (
            '# P\n\n```yaml\nstatus: ACTIVE\ncreated: ""\npurpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\ncloses: "#16"\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, publish=publish)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'non-empty',
        )


def test_schema_rejects_wrong_badge_status_draft_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            '# B\n\n```yaml\nstatus: DRAFT\ntier: 1\ncreated: "2026-09-13"\nowner: copilot\nscope: x\nedit_policy: "do not invent product badges"\ncloses: "#16"\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'ACTIVE',
        )


def test_schema_rejects_autonomy_two_vs_expected_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            '# AGENTS\n\n```yaml\nversion: "1.0.0"\nlast_updated: "2026-04-13"\nmaintainer: smtp.eth\nscope: repository-specific\nparent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 2\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'autonomy_level',
        )


def test_schema_rejects_wrong_claude_parent_still_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        claude = (
            '# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: "fuzzywigg (smtp.eth)"\nsurface: copilot\nautonomy_level: 1\nlast_updated: "2026-04-13"\nparent_governance: github.com/other/repo\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, claude=claude)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'parent_governance',
        )


def test_schema_rejects_closes_without_hash_still_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            '# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: "2026-09-13"\nowner: copilot\nscope: x\nedit_policy: "do not invent product badges"\ncloses: "issue 16"\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'closes',
        )


def test_schema_rejects_tier_zero_still_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            '# B\n\n```yaml\nstatus: ACTIVE\ntier: 0\ncreated: "2026-09-13"\nowner: copilot\nscope: x\nedit_policy: "do not invent product badges"\ncloses: "#16"\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'tier',
        )


def test_schema_rejects_semver_prerelease_still_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            '# AGENTS\n\n```yaml\nversion: "1.0.0-rc1"\nlast_updated: "2026-04-13"\nmaintainer: smtp.eth\nscope: repository-specific\nparent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'semver',
        )


def test_schema_rejects_wrong_agents_scope_still_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            '# AGENTS\n\n```yaml\nversion: "1.0.0"\nlast_updated: "2026-04-13"\nmaintainer: smtp.eth\nscope: ecosystem-wide\nparent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'scope',
        )


def test_schema_rejects_wrong_publish_status_draft_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        publish = (
            '# P\n\n```yaml\nstatus: DRAFT\ncreated: "2026-09-13"\npurpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\ncloses: "#16"\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, publish=publish)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'ACTIVE',
        )


def test_schema_rejects_autonomy_negative_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            '# AGENTS\n\n```yaml\nversion: "1.0.0"\nlast_updated: "2026-04-13"\nmaintainer: smtp.eth\nscope: repository-specific\nparent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: -1\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'autonomy_level',
        )


def test_schema_rejects_empty_badge_scope_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            '# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: "2026-09-13"\nowner: copilot\nscope: ""\nedit_policy: "do not invent product badges"\ncloses: "#16"\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'non-empty',
        )


def test_schema_rejects_missing_agents_maintainer_key_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            '# AGENTS\n\n```yaml\nversion: "1.0.0"\nlast_updated: "2026-04-13"\nscope: repository-specific\nparent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'maintainer',
        )


def test_schema_rejects_missing_claude_repo_key_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        claude = (
            '# CLAUDE\n\n```yaml\nowner: "fuzzywigg (smtp.eth)"\nsurface: copilot\nautonomy_level: 1\nlast_updated: "2026-04-13"\nparent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, claude=claude)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'repo',
        )


def test_schema_rejects_empty_publish_closes_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        publish = (
            '# P\n\n```yaml\nstatus: ACTIVE\ncreated: "2026-09-13"\npurpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\ncloses: ""\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, publish=publish)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'non-empty',
        )


def test_schema_rejects_badge_missing_invent_wording_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            '# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: "2026-09-13"\nowner: copilot\nscope: x\nedit_policy: "agents may edit freely"\ncloses: "#16"\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'invent',
        )


def test_schema_rejects_wrong_badge_owner_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            '# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: "2026-09-13"\nowner: geryon\nscope: x\nedit_policy: "do not invent product badges"\ncloses: "#16"\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'owner',
        )


def test_schema_rejects_wrong_claude_surface_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        claude = (
            '# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: "fuzzywigg (smtp.eth)"\nsurface: geryon\nautonomy_level: 1\nlast_updated: "2026-04-13"\nparent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, claude=claude)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'surface',
        )


def test_schema_rejects_bad_iso_date_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            '# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: "13-09-2026"\nowner: copilot\nscope: x\nedit_policy: "do not invent product badges"\ncloses: "#16"\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'ISO-8601',
        )


def test_schema_rejects_backlog_tier_zero_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        backlog = (
            '# I\n\n```yaml\nstatus: ACTIVE\ntier: 0\ncreated: "2026-09-13"\nowner: copilot\nedit_policy: x\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, backlog=backlog)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'tier',
        )


def test_schema_rejects_backlog_draft_status_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        backlog = (
            '# I\n\n```yaml\nstatus: DRAFT\ntier: 1\ncreated: "2026-09-13"\nowner: copilot\nedit_policy: x\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, backlog=backlog)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'ACTIVE',
        )


def test_schema_rejects_publish_closes_without_hash_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        publish = (
            '# P\n\n```yaml\nstatus: ACTIVE\ncreated: "2026-09-13"\npurpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\ncloses: "sixteen"\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, publish=publish)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'closes',
        )


def test_schema_rejects_missing_backlog_edit_policy_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        backlog = (
            '# I\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: "2026-09-13"\nowner: copilot\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, backlog=backlog)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'edit_policy',
        )


def test_schema_rejects_agents_wrong_maintainer_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            '# AGENTS\n\n```yaml\nversion: "1.0.0"\nlast_updated: "2026-04-13"\nmaintainer: other.eth\nscope: repository-specific\nparent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'maintainer',
        )


def test_schema_rejects_claude_wrong_repo_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        claude = (
            '# CLAUDE\n\n```yaml\nrepo: other-repo\nowner: "fuzzywigg (smtp.eth)"\nsurface: copilot\nautonomy_level: 1\nlast_updated: "2026-04-13"\nparent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, claude=claude)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'repo',
        )


def test_schema_rejects_secret_pattern_in_badge_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            '# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: "2026-09-13"\nowner: copilot\nscope: x\nedit_policy: "do not invent product badges"\ncloses: "#16"\n```\n\nghp_AAAAAAAAAAAAAAAAAAAA\n'
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'secret',
        )


def test_schema_rejects_autonomy_four_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            '# AGENTS\n\n```yaml\nversion: "1.0.0"\nlast_updated: "2026-04-13"\nmaintainer: smtp.eth\nscope: repository-specific\nparent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 4\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            '0..3',
        )


def test_schema_rejects_empty_agents_scope_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            '# AGENTS\n\n```yaml\nversion: "1.0.0"\nlast_updated: "2026-04-13"\nmaintainer: smtp.eth\nscope: ""\nparent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'non-empty',
        )


def test_schema_rejects_wrong_agents_parent_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        agents = (
            '# AGENTS\n\n```yaml\nversion: "1.0.0"\nlast_updated: "2026-04-13"\nmaintainer: smtp.eth\nscope: repository-specific\nparent_governance: github.com/other/governance\nautonomy_level: 1\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, agents=agents)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'parent_governance',
        )


def test_schema_rejects_missing_badge_closes_key_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            '# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: "2026-09-13"\nowner: copilot\nscope: x\nedit_policy: "do not invent product badges"\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'closes',
        )


def test_schema_rejects_non_mapping_yaml_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        badge = (
            '# B\n\n```yaml\n- just\n- a\n- list\n```\n'
        )
        scripts = _seed_schema_tree(tmp_path, badge=badge)
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'mapping',
        )


def test_schema_passes_good_fixture_still_after_45() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(tmp_path)
        assert_pass_script(scripts / "check_stewardship_schema.py", tmp_path)



# --- TOKENMAXX deepen after #46: stewardship_common contract / fail-closed pins ---

def test_common_gate_file_must_exist_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        (tmp_path / "scripts" / "stewardship_common.py").unlink()
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "stewardship_common",
        )


def test_common_gate_requires_secret_patterns_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace("SECRET_PATTERNS", "SECRET_REGEXES")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "SECRET_PATTERNS",
        )


def test_common_gate_requires_secret_url_hints_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace("SECRET_URL_HINTS", "SECRET_URL_HINTS_X")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "SECRET_URL_HINTS",
        )


def test_common_gate_requires_forbidden_badge_hints_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace("FORBIDDEN_BADGE_HINTS", "FORBIDDEN_BADGE_HINTS_X")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "FORBIDDEN_BADGE_HINTS",
        )


def test_common_gate_requires_dangerous_link_schemes_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace(
            "DANGEROUS_LINK_SCHEMES", "BAD_LINK_SCHEMES"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "DANGEROUS_LINK_SCHEMES",
        )


def test_common_gate_requires_fenced_block_re_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace("FENCED_BLOCK_RE", "FENCE_RE")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "FENCED_BLOCK_RE",
        )


def test_common_gate_requires_strip_fenced_code_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace(
            "def strip_fenced_code(", "def strip_fenced_code_x("
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "strip_fenced_code()",
        )


def test_common_gate_requires_has_dangerous_scheme_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace(
            "def has_dangerous_scheme(", "def has_dangerous_scheme_x("
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "has_dangerous_scheme()",
        )


def test_common_gate_requires_markdown_files_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace(
            "def markdown_files(", "def markdown_files_x("
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "markdown_files()",
        )


def test_common_gate_requires_fail_helper_pin_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace(
            "Gate fail-closed helper", "Gate soft helper"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Gate fail-closed helper",
        )


def test_common_gate_requires_load_workflow_pin_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace(
            "Load .github/workflows/", "Read .github/workflows/"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "workflows load pin",
        )


def test_common_gate_requires_secret_ghp_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('ghp_', 'ghpX_GONE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ghp_',
        )


def test_common_gate_requires_secret_gho_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('gho_', 'ghoX_GONE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'gho_',
        )


def test_common_gate_requires_secret_ghu_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('ghu_', 'ghuX_GONE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ghu_',
        )


def test_common_gate_requires_secret_ghs_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('ghs_', 'ghsX_GONE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ghs_',
        )


def test_common_gate_requires_secret_ghr_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('ghr_', 'ghrX_GONE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ghr_',
        )


def test_common_gate_requires_secret_github_pat_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('github_pat_', 'githubXp_GONE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'github_pat_',
        )


def test_common_gate_requires_secret_private_key_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('PRIVATE KEY', 'SECRET KEY')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'PRIVATE KEY',
        )


def test_common_gate_requires_secret_sk_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('sk-', 'skX_GONE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'sk-',
        )


def test_common_gate_requires_secret_rk_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('rk-', 'rkX_GONE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'rk-',
        )


def test_common_gate_requires_secret_api_key_re_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('api[_-]?key', 'api[-_]?key')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'api[_-]?key',
        )


def test_common_gate_requires_secret_aws_secret_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('aws_secret_access_key', 'awsXsecr_GONE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'aws_secret_access_key',
        )


def test_common_gate_requires_secret_xox_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace("xox", "slacktok")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'xox',
        )


def test_common_gate_requires_secret_npm_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('npm_', 'npmX_GONE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'npm_',
        )


def test_common_gate_requires_secret_aiza_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace("AIza", "GAPI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'AIza',
        )


def test_common_gate_requires_url_hint_token_eq_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('token=', 'token_eq')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'token=',
        )


def test_common_gate_requires_url_hint_access_token_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('access_token=', 'access_token_eq')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'access_token=',
        )


def test_common_gate_requires_url_hint_api_key_eq_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('api_key=', 'api_key_eq')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'api_key=',
        )


def test_common_gate_requires_url_hint_apikey_eq_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('apikey=', 'apikey_eq')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'apikey=',
        )


def test_common_gate_requires_url_hint_client_secret_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('client_secret=', 'client_secret_eq')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'client_secret=',
        )


def test_common_gate_requires_forbidden_coverage_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('coverage', 'xx_coverage')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'coverage',
        )


def test_common_gate_requires_forbidden_codecov_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('codecov', 'xx_codecov')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'codecov',
        )


def test_common_gate_requires_forbidden_coveralls_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('coveralls', 'xx_coveralls')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'coveralls',
        )


def test_common_gate_requires_forbidden_downloads_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('downloads', 'xx_downloads')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'downloads',
        )


def test_common_gate_requires_forbidden_discord_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('discord', 'xx_discord')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'discord',
        )


def test_common_gate_requires_forbidden_twitter_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('twitter', 'xx_twitter')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'twitter',
        )


def test_common_gate_requires_forbidden_x_com_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('x.com', 'xx_x.com')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'x.com',
        )


def test_common_gate_requires_forbidden_stars_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('stars', 'xx_stars')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'stars',
        )


def test_common_gate_requires_forbidden_forks_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('forks', 'xx_forks')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'forks',
        )


def test_common_gate_requires_forbidden_followers_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('followers', 'xx_followers')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'followers',
        )


def test_common_gate_requires_forbidden_npm__after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('npm/', 'xx_npm/')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'npm/',
        )


def test_common_gate_requires_forbidden_pypi__after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('pypi/', 'xx_pypi/')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'pypi/',
        )


def test_common_gate_requires_forbidden_producthunt_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('producthunt', 'xx_producthunt')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'producthunt',
        )


def test_common_gate_requires_forbidden_buymeacoffee_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('buymeacoffee', 'xx_buymeacoffee')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'buymeacoffee',
        )


def test_common_gate_requires_forbidden_opencollective_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('opencollective', 'xx_opencollective')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'opencollective',
        )


def test_common_gate_requires_scheme_js_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('javascript:', 'javascript_x')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'javascript:',
        )


def test_common_gate_requires_scheme_data_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('data:', 'data_x')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'data:',
        )


def test_common_gate_requires_scheme_vbscript_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('vbscript:', 'vbscript_x')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'vbscript:',
        )


def test_common_gate_requires_scheme_file_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('file:', 'file_x')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'file:',
        )


def test_common_gate_requires_tilde_fence_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace("~~~", "```")
        # keep at least one ``` for other checks; remove ~~~ only
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "~~~",
        )


def test_common_gate_requires_endswith_urlish_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace(
            "URL-ish secret hints", "query-param secret hints"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "URL-ish secret hints",
        )


def test_common_gate_requires_invent_wording_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8")
        for old in ("invent-product", "Invent-product", "invent"):
            text = text.replace(old, "feature")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "invent-product",
        )


def test_common_gate_requires_live_secret_scan_pin_after_46() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace(
            "live secret scan covers CI tokens",
            "live secret scan covers agent tokens",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "live secret scan covers CI tokens",
        )


def test_common_secret_patterns_ghp_still_after_46() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402
    sample = "token ghp_" + ("a" * 20)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missed ghp_ after #46")


def test_common_secret_patterns_github_pat_still_after_46() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402
    sample = "github_pat_" + ("A" * 20)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missed github_pat_ after #46")


def test_common_secret_patterns_npm_still_after_46() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402
    sample = "npm_" + ("b" * 20)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missed npm_ after #46")


def test_common_secret_patterns_aiza_still_after_46() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402
    sample = "AIza" + ("c" * 20)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS missed AIza after #46")


def test_common_dangerous_schemes_complete_still_after_46() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import DANGEROUS_LINK_SCHEMES  # noqa: E402
    for scheme in ("javascript:", "data:", "vbscript:", "file:"):
        if scheme not in DANGEROUS_LINK_SCHEMES:
            raise AssertionError(f"DANGEROUS_LINK_SCHEMES missing {scheme}")


def test_common_has_dangerous_javascript_still_after_46() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402
    if has_dangerous_scheme("javascript:alert(1)") != "javascript:":
        raise AssertionError("has_dangerous_scheme missed javascript:")


def test_common_has_dangerous_data_still_after_46() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402
    if has_dangerous_scheme("data:text/html,x") != "data:":
        raise AssertionError("has_dangerous_scheme missed data:")


def test_common_forbidden_hints_coverage_still_after_46() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import FORBIDDEN_BADGE_HINTS  # noqa: E402
    for hint in ("coverage", "codecov", "discord", "producthunt", "opencollective"):
        if hint not in FORBIDDEN_BADGE_HINTS:
            raise AssertionError(f"FORBIDDEN_BADGE_HINTS missing {hint}")


def test_common_strip_fenced_code_tilde_still_after_46() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import strip_fenced_code  # noqa: E402
    text = "before\n~~~js\n[x](http://evil)\n~~~\nafter\n"
    out = strip_fenced_code(text)
    if "http://evil" in out or "before" not in out or "after" not in out:
        raise AssertionError("strip_fenced_code failed on ~~~ fences after #46")


def test_common_strip_fenced_code_backtick_still_after_46() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import strip_fenced_code  # noqa: E402
    text = "before\n```md\n[x](javascript:1)\n```\nafter\n"
    out = strip_fenced_code(text)
    if "javascript:1" in out:
        raise AssertionError("strip_fenced_code failed on ``` fences after #46")


def test_common_scan_secrets_url_hint_still_after_46() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import scan_secrets  # noqa: E402
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        path = tmp_path / "doc.md"
        path.write_text(
            "see https://example.com/?token=supersecrettokenvalue\n",
            encoding="utf-8",
        )
        errors: list[str] = []
        scan_secrets(path, errors, label="doc.md")
        if not errors:
            raise AssertionError("scan_secrets missed token= URL hint after #46")


def test_common_scan_secrets_ghp_prefix_still_after_46() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import scan_secrets  # noqa: E402
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        path = tmp_path / "doc.md"
        path.write_text("leak ghp_" + ("d" * 20) + "\n", encoding="utf-8")
        errors: list[str] = []
        scan_secrets(path, errors, label="doc.md")
        if not errors:
            raise AssertionError("scan_secrets missed ghp_ prefix after #46")


def test_common_load_workflow_text_live_still_after_46() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import load_workflow_text  # noqa: E402
    text = load_workflow_text("stewardship-checks.yml")
    if not text or "run_stewardship_checks.sh" not in text:
        raise AssertionError("load_workflow_text missed stewardship-checks.yml")


def test_common_markdown_files_readme_still_after_46() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import markdown_files  # noqa: E402
    files = markdown_files("README.md")
    if not any(p.name == "README.md" for p in files):
        raise AssertionError("markdown_files missed README.md after #46")


def test_common_fail_appends_still_after_46() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import fail  # noqa: E402
    errors: list[str] = []
    fail("pin-check", errors)
    if errors != ["pin-check"]:
        raise AssertionError("fail() did not append after #46")


def test_common_secret_url_hints_complete_still_after_46() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_URL_HINTS  # noqa: E402
    for hint in ("token=", "access_token=", "client_secret=", "github_pat_"):
        if hint not in SECRET_URL_HINTS:
            raise AssertionError(f"SECRET_URL_HINTS missing {hint}")



# --- TOKENMAXX deepen after #48: badge-standard gate contract / fail-closed pins ---

def test_badge_gate_requires_required_order_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('REQUIRED_ORDER', 'REQUIRED_LABELS')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'REQUIRED_ORDER',
        )

def test_badge_gate_requires_max_badges_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('MAX_BADGES', 'MAX_BADGE_COUNT')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MAX_BADGES',
        )

def test_badge_gate_requires_expected_repo_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('EXPECTED_REPO', 'EXPECTED_SLUG')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'EXPECTED_REPO',
        )

def test_badge_gate_requires_required_workflows_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('REQUIRED_WORKFLOWS', 'REQUIRED_WF')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'REQUIRED_WORKFLOWS',
        )

def test_badge_gate_requires_badge_line_re_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('BADGE_LINE_RE', 'BADGE_RE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'BADGE_LINE_RE',
        )

def test_badge_gate_requires_repo_from_github_re_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('REPO_FROM_GITHUB_RE', 'REPO_GH_RE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'REPO_FROM_GITHUB_RE',
        )

def test_badge_gate_requires_repo_from_shields_re_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('REPO_FROM_SHIELDS_RE', 'REPO_SH_RE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'REPO_FROM_SHIELDS_RE',
        )

def test_badge_gate_requires_label_link_check_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"Link Check"', '"Link Checks"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Link Check',
        )

def test_badge_gate_requires_label_markdown_lint_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"Markdown Lint"', '"Markdown Lints"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Markdown Lint',
        )

def test_badge_gate_requires_label_license_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"License"', '"Licenses"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'License',
        )

def test_badge_gate_requires_max_badges_eq_3_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('MAX_BADGES = 3', 'MAX_BADGES = 4')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MAX_BADGES = 3',
        )

def test_badge_gate_requires_repo_slug_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('fuzzywigg/agents-governance', 'fuzzywigg/agents-gov')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'fuzzywigg/agents-governance',
        )

def test_badge_gate_requires_wf_link_check_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('link-check.yml', 'link-checks.yml')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'link-check.yml',
        )

def test_badge_gate_requires_wf_markdown_lint_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('markdown-lint.yml', 'markdown-lints.yml')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'markdown-lint.yml',
        )

def test_badge_gate_requires_wf_stewardship_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('stewardship-checks.yml', 'stewardship-check.yml')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'stewardship-checks.yml',
        )

def test_badge_gate_requires_fn_extract_badge_row_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def extract_badge_row(', 'def extract_badge_rows(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'extract_badge_row()',
        )

def test_badge_gate_requires_fn_check_badges_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def check_badges(', 'def check_badge_row(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_badges()',
        )

def test_badge_gate_requires_fn_check_badge_doc_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def check_badge_standard_doc(', 'def check_badge_doc(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_badge_standard_doc()',
        )

def test_badge_gate_requires_fn_readme_consistency_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def check_readme_consistency(', 'def check_readme_ok(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_readme_consistency()',
        )

def test_badge_gate_requires_fn_contributing_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def check_contributing_and_agents(', 'def check_contrib_agents(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_contributing_and_agents()',
        )

def test_badge_gate_requires_fn_lycheeignore_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def check_lycheeignore(', 'def check_lychee_ignore(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_lycheeignore()',
        )

def test_badge_gate_requires_fn_actionlint_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def check_actionlint_style(', 'def check_actionlint_ok(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_actionlint_style()',
        )

def test_badge_gate_requires_fn_workflow_hardening_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def check_workflow_hardening(', 'def check_workflow_hard(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_workflow_hardening()',
        )

def test_badge_gate_requires_fn_workflows_license_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def check_workflows_and_license(', 'def check_workflows_license(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_workflows_and_license()',
        )

def test_badge_gate_requires_link_check_badge_svg_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('link-check.yml/badge.svg', 'link-check.yml/badge.png')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'link-check.yml/badge.svg',
        )

def test_badge_gate_requires_markdown_lint_badge_svg_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('markdown-lint.yml/badge.svg', 'markdown-lint.yml/badge.png')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'markdown-lint.yml/badge.svg',
        )

def test_badge_gate_requires_shields_license_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('img.shields.io/github/license/', 'img.shields.io/github/licence/')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'img.shields.io/github/license/',
        )

def test_badge_gate_requires_contiguous_pin_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('contiguous', 'adjacent')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'contiguous',
        )

def test_badge_gate_requires_fourth_refusal_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace("fourth", "quaternary")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "fourth",
        )

def test_badge_gate_requires_stewardship_product_reject_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Stewardship product/status badge', 'Stewardship status badge')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Stewardship product/status badge',
        )

def test_badge_gate_requires_live_badge_row_pin_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('live badge row is Link Check', 'live badge row is CI Check')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'live badge row is Link Check',
        )

def test_badge_gate_requires_forbidden_hints_import_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "FORBIDDEN_BADGE_HINTS", "FORBIDDEN_BADGE_HINTS_X"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "FORBIDDEN_BADGE_HINTS",
        )

def test_badge_gate_requires_secret_url_hints_import_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('SECRET_URL_HINTS', 'SECRET_URL_HINTS_X')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'SECRET_URL_HINTS',
        )

def test_badge_gate_requires_scan_secrets_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "scan_secrets", "scan_secrets_x"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "scan_secrets",
        )

def test_badge_gate_requires_invent_wording_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        for old in ("invent-product", "Invent-product", "invent"):
            text = text.replace(old, "feature")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "invent-product",
        )

def test_badge_gate_requires_https_images_after_48() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace("https://", "http://")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "https://",
        )

def test_badge_gate_required_order_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    if cbs.REQUIRED_ORDER != ("Link Check", "Markdown Lint", "License"):
        raise AssertionError(f"REQUIRED_ORDER drifted: {cbs.REQUIRED_ORDER}")

def test_badge_gate_max_badges_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    if cbs.MAX_BADGES != 3:
        raise AssertionError(f"MAX_BADGES drifted: {cbs.MAX_BADGES}")

def test_badge_gate_expected_repo_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    if cbs.EXPECTED_REPO != "fuzzywigg/agents-governance":
        raise AssertionError(f"EXPECTED_REPO drifted: {cbs.EXPECTED_REPO}")

def test_badge_gate_required_workflows_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    expected = ("link-check.yml", "markdown-lint.yml", "stewardship-checks.yml")
    if cbs.REQUIRED_WORKFLOWS != expected:
        raise AssertionError(f"REQUIRED_WORKFLOWS drifted: {cbs.REQUIRED_WORKFLOWS}")

def test_badge_gate_contract_fn_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    if not callable(getattr(cbs, "check_badge_standard_gate_contract", None)):
        raise AssertionError("check_badge_standard_gate_contract missing")

def test_badge_gate_extract_row_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    readme = "# Title\n\n[![Link Check](https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml/badge.svg)](https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
    badges, errors = cbs.extract_badge_row(readme)
    if len(badges) != 1 or errors:
        raise AssertionError(f"extract_badge_row failed: {len(badges)} {errors}")

def test_badge_gate_check_badges_order_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    text = (
        "# Title\n\n"
        "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml/badge.svg)]"
        "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
        "[![Link Check](https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml/badge.svg)]"
        "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
        "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)](LICENSE)\n"
    )
    badges, _ = cbs.extract_badge_row(text)
    errors: list[str] = []
    cbs.check_badges(badges, errors)
    if not any("order" in e.lower() for e in errors):
        raise AssertionError(f"check_badges missed order reject: {errors}")

def test_badge_gate_check_badges_https_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    text = (
        "# Title\n\n"
        "[![Link Check](http://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml/badge.svg)]"
        "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
        "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml/badge.svg)]"
        "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
        "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)](LICENSE)\n"
    )
    badges, _ = cbs.extract_badge_row(text)
    errors: list[str] = []
    cbs.check_badges(badges, errors)
    if not any("https://" in e for e in errors):
        raise AssertionError(f"check_badges missed http image: {errors}")

def test_badge_gate_doc_fourth_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    errors: list[str] = []
    cbs.check_badge_standard_gate_contract(errors)
    if errors:
        raise AssertionError(f"live badge gate contract failed: {errors}")

def test_badge_gate_badge_line_re_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    line = "[![Link Check](https://example.com/a.svg)](https://example.com/a)"
    if not cbs.BADGE_LINE_RE.match(line):
        raise AssertionError("BADGE_LINE_RE missed valid badge line")

def test_badge_gate_repo_github_re_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    m = cbs.REPO_FROM_GITHUB_RE.search(
        "https://github.com/fuzzywigg/agents-governance/actions"
    )
    if not m or f"{m.group('owner')}/{m.group('repo')}" != "fuzzywigg/agents-governance":
        raise AssertionError("REPO_FROM_GITHUB_RE missed slug")

def test_badge_gate_repo_shields_re_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    m = cbs.REPO_FROM_SHIELDS_RE.search(
        "https://img.shields.io/github/license/fuzzywigg/agents-governance"
    )
    if not m or f"{m.group('owner')}/{m.group('repo')}" != "fuzzywigg/agents-governance":
        raise AssertionError("REPO_FROM_SHIELDS_RE missed slug")

def test_badge_gate_main_calls_contract_still_after_48() -> None:
    text = (SCRIPTS / "check_badge_standard.py").read_text(encoding="utf-8")
    if "check_badge_standard_gate_contract(errors)" not in text:
        raise AssertionError("main must call check_badge_standard_gate_contract")

def test_badge_gate_docstring_pin_still_after_48() -> None:
    text = (SCRIPTS / "check_badge_standard.py").read_text(encoding="utf-8")
    if "live badge row is Link Check" not in text:
        raise AssertionError("module docstring missing live badge row pin")

def test_badge_gate_three_max_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    if len(cbs.REQUIRED_ORDER) != cbs.MAX_BADGES:
        raise AssertionError("REQUIRED_ORDER length must equal MAX_BADGES")

def test_badge_gate_contract_errors_empty_live_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    errors: list[str] = []
    cbs.check_badge_standard_gate_contract(errors)
    if errors:
        raise AssertionError(f"unexpected contract errors: {errors}")

def test_badge_gate_no_fourth_in_order_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    if "Stewardship" in cbs.REQUIRED_ORDER:
        raise AssertionError("REQUIRED_ORDER must not include Stewardship badge")

def test_badge_gate_license_last_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    if cbs.REQUIRED_ORDER[-1] != "License":
        raise AssertionError("License must be last in REQUIRED_ORDER")

def test_badge_gate_link_check_first_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    if cbs.REQUIRED_ORDER[0] != "Link Check":
        raise AssertionError("Link Check must be first in REQUIRED_ORDER")

def test_badge_gate_markdown_lint_middle_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    if cbs.REQUIRED_ORDER[1] != "Markdown Lint":
        raise AssertionError("Markdown Lint must be middle in REQUIRED_ORDER")

def test_badge_gate_badge_gate_path_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    if cbs.BADGE_GATE.name != "check_badge_standard.py":
        raise AssertionError(f"BADGE_GATE drifted: {cbs.BADGE_GATE}")

def test_badge_gate_forbidden_hints_wired_still_after_48() -> None:
    text = (SCRIPTS / "check_badge_standard.py").read_text(encoding="utf-8")
    if "for hint in FORBIDDEN_BADGE_HINTS" not in text:
        raise AssertionError("check_badges must iterate FORBIDDEN_BADGE_HINTS")

def test_badge_gate_secret_hints_wired_still_after_48() -> None:
    text = (SCRIPTS / "check_badge_standard.py").read_text(encoding="utf-8")
    if "for hint in SECRET_URL_HINTS" not in text:
        raise AssertionError("check_badges must iterate SECRET_URL_HINTS")

def test_badge_gate_passes_live_contract_still_after_48() -> None:
    assert_pass_live("check_badge_standard.py")

def test_badge_gate_doc_mentions_three_still_after_48() -> None:
    text = (ROOT / "docs" / "badge-standard.md").read_text(encoding="utf-8")
    lowered = text.lower()
    if "three badges" not in lowered and "badges max" not in lowered:
        raise AssertionError("badge-standard.md missing three-badge max prose")

def test_badge_gate_readme_has_three_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    text = (ROOT / "README.md").read_text(encoding="utf-8")
    badges, errors = cbs.extract_badge_row(text)
    if errors or len(badges) != 3:
        raise AssertionError(f"live README badge row broken: {len(badges)} {errors}")

def test_badge_gate_check_badges_live_still_after_48() -> None:
    sys.path.insert(0, str(SCRIPTS))
    import importlib
    import check_badge_standard as cbs  # noqa: E402
    importlib.reload(cbs)
    text = (ROOT / "README.md").read_text(encoding="utf-8")
    badges, _ = cbs.extract_badge_row(text)
    errors: list[str] = []
    cbs.check_badges(badges, errors)
    if errors:
        raise AssertionError(f"live check_badges failed: {errors}")

def test_badge_gate_contributing_invent_still_after_48() -> None:
    text = (ROOT / "CONTRIBUTING.md").read_text(encoding="utf-8")
    if "invent" not in text.lower():
        raise AssertionError("CONTRIBUTING.md missing invent-product warning")

def test_badge_gate_agents_selftest_still_after_48() -> None:
    text = (ROOT / "AGENTS.md").read_text(encoding="utf-8")
    if "test_stewardship_gates.py" not in text:
        raise AssertionError("AGENTS.md missing self-test needle")

def test_badge_gate_lycheeignore_shields_still_after_48() -> None:
    text = (ROOT / ".lycheeignore").read_text(encoding="utf-8")
    if "img.shields.io" not in text and r"img\.shields\.io" not in text:
        raise AssertionError(".lycheeignore missing shields exclude")

def test_badge_gate_source_has_contract_section_still_after_48() -> None:
    text = (SCRIPTS / "check_badge_standard.py").read_text(encoding="utf-8")
    if "deepen after #48" not in text and "after #48; not common-pin spam" not in text:
        raise AssertionError("badge gate contract docstring missing #48 pin")

def test_badge_gate_quiet_stewardship_doc_still_after_48() -> None:
    text = (SCRIPTS / "check_badge_standard.py").read_text(encoding="utf-8")
    if "Quiet stewardship" not in text:
        raise AssertionError("module docstring missing Quiet stewardship pin")

# --- TOKENMAXX deepen after #53: stewardship-schema scalar / STRING_KEYS / live pins ---

def test_schema_gate_requires_string_keys_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('STRING_KEYS', 'STR_KEYS')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'STRING_KEYS',
        )


def test_schema_gate_requires_reject_non_scalar_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('reject_non_scalar', 'reject_nested')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'reject_non_scalar',
        )


def test_schema_gate_requires_front_door_scope_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('public governance front-door repos', 'public governance repos')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'public governance front-door repos',
        )


def test_schema_gate_requires_reversible_purpose_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('Reversible publish path for docs/wiki', 'Publish path for docs/wiki')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Reversible publish path for docs/wiki',
        )


def test_schema_gate_requires_closes_16_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"#16"', '"#99"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "closes #16",
        )


def test_schema_gate_requires_empty_yaml_needle_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace(
            "empty yaml metadata block", "blank metadata block"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "empty yaml",
        )


def test_schema_gate_requires_bool_int_reject_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8")
        text = text.replace("isinstance(level, bool)", "isinstance(level, float)")
        text = text.replace("isinstance(tier, bool)", "isinstance(tier, float)")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "bool",
        )


def test_schema_gate_requires_ecosystem_md_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('AGENTS-ECOSYSTEM.md', 'AGENTS-ECO.md')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'AGENTS-ECOSYSTEM.md',
        )


def test_schema_gate_requires_parent_governance_pin_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('github.com/fuzzywigg/agents-governance', 'github.com/example/other')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'github.com/fuzzywigg/agents-governance',
        )


def test_schema_gate_requires_fuzzywigg_owner_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('fuzzywigg (smtp.eth)', 'fuzzywigg')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'fuzzywigg (smtp.eth)',
        )


def test_schema_gate_requires_agents_governance_repo_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace(
            'repo": "agents-governance"',
            'repo": "agents-gov"',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'agents-governance',
        )


def test_schema_rejects_bool_autonomy_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\n"
                "autonomy_level: true\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "autonomy_level",
        )


def test_schema_rejects_bool_tier_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: true\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "tier",
        )


def test_schema_rejects_nested_scope_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope:\n  nested: yes\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "scalar",
        )


def test_schema_rejects_list_owner_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner:\n  - copilot\n"
                "scope: \"public governance front-door repos\"\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "scalar",
        )


def test_schema_rejects_empty_yaml_block_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents="# AGENTS\n\n```yaml\n\n```\n",
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "empty yaml",
        )


def test_schema_rejects_null_maintainer_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: null\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\n"
                "autonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_tilde_null_purpose_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            publish=(
                "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\n"
                "purpose: ~\ncloses: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_int_scope_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: 42\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "must be a string",
        )


def test_schema_rejects_wrong_badge_scope_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"other repos\"\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "expected",
        )


def test_schema_rejects_wrong_publish_purpose_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            publish=(
                "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\n"
                "purpose: \"Something else\"\ncloses: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "expected",
        )


def test_schema_rejects_wrong_badge_closes_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: \"#99\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "expected",
        )


def test_schema_rejects_wrong_publish_closes_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            publish=(
                "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\n"
                "purpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\n"
                "closes: \"#42\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "expected",
        )


def test_schema_rejects_yes_autonomy_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo: agents-governance\n"
                "owner: \"fuzzywigg (smtp.eth)\"\nsurface: copilot\n"
                "autonomy_level: yes\nlast_updated: \"2026-04-13\"\n"
                "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "autonomy_level",
        )


def test_schema_rejects_nested_purpose_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            publish=(
                "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\n"
                "purpose:\n  text: reversible\ncloses: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "scalar",
        )


def test_schema_rejects_list_closes_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            publish=(
                "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\n"
                "purpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\n"
                "closes:\n  - \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "scalar",
        )


def test_schema_rejects_bool_status_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: true\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "must be a string",
        )


def test_schema_rejects_wrong_value_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\n"
                "autonomy_level: 0\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'expected',
        )


def test_schema_rejects_inactive_status_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: DRAFT\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'ACTIVE',
        )


def test_schema_rejects_missing_key_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'missing',
        )


def test_schema_rejects_bad_semver_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\n"
                "autonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'semver',
        )


def test_schema_rejects_edit_policy_without_invent_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"agents may edit freely\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'invent',
        )


def test_schema_rejects_wrong_surface_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo: agents-governance\n"
                "owner: \"fuzzywigg (smtp.eth)\"\nsurface: geryon\n"
                "autonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
                "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'expected',
        )


def test_schema_rejects_closes_without_issue_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: \"sixteen\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'closes',
        )


def test_schema_rejects_bad_iso_date_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"04-13-2026\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\n"
                "autonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'ISO-8601',
        )


def test_schema_rejects_wrong_maintainer_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: other\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\n"
                "autonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'expected',
        )


def test_schema_rejects_float_autonomy_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\n"
                "autonomy_level: 1.5\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'autonomy_level',
        )


def test_schema_rejects_autonomy_as_string_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\n"
                "autonomy_level: \"1\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'autonomy_level',
        )


def test_schema_rejects_tier_as_string_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: \"1\"\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'tier',
        )


def test_schema_rejects_wrong_claude_repo_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo: other-repo\n"
                "owner: \"fuzzywigg (smtp.eth)\"\nsurface: copilot\n"
                "autonomy_level: 1\nlast_updated: \"2026-04-13\"\n"
                "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'expected',
        )


def test_schema_rejects_empty_badge_edit_policy_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"\"\ncloses: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            'non-empty',
        )


def test_schema_rejects_secret_in_doc_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(tmp_path)
        path = tmp_path / "AGENTS.md"
        path.write_text(
            path.read_text(encoding="utf-8")
            + "\nghp_abcdefghijklmnopqrstuvwxyz0123456789\n",
            encoding="utf-8",
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "secret",
        )


def test_schema_rejects_missing_yaml_block_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(tmp_path, agents="# AGENTS\n\nNo yaml here.\n")
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "yaml",
        )


def test_schema_rejects_missing_file_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(tmp_path)
        (tmp_path / "CLAUDE.md").unlink()
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "missing",
        )


def test_schema_rejects_non_mapping_yaml_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents="# AGENTS\n\n```yaml\n- just\n- a\n- list\n```\n",
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "mapping",
        )


def test_schema_rejects_unparseable_yaml_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents="# AGENTS\n\n```yaml\nversion: [unterminated\n```\n",
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "AGENTS.md",
        )


def test_schema_rejects_wrong_backlog_owner_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            backlog=(
                "# I\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: geryon\nedit_policy: x\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "expected",
        )

def test_schema_rejects_list_status_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus:\n  - ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "scalar",
        )


def test_schema_rejects_nested_edit_policy_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy:\n  rule: invent\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "scalar",
        )


def test_schema_rejects_bool_closes_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: true\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "string",
        )


def test_schema_rejects_int_purpose_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            publish=(
                "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\n"
                "purpose: 99\ncloses: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "string",
        )


def test_schema_rejects_bool_owner_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: false\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "string",
        )


def test_schema_rejects_nested_parent_governance_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance:\n  url: github.com/fuzzywigg/agents-governance\n"
                "autonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "scalar",
        )


def test_schema_rejects_list_maintainer_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer:\n  - smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\n"
                "autonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "scalar",
        )


def test_schema_rejects_float_tier_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1.5\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "tier",
        )


def test_schema_rejects_yes_tier_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: yes\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "tier",
        )


def test_schema_rejects_null_closes_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            publish=(
                "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\n"
                "purpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\n"
                "closes: null\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "non-empty",
        )


def test_schema_rejects_int_surface_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo: agents-governance\n"
                "owner: \"fuzzywigg (smtp.eth)\"\nsurface: 7\nautonomy_level: 1\n"
                "last_updated: \"2026-04-13\"\n"
                "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "string",
        )


def test_schema_rejects_nested_repo_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo:\n  name: agents-governance\n"
                "owner: \"fuzzywigg (smtp.eth)\"\nsurface: copilot\nautonomy_level: 1\n"
                "last_updated: \"2026-04-13\"\n"
                "parent_governance: github.com/fuzzywigg/agents-governance/AGENTS-ECOSYSTEM.md\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "scalar",
        )


def test_schema_rejects_list_version_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion:\n  - \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\n"
                "autonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "scalar",
        )


def test_schema_rejects_bool_version_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: true\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\n"
                "autonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "string",
        )


def test_schema_rejects_wrong_badge_closes_hash_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: \"#99\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "closes",
        )


def test_schema_rejects_wrong_publish_purpose_short_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            publish=(
                "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\n"
                "purpose: \"wiki publish\"\ncloses: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "purpose",
        )


def test_schema_rejects_wrong_front_door_scope_typo_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front door repos\"\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "scope",
        )


def test_schema_gate_requires_string_keys_const_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace("STRING_KEYS = frozenset", "STRING_FIELDS = frozenset")
        # keep STRING_KEYS string elsewhere? replace declaration only already done via STRING_KEYS = 
        text = text.replace("STRING_KEYS", "STRING_FIELDS")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "STRING_KEYS",
        )


def test_schema_gate_requires_reject_non_scalar_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace("reject_non_scalar", "reject_complex")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "reject_non_scalar",
        )


def test_schema_gate_requires_empty_yaml_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace(
            "empty yaml metadata block", "blank yaml metadata block"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "empty yaml",
        )


def test_schema_gate_requires_front_door_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace(
            "public governance front-door repos", "public governance front door"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "front-door",
        )


def test_schema_gate_requires_reversible_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace(
            "Reversible publish path for docs/wiki", "Publish path for docs/wiki"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Reversible publish path",
        )


def test_schema_passes_good_fixture_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(tmp_path)
        assert_pass_script(scripts / "check_stewardship_schema.py", tmp_path)


def test_schema_rejects_tier_zero_still_after_53() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 0\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(
            scripts / "check_stewardship_schema.py",
            tmp_path,
            "tier",
        )


# --- TOKENMAXX deepen after #55: relative-link second-pass helper/constant pins ---

def test_relative_gate_requires_md_link_re_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("MD_LINK_RE", "MARKDOWN_LINK_RE")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "MD_LINK_RE",
        )


def test_relative_gate_requires_skip_parts_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("SKIP_PARTS", "SKIP_DIRS")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "SKIP_PARTS",
        )


def test_relative_gate_requires_skip_prefixes_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("SKIP_PREFIXES", "SKIP_PREFIX")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "SKIP_PREFIXES",
        )


def test_relative_gate_requires_skip_files_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("SKIP_FILES", "SKIP_NAMES")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "SKIP_FILES",
        )


def test_relative_gate_requires_max_unquote_passes_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            "_MAX_UNQUOTE_PASSES", "_MAX_DECODE_PASSES"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "_MAX_UNQUOTE_PASSES",
        )


def test_relative_gate_requires_max_unquote_eq_4_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            "_MAX_UNQUOTE_PASSES = 4", "_MAX_UNQUOTE_PASSES = 8"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "_MAX_UNQUOTE_PASSES = 4",
        )


def test_relative_gate_requires_should_skip_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("should_skip", "must_skip")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "should_skip",
        )


def test_relative_gate_requires_iter_markdown_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("iter_markdown", "list_markdown")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "iter_markdown",
        )


def test_relative_gate_requires_headings_in_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("headings_in", "collect_headings")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "headings_in",
        )


def test_relative_gate_requires_check_file_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("def check_file", "def scan_file")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "check_file",
        )


def test_relative_gate_requires_mailto_allow_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("mailto:", "mail:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "mailto:",
        )


def test_relative_gate_requires_tel_allow_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("tel:", "phone:")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "tel:",
        )


def test_relative_gate_requires_nul_reject_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        # ZERO_BYTE must not contain the substring NUL (NULLBYTE would).
        text = path.read_text(encoding="utf-8").replace("NUL", "ZERO_BYTE")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "NUL",
        )


def test_relative_gate_requires_angle_bracket_strip_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace('strip("<>")', 'strip("[]")')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "angle brackets",
        )


def test_relative_gate_requires_image_link_re_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(r"!?\[", r"\[")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "image links",
        )


def test_relative_gate_requires_escapes_repo_needle_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("escapes repo", "leaves repo")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "escape repo",
        )


def test_relative_gate_requires_broken_link_needle_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            "broken relative link", "missing relative link"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "broken relative link",
        )


def test_relative_gate_requires_missing_heading_needle_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("missing heading", "absent heading")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "missing heading",
        )


def test_relative_gate_requires_rglob_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("rglob", "glob")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "rglob",
        )


def test_relative_gate_requires_no_md_fail_closed_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            "no markdown files found", "no docs found"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "no markdown files found",
        )


def test_relative_links_reject_triple_encoded_escape_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(
            tmp_path / "README.md",
            "# T\n\n[x](%25252e%25252e/etc/passwd)\n",
        )
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "escapes repo",
        )


def test_relative_links_reject_query_with_amp_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](README.md?a=1&b=2)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "query string",
        )


def test_relative_links_reject_empty_frag_whitespace_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](README.md#   )\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "empty fragment",
        )


def test_relative_links_reject_image_http_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n![x](http://example.com/a.png)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "insecure http://",
        )


def test_relative_links_reject_image_data_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n![x](data:image/png;base64,xx)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_image_file_scheme_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n![x](file:///etc/passwd)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_image_vbscript_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n![x](vbscript:msgbox(1))\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_accept_angle_mailto_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](<mailto:a@b.c>)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_accept_angle_tel_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](<tel:+15551212>)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_accept_title_on_https_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(
            tmp_path / "README.md",
            '# T\n\n[x](https://example.com "title")\n',
        )
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_abs_windows_style_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](../../../../../../etc/passwd)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "escapes repo",
        )


def test_relative_links_reject_query_on_image_path_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "a.png", "x")
        _write(tmp_path / "README.md", "# T\n\n![x](a.png?x=1)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "query string",
        )


def test_relative_links_reject_empty_frag_on_image_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "a.png", "x")
        _write(tmp_path / "README.md", "# T\n\n![x](a.png#)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "empty fragment",
        )


def test_relative_links_skip_git_dir_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[ok](LICENSE)\n")
        _write(tmp_path / "LICENSE", "MIT\n")
        _write(tmp_path / ".git" / "broken.md", "# B\n\n[x](./missing.md)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_accept_nested_cross_frag_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "a.md", "# Alpha\n\ntext\n")
        _write(tmp_path / "README.md", "# T\n\n[x](docs/a.md#alpha)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_nested_missing_frag_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "docs" / "a.md", "# Alpha\n\ntext\n")
        _write(tmp_path / "README.md", "# T\n\n[x](docs/a.md#beta)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "missing heading",
        )


def test_relative_links_accept_self_frag_slug_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Hello World\n\n[x](#hello-world)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_self_frag_missing_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Hello World\n\n[x](#goodbye)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "missing heading",
        )


def test_relative_links_reject_whitespace_only_target_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](   )\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "empty relative link",
        )


def test_relative_links_accept_https_image_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(
            tmp_path / "README.md",
            "# T\n\n![x](https://example.com/a.png)\n",
        )
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_fully_unquote_cap_helper_after_55() -> None:
    import sys

    sys.path.insert(0, str(SCRIPTS))
    from check_relative_links import _MAX_UNQUOTE_PASSES, fully_unquote  # noqa: E402

    if _MAX_UNQUOTE_PASSES != 4:
        raise AssertionError(f"_MAX_UNQUOTE_PASSES must be 4; got {_MAX_UNQUOTE_PASSES}")
    decoded = fully_unquote("%252e%252e")
    if ".." not in decoded and decoded == "%252e%252e":
        raise AssertionError("fully_unquote should decode nested percent sequences")


def test_relative_gate_requires_owasp_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("OWASP-AGENTIC.md", "OTHER.md")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "OWASP-AGENTIC.md",
        )


def test_relative_gate_requires_fully_unquote_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("fully_unquote", "decode_all")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "fully_unquote",
        )


def test_relative_gate_requires_empty_fragment_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            "empty fragment", "blank fragment"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "empty path#",
        )


def test_relative_gate_requires_query_string_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("query string", "query param")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "query string",
        )


def test_relative_gate_requires_github_slug_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("github_slug", "gh_slug")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "github_slug",
        )


def test_relative_gate_requires_atx_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace("ATX_HEADING_RE", "HEADING_RE")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "ATX",
        )


def test_relative_gate_requires_strip_fenced_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            "strip_fenced_code", "strip_code_fences"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "fenced code",
        )


def test_relative_gate_requires_dangerous_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            "has_dangerous_scheme", "has_bad_scheme"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "dangerous",
        )


def test_run_stewardship_requires_gate_order_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        text = text.replace(
            "python3 scripts/check_stewardship_schema.py\n"
            "python3 scripts/check_relative_links.py\n",
            "python3 scripts/check_relative_links.py\n"
            "python3 scripts/check_stewardship_schema.py\n",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "badge → wiki → schema → relative",
        )


def test_relative_links_reject_http_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](http://example.com)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "insecure http://",
        )


def test_relative_links_reject_protocol_rel_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](//example.com/a)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "protocol-relative",
        )


def test_relative_links_reject_javascript_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](javascript:alert(1))\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_data_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](data:text/html,hi)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_file_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](file:///tmp/x)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_vbscript_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](vbscript:msgbox(1))\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "dangerous link scheme",
        )


def test_relative_links_reject_empty_parens_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x]()\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "empty relative link",
        )


def test_relative_links_reject_bare_hash_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](#)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "empty relative link",
        )


def test_relative_links_reject_broken_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](./nope.md)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "broken relative link",
        )


def test_relative_links_reject_empty_path_frag_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](README.md#)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "empty fragment",
        )


def test_relative_links_reject_query_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](README.md?x=1)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "query string",
        )


def test_relative_links_accept_https_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](https://example.com)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_accept_mailto_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](mailto:a@b.c)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_accept_tel_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](tel:+1)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_ignore_fenced_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(
            tmp_path / "README.md",
            "# T\n\n```md\n[x](./missing.md)\n```\n",
        )
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_ignore_tilde_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(
            tmp_path / "README.md",
            "# T\n\n~~~\n[x](./missing.md)\n~~~\n",
        )
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_skip_owasp_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[ok](LICENSE)\n")
        _write(tmp_path / "LICENSE", "MIT\n")
        _write(tmp_path / "OWASP-AGENTIC.md", "# O\n\n[x](./missing.md)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_skip_agents_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[ok](LICENSE)\n")
        _write(tmp_path / "LICENSE", "MIT\n")
        _write(
            tmp_path / ".github" / "agents" / "broken.md",
            "# A\n\n[x](./missing.md)\n",
        )
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_skip_node_modules_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[ok](LICENSE)\n")
        _write(tmp_path / "LICENSE", "MIT\n")
        _write(
            tmp_path / "node_modules" / "pkg" / "broken.md",
            "# N\n\n[x](./missing.md)\n",
        )
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_accept_valid_fragment_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# Title\n\n[x](#title)\n")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)


def test_relative_links_reject_nul_still_after_55() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        _write(tmp_path / "README.md", "# T\n\n[x](foo%00.md)\n")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "NUL",
        )


# --- TOKENMAXX deepen after #59: wiki-outline second-pass helper/constant pins ---


def test_wiki_gate_requires_operator_only_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace("OPERATOR_ONLY", "OPERATOR_PAGE")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "OPERATOR_ONLY",
        )


def test_wiki_gate_requires_operator_only_eq_publish_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            'OPERATOR_ONLY = "PUBLISH.md"', 'OPERATOR_ONLY = "DRAFT.md"'
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'OPERATOR_ONLY = "PUBLISH.md"',
        )


def test_wiki_gate_requires_docs_path_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(' / "docs" / ', ' / "documentation" / ')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "docs/",
        )


def test_wiki_gate_requires_wiki_path_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        # Only rewrite the WIKI path segment; leave docs/wiki prose needles intact.
        text = path.read_text(encoding="utf-8").replace(
            'ROOT / "docs" / "wiki"', 'ROOT / "docs" / "pages"'
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "docs/wiki",
        )


def test_wiki_gate_requires_reject_invent_helper_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "_reject_invent_badge_chrome", "_reject_product_chrome"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "_reject_invent_badge_chrome",
        )


def test_wiki_gate_requires_autonomy_topic_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"autonomy"', '"agency"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "autonomy",
        )


def test_wiki_gate_requires_governance_topic_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"governance"', '"policy"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "governance",
        )


def test_wiki_gate_requires_public_topic_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"public"', '"open"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "public",
        )


def test_wiki_gate_requires_kill_topic_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"kill"', '"halt"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "kill",
        )


def test_wiki_gate_requires_secret_topic_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"secret"', '"credential-secret"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "secret",
        )


def test_wiki_gate_requires_surface_topic_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"surface"', '"channel"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "surface",
        )


def test_wiki_gate_requires_routing_topic_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"routing"', '"dispatch"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "routing",
        )


def test_wiki_gate_requires_run_script_topic_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            '"run_stewardship_checks.sh"', '"run_checks.sh"'
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "run_stewardship_checks.sh",
        )


def test_wiki_gate_requires_badge_topic_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"badge"', '"emblem"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "badge",
        )


def test_wiki_gate_requires_home_backlink_needle_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace("](Home.md)", "](Index.md)")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "](Home.md)",
        )


def test_wiki_gate_requires_unexpected_md_needle_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "Unexpected markdown", "Extra markdown"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Unexpected markdown",
        )


def test_wiki_gate_requires_missing_wiki_needle_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "Missing required wiki", "Absent required wiki"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Missing required wiki",
        )


def test_wiki_gate_requires_failed_banner_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "Wiki outline check FAILED", "Wiki outline check ERROR"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Wiki outline check FAILED",
        )


def test_wiki_gate_requires_ok_match_needle_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "docs/wiki matches PUBLISH.md", "docs/wiki aligns PUBLISH.md"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "docs/wiki matches PUBLISH.md",
        )


def test_wiki_gate_requires_angle_bracket_strip_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('strip("<>")', 'strip("[]")')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "angle brackets",
        )


def test_wiki_gate_requires_image_link_re_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(r"!?\[" , r"\[")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "image links",
        )


def test_wiki_gate_requires_do_not_push_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8")
        text = text.replace("Do **not** push", "Never push")
        text = text.replace("Do not push", "Never push")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Do not push",
        )


def test_wiki_gate_requires_readme_blob_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "https://github.com/fuzzywigg/agents-governance/blob/main/README.md",
            "https://example.com/README.md",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "README blob URL",
        )


def test_wiki_gate_requires_badge_hint_path_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "../badge-standard.md", "../badges.md"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "../badge-standard.md",
        )


def test_wiki_gate_requires_stars_special_case_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"stars"', '"stargazers"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "stars",
        )


def test_wiki_gate_requires_forks_special_case_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"forks"', '"fork-count"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "forks",
        )


def test_wiki_gate_requires_followers_special_case_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"followers"', '"subs"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "followers",
        )


def test_wiki_gate_requires_invent_chrome_needle_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "invent-product / social badge chrome",
            "invent-product / social chrome",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "invent-product / social badge chrome",
        )


def test_wiki_gate_requires_home_table_row_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace("| `Home.md` |", "| `Index.md` |")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Home.md",
        )


def test_wiki_rejects_missing_public_topic_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = "# O\n\n[Home](Home.md)\n\ngovernance\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "public",
        )


def test_wiki_rejects_missing_autonomy_topic_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Autonomy-Levels.md"] = "# A\n\n[Home](Home.md)\n\nL0 L1 L2 L3\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "autonomy",
        )


def test_wiki_rejects_missing_routing_topic_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Agent-Routing.md"] = "# R\n\n[Home](Home.md)\n\nsurface copilot\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "routing",
        )


def test_wiki_rejects_missing_governance_topic_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = "# O\n\n[Home](Home.md)\n\npublic\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "governance",
        )


def test_wiki_rejects_followers_chrome_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\nFollowers badge chrome.\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "followers",
        )


def test_wiki_rejects_twitter_chrome_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\nTwitter badge chrome.\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "twitter",
        )


def test_wiki_rejects_xcom_chrome_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\nx.com badge chrome.\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "x.com",
        )


def test_wiki_rejects_coveralls_chrome_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n![c](https://img.shields.io/coveralls/x)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "coveralls",
        )


def test_wiki_rejects_image_http_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n![x](http://example.com/a.png)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "http://",
        )


def test_wiki_rejects_image_javascript_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n![x](javascript:alert(1))\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "javascript:",
        )


def test_wiki_rejects_image_data_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n![x](data:text/plain,hi)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "data:",
        )


def test_wiki_rejects_image_file_scheme_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n![x](file:///etc/passwd)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "file:",
        )


def test_wiki_accepts_angle_https_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n[x](<https://example.com>)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_pass_script(scripts / "check_wiki_outline.py", tmp_path)


def test_wiki_accepts_title_attr_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += '\n[x](https://example.com "t")\n'

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_pass_script(scripts / "check_wiki_outline.py", tmp_path)


def test_wiki_accepts_mailto_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n[x](mailto:a@example.com)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_pass_script(scripts / "check_wiki_outline.py", tmp_path)


def test_wiki_accepts_tel_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n[x](tel:+15551212)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_pass_script(scripts / "check_wiki_outline.py", tmp_path)


def test_wiki_rejects_missing_home_to_overview_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace(
                "[Overview](Overview.md)\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Overview.md",
        )


def test_wiki_rejects_missing_home_to_autonomy_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace(
                "[Autonomy-Levels](Autonomy-Levels.md)\n", ""
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Autonomy-Levels.md",
        )


def test_wiki_rejects_missing_overview_home_backlink_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] = "# O\n\ngovernance public\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Home.md",
        )


def test_wiki_rejects_missing_publish_secrets_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = pages["PUBLISH.md"].replace(
                "No secrets.", "No private keys."
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "secrets",
        )


def test_wiki_rejects_missing_publish_link_check_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = pages["PUBLISH.md"].replace(
                "Link Check and Markdown Lint. No secrets.\n",
                "Markdown Lint. No secrets.\n",
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "Link Check",
        )


def test_wiki_rejects_secret_in_publish_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] += "\nghp_abcdefghijklmnopqrstuvwxyz0123456789\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "secret",
        )


def test_wiki_rejects_downloads_chrome_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\nDownloads badge chrome.\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "downloads",
        )


def test_wiki_rejects_opencollective_chrome_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += (
                "\n![oc](https://img.shields.io/opencollective/all/x)\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "opencollective",
        )


def test_wiki_ignores_fenced_protocol_rel_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += (
                "\n```md\n"
                "[x](//evil.example/x)\n"
                "```\n"
            )

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_pass_script(scripts / "check_wiki_outline.py", tmp_path)


def test_wiki_rejects_http_title_attr_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += '\n[x](http://example.com "t")\n'

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "http://",
        )


def test_wiki_rejects_protocol_rel_title_attr_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += '\n[x](//evil.example/x "t")\n'

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            "protocol-relative",
        )


def test_wiki_gate_requires_publishable_pages_still_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "PUBLISHABLE_PAGES", "PUBLISH_PAGES"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "PUBLISHABLE_PAGES",
        )


def test_wiki_gate_requires_topic_hints_still_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "PAGE_TOPIC_HINTS", "PAGE_HINTS"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "PAGE_TOPIC_HINTS",
        )


def test_wiki_gate_requires_l2_still_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"L2"', '"Ltwo"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "L2",
        )


def test_wiki_gate_requires_credential_still_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"credential"', '"creds"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "credential",
        )


def test_wiki_gate_requires_copilot_still_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"copilot"', '"pilot"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "copilot",
        )


def test_wiki_gate_requires_strip_fenced_still_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "strip_fenced_code", "strip_code_fences"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "fenced code",
        )


def test_wiki_gate_requires_dangerous_still_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "has_dangerous_scheme", "has_unsafe_scheme"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "dangerous",
        )


def test_wiki_gate_requires_protocol_rel_still_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "protocol-relative", "scheme-relative"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "protocol-relative",
        )


def test_wiki_gate_requires_http_reject_still_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace("http://", "HTTP_SCHEME")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "http://",
        )


def test_wiki_gate_requires_forbidden_hints_still_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "FORBIDDEN_BADGE_HINTS", "BANNED_BADGE_HINTS"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "FORBIDDEN_BADGE_HINTS",
        )


def test_wiki_gate_requires_stewardship_ci_still_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace(
            "STEWARDSHIP_CI_HINTS", "STEWARD_CI_HINTS"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "STEWARDSHIP_CI_HINTS",
        )


def test_wiki_gate_requires_actionlint_still_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace("actionlint", "workflowlint")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "actionlint",
        )


def test_wiki_gate_requires_invent_still_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace("invent", "fabricate")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "invent",
        )


def test_wiki_gate_requires_kill_switch_still_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace("kill-switch", "halt-switch")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "kill-switch",
        )


def test_wiki_gate_requires_scan_secrets_still_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace("scan_secrets", "scan_tokens")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "scan_secrets",
        )


def test_wiki_passes_good_fixture_still_after_59() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_wiki_tree(tmp_path)
        assert_pass_script(scripts / "check_wiki_outline.py", tmp_path)


# --- TOKENMAXX deepen after #61: badge-standard second-pass helper/constant pins ---

def test_badge_gate_requires_required_order_eq_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('REQUIRED_ORDER = ("Link Check", "Markdown Lint", "License")', 'REQUIRED_ORDER = ("License", "Markdown Lint", "Link Check")')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'REQUIRED_ORDER = ("Link Check", "Markdown Lint", "License")',
        )


def test_badge_gate_requires_expected_repo_eq_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('EXPECTED_REPO = "fuzzywigg/agents-governance"', 'EXPECTED_REPO = "fuzzywigg/other-repo"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'EXPECTED_REPO = "fuzzywigg/agents-governance"',
        )


def test_badge_gate_requires_readme_path_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(' / "README.md"', ' / "Readme.md"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'README.md',
        )


def test_badge_gate_requires_license_path_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('ROOT / "LICENSE"', 'ROOT / "LICENCE"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'LICENSE',
        )


def test_badge_gate_requires_badge_standard_path_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"badge-standard.md"', '"badge-policy.md"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'badge-standard.md',
        )


def test_badge_gate_requires_contributing_path_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"CONTRIBUTING.md"', '"CONTRIBUTE.md"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'CONTRIBUTING.md',
        )


def test_badge_gate_requires_agents_path_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"AGENTS.md"', '"AGENT.md"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'AGENTS.md',
        )


def test_badge_gate_requires_lycheeignore_path_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('".lycheeignore"', '".lychee-ignore"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '.lycheeignore',
        )


def test_badge_gate_requires_markdownlint_path_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('".markdownlint.json"', '".markdown-lint.json"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '.markdownlint.json',
        )


def test_badge_gate_requires_contract_fn_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('check_badge_standard_gate_contract', 'check_badge_gate_contract')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_badge_standard_gate_contract',
        )


def test_badge_gate_requires_main_fn_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def main(', 'def run_main(')
        text = text.replace("sys.exit(main())", "sys.exit(run_main())")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'main()',
        )


def test_badge_gate_requires_full_link_check_svg_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('actions/workflows/link-check.yml/badge.svg', 'actions/workflows/link-check.yml/badge.png')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'actions/workflows/link-check.yml/badge.svg',
        )


def test_badge_gate_requires_full_md_lint_svg_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('actions/workflows/markdown-lint.yml/badge.svg', 'actions/workflows/markdown-lint.yml/badge.png')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'actions/workflows/markdown-lint.yml/badge.svg',
        )


def test_badge_gate_requires_exactly_needle_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Badge row must have exactly', 'Badge row must contain exactly')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Badge row must have exactly',
        )


def test_badge_gate_requires_order_needle_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Badge labels must be in order', 'Badge labels must follow order')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Badge labels must be in order',
        )


def test_badge_gate_requires_contiguous_needle_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Badge row must be contiguous', 'Badge row must be adjacent')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Badge row must be contiguous',
        )


def test_badge_gate_requires_missing_h1_needle_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('missing H1', 'missing title')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'missing H1',
        )


def test_badge_gate_requires_unexpected_label_needle_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Unexpected badge label', 'Unknown badge label')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Unexpected badge label',
        )


def test_badge_gate_requires_forbidden_hint_needle_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Forbidden invent-product / social badge hint', 'Forbidden invent-product / social chrome hint')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Forbidden invent-product / social badge hint',
        )


def test_badge_gate_requires_secret_token_needle_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Secret-like token in badge URL', 'Secret-like value in badge URL')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Secret-like token in badge URL',
        )


def test_badge_gate_requires_failed_banner_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Badge standard check FAILED', 'Badge standard check ERROR')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Badge standard check FAILED',
        )


def test_badge_gate_requires_ok_match_needle_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('OK: README badge row matches docs/badge-standard.md', 'OK: README badge row aligns docs/badge-standard.md')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'OK: README badge row matches docs/badge-standard.md',
        )


def test_badge_gate_requires_endswith_license_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('endswith("/LICENSE")', 'endswith("/licence")')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'endswith /LICENSE',
        )


def test_badge_gate_requires_dot_slash_license_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"./LICENSE"', '"./LICENCE"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            './LICENSE',
        )


def test_badge_gate_requires_load_workflow_text_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('load_workflow_text', 'read_workflow_text')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'load_workflow_text',
        )


def test_badge_gate_requires_invent_product_doc_pin_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('do not invent product badges', 'do not invent product chrome')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'do not invent product badges',
        )


def test_badge_gate_requires_intentionally_pin_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('intentionally', 'deliberately')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'intentionally',
        )


def test_badge_gate_requires_quiet_stewardship_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('quiet stewardship', 'quiet governance')
        text = text.replace("Quiet stewardship", "Quiet governance")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'quiet stewardship',
        )


def test_badge_gate_requires_selftest_agents_pin_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('test_stewardship_gates.py', 'test_stewardship.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'test_stewardship_gates.py',
        )


def test_badge_gate_requires_relative_agents_pin_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"relative"', '"rel-links"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'relative',
        )


def test_badge_gate_requires_img_startswith_https_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('img.startswith("https://")', 'img.startswith("HTTPS://")')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'img.startswith(https://)',
        )


def test_badge_gate_requires_link_http_reject_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('link.startswith("http://")', 'link.startswith("ftp://")')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'link.startswith(http://)',
        )


def test_badge_rejects_wrong_order_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme(("Markdown Lint", "Link Check", "License")))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Badge labels must be in order',
        )


def test_badge_rejects_four_badges_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n"
            "[![coverage](https://img.shields.io/badge/coverage-1-red)](https://example.com)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'exactly 3 badges',
        )


def test_badge_rejects_http_image_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](http://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'must be https://',
        )


def test_badge_rejects_stewardship_product_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![Stewardship](https://img.shields.io/badge/stewardship-x-blue)]"
            "(https://example.com)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Stewardship',
        )


def test_badge_rejects_wrong_repo_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/other/repo/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/other/repo/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'fuzzywigg/agents-governance',
        )


def test_badge_rejects_noncontiguous_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'contiguous',
        )


def test_badge_rejects_secret_url_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?token=abc)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Secret-like token',
        )


def test_badge_rejects_forbidden_coverage_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/badge/coverage-99-green)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'coverage',
        )


def test_badge_rejects_two_badges_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'exactly 3 badges',
        )


def test_badge_rejects_missing_h1_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'missing H1',
        )


def test_badge_rejects_wrong_license_link_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(https://example.com/COPYING)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'LICENSE',
        )


def test_badge_rejects_http_badge_link_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(http://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'https://',
        )


def test_badge_accepts_dot_slash_license_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(./LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)


def test_badge_accepts_absolute_license_blob_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(https://github.com/fuzzywigg/agents-governance/blob/main/LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)


def test_badge_passes_good_fixture_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)


def test_badge_rejects_codecov_hint_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/badge/codecov-1-red)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'codecov',
        )


def test_badge_rejects_downloads_hint_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/badge/downloads-1-red)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'downloads',
        )


def test_badge_rejects_stars_hint_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/badge/stars-1-red)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'stars',
        )


def test_badge_rejects_wrong_link_check_image_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/other.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'link-check.yml/badge.svg',
        )


def test_badge_rejects_wrong_md_lint_image_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/other.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'markdown-lint.yml/badge.svg',
        )


def test_badge_gate_requires_required_order_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('REQUIRED_ORDER', 'REQUIRED_LABELS')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'REQUIRED_ORDER',
        )


def test_badge_gate_requires_max_badges_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('MAX_BADGES', 'MAX_BADGE_COUNT')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MAX_BADGES',
        )


def test_badge_gate_requires_expected_repo_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('EXPECTED_REPO', 'EXPECTED_SLUG')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'EXPECTED_REPO',
        )


def test_badge_gate_requires_required_workflows_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('REQUIRED_WORKFLOWS', 'REQUIRED_WF')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'REQUIRED_WORKFLOWS',
        )


def test_badge_gate_requires_badge_line_re_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('BADGE_LINE_RE', 'BADGE_RE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'BADGE_LINE_RE',
        )


def test_badge_gate_requires_max_eq_3_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('MAX_BADGES = 3', 'MAX_BADGES = 4')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MAX_BADGES = 3',
        )


def test_badge_gate_requires_link_svg_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('link-check.yml/badge.svg', 'link-check.yml/badge.png')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'link-check.yml/badge.svg',
        )


def test_badge_gate_requires_md_svg_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('markdown-lint.yml/badge.svg', 'markdown-lint.yml/badge.png')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'markdown-lint.yml/badge.svg',
        )


def test_badge_gate_requires_shields_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('img.shields.io/github/license/', 'img.shields.io/github/licence/')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'img.shields.io/github/license/',
        )


def test_badge_gate_requires_contiguous_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('contiguous', 'adjacent')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'contiguous',
        )


def test_badge_gate_requires_fourth_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('fourth', 'quaternary')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'fourth',
        )


def test_badge_gate_requires_stew_reject_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Stewardship product/status badge', 'Stewardship product/status emblem')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Stewardship product/status badge',
        )


def test_badge_gate_requires_live_row_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('live badge row is Link Check', 'live badge row is License')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'live badge row is Link Check',
        )


def test_badge_gate_requires_forbidden_import_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('FORBIDDEN_BADGE_HINTS', 'FORBIDDEN_HINTS')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'FORBIDDEN_BADGE_HINTS',
        )


def test_badge_gate_requires_scan_secrets_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('scan_secrets', 'scan_for_secrets')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'scan_secrets',
        )


def test_badge_passes_live_contract_still_after_61() -> None:
    assert_pass_live("check_badge_standard.py")


def test_badge_rejects_wrong_order_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme(("License", "Link Check", "Markdown Lint")))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Badge labels must be in order',
        )


def test_badge_passes_good_fixture_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)


def test_badge_rejects_invent_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n"
            "[![coverage](https://codecov.io/gh/x/y/badge.svg)](https://codecov.io)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'exactly 3 badges',
        )


def test_badge_rejects_secret_url_still_after_61() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?api_key=x)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Secret-like token',
        )



# --- TOKENMAXX deepen after #65: stewardship_common second-pass pins ---

def test_common_gate_requires_root_parents_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace(
            'Path(__file__).resolve().parents[1]',
            'Path(__file__).resolve().parent.parent',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'parents[1]',
        )

def test_common_gate_requires_fence_dotall_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('re.DOTALL', 're.MULTILINE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'DOTALL',
        )

def test_common_gate_requires_fence_pattern_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('(?:```|~~~).*?(?:```|~~~)', '(?:```).*?(?:```)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '~~~',
        )

def test_common_gate_requires_strip_doc_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('Remove fenced code blocks so example links do not fail integrity gates.', 'Remove fenced code blocks so example links do not fail soft gates.')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'integrity gates',
        )

def test_common_gate_requires_danger_doc_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('Return the matched dangerous scheme prefix, or None.', 'Return the matched dangerous scheme prefix, or empty.')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'scheme prefix doc pin',
        )

def test_common_gate_requires_scan_doc_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('Append errors if path content matches forbidden secret-like patterns.', 'Append errors if path content matches forbidden secret-like values.')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'secret-like patterns',
        )

def test_common_gate_requires_md_doc_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('Collect markdown paths under ROOT for the given relative globs.', 'Collect markdown paths under ROOT for the given relative paths.')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'relative globs',
        )

def test_common_gate_requires_pattern_needle_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('matches forbidden secret-like pattern', 'matches forbidden secret-like value')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'secret-like pattern',
        )

def test_common_gate_requires_url_hint_needle_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('secret-like URL hint', 'secret-like URL marker')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'URL hint',
        )

def test_common_gate_requires_token_hint_needle_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('secret-like token hint', 'secret-like token marker')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'token hint',
        )

def test_common_gate_requires_relative_to_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('relative_to', 'relative_path')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'relative_to',
        )

def test_common_gate_requires_strip_lower_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('strip().lower()', 'casefold()')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'strip().lower()',
        )

def test_common_gate_requires_startswith_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('startswith', 'begins_with')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'startswith',
        )

def test_common_gate_requires_errors_append_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('errors.append(msg)', 'errors.append(str(msg))')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'errors.append(msg)',
        )

def test_common_gate_requires_passwd_token_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('password|passwd|token', 'password|passwd|secret')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'password|passwd|token',
        )

def test_common_gate_requires_openssh_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('OPENSSH', 'OPEN_SSH')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'OPENSSH',
        )

def test_common_gate_requires_openssh_ec_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('OPENSSH |EC', 'OPENSSH |DSA')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'OPENSSH|EC',
        )

def test_common_gate_requires_public_docs_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('Public docs must not ship secrets', 'Public docs must not ship credentials')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Public docs must not ship secrets',
        )

def test_common_gate_requires_invent_surface_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('no invent-product surface', 'no invent-product chrome')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'invent-product surface',
        )

def test_common_gate_requires_social_chrome_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('Invent-product / social chrome', 'Invent-product / social badges')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'social chrome',
        )

def test_common_gate_requires_link_schemes_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('Link schemes that must never appear', 'Link schemes that must never show')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'must never appear',
        )

def test_common_gate_requires_is_file_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('is_file()', 'exists()')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'is_file()',
        )

def test_common_gate_requires_sorted_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('sorted(', 'list(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'sorted(',
        )

def test_common_gate_requires_workflows_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('"workflows"', '"workflow"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'workflows',
        )

def test_common_gate_requires_return_none_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('return None', "return ''")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'return None',
        )

def test_common_gate_requires_url_hints_head_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('SECRET_URL_HINTS = (\n    "token=",', 'SECRET_URL_HINTS = (\n    "access_token=",')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '"token="',
        )

def test_common_gate_requires_schemes_js_first_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('DANGEROUS_LINK_SCHEMES = (\n    "javascript:",\n    "data:",\n    "vbscript:",\n    "file:",\n)', 'DANGEROUS_LINK_SCHEMES = (\n    "data:",\n    "javascript:",\n    "vbscript:",\n    "file:",\n)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'javascript:',
        )

def test_common_gate_requires_contract_fn_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def check_stewardship_common_contract(', 'def check_stewardship_common_contract_x(', 1)
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_stewardship_common_contract',
        )

def test_common_gate_requires_contract_call_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('check_stewardship_common_contract(errors)', 'check_stewardship_common_contract_x(errors)', 1)
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_stewardship_common_contract',
        )

def test_common_secret_patterns_ghp_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402
    sample = "auth ghp_" + ("A" * 24) + " end"
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS must still match ghp_ after #65")

def test_common_secret_patterns_github_pat_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402
    sample = "github_pat_" + ("B" * 24)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS must still match github_pat_ after #65")

def test_common_secret_patterns_npm_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402
    sample = "npm_" + ("C" * 24)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS must still match npm_ after #65")

def test_common_secret_patterns_aiza_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402
    sample = "AIza" + ("D" * 24)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS must still match AIza after #65")

def test_common_secret_patterns_passwd_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402
    sample = 'password = "hunter2xx"'
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS must still match password= after #65")

def test_common_secret_patterns_openssh_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402
    sample = "-----BEGIN OPENSSH PRIVATE KEY-----"
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS must still match OPENSSH after #65")

def test_common_dangerous_schemes_complete_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import DANGEROUS_LINK_SCHEMES  # noqa: E402
    for scheme in ("javascript:", "data:", "vbscript:", "file:"):
        if scheme not in DANGEROUS_LINK_SCHEMES:
            raise AssertionError(f"DANGEROUS_LINK_SCHEMES missing {scheme} after #65")

def test_common_has_dangerous_javascript_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402
    if has_dangerous_scheme("  JavaScript:alert(1) ") != "javascript:":
        raise AssertionError("has_dangerous_scheme must casefold javascript after #65")

def test_common_has_dangerous_data_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402
    if has_dangerous_scheme("data:text/html,hi") != "data:":
        raise AssertionError("has_dangerous_scheme must match data: after #65")

def test_common_has_dangerous_none_https_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402
    if has_dangerous_scheme("https://example.com") is not None:
        raise AssertionError("has_dangerous_scheme must allow https after #65")

def test_common_forbidden_hints_coverage_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import FORBIDDEN_BADGE_HINTS  # noqa: E402
    for hint in ("coverage", "codecov", "stars", "opencollective"):
        if hint not in FORBIDDEN_BADGE_HINTS:
            raise AssertionError(f"FORBIDDEN_BADGE_HINTS missing {hint} after #65")

def test_common_strip_fenced_tilde_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import strip_fenced_code  # noqa: E402
    out = strip_fenced_code("before\n~~~js\n[x](javascript:1)\n~~~\nafter")
    if "javascript:" in out or "~~~" in out:
        raise AssertionError("strip_fenced_code must strip tilde fences after #65")

def test_common_strip_fenced_backtick_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import strip_fenced_code  # noqa: E402
    out = strip_fenced_code("before\n```md\n[x](data:1)\n```\nafter")
    if "data:" in out or "```" in out:
        raise AssertionError("strip_fenced_code must strip backtick fences after #65")

def test_common_scan_secrets_url_hint_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        doc = tmp_path / "docs" / "badge-standard.md"
        doc.write_text(doc.read_text(encoding="utf-8") + "\nhttps://example.com/?token=abc\n", encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "token=")

def test_common_scan_secrets_ghp_prefix_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        doc = tmp_path / "README.md"
        doc.write_text(doc.read_text(encoding="utf-8") + "\nghp_" + ("E" * 24) + "\n", encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "ghp_")

def test_common_load_workflow_text_live_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import load_workflow_text  # noqa: E402
    text = load_workflow_text("link-check.yml")
    if text is None or "lychee" not in text.lower():
        raise AssertionError("load_workflow_text must load link-check.yml after #65")

def test_common_load_workflow_text_missing_none_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import load_workflow_text  # noqa: E402
    if load_workflow_text("does-not-exist.yml") is not None:
        raise AssertionError("load_workflow_text must return None for missing after #65")

def test_common_markdown_files_readme_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import markdown_files  # noqa: E402
    found = markdown_files("README.md")
    if not found or found[0].name != "README.md":
        raise AssertionError("markdown_files must find README.md after #65")

def test_common_fail_appends_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import fail  # noqa: E402
    errors: list[str] = []
    fail("pin-check-65", errors)
    if errors != ["pin-check-65"]:
        raise AssertionError("fail() did not append after #65")

def test_common_secret_url_hints_complete_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_URL_HINTS  # noqa: E402
    for hint in ("token=", "access_token=", "client_secret=", "github_pat_", "ghp_", "gho_"):
        if hint not in SECRET_URL_HINTS:
            raise AssertionError(f"SECRET_URL_HINTS missing {hint} after #65")

def test_common_root_parents_live_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import ROOT  # noqa: E402
    if not (ROOT / "scripts" / "stewardship_common.py").is_file():
        raise AssertionError("ROOT parents[1] must point at repo root after #65")

def test_common_gate_requires_secret_patterns_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('SECRET_PATTERNS', 'SECRET_REGEXES')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'SECRET_PATTERNS',
        )

def test_common_gate_requires_secret_url_hints_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('SECRET_URL_HINTS', 'SECRET_URL_HINTS_X')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'SECRET_URL_HINTS',
        )

def test_common_gate_requires_forbidden_badge_hints_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('FORBIDDEN_BADGE_HINTS', 'FORBIDDEN_BADGE_HINTS_X')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'FORBIDDEN_BADGE_HINTS',
        )

def test_common_gate_requires_dangerous_link_schemes_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('DANGEROUS_LINK_SCHEMES', 'BAD_LINK_SCHEMES')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'DANGEROUS_LINK_SCHEMES',
        )

def test_common_gate_requires_fenced_block_re_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('FENCED_BLOCK_RE', 'FENCE_RE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'FENCED_BLOCK_RE',
        )

def test_common_gate_requires_strip_fenced_code_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('def strip_fenced_code(', 'def strip_fenced_code_x(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'strip_fenced_code()',
        )

def test_common_gate_requires_has_dangerous_scheme_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('def has_dangerous_scheme(', 'def has_dangerous_scheme_x(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'has_dangerous_scheme()',
        )

def test_common_gate_requires_markdown_files_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('def markdown_files(', 'def markdown_files_x(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'markdown_files()',
        )

def test_common_gate_requires_fail_helper_pin_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('Gate fail-closed helper', 'Gate soft helper')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Gate fail-closed helper',
        )

def test_common_gate_requires_load_workflow_pin_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('Load .github/workflows/', 'Load github/workflows/')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'workflows load pin',
        )

def test_common_gate_requires_secret_ghp_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('ghp_', 'ghx_')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ghp_',
        )

def test_common_gate_requires_secret_github_pat_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('github_pat_', 'github_token_')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'github_pat_',
        )

def test_common_gate_requires_url_hint_token_eq_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('"token="', '"tok="')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'token=',
        )

def test_common_gate_requires_forbidden_coverage_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('"coverage"', '"cover"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'coverage',
        )

def test_common_gate_requires_scheme_js_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('"javascript:"', '"jscript:"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'javascript:',
        )

def test_common_gate_requires_endswith_urlish_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('endswith', 'ends_with')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'endswith',
        )

def test_common_gate_requires_invent_wording_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('invent', 'innovate')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'invent',
        )

def test_common_gate_requires_live_secret_scan_pin_still_after_65() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "stewardship_common.py"
        text = path.read_text(encoding="utf-8").replace('live secret scan covers CI tokens', 'live secret scan covers tokens')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'live secret scan covers CI tokens',
        )

def test_common_secret_patterns_sk_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402
    sample = "sk-" + ("F" * 24)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS must still match sk- after #65")

def test_common_secret_patterns_xox_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import SECRET_PATTERNS  # noqa: E402
    sample = "xoxb-" + ("G" * 12)
    if not any(p.search(sample) for p in SECRET_PATTERNS):
        raise AssertionError("SECRET_PATTERNS must still match xox after #65")

def test_common_has_dangerous_vbscript_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402
    if has_dangerous_scheme("vbscript:msgbox") != "vbscript:":
        raise AssertionError("has_dangerous_scheme must match vbscript: after #65")

def test_common_has_dangerous_file_still_after_65() -> None:
    sys.path.insert(0, str(SCRIPTS))
    from stewardship_common import has_dangerous_scheme  # noqa: E402
    if has_dangerous_scheme("file:///etc/passwd") != "file:":
        raise AssertionError("has_dangerous_scheme must match file: after #65")



# --- TOKENMAXX deepen after #72: CI workflow second-pass pins ---


def test_link_check_requires_name_link_check_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('name: Link Check', 'name: URL Check')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'name: Link Check',
        )

def test_markdown_lint_requires_name_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('name: Markdown Lint', 'name: MD Lint')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'name: Markdown Lint',
        )

def test_stewardship_requires_name_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('name: Stewardship Checks', 'name: Stewardship Gate')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'name: Stewardship Checks',
        )

def test_link_check_requires_branches_glob_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('branches: ["**"]', 'branches: ["main"]')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'branches: ["**"]',
        )

def test_markdown_lint_requires_branches_glob_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('branches: ["**"]', 'branches: ["main"]')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'branches: ["**"]',
        )

def test_stewardship_requires_branches_glob_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('branches: ["**"]', 'branches: ["main"]')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'branches: ["**"]',
        )

def test_link_check_requires_concurrency_prefix_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('link-check-', 'lnk-check-')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'link-check-',
        )

def test_markdown_lint_requires_concurrency_prefix_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('markdown-lint-', 'md-lint-')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'markdown-lint-',
        )

def test_stewardship_requires_concurrency_prefix_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('stewardship-checks-', 'stew-checks-')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'stewardship-checks-',
        )

def test_link_check_requires_github_workflow_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('github.workflow', 'github.job')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'github.workflow',
        )

def test_link_check_requires_github_ref_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('github.ref', 'github.sha')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'github.ref',
        )

def test_markdown_lint_requires_github_workflow_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('github.workflow', 'github.job')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'github.workflow',
        )

def test_stewardship_requires_github_ref_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('github.ref', 'github.sha')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'github.ref',
        )

def test_link_check_requires_job_id_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('\n  link-check:\n', '\n  links:\n')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'link-check:',
        )

def test_markdown_lint_requires_job_id_lint_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('\n  lint:\n', '\n  markdown:\n')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'lint:',
        )

def test_stewardship_requires_job_id_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('\n  stewardship:\n', '\n  gates:\n')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'stewardship:',
        )

def test_link_check_requires_self_path_filter_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('.github/workflows/link-check.yml', '.github/workflows/other.yml')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '.github/workflows/link-check.yml',
        )

def test_markdown_lint_requires_self_path_filter_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('.github/workflows/markdown-lint.yml', '.github/workflows/other.yml')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '.github/workflows/markdown-lint.yml',
        )

def test_stewardship_requires_scripts_path_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('scripts/**', 'script/**')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'scripts/**',
        )

def test_stewardship_requires_docs_path_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('docs/**', 'doc/**')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'docs/**',
        )

def test_stewardship_requires_readme_path_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('"README.md"', '"READMEX.md"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'README.md',
        )

def test_link_check_requires_secrets_github_token_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('secrets.GITHUB_TOKEN', 'secrets.TOKEN')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'secrets.GITHUB_TOKEN',
        )

def test_stewardship_requires_python_312_exact_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('python-version: "3.12"', 'python-version: "3.11"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python-version: "3.12"',
        )

def test_stewardship_requires_pip_quiet_pyyaml_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('pip install --quiet pyyaml', 'pip install pyyaml')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'pip install --quiet pyyaml',
        )

def test_stewardship_requires_bash_run_script_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('bash scripts/run_stewardship_checks.sh', 'sh scripts/run_stewardship_checks.sh')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'bash scripts/run_stewardship_checks.sh',
        )

def test_stewardship_requires_python3_selftest_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('python3 scripts/test_stewardship_gates.py', 'python scripts/test_stewardship_gates.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python3 scripts/test_stewardship_gates.py',
        )

def test_markdown_lint_requires_config_key_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('config: ".markdownlint.json"', 'config_path: ".markdownlint.json"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'config: ".markdownlint.json"',
        )

def test_markdown_lint_requires_agents_glob_bang_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('!.github/agents/**', '!.github/agent/**')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '!.github/agents/**',
        )

def test_markdown_lint_requires_owasp_bang_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('!OWASP-AGENTIC.md', '!OWASP.md')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '!OWASP-AGENTIC.md',
        )

def test_stewardship_actionlint_lists_link_check_path_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            ".github/workflows/link-check.yml",
            ".github/workflows/lnk-check.yml",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "workflow path link-check.yml",
        )

def test_stewardship_actionlint_lists_markdown_path_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            ".github/workflows/markdown-lint.yml",
            ".github/workflows/md-lint.yml",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "workflow path markdown-lint.yml",
        )

def test_stewardship_download_passes_177_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('download-actionlint.bash) 1.7.7', 'download-actionlint.bash) 1.7.8')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '1.7.7 to download script',
        )

def test_link_check_requires_checkout_v7_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('actions/checkout@v7', 'actions/checkout@v4')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'checkout@v7',
        )

def test_markdown_lint_requires_checkout_v7_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('actions/checkout@v7', 'actions/checkout@v4')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'checkout@v7',
        )

def test_stewardship_requires_checkout_v7_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('actions/checkout@v7', 'actions/checkout@v4')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'checkout@v7',
        )

def test_link_check_requires_lychee_v2_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('lycheeverse/lychee-action@v2', 'lycheeverse/lychee-action@v1')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'lychee-action@v2',
        )

def test_link_check_requires_timeout_20_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('timeout-minutes: 20', 'timeout-minutes: 30')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'timeout-minutes: 20',
        )

def test_markdown_lint_requires_timeout_10_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('timeout-minutes: 10', 'timeout-minutes: 20')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'timeout-minutes: 10',
        )

def test_stewardship_requires_timeout_15_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('timeout-minutes: 15', 'timeout-minutes: 20')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'timeout-minutes: 15',
        )

def test_link_check_requires_cron_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('cron: "0 6 * * 1"', 'cron: "0 7 * * 1"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'cron: "0 6 * * 1"',
        )

def test_markdown_lint_requires_cron_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('cron: "30 6 * * 1"', 'cron: "30 7 * * 1"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'cron: "30 6 * * 1"',
        )

def test_stewardship_requires_cron_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('cron: "15 6 * * 1"', 'cron: "15 7 * * 1"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'cron: "15 6 * * 1"',
        )

def test_link_check_requires_ubuntu_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('ubuntu-latest', 'ubuntu-22.04')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ubuntu-latest',
        )

def test_markdown_lint_requires_ubuntu_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('ubuntu-latest', 'ubuntu-22.04')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ubuntu-latest',
        )

def test_stewardship_requires_ubuntu_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('ubuntu-latest', 'ubuntu-22.04')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ubuntu-latest',
        )

def test_stewardship_requires_setup_python_v5_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('actions/setup-python@v5', 'actions/setup-python@v4')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'setup-python@v5',
        )

def test_stewardship_requires_actionlint_color_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace(' -color ', ' -plain ')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '-color',
        )

def test_stewardship_requires_shell_bash_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('shell: bash', 'shell: sh')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'shell: bash',
        )

def test_link_check_requires_fail_true_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('fail: true', 'fail: false')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'fail: true',
        )

def test_link_check_requires_verbose_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('--verbose', '--debug')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '--verbose',
        )

def test_link_check_requires_no_progress_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('--no-progress', '--progress')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '--no-progress',
        )

def test_markdown_lint_requires_cli2_v24_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('DavidAnson/markdownlint-cli2-action@v24', 'DavidAnson/markdownlint-cli2-action@v20')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'markdownlint-cli2-action@v24',
        )

def test_stewardship_requires_get_actionlint_id_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('id: get_actionlint', 'id: actionlint')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'get_actionlint',
        )

def test_stewardship_requires_pyyaml_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('pyyaml', 'ruamel')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'pyyaml',
        )

def test_link_check_requires_exclude_loopback_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('--exclude-loopback', '--exclude-localhost')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'exclude loopback',
        )

def test_link_check_requires_max_concurrency_8_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('--max-concurrency 8', '--max-concurrency 4')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '--max-concurrency 8',
        )

def test_stewardship_requires_actionlint_177_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('1.7.7', '1.7.8')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '1.7.7',
        )

def test_stewardship_rejects_cancel_in_progress_false_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "cancel-in-progress: true", "cancel-in-progress: false"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "cancel-in-progress: true",
        )

def test_link_check_rejects_missing_pull_request_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("pull_request:\n", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pull_request",
        )

def test_markdown_lint_rejects_missing_workflow_dispatch_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace("workflow_dispatch:\n", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "workflow_dispatch",
        )

def test_stewardship_rejects_missing_contents_read_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "contents: read", "contents: write"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "contents: read",
        )

def test_link_check_rejects_missing_schedule_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8")
        text = text.replace("  schedule:\n", "")
        text = text.replace(
            "    # Run weekly to catch externally broken links\n", ""
        )
        text = text.replace('    - cron: "0 6 * * 1"\n', "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "schedule",
        )

def test_stewardship_rejects_missing_concurrency_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8")
        text = text.replace("concurrency:\n", "")
        text = text.replace(
            "  group: stewardship-checks-${{ github.workflow }}-${{ github.ref }}\n",
            "",
        )
        text = text.replace("  cancel-in-progress: true\n", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "concurrency",
        )

def test_markdown_lint_rejects_missing_owasp_exclude_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace("!OWASP-AGENTIC.md\n", "")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "OWASP-AGENTIC.md",
        )

def test_link_check_requires_lycheeignore_path_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(".lycheeignore", ".lychee-ignore")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            ".lycheeignore",
        )


def test_markdown_lint_requires_markdownlint_json_path_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            ".markdownlint.json", ".markdown-lint.json"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            ".markdownlint.json",
        )


def test_link_check_requires_max_retries_3_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "--max-retries 3", "--max-retries 5"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "--max-retries 3",
        )


def test_link_check_requires_timeout_arg_20_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace("--timeout 20", "--timeout 30")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "--timeout 20",
        )


def test_stewardship_requires_raw_githubusercontent_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace(
            "raw.githubusercontent.com", "githubusercontent.com"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "raw.githubusercontent.com",
        )


def test_stewardship_requires_curl_fssl_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "stewardship-checks.yml"
        text = path.read_text(encoding="utf-8").replace("curl -fsSL", "curl -sL")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "fsSL",
        )


def test_markdown_lint_requires_davidanson_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "markdown-lint.yml"
        text = path.read_text(encoding="utf-8").replace(
            "DavidAnson/markdownlint-cli2-action",
            "Other/markdownlint-cli2-action",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "DavidAnson/markdownlint-cli2-action",
        )


def test_link_check_requires_lycheeverse_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / "link-check.yml"
        text = path.read_text(encoding="utf-8").replace(
            "lycheeverse/lychee-action", "other/lychee-action"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "lycheeverse/lychee-action",
        )

# --- TOKENMAXX actionlint-style gate pins after #75 (+71) --------------------

def test_actionlint_style_gate_requires_fn_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def check_actionlint_style(', 'def check_actionlint_ok(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_actionlint_style()',
        )


def test_actionlint_style_gate_requires_static_doc_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Static actionlint-like checks', 'Static actionlint soft checks')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Static actionlint-like checks',
        )


def test_actionlint_style_gate_requires_live_pins_doc_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Live fail-closed pins after #75', 'Live fail-closed pins after #00')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Live fail-closed pins after #75',
        )


def test_actionlint_style_gate_requires_load_workflow_text_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('load_workflow_text', 'load_workflow_body')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'load_workflow_text',
        )


def test_actionlint_style_gate_requires_top_level_name_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('top-level name:', 'top-level title:')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'top-level name:',
        )


def test_actionlint_style_gate_requires_jobs_runs_on_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('jobs.*.runs-on', 'jobs.*.runner')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'jobs.*.runs-on',
        )


def test_actionlint_style_gate_requires_jobs_steps_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('jobs.*.steps', 'jobs.*.tasks')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'jobs.*.steps',
        )


def test_actionlint_style_gate_requires_timeout_minutes_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('timeout-minutes:', 'deadline-minutes:')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'timeout-minutes:',
        )


def test_actionlint_style_gate_requires_prt_reject_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('pull_request_target', 'pull_request_source')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'pull_request_target',
        )


def test_actionlint_style_gate_requires_harden_wording_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('actionlint harden', 'actionlint soft')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'actionlint harden',
        )


def test_actionlint_style_gate_requires_write_all_reject_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('write-all', 'read-all')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'write-all',
        )


def test_actionlint_style_gate_requires_contents_write_reject_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('contents: write', 'contents: read')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'contents: write',
        )


def test_actionlint_style_gate_requires_id_token_write_reject_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('id-token: write', 'oidc-token: write')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'id-token: write',
        )


def test_actionlint_style_gate_requires_docker_skip_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('docker://', 'oci://')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'docker://',
        )


def test_actionlint_style_gate_requires_unpinned_reject_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('unpinned action uses', 'floating action uses')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'unpinned action uses',
        )


def test_actionlint_style_gate_requires_float_main_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"main", "master"', '"trunk", "master"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'float @main',
        )


def test_actionlint_style_gate_requires_float_master_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(', "master", "latest"', ', "primary", "latest"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'float @master',
        )


def test_actionlint_style_gate_requires_float_latest_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"master", "latest"', '"master", "tip"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'float @latest',
        )


def test_actionlint_style_gate_requires_float_set_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('{"main", "master", "latest"}', '{"main", "master", "tip"}')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '{main, master, latest}',
        )


def test_actionlint_style_gate_requires_main_call_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('check_actionlint_style(errors)', 'check_actionlint_ok(errors)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_actionlint_style(errors)',
        )


def test_actionlint_style_gate_requires_fn_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def check_actionlint_style(', 'def check_actionlint_x(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_actionlint_style()',
        )


def test_actionlint_style_gate_requires_id_token_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('id-token: write', 'id-token: read')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'id-token: write',
        )


def test_actionlint_style_gate_requires_write_all_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('write-all', 'write_all')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'write-all',
        )


def test_actionlint_style_gate_requires_prt_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('pull_request_target', 'pull_request_forge')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'pull_request_target',
        )


def test_actionlint_style_gate_requires_docker_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('docker://', 'container://')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'docker://',
        )


def test_actionlint_style_gate_requires_unpinned_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('unpinned action uses', 'loose action uses')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'unpinned action uses',
        )


def test_actionlint_style_gate_requires_timeout_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('timeout-minutes:', 'timeout_minutes:')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'timeout-minutes:',
        )


def test_actionlint_style_gate_requires_runs_on_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('jobs.*.runs-on', 'jobs.*.runs_on')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'jobs.*.runs-on',
        )


def test_actionlint_style_gate_requires_steps_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('jobs.*.steps', 'jobs.*.step_list')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'jobs.*.steps',
        )


def test_actionlint_style_gate_requires_live_doc_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Live fail-closed pins after #75', 'Live fail-closed pins after #58')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Live fail-closed pins after #75',
        )


def test_actionlint_rejects_id_token_write_on_stewardship_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('contents: read', 'contents: read\n      id-token: write')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'id-token: write',
        )


def test_actionlint_rejects_id_token_write_on_link_check_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('contents: read', 'contents: read\n      id-token: write')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'id-token: write',
        )


def test_actionlint_rejects_id_token_write_on_markdown_lint_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('contents: read', 'contents: read\n      id-token: write')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'id-token: write',
        )


def test_actionlint_rejects_write_all_on_stewardship_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('permissions:\n  contents: read', 'permissions: write-all')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'write-all',
        )


def test_actionlint_rejects_write_all_on_link_check_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('permissions:\n  contents: read', 'permissions: write-all')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'write-all',
        )


def test_actionlint_rejects_contents_write_on_stewardship_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('contents: read', 'contents: write')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'contents: write',
        )


def test_actionlint_rejects_contents_write_on_link_check_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('contents: read', 'contents: write')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'contents: write',
        )


def test_actionlint_rejects_prt_on_stewardship_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('pull_request:', 'pull_request_target:')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'pull_request_target',
        )


def test_actionlint_rejects_prt_on_link_check_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('pull_request:', 'pull_request_target:')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'pull_request_target',
        )


def test_actionlint_rejects_float_main_checkout_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('actions/checkout@v7', 'actions/checkout@main')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'float on @main',
        )


def test_actionlint_rejects_float_master_checkout_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('actions/checkout@v7', 'actions/checkout@master')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'float on @master',
        )


def test_actionlint_rejects_float_latest_checkout_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('actions/checkout@v7', 'actions/checkout@latest')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'float on @latest',
        )


def test_actionlint_rejects_unpinned_checkout_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('actions/checkout@v7', 'actions/checkout')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'unpinned action uses',
        )


def test_actionlint_rejects_float_main_setup_python_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('actions/setup-python@v5', 'actions/setup-python@main')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'float on @main',
        )


def test_actionlint_rejects_missing_timeout_on_stewardship_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('timeout-minutes: 15', '# timeout removed')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'timeout-minutes',
        )


def test_actionlint_rejects_missing_runs_on_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('runs-on: ubuntu-latest', '# runs-on removed')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'runs-on',
        )


def test_actionlint_rejects_missing_steps_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('steps:', 'tasks:')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'steps',
        )


def test_actionlint_rejects_id_token_write_stewardship_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('contents: read', 'contents: read\n      id-token: write')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'id-token: write',
        )


def test_actionlint_rejects_id_token_write_link_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('contents: read', 'contents: read\n      id-token: write')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'id-token: write',
        )


def test_actionlint_rejects_write_all_stewardship_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('permissions:\n  contents: read', 'permissions: write-all')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'write-all',
        )


def test_actionlint_rejects_contents_write_stewardship_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('contents: read', 'contents: write')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'contents: write',
        )


def test_actionlint_rejects_prt_stewardship_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('pull_request:', 'pull_request_target:')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'pull_request_target',
        )


def test_actionlint_rejects_float_main_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('actions/checkout@v7', 'actions/checkout@main')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'float on @main',
        )


def test_actionlint_rejects_float_latest_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('actions/checkout@v7', 'actions/checkout@latest')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'float on @latest',
        )


def test_actionlint_rejects_unpinned_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('actions/checkout@v7', 'actions/checkout')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'unpinned action uses',
        )


def test_actionlint_rejects_float_master_setup_python_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('actions/setup-python@v5', 'actions/setup-python@master')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'float on @master',
        )


def test_actionlint_rejects_missing_name_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('name: Stewardship Checks', '# name removed')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'top-level name',
        )


def test_actionlint_rejects_prt_markdown_lint_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('pull_request:', 'pull_request_target:')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'pull_request_target',
        )


def test_actionlint_rejects_write_all_markdown_lint_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('permissions:\n  contents: read', 'permissions: write-all')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'write-all',
        )


def test_actionlint_rejects_contents_write_markdown_lint_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('contents: read', 'contents: write')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'contents: write',
        )


def test_actionlint_rejects_float_main_markdown_lint_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('actions/checkout@v7', 'actions/checkout@main')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'float on @main',
        )


def test_actionlint_rejects_unpinned_markdown_lint_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('actions/checkout@v7', 'actions/checkout')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'unpinned action uses',
        )


def test_actionlint_rejects_missing_timeout_link_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('timeout-minutes: 20', '# timeout removed')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'timeout-minutes',
        )


def test_actionlint_rejects_missing_timeout_markdown_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('timeout-minutes: 10', '# timeout removed')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'timeout-minutes',
        )


def test_actionlint_rejects_id_token_write_markdown_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('contents: read', 'contents: read\n      id-token: write')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'id-token: write',
        )


def test_actionlint_rejects_float_latest_setup_python_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('actions/setup-python@v5', 'actions/setup-python@latest')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'float on @latest',
        )


def test_actionlint_rejects_unpinned_setup_python_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('actions/setup-python@v5', 'actions/setup-python')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'unpinned action uses',
        )


def test_actionlint_rejects_contents_write_link_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('contents: read', 'contents: write')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'contents: write',
        )


def test_actionlint_rejects_write_all_link_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('permissions:\n  contents: read', 'permissions: write-all')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'write-all',
        )


def test_actionlint_rejects_prt_link_still_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('pull_request:', 'pull_request_target:')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'pull_request_target',
        )


def test_actionlint_rejects_float_master_link_after_75() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('actions/checkout@v7', 'actions/checkout@master')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'float on @master',
        )

# --- TOKENMAXX deepen after #72/#75: stewardship-schema second-pass pins ---

def test_schema_gate_requires_fenced_yaml_exact_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('^```yaml\\n(.*?)\\n```', '^```yml\\n(.*?)\\n```')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '```yaml',
        )

def test_schema_gate_requires_iso_date_exact_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('^\\d{4}-\\d{2}-\\d{2}', '^\\d{4}/\\d{2}/\\d{2}')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ISO_DATE_RE',
        )

def test_schema_gate_requires_semver_exact_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('^\\d+\\.\\d+\\.\\d+$', '^\\d+\\.\\d+$')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'SEMVER_RE',
        )

def test_schema_gate_requires_issue_ref_exact_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('#\\d+', '#\\d{2,}')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ISSUE_REF_RE',
        )

def test_schema_gate_requires_date_keys_exact_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('DATE_KEYS = ("created", "last_updated")', 'DATE_KEYS = ("created",)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'last_updated',
        )

def test_schema_gate_requires_tiny_yaml_doc_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('Tiny YAML subset parser', 'Tiny YAML subset reader')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Tiny YAML subset',
        )

def test_schema_gate_requires_scalar_doc_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('stewardship metadata values must be scalars', 'stewardship metadata values must be simple')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'scalars',
        )

def test_schema_gate_requires_no_fence_needle_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('no fenced ```yaml metadata block found', 'no fenced yaml metadata block found')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '```yaml',
        )

def test_schema_gate_requires_unsupported_yaml_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('unsupported YAML line', 'unsupported YAML row')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'unsupported YAML line',
        )

def test_schema_gate_requires_empty_key_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('empty key in YAML line', 'blank key in YAML line')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'empty key',
        )

def test_schema_gate_requires_scalar_needle_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('must be a scalar', 'must be a value')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'must be a scalar',
        )

def test_schema_gate_requires_nonempty_needle_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('must be non-empty', 'must be present')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'must be non-empty',
        )

def test_schema_gate_requires_string_needle_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('must be a string', 'must be a text')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'must be a string',
        )

def test_schema_gate_requires_active_needle_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('status must be ACTIVE for active stewardship docs', 'status must be ACTIVE for stewardship docs')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ACTIVE stewardship docs',
        )

def test_schema_gate_requires_tier_needle_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('tier must be a positive int', 'tier must be a positive number')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'positive int',
        )

def test_schema_gate_requires_autonomy_needle_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('autonomy_level must be int in 0..3', 'autonomy_level must be int in 0..2')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '0..3',
        )

def test_schema_gate_requires_iso_needle_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('must be ISO-8601 date-prefixed', 'must be ISO-8601 date-like')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'date-prefixed',
        )

def test_schema_gate_requires_invent_needle_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('must retain no-invent-product wording', 'must retain no-invent wording')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'no-invent-product wording',
        )

def test_schema_gate_requires_semver_needle_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('version must be semver X.Y.Z', 'version must be semver X.Y')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'semver X.Y.Z',
        )

def test_schema_gate_requires_closes_needle_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace(
            'closes must reference an issue like #N (',
            'closes must reference an issue like #M (',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'like #N (',
        )

def test_schema_gate_requires_failed_banner_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('Stewardship schema check FAILED', 'Stewardship schema check ERROR')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'FAILED',
        )

def test_schema_gate_requires_ok_banner_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('OK: stewardship metadata schemas valid', 'OK: stewardship metadata schemas ok')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'schemas valid',
        )

def test_schema_gate_requires_stdlib_subset_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('stdlib-subset', 'stdlib-fallback')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'stdlib-subset',
        )

def test_schema_gate_requires_pyyaml_label_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('PyYAML', 'pyyaml')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'PyYAML',
        )

def test_schema_gate_requires_bool_subclass_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('bool is a subclass of int', 'bool is a subtype of int')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'subclass of int',
        )

def test_schema_gate_requires_group_one_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('match.group(1)', 'match.group(0)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'match.group(1)',
        )

def test_schema_gate_requires_missing_keys_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('missing metadata keys', 'missing metadata fields')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'missing metadata keys',
        )

def test_schema_gate_requires_utf8_encoding_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('encoding="utf-8"', 'encoding="utf8"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'utf-8',
        )

def test_schema_gate_requires_string_keys_status_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"status",', '"state",')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'STRING_KEYS',
        )

def test_schema_gate_requires_string_keys_edit_policy_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"edit_policy"', '"edit_rule"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'edit_policy',
        )

def test_schema_gate_requires_string_keys_closes_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"closes"', '"close"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'closes',
        )

def test_schema_gate_requires_string_keys_purpose_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"purpose"', '"goal"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'purpose',
        )

def test_schema_gate_requires_string_keys_version_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"version"', '"ver"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'version',
        )

def test_schema_gate_requires_string_keys_maintainer_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"maintainer"', '"maintainers"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'maintainer',
        )

def test_schema_gate_requires_string_keys_parent_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"parent_governance"', '"parent"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'parent_governance',
        )

def test_schema_gate_requires_string_keys_repo_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"repo"', '"repository"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '"repo"',
        )

def test_schema_gate_requires_string_keys_surface_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"surface"', '"surfaces"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'surface',
        )

def test_schema_gate_requires_string_keys_last_updated_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"last_updated"', '"updated"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'last_updated',
        )

def test_schema_gate_requires_second_pass_doc_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('Second-pass: FENCED_YAML_RE exact', 'Second-pass: FENCED_YAML_RE pattern')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'FENCED_YAML_RE exact',
        )

def test_schema_gate_requires_contract_fn_def_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def check_stewardship_schema_gate_contract(', 'def check_stewardship_schema_gate_verify(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_stewardship_schema_gate_contract',
        )

def test_schema_rejects_bool_autonomy_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: true\n```\n"
            ),
        )
        assert_fail_script(scripts / "check_stewardship_schema.py", tmp_path, "0..3")


def test_schema_rejects_bool_tier_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: true\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"do not invent product badges\"\ncloses: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(scripts / "check_stewardship_schema.py", tmp_path, "positive int")


def test_schema_rejects_nested_list_value_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope:\n  - public\nedit_policy: \"do not invent product badges\"\n"
                "closes: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(scripts / "check_stewardship_schema.py", tmp_path, "scalar")


def test_schema_rejects_empty_yaml_block_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            publish="# P\n\n```yaml\n\n```\n",
        )
        assert_fail_script(scripts / "check_stewardship_schema.py", tmp_path, "empty yaml")


def test_schema_rejects_non_string_owner_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            backlog=(
                "# I\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: 1\nedit_policy: x\n```\n"
            ),
        )
        assert_fail_script(scripts / "check_stewardship_schema.py", tmp_path, "must be a string")


def test_schema_rejects_bad_semver_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(scripts / "check_stewardship_schema.py", tmp_path, "semver")


def test_schema_rejects_closes_without_hash_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            publish=(
                "# P\n\n```yaml\nstatus: ACTIVE\ncreated: \"2026-09-13\"\n"
                "purpose: \"Reversible publish path for docs/wiki → GitHub Wiki\"\n"
                "closes: \"16\"\n```\n"
            ),
        )
        assert_fail_script(scripts / "check_stewardship_schema.py", tmp_path, "issue")


def test_schema_rejects_inactive_status_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: DRAFT\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"do not invent product badges\"\ncloses: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(scripts / "check_stewardship_schema.py", tmp_path, "ACTIVE")


def test_schema_rejects_bad_iso_date_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"04-13-2026\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(scripts / "check_stewardship_schema.py", tmp_path, "ISO-8601")


def test_schema_passes_good_fixture_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(tmp_path)
        assert_pass_script(scripts / "check_stewardship_schema.py", tmp_path)


def test_schema_gate_requires_doc_schemas_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace("DOC_SCHEMAS", "DOC_MAPS")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "DOC_SCHEMAS")


def test_schema_gate_requires_expected_values_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace("EXPECTED_VALUES", "EXPECTED_MAP")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "EXPECTED_VALUES")


def test_schema_gate_requires_string_keys_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace("STRING_KEYS", "STR_KEYS")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "STRING_KEYS")


def test_schema_gate_requires_reject_non_scalar_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace("reject_non_scalar", "reject_nested")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "reject_non_scalar")


def test_schema_gate_requires_front_door_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace(
            "public governance front-door repos", "public governance repos"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "public governance front-door repos",
        )


def test_schema_gate_requires_closes_16_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace('"#16"', '"#99"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "closes #16")


def test_schema_gate_requires_copilot_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace("copilot", "cursor")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "copilot")


def test_schema_gate_requires_scan_secrets_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace("scan_secrets", "scan_tokens")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "scan_secrets")


def test_schema_gate_requires_safe_load_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace("safe_load", "load")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "safe_load")


def test_schema_gate_requires_mapping_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace("must be a mapping", "must be a dict")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "must be a mapping")


def test_schema_gate_requires_empty_yaml_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace(
            "empty yaml metadata block", "blank metadata block"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "empty yaml")


def test_schema_gate_requires_smtp_eth_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace("smtp.eth", "smtp")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "smtp.eth")


def test_schema_gate_requires_agents_governance_repo_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace(
            'repo": "agents-governance"', 'repo": "agents-gov"'
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'repo": "agents-governance"',
        )


def test_schema_gate_requires_reversible_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8").replace(
            "Reversible publish path for docs/wiki", "Publish path for docs/wiki"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Reversible publish path for docs/wiki",
        )


def test_schema_gate_requires_contract_call_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "check_stewardship_schema_gate_contract(errors)",
            "check_stewardship_schema_gate_contract_x(errors)",
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "check_stewardship_schema_gate_contract",
        )


def test_schema_rejects_missing_key_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            claude=(
                "# CLAUDE\n\n```yaml\nrepo: agents-governance\nowner: \"fuzzywigg (smtp.eth)\"\n"
                "surface: copilot\nautonomy_level: 1\nlast_updated: \"2026-04-13\"\n```\n"
            ),
        )
        assert_fail_script(scripts / "check_stewardship_schema.py", tmp_path, "missing metadata keys")


def test_schema_rejects_wrong_maintainer_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: other.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 1\n```\n"
            ),
        )
        assert_fail_script(scripts / "check_stewardship_schema.py", tmp_path, "smtp.eth")


def test_schema_rejects_edit_policy_without_invent_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            badge=(
                "# B\n\n```yaml\nstatus: ACTIVE\ntier: 1\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nscope: \"public governance front-door repos\"\n"
                "edit_policy: \"keep badges quiet\"\ncloses: \"#16\"\n```\n"
            ),
        )
        assert_fail_script(scripts / "check_stewardship_schema.py", tmp_path, "invent")


def test_schema_rejects_autonomy_out_of_range_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            agents=(
                "# AGENTS\n\n```yaml\nversion: \"1.0.0\"\nlast_updated: \"2026-04-13\"\n"
                "maintainer: smtp.eth\nscope: repository-specific\n"
                "parent_governance: github.com/fuzzywigg/agents-governance\nautonomy_level: 9\n```\n"
            ),
        )
        assert_fail_script(scripts / "check_stewardship_schema.py", tmp_path, "0..3")


def test_schema_rejects_tier_zero_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(
            tmp_path,
            backlog=(
                "# I\n\n```yaml\nstatus: ACTIVE\ntier: 0\ncreated: \"2026-09-13\"\n"
                "owner: copilot\nedit_policy: x\n```\n"
            ),
        )
        assert_fail_script(scripts / "check_stewardship_schema.py", tmp_path, "positive int")


def test_schema_rejects_missing_yaml_fence_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_schema_tree(tmp_path, agents="# AGENTS\n\nno yaml here\n")
        assert_fail_script(scripts / "check_stewardship_schema.py", tmp_path, "```yaml")


def test_schema_gate_requires_bool_int_reject_still_after_72() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_stewardship_schema.py"
        text = path.read_text(encoding="utf-8")
        text = text.replace("isinstance(level, bool)", "isinstance(level, float)")
        text = text.replace("isinstance(tier, bool)", "isinstance(tier, float)")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "bool pretending",
        )



# --- TOKENMAXX deepen after #83/#86: actionlint-style second-pass pins ---

def test_actionlint_style_gate_requires_name_re_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('(?m)^name:\\s*\\S', '(?m)^title:\\s*\\S')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'exact top-level name: regex',
        )

def test_actionlint_style_gate_requires_uses_re_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('(?m)^\\s*-\\s*uses:\\s*([^\\s#]+)', '(?m)^\\s*-\\s*needs:\\s*([^\\s#]+)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'exact uses: capture regex',
        )

def test_actionlint_style_gate_requires_write_all_re_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('(?m)^\\s*permissions:\\s*write-all\\s*$', '(?m)^\\s*permissions:\\s*write_all\\s*$')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'exact permissions: write-all regex',
        )

def test_actionlint_style_gate_requires_contents_write_re_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('(?m)^\\s*contents:\\s*write\\s*$', '(?m)^\\s*contents:\\s*wrote\\s*$')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'exact contents: write regex',
        )

def test_actionlint_style_gate_requires_id_token_re_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('(?m)^\\s*id-token:\\s*write\\s*$', '(?m)^\\s*id-token:\\s*wrote\\s*$')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'exact id-token: write regex',
        )

def test_actionlint_style_gate_requires_docker_startswith_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('startswith("docker://")', 'startswith("oci://")')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'startswith',
        )

def test_actionlint_style_gate_requires_at_not_in_uses_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"@" not in uses', '"@" not in refs')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '"@" not in uses',
        )

def test_actionlint_style_gate_requires_rsplit_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('rsplit("@", 1)', 'rsplit("@", 2)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'rsplit("@", 1)',
        )

def test_actionlint_style_gate_requires_group_strip_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('match.group(1).strip()', 'match.group(1).rstrip()')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'group(1).strip()',
        )

def test_actionlint_style_gate_requires_runs_membership_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"runs-on:" not in text', '"runner:" not in text')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'runs-on: via membership',
        )

def test_actionlint_style_gate_requires_steps_membership_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"steps:" not in text', '"tasks:" not in text')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'steps: via membership',
        )

def test_actionlint_style_gate_requires_prt_membership_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"pull_request_target:" in text', '"pull_request_fork:" in text')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'pull_request_target: via membership',
        )

def test_actionlint_style_gate_requires_timeout_membership_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"timeout-minutes:" not in text', '"timeout-seconds:" not in text')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'timeout-minutes: via membership',
        )

def test_actionlint_style_gate_requires_none_continue_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('if text is None:', 'if text is False:')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'returns None',
        )

def test_actionlint_style_gate_requires_required_workflows_loop_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('for name in REQUIRED_WORKFLOWS:', 'for name in REQUIRED_JOBS:')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'REQUIRED_WORKFLOWS',
        )

def test_actionlint_style_gate_requires_least_privilege_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Prefer least privilege', 'Prefer least authority')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Prefer least privilege',
        )

def test_actionlint_style_gate_requires_oidc_comment_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('OIDC write not needed for docs CI paths', 'OIDC write not needed for docs CI jobs')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'OIDC write not needed',
        )

def test_actionlint_style_gate_requires_majors_comment_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Pin GitHub Actions majors', 'Pin GitHub Actions minors')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Pin GitHub Actions majors',
        )

def test_actionlint_style_gate_requires_timeout_comment_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('jobs must declare timeout', 'jobs must declare duration')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'jobs must declare timeout',
        )

def test_actionlint_style_gate_requires_fail_top_name_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('actionlint-style requires top-level name:', 'actionlint-style requires workflow name:')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'top-level name: fail needle',
        )

def test_actionlint_style_gate_requires_fail_runs_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('actionlint-style requires jobs.*.runs-on', 'actionlint-style requires jobs.*.runner')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'jobs.*.runs-on fail needle',
        )

def test_actionlint_style_gate_requires_fail_steps_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('actionlint-style requires jobs.*.steps', 'actionlint-style requires jobs.*.tasks')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'jobs.*.steps fail needle',
        )

def test_actionlint_style_gate_requires_fail_prt_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('must not use pull_request_target (actionlint harden)', 'must not use pull_request_target (actionlint soft)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'actionlint harden',
        )

def test_actionlint_style_gate_requires_fail_write_all_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('must not set permissions: write-all', 'must not set permissions: write_all')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'permissions: write-all fail needle',
        )

def test_actionlint_style_gate_requires_fail_contents_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('contents: write is forbidden on stewardship workflows', 'contents: write is forbidden on stewardship jobs')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'contents: write is forbidden on stewardship workflows fail needle',
        )

def test_actionlint_style_gate_requires_fail_id_token_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('id-token: write is forbidden on stewardship workflows', 'id-token: write is forbidden on stewardship jobs')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'id-token: write is forbidden on stewardship workflows fail needle',
        )

def test_actionlint_style_gate_requires_fail_unpinned_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('unpinned action uses:', 'unpinned action refs:')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'unpinned action uses: fail needle',
        )

def test_actionlint_style_gate_requires_fail_float_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('action must not float on @', 'action must not float at @')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'float on @ fail needle',
        )

def test_actionlint_style_gate_requires_fail_timeout_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('actionlint-style requires timeout-minutes on jobs', 'actionlint-style requires timeout-minutes on job')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'timeout-minutes on jobs fail needle',
        )

def test_actionlint_style_gate_requires_second_pass_doc_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Second-pass after #83/#86: exact name/uses', 'Second-pass after #00/#00: exact name/uses')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Second-pass after #83/#86',
        )

def test_actionlint_style_gate_requires_module_second_pass_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('second-pass after #83/#86', 'second-pass after #00/#00')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'actionlint second-pass',
        )

def test_actionlint_style_gate_requires_fn_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def check_actionlint_style(', 'def check_actionlint_ok(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_actionlint_style()',
        )

def test_actionlint_style_gate_requires_static_doc_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Static actionlint-like checks', 'Static actionlint soft checks')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Static actionlint-like checks',
        )

def test_actionlint_style_gate_requires_live_pins_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Live fail-closed pins after #75', 'Live fail-closed pins after #00')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Live fail-closed pins after #75',
        )

def test_actionlint_style_gate_requires_load_workflow_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('load_workflow_text', 'load_workflow_body')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'load_workflow_text',
        )

def test_actionlint_style_gate_requires_float_set_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('{"main", "master", "latest"}', '{"trunk", "master", "latest"}')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'float @main',
        )

def test_actionlint_style_gate_requires_main_call_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('check_actionlint_style(errors)', 'check_actionlint_ok(errors)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_actionlint_style(errors)',
        )

def test_actionlint_style_gate_requires_docker_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('docker://', 'oci://')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'docker://',
        )

def test_actionlint_style_gate_requires_id_token_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('id-token: write', 'id-token: read')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'id-token: write',
        )

def test_actionlint_style_gate_requires_write_all_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('write-all', 'write_all')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'write-all',
        )

def test_actionlint_style_gate_requires_prt_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('pull_request_target', 'pull_request_fork')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'pull_request_target',
        )

def test_actionlint_rejects_float_main_lychee_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('lycheeverse/lychee-action@v2', 'lycheeverse/lychee-action@main')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'main',
        )

def test_actionlint_rejects_float_master_lychee_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('lycheeverse/lychee-action@v2', 'lycheeverse/lychee-action@master')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'master',
        )

def test_actionlint_rejects_float_latest_lychee_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('lycheeverse/lychee-action@v2', 'lycheeverse/lychee-action@latest')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'latest',
        )

def test_actionlint_rejects_unpinned_lychee_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('lycheeverse/lychee-action@v2', 'lycheeverse/lychee-action')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'unpinned',
        )

def test_actionlint_rejects_float_main_markdownlint_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('DavidAnson/markdownlint-cli2-action@v24', 'DavidAnson/markdownlint-cli2-action@main')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'main',
        )

def test_actionlint_rejects_float_master_markdownlint_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('DavidAnson/markdownlint-cli2-action@v24', 'DavidAnson/markdownlint-cli2-action@master')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'master',
        )

def test_actionlint_rejects_float_latest_markdownlint_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('DavidAnson/markdownlint-cli2-action@v24', 'DavidAnson/markdownlint-cli2-action@latest')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'latest',
        )

def test_actionlint_rejects_unpinned_markdownlint_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('DavidAnson/markdownlint-cli2-action@v24', 'DavidAnson/markdownlint-cli2-action')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'unpinned',
        )

def test_actionlint_rejects_missing_name_link_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('name: Link Check\n', '')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'top-level name',
        )

def test_actionlint_rejects_missing_name_markdown_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('name: Markdown Lint\n', '')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'top-level name',
        )

def test_actionlint_rejects_missing_runs_on_link_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('runs-on: ubuntu-latest\n', '')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'runs-on',
        )

def test_actionlint_rejects_missing_runs_on_markdown_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('runs-on: ubuntu-latest\n', '')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'runs-on',
        )

def test_actionlint_rejects_missing_steps_link_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('steps:\n', 'tasks:\n')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'steps',
        )

def test_actionlint_rejects_missing_steps_markdown_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('steps:\n', 'tasks:\n')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'steps',
        )

def test_actionlint_rejects_id_token_write_link_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('contents: read', 'contents: read\n      id-token: write')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'id-token: write',
        )

def test_actionlint_rejects_id_token_write_markdown_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('contents: read', 'contents: read\n      id-token: write')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'id-token: write',
        )

def test_actionlint_rejects_write_all_link_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('permissions:\n  contents: read', 'permissions: write-all')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'write-all',
        )

def test_actionlint_rejects_write_all_markdown_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('permissions:\n  contents: read', 'permissions: write-all')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'write-all',
        )

def test_actionlint_rejects_contents_write_link_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('contents: read', 'contents: write')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'contents: write',
        )

def test_actionlint_rejects_contents_write_markdown_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('contents: read', 'contents: write')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'contents: write',
        )

def test_actionlint_rejects_prt_link_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('pull_request:', 'pull_request_target:')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'pull_request_target',
        )

def test_actionlint_rejects_prt_markdown_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('pull_request:', 'pull_request_target:')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'pull_request_target',
        )

def test_actionlint_rejects_float_main_checkout_link_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('actions/checkout@v7', 'actions/checkout@main')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'main',
        )

def test_actionlint_rejects_float_latest_checkout_markdown_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('actions/checkout@v7', 'actions/checkout@latest')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'latest',
        )

def test_actionlint_rejects_unpinned_checkout_stewardship_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('actions/checkout@v7', 'actions/checkout')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'unpinned',
        )

def test_actionlint_rejects_missing_timeout_stewardship_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('timeout-minutes: 15\n', '')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'timeout-minutes',
        )

def test_actionlint_rejects_missing_timeout_link_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace('timeout-minutes: 20\n', '')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'timeout-minutes',
        )

def test_actionlint_rejects_missing_timeout_markdown_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace('timeout-minutes: 10\n', '')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'timeout-minutes',
        )

def test_actionlint_rejects_float_master_setup_python_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('actions/setup-python@v5', 'actions/setup-python@master')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'master',
        )

def test_actionlint_rejects_float_latest_setup_python_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('actions/setup-python@v5', 'actions/setup-python@latest')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'latest',
        )

def test_actionlint_rejects_unpinned_setup_python_still_after_86() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace('actions/setup-python@v5', 'actions/setup-python')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'unpinned',
        )


# --- TOKENMAXX relative-link third-pass after #90 (ECO-049) ---
# --- TOKENMAXX relative-link third-pass after #90 (ECO-049) ---

def test_relative_gate_requires_md_link_exact_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            '([^)\\s]*)',
            '([^)]*)',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD_LINK_RE exact',
        )

def test_relative_gate_requires_atx_exact_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            're.MULTILINE)',
            're.DOTALL)',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ATX_HEADING_RE exact',
        )

def test_relative_gate_requires_skip_parts_exact_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'SKIP_PARTS = {".git", "node_modules"}',
            'SKIP_PARTS = {".git", "vendor"}',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'SKIP_PARTS',
        )

def test_relative_gate_requires_skip_files_exact_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            '"OWASP-AGENTIC.md"',
            '"OTHER.md"',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'OWASP-AGENTIC.md',
        )

def test_relative_gate_requires_skip_prefix_path_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'Path(".github") / "agents"',
            'Path(".github") / "bots"',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Path(".github") / "agents"',
        )

def test_relative_gate_requires_ok_banner_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'OK: relative markdown links resolve',
            'OK: relative markdown links ok',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'relative markdown links resolve',
        )

def test_relative_gate_requires_failed_banner_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'Relative link check FAILED',
            'Relative link check ERROR',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Relative link check FAILED',
        )

def test_relative_gate_requires_empty_target_needle_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'empty relative link target',
            'blank relative link target',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'empty relative link target',
        )

def test_relative_gate_requires_http_needle_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'insecure http:// link (use https://)',
            'insecure http:// link (prefer https://)',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'insecure http:// link (use https://)',
        )

def test_relative_gate_requires_proto_needle_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'protocol-relative link not allowed',
            'protocol-relative link disallowed',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'protocol-relative link not allowed',
        )

def test_relative_gate_requires_dangerous_needle_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'dangerous link scheme',
            'unsafe link scheme',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'dangerous link scheme',
        )

def test_relative_gate_requires_utf8_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'encoding="utf-8"',
            'encoding="latin-1"',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'utf-8',
        )

def test_relative_gate_requires_as_posix_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'as_posix()',
            'as_uri()',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'as_posix()',
        )

def test_relative_gate_requires_md_suffix_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'dest.suffix.lower() == ".md"',
            'dest.suffix.lower() == ".markdown"',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '.md',
        )

def test_relative_gate_requires_value_error_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'ValueError',
            'RuntimeError',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ValueError',
        )

def test_relative_gate_requires_sorted_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'sorted(',
            'list(',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'sorted(',
        )

def test_relative_gate_requires_unicode_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            're.UNICODE',
            're.ASCII',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            're.UNICODE',
        )

def test_relative_gate_requires_space_dash_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            '.replace(" ", "-")',
            '.replace(" ", "_")',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'spaces with "-"',
        )

def test_relative_gate_requires_percent_stable_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'Percent-decode until stable',
            'Percent-decode until done',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Percent-decode until stable',
        )

def test_relative_gate_requires_cap_nested_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            "Cap nested percent-decoding",
            "Limit nested percent-decoding",
        ).replace("%252e", "%2e2e")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Cap nested percent-decoding",
        )

def test_relative_gate_requires_empty_parens_pin_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'Allow empty () so missing targets fail closed',
            'Allow empty () so missing targets fail open',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'fail-closed',
        )

def test_relative_gate_requires_sys_exit_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'sys.exit(main())',
            'raise SystemExit(main())',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'sys.exit(main())',
        )

def test_relative_gate_requires_urllib_unquote_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'from urllib.parse import unquote',
            'from urllib.parse import quote',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'unquote from urllib.parse',
        )

def test_relative_gate_requires_group2_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'match.group(2)',
            'match.group(1)',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'match.group(2)',
        )

def test_relative_gate_requires_startswith_hash_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'startswith("#")',
            'startswith("##")',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'same-file anchors',
        )

def test_relative_gate_requires_split_hash_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'split("#", 1)',
            'split("#", 2)',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'split fragments',
        )

def test_relative_gate_requires_files_scanned_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'files scanned',
            'files checked',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'files scanned',
        )

def test_relative_gate_requires_common_import_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'from stewardship_common import',
            'from stewardship_shared import',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'stewardship_common',
        )

def test_relative_gate_requires_title_attr_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            '(?:\\s+\\"[^\\"]*\\")?',
            "(?:\\s+\\'[^\\']*\\')?",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'title',
        )

def test_relative_gate_requires_atx_hash_range_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            '#{1,6}',
            '#{1,5}',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '#{1,6}',
        )

def test_relative_gate_requires_slug_punct_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            '[`*_~]',
            '[`*_]',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '[`*_~]',
        )

def test_relative_gate_requires_offline_doc_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'Offline relative markdown link integrity',
            'Offline relative markdown link checks',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Offline relative markdown link integrity',
        )

def test_relative_gate_requires_lychee_doc_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'complements lychee',
            'complements external',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'complements lychee',
        )

def test_relative_gate_requires_path_parent_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'path.parent / target',
            'path.parent.joinpath(target)',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'path.parent / target',
        )

def test_relative_gate_requires_fully_unquote_call_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'fully_unquote(raw)',
            'fully_unquote(target)',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'fully_unquote(raw)',
        )

def test_relative_gate_requires_dangerous_call_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'has_dangerous_scheme(raw)',
            'has_dangerous_scheme(target)',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'has_dangerous_scheme(raw)',
        )

def test_relative_gate_requires_third_pass_doc_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'Third-pass: MD_LINK_RE+ATX_HEADING_RE exact',
            'Third-pass: MD_LINK_RE+ATX_HEADING_RE pattern',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Third-pass',
        )

def test_relative_gate_requires_def_main_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'def main',
            'def run_main',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'def main',
        )

def test_relative_gate_requires_relative_to_root_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'relative_to(ROOT',
            'relative_to(BASE',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'relative_to(ROOT',
        )

def test_relative_gate_requires_contract_fn_def_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "def check_relative_link_gate_contract(",
            "def check_relative_link_gate_verify(",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "check_relative_link_gate_contract",
        )

def test_relative_links_reject_image_protocol_rel_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        (tmp_path / "a.md").write_text('![x](//evil.example/x.png)\n', encoding="utf-8")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'protocol-relative',
        )

def test_relative_links_reject_image_empty_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        (tmp_path / "a.md").write_text('![x]()\n', encoding="utf-8")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'empty',
        )

def test_relative_links_reject_image_bare_hash_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        (tmp_path / "a.md").write_text('![x](#)\n', encoding="utf-8")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'empty',
        )

def test_relative_links_reject_encoded_nul_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        (tmp_path / "a.md").write_text('[x](foo%00bar.md)\n', encoding="utf-8")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'NUL',
        )

def test_relative_links_reject_http_uppercase_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        (tmp_path / "a.md").write_text('[x](HTTP://example.com)\n', encoding="utf-8")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'http://',
        )

def test_relative_links_accept_https_with_frag_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        (tmp_path / "a.md").write_text('[x](https://example.com/doc#section)\n', encoding="utf-8")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)

def test_relative_links_accept_mailto_with_subject_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        (tmp_path / "a.md").write_text('[x](mailto:a@b.com?subject=hi)\n', encoding="utf-8")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)

def test_relative_links_reject_query_on_nested_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        nested = tmp_path / "docs"
        nested.mkdir()
        (nested / "a.md").write_text('[x](b.md?x=1)\n', encoding="utf-8")
        (nested / "b.md").write_text('# B\n', encoding="utf-8")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'query',
        )

def test_relative_links_reject_empty_frag_nested_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        nested = tmp_path / "docs"
        nested.mkdir()
        (nested / "a.md").write_text('[x](b.md#)\n', encoding="utf-8")
        (nested / "b.md").write_text('# B\n', encoding="utf-8")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'empty fragment',
        )

def test_relative_links_accept_angle_https_title_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        (tmp_path / "a.md").write_text('[x](<https://example.com> "t")\n', encoding="utf-8")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)

def test_relative_links_reject_js_uppercase_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        (tmp_path / "a.md").write_text('[x](JAVASCRIPT:alert(1))\n', encoding="utf-8")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'javascript:',
        )

def test_relative_links_github_slug_underscore_heading_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        (tmp_path / "a.md").write_text('# foo_bar\n\n[x](#foo_bar)\n', encoding="utf-8")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)

def test_relative_links_reject_dotdot_escape_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        (tmp_path / "a.md").write_text('[x](../../etc/passwd)\n', encoding="utf-8")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'escapes',
        )

def test_relative_links_accept_self_dot_path_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        (tmp_path / "a.md").write_text('# Hi\n\n[x](./a.md#hi)\n', encoding="utf-8")
        assert_pass_script(scripts / "check_relative_links.py", tmp_path)

def test_relative_links_reject_missing_image_nested_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        nested = tmp_path / "docs"
        nested.mkdir()
        (nested / "a.md").write_text("![x](missing.png)\n", encoding="utf-8")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            "broken",
        )

def test_relative_gate_requires_md_link_re_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'MD_LINK_RE',
            'MARKDOWN_LINK_RE',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD_LINK_RE',
        )

def test_relative_gate_requires_max_unquote_4_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            '_MAX_UNQUOTE_PASSES = 4',
            '_MAX_UNQUOTE_PASSES = 8',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '_MAX_UNQUOTE_PASSES = 4',
        )

def test_relative_gate_requires_should_skip_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'should_skip',
            'must_skip',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'should_skip',
        )

def test_relative_gate_requires_iter_markdown_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'iter_markdown',
            'list_markdown',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'iter_markdown',
        )

def test_relative_gate_requires_headings_in_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'headings_in',
            'slugs_in',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'headings_in',
        )

def test_relative_gate_requires_check_file_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'def check_file',
            'def scan_file',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_file',
        )

def test_relative_gate_requires_mailto_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'mailto:',
            'mail:',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'mailto:',
        )

def test_relative_gate_requires_tel_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'tel:',
            'phone:',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'tel:',
        )

def test_relative_gate_requires_nul_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'NUL',
            'ZERO_BYTE',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'NUL',
        )

def test_relative_gate_requires_angle_strip_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'strip("<>")',
            'strip("[]")',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'angle brackets',
        )

def test_relative_gate_requires_escapes_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'escapes repo',
            'leaves repo',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'escape repo',
        )

def test_relative_gate_requires_broken_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'broken relative link',
            'missing relative link',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'broken relative link',
        )

def test_relative_gate_requires_missing_heading_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'missing heading',
            'absent heading',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'missing heading',
        )

def test_relative_gate_requires_rglob_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'rglob',
            'glob',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'rglob',
        )

def test_relative_gate_requires_no_md_fail_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_relative_links.py"
        text = path.read_text(encoding="utf-8").replace(
            'no markdown files found',
            'no md files found',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'no markdown files found',
        )

def test_relative_links_reject_http_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        (tmp_path / "a.md").write_text('[x](http://example.com)\n', encoding="utf-8")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'http://',
        )

def test_relative_links_reject_protocol_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_scripts(tmp_path, "check_relative_links.py")
        (tmp_path / "a.md").write_text('[x](//example.com)\n', encoding="utf-8")
        assert_fail_script(
            scripts / "check_relative_links.py",
            tmp_path,
            'protocol-relative',
        )



# --- TOKENMAXX wiki-outline third-pass after #90 (ECO-049) ---

def test_wiki_gate_requires_wiki_exact_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('WIKI = ROOT / "docs" / "wiki"', 'WIKI = ROOT / "docs" / "pages"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'WIKI = ROOT / "docs" / "wiki"',
        )

def test_wiki_gate_requires_removesuffix_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('removesuffix(".md")', 'removesuffix(".markdown")')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'removesuffix(".md")',
        )

def test_wiki_gate_requires_glob_md_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('glob("*.md")', 'glob("*.markdown")')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'glob("*.md")',
        )

def test_wiki_gate_requires_sorted_unexpected_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('sorted(unexpected)', 'list(unexpected)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'sorted(unexpected)',
        )

def test_wiki_gate_requires_link_check_accept_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"Link Check"', '"LinkChecker"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Link Check',
        )

def test_wiki_gate_requires_markdown_lint_accept_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"Markdown Lint"', '"MarkdownLint"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Markdown Lint',
        )

def test_wiki_gate_requires_no_secrets_accept_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"No secrets"', '"No Secrets"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'No secrets',
        )

def test_wiki_gate_requires_invent_home_needle_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('invent-product out-of-scope wording', 'invent-product out of scope wording')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'invent-product out-of-scope wording',
        )

def test_wiki_gate_requires_secrets_home_needle_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('secrets out-of-scope wording', 'secrets out of scope wording')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'secrets out-of-scope wording',
        )

def test_wiki_gate_requires_kill_home_needle_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('kill-switch security callout', 'kill switch security callout')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'kill-switch security callout',
        )

def test_wiki_gate_requires_out_of_scope_section_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('Out of scope section', 'Out-of-scope section')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Out of scope section',
        )

def test_wiki_gate_requires_relative_steward_needle_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('relative-link gate coverage', 'relative link gate coverage')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'relative-link gate coverage',
        )

def test_wiki_gate_requires_invent_steward_needle_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('no-invent-product stewardship wording', 'no invent-product stewardship wording')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'no-invent-product stewardship wording',
        )

def test_wiki_gate_requires_actionlint_steward_needle_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('actionlint on existing workflow paths', 'actionlint on existing workflows')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'actionlint on existing workflow paths',
        )

def test_wiki_gate_requires_run_script_needle_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('bash scripts/run_stewardship_checks.sh', 'bash scripts/run_stewardship.sh')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'bash scripts/run_stewardship_checks.sh',
        )

def test_wiki_gate_requires_startswith_slash_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('startswith("//")', 'startswith("\\\\\\\\")')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'startswith("//")',
        )

def test_wiki_gate_requires_startswith_http_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('startswith("http://")', 'startswith("https://")')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'startswith("http://")',
        )

def test_wiki_gate_requires_group2_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('match.group(2)', 'match.group(1)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'match.group(2)',
        )

def test_wiki_gate_requires_utf8_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('encoding="utf-8"', 'encoding="utf8"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'utf-8',
        )

def test_wiki_gate_requires_sys_exit_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('sys.exit(main())', 'main()')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'sys.exit(main())',
        )

def test_wiki_gate_requires_common_import_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('from stewardship_common import', 'from stewardship_shared import')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'stewardship_common',
        )

def test_wiki_gate_requires_downloads_special_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"downloads"', '"download"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'downloads',
        )

def test_wiki_gate_requires_discord_special_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"discord"', '"Discord"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'discord',
        )

def test_wiki_gate_requires_twitter_special_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"twitter"', '"Twitter"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'twitter',
        )

def test_wiki_gate_requires_xcom_special_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"x.com"', '"x-com"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'x.com',
        )

def test_wiki_gate_requires_shields_io_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('shields.io', 'shields_io')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'shields.io',
        )

def test_wiki_gate_requires_md_badge_open_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('[![', '[!]')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '[![',
        )

def test_wiki_gate_requires_badge_in_lowered_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"badge" in lowered', '"badges" in lowered')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'badge in lowered',
        )

def test_wiki_gate_requires_readme_up_up_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"../../README.md"', '"../../../README.md"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '../../README.md',
        )

def test_wiki_gate_requires_readme_up_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"../README.md"', '"../Readme.md"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '../README.md',
        )

def test_wiki_gate_requires_badge_docs_path_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"docs/badge-standard.md"', '"doc/badge-standard.md"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'docs/badge-standard.md',
        )

def test_wiki_gate_requires_badge_blob_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('blob/main/docs/badge-standard.md', 'blob/main/docs/badge_standard.md')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'badge-standard blob',
        )

def test_wiki_gate_requires_pages_operator_ok_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('pages + operator PUBLISH.md', 'pages + operator publish.md')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'pages + operator PUBLISH.md',
        )

def test_wiki_gate_requires_intentional_pin_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('update PUBLISH.md page list if intentional', 'update PUBLISH.md page list when intentional')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'intentional',
        )

def test_wiki_gate_requires_topic_get_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('PAGE_TOPIC_HINTS.get(name, ())', 'PAGE_TOPIC_HINTS.get(name, [])', 1)
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'PAGE_TOPIC_HINTS.get(name, ())',
        )

def test_wiki_gate_requires_strip_fenced_call_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('strip_fenced_code(text)', 'strip_fenced_code(scan)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'strip_fenced_code(text)',
        )

def test_wiki_gate_requires_dangerous_call_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('has_dangerous_scheme(target)', 'has_dangerous_scheme(url)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'has_dangerous_scheme(target)',
        )

def test_wiki_gate_requires_scan_path_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('scan_secrets(path, errors)', 'scan_secrets(file, errors)', 1)
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'scan_secrets(path, errors)',
        )

def test_wiki_gate_requires_scan_publish_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('scan_secrets(publish, errors)', 'scan_secrets(operator, errors)', 1)
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'scan_secrets(publish, errors)',
        )

def test_wiki_gate_requires_third_pass_doc_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('Third-pass: WIKI exact / removesuffix', 'Third-pass: WIKI path / removesuffix')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Third-pass WIKI exact / removesuffix',
        )

def test_wiki_gate_requires_contract_fn_def_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def check_wiki_outline_gate_contract(', 'def check_wiki_outline_gate_verify(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_wiki_outline_gate_contract',
        )

def test_wiki_rejects_missing_link_check_publish_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = pages["PUBLISH.md"].replace("Link Check", "Links")

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            'Link Check',
        )

def test_wiki_rejects_missing_markdown_lint_publish_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["PUBLISH.md"] = pages["PUBLISH.md"].replace("Markdown Lint", "MD Lint")

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            'Markdown Lint',
        )

def test_wiki_rejects_missing_home_invent_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace("invent", "framework")

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            'invent',
        )

def test_wiki_rejects_missing_home_secret_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace("Secrets", "Credentials")

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            'secret',
        )

def test_wiki_rejects_missing_home_kill_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Home.md"] = pages["Home.md"].replace("Kill", "Stop")

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            'kill',
        )

def test_wiki_rejects_missing_steward_relative_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace("relative", "offline")

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            'relative',
        )

def test_wiki_rejects_missing_steward_actionlint_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace("actionlint", "workflowlint")

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            'actionlint',
        )

def test_wiki_rejects_protocol_rel_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n[x](//evil.example/x)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            'protocol-relative',
        )

def test_wiki_rejects_http_link_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n[x](http://example.com)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            'http://',
        )

def test_wiki_rejects_javascript_link_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n[x](javascript:alert(1))\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            'dangerous',
        )

def test_wiki_rejects_discord_chrome_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\nDiscord badge chrome.\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            'discord',
        )

def test_wiki_rejects_unexpected_page_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Extra.md"] = "# Extra\n\n[Home](Home.md)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            'Unexpected',
        )

def test_wiki_accepts_angle_https_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n[x](<https://example.com/doc>)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_pass_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
        )

def test_wiki_accepts_mailto_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n[x](mailto:ops@example.com)\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_pass_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
        )

def test_wiki_ignores_fenced_http_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Overview.md"] += "\n```\n[x](http://example.com)\n```\n"

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_pass_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
        )

def test_wiki_rejects_missing_run_script_steward_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)

        def mutate(pages: dict[str, str]) -> None:
            pages["Repo-Stewardship.md"] = pages["Repo-Stewardship.md"].replace("run_stewardship_checks.sh", "run_checks.sh")

        scripts = _seed_wiki_tree(tmp_path, mutate=mutate)
        assert_fail_script(
            scripts / "check_wiki_outline.py",
            tmp_path,
            'run_stewardship_checks.sh',
        )

def test_wiki_gate_requires_def_main_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('def main', 'def run_main')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'def main',
        )

def test_wiki_gate_requires_text_lower_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('text.lower()', 'text.casefold()')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'text.lower()',
        )

def test_wiki_gate_requires_present_set_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('present - set(PUBLISHABLE_PAGES)', 'present - list(PUBLISHABLE_PAGES)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'present - set(PUBLISHABLE_PAGES)',
        )

def test_wiki_gate_requires_wiki_is_dir_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('WIKI.is_dir()', 'WIKI.exists()')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'WIKI.is_dir()',
        )

def test_wiki_gate_requires_third_pass_after_90_pin() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('third-pass after #90', 'third-pass after #91')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'third-pass after #90',
        )

def test_wiki_gate_requires_operator_only_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('OPERATOR_ONLY', 'OPERATOR_PAGE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'OPERATOR_ONLY',
        )

def test_wiki_gate_requires_reject_invent_helper_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('_reject_invent_badge_chrome', '_reject_product_chrome')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '_reject_invent_badge_chrome',
        )

def test_wiki_gate_requires_home_backlink_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('](Home.md)', '](Index.md)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '](Home.md)',
        )

def test_wiki_gate_requires_failed_banner_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('Wiki outline check FAILED', 'Wiki outline check ERRORED')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Wiki outline check FAILED',
        )

def test_wiki_gate_requires_ok_match_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('docs/wiki matches PUBLISH.md', 'docs/wiki matches publish.md')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'docs/wiki matches PUBLISH.md',
        )

def test_wiki_gate_requires_publishable_pages_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('PUBLISHABLE_PAGES', 'PUBLISH_PAGES')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'PUBLISHABLE_PAGES',
        )

def test_wiki_gate_requires_topic_hints_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('PAGE_TOPIC_HINTS', 'PAGE_HINTS')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'PAGE_TOPIC_HINTS',
        )

def test_wiki_gate_requires_stars_still_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('"stars"', '"star"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'stars',
        )

def test_wiki_gate_requires_len_pages_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('len(PUBLISHABLE_PAGES)', 'len(PUBLISH_PAGES)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'len(PUBLISHABLE_PAGES)',
        )

def test_wiki_gate_requires_home_is_file_after_90() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_wiki_outline.py"
        text = path.read_text(encoding="utf-8").replace('home.is_file()', 'home.exists()')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'home.is_file()',
        )


# --- TOKENMAXX docs-lint after #100 (+72) ---
def test_docs_lint_gate_requires_lycheeignore_const_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('LYCHEEIGNORE', 'LYCHEE_SKIP')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'LYCHEEIGNORE',
        )


def test_docs_lint_gate_requires_markdownlint_const_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('MARKDOWNLINT_CONFIG', 'MARKDOWN_LINT_CFG')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MARKDOWNLINT_CONFIG',
        )


def test_docs_lint_gate_requires_lycheeignore_path_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('".lycheeignore"', '".lychee-skip"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '.lycheeignore',
        )


def test_docs_lint_gate_requires_markdownlint_path_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('".markdownlint.json"', '".markdown-lint.json"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '.markdownlint.json',
        )


def test_docs_lint_gate_requires_fn_lycheeignore_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def check_lycheeignore(', 'def check_lychee_skip(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_lycheeignore()',
        )


def test_docs_lint_gate_requires_live_doc_pin_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Live fail-closed pins after #100', 'Live fail-closed pins after #00')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Live fail-closed pins after #100',
        )


def test_docs_lint_gate_requires_escaped_shields_pin_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('img\\.shields\\.io', 'img.shields.io')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_lycheeignore must pin escaped'
        )


def test_docs_lint_gate_requires_mcp_pin_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('modelcontextprotocol.io', 'modelcontext.example.io')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_lycheeignore must pin'
        )


def test_docs_lint_gate_requires_lfs_pin_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('linuxfoundation.org', 'linux-foundation.org')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_lycheeignore must pin'
        )


def test_docs_lint_gate_requires_stewardship_note_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('stewardship/license-badge enforcement', 'stewardship/license-badge reminder')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'stewardship/license-badge enforcement',
        )


def test_docs_lint_gate_requires_https_star_reject_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('https://*', 'https://example/*')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'https://*',
        )


def test_docs_lint_gate_requires_http_star_reject_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('http://*', 'http://example/*')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'http://*',
        )


def test_docs_lint_gate_requires_md013_obj_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"MD013": { "line_length": 200 }', '"MD013": { "line_length": 180 }')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD013 line_length 200 object'
        )


def test_docs_lint_gate_requires_md024_obj_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"MD024": { "siblings_only": true }', '"MD024": { "siblings_only": false }')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD024 siblings_only true object'
        )


def test_docs_lint_gate_requires_md033_false_re_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"MD033"\\s*:\\s*false', '"MD033"\\s*:\\s*true')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD033: false',
        )


def test_docs_lint_gate_requires_md041_false_re_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"MD041"\\s*:\\s*false', '"MD041"\\s*:\\s*true')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD041: false',
        )


def test_docs_lint_gate_requires_md060_false_re_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"MD060"\\s*:\\s*false', '"MD060"\\s*:\\s*true')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD060: false',
        )


def test_docs_lint_gate_requires_default_true_re_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"default"\\s*:\\s*true', '"default"\\s*:\\s*false')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'default: true',
        )


def test_docs_lint_gate_requires_line_length_re_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"line_length"\\s*:\\s*200', '"line_length"\\s*:\\s*120')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'line_length: 200',
        )


def test_docs_lint_gate_requires_siblings_re_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"siblings_only"\\s*:\\s*true', '"siblings_only"\\s*:\\s*false')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'siblings_only: true',
        )


def test_docs_lint_gate_requires_main_call_lychee_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('check_lycheeignore(errors)', 'check_lychee_skip(errors)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_lycheeignore(errors)',
        )


def test_docs_lint_gate_requires_docs_slice_wording_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('docs-lint slice', 'docs-lint spam')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'docs-lint slice',
        )


def test_docs_lint_gate_requires_host_pin_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('docs-lint host', 'docs-lint guest')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'docs-lint host',
        )


def test_docs_lint_gate_requires_contract_spam_pin_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('not CI workflow spam', 'not badge-row spam')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'not CI workflow spam',
        )


def test_lycheeignore_rejects_missing_escaped_shields_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".lycheeignore", "# stewardship license badge\nhttps://modelcontextprotocol.io/\nhttps://www.linuxfoundation.org/\nhttps://img.shields.io/\n")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'img\\.shields\\.io',
        )


def test_lycheeignore_rejects_missing_mcp_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".lycheeignore", "# stewardship license badge\nhttps://www.linuxfoundation.org/\nhttps://img\\.shields\\.io\n")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'modelcontextprotocol.io',
        )


def test_lycheeignore_rejects_missing_lfs_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".lycheeignore", "# stewardship license badge\nhttps://modelcontextprotocol.io/\nhttps://img\\.shields\\.io\n")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'linuxfoundation.org',
        )


def test_lycheeignore_rejects_missing_stewardship_note_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".lycheeignore", "https://modelcontextprotocol.io/\nhttps://www.linuxfoundation.org/\nhttps://img\\.shields\\.io\n")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'stewardship/license-badge',
        )


def test_lycheeignore_rejects_https_star_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".lycheeignore", "# stewardship license badge\nhttps://modelcontextprotocol.io/\nhttps://www.linuxfoundation.org/\nhttps://img\\.shields\\.io\nhttps://*\n")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'https://*',
        )


def test_lycheeignore_rejects_http_star_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".lycheeignore", "# stewardship license badge\nhttps://modelcontextprotocol.io/\nhttps://www.linuxfoundation.org/\nhttps://img\\.shields\\.io\nhttp://*\n")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'http://*',
        )


def test_lycheeignore_rejects_bare_star_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".lycheeignore", "*\n")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'must not exclude all http',
        )


def test_lycheeignore_rejects_missing_shields_any_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".lycheeignore", "# stewardship license badge\nhttps://modelcontextprotocol.io/\nhttps://www.linuxfoundation.org/\n")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'img.shields.io',
        )


def test_markdownlint_rejects_wrong_md013_obj_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".markdownlint.json", '{\n  "default": true,\n  "MD013": { "line_length": 180 },\n'
        '  "MD024": { "siblings_only": true },\n'
        '  "MD033": false,\n  "MD041": false,\n  "MD060": false\n}\n')
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '"MD013": { "line_length": 200 }',
        )


def test_markdownlint_rejects_wrong_md024_obj_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".markdownlint.json", '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n'
        '  "MD024": { "siblings_only": false },\n'
        '  "MD033": false,\n  "MD041": false,\n  "MD060": false\n}\n')
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '"MD024": { "siblings_only": true }',
        )


def test_markdownlint_rejects_md033_true_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".markdownlint.json", '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n'
        '  "MD024": { "siblings_only": true },\n'
        '  "MD033": true,\n  "MD041": false,\n  "MD060": false\n}\n')
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD033: false',
        )


def test_markdownlint_rejects_md041_true_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".markdownlint.json", '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n'
        '  "MD024": { "siblings_only": true },\n'
        '  "MD033": false,\n  "MD041": true,\n  "MD060": false\n}\n')
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD041: false',
        )


def test_markdownlint_rejects_md060_true_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".markdownlint.json", '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n'
        '  "MD024": { "siblings_only": true },\n'
        '  "MD033": false,\n  "MD041": false,\n  "MD060": true\n}\n')
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD060: false',
        )


def test_markdownlint_rejects_default_false_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".markdownlint.json", '{\n  "default": false,\n  "MD013": { "line_length": 200 },\n'
        '  "MD024": { "siblings_only": true },\n'
        '  "MD033": false,\n  "MD041": false,\n  "MD060": false\n}\n')
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'default: true',
        )


def test_markdownlint_rejects_missing_md013_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".markdownlint.json", '{\n  "default": true,\n'
        '  "MD024": { "siblings_only": true },\n'
        '  "MD033": false,\n  "MD041": false,\n  "MD060": false\n}\n')
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD013',
        )


def test_markdownlint_rejects_missing_md024_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".markdownlint.json", '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n'
        '  "MD033": false,\n  "MD041": false,\n  "MD060": false\n}\n')
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD024',
        )


def test_docs_lint_gate_requires_lycheeignore_const_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('LYCHEEIGNORE', 'LYCHEE_FILE')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'LYCHEEIGNORE',
        )


def test_docs_lint_gate_requires_markdownlint_const_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('MARKDOWNLINT_CONFIG', 'MDLINT_CONFIG')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MARKDOWNLINT_CONFIG',
        )


def test_docs_lint_gate_requires_escaped_shields_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('img\\.shields\\.io', 'img#shields#io')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_lycheeignore must pin escaped'
        )


def test_docs_lint_gate_requires_mcp_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('modelcontextprotocol.io', 'mcp.example.io')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_lycheeignore must pin'
        )


def test_docs_lint_gate_requires_lfs_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('linuxfoundation.org', 'lf.example.org')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_lycheeignore must pin'
        )


def test_docs_lint_gate_requires_md013_obj_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"MD013": { "line_length": 200 }', '"MD013": { "line_length": 220 }')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD013 line_length 200 object'
        )


def test_docs_lint_gate_requires_md024_obj_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"MD024": { "siblings_only": true }', '"MD024": { "siblings_only": 1 }')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD024 siblings_only true object'
        )


def test_docs_lint_gate_requires_fn_lycheeignore_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('def check_lycheeignore(', 'def check_ignore_lychee(')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_lycheeignore()',
        )


def test_docs_lint_gate_requires_live_doc_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Live fail-closed pins after #100', 'Live fail-closed pins after #82')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Live fail-closed pins after #100',
        )


def test_docs_lint_gate_requires_https_star_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('https://*', 'https://all')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'https://*',
        )


def test_docs_lint_gate_requires_stewardship_note_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('stewardship/license-badge enforcement', 'stewardship license badge note')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'stewardship/license-badge enforcement',
        )


def test_docs_lint_gate_requires_default_true_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('"default"\\s*:\\s*true', '"default"\\s*:\\s*yes')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'default: true',
        )


def test_docs_lint_gate_requires_docs_slice_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('docs-lint slice', 'docs-lint area')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'docs-lint slice',
        )


def test_lycheeignore_rejects_missing_mcp_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".lycheeignore", "# license badge stewardship\nhttps://www.linuxfoundation.org/\nhttps://img\\.shields\\.io\n")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'modelcontextprotocol.io',
        )


def test_lycheeignore_rejects_missing_lfs_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".lycheeignore", "# license badge stewardship\nhttps://modelcontextprotocol.io/\nhttps://img\\.shields\\.io\n")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'linuxfoundation.org',
        )


def test_lycheeignore_rejects_missing_escaped_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".lycheeignore", "# license badge stewardship\nhttps://modelcontextprotocol.io/\nhttps://www.linuxfoundation.org/\nhttps://img.shields.io\n")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'img\\.shields\\.io',
        )


def test_markdownlint_rejects_line_length_120_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".markdownlint.json", '{\n  "default": true,\n  "MD013": { "line_length": 120 },\n'
        '  "MD024": { "siblings_only": true },\n'
        '  "MD033": false,\n  "MD041": false,\n  "MD060": false\n}\n')
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'line_length: 200',
        )


def test_markdownlint_rejects_siblings_false_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".markdownlint.json", '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n'
        '  "MD024": { "siblings_only": false },\n'
        '  "MD033": false,\n  "MD041": false,\n  "MD060": false\n}\n')
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'siblings_only: true',
        )


def test_markdownlint_rejects_missing_default_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(tmp_path / ".markdownlint.json", '{\n  "MD013": { "line_length": 200 },\n'
        '  "MD024": { "siblings_only": true },\n'
        '  "MD033": false,\n  "MD041": false,\n  "MD060": false\n}\n')
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'default: true',
        )


def test_docs_lint_gate_requires_contract_fn_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "docs-lint host",
            "docs-lint guest",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "docs-lint host",
        )


def test_docs_lint_gate_requires_spam_wording_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "not CI workflow spam",
            "not CI workflow noise",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "not CI workflow spam",
        )


def test_lycheeignore_rejects_missing_note_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(
            tmp_path / ".lycheeignore",
            "https://modelcontextprotocol.io/\n"
            "https://www.linuxfoundation.org/\n"
            "https://img\\.shields\\.io\n",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "stewardship/license-badge",
        )


def test_markdownlint_rejects_md033_true_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(
            tmp_path / ".markdownlint.json",
            '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n'
            '  "MD024": { "siblings_only": true },\n'
            '  "MD033": true,\n  "MD041": false,\n  "MD060": false\n}\n',
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "MD033: false",
        )


def test_markdownlint_rejects_md041_true_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(
            tmp_path / ".markdownlint.json",
            '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n'
            '  "MD024": { "siblings_only": true },\n'
            '  "MD033": false,\n  "MD041": true,\n  "MD060": false\n}\n',
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "MD041: false",
        )


def test_markdownlint_rejects_md060_true_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(
            tmp_path / ".markdownlint.json",
            '{\n  "default": true,\n  "MD013": { "line_length": 200 },\n'
            '  "MD024": { "siblings_only": true },\n'
            '  "MD033": false,\n  "MD041": false,\n  "MD060": true\n}\n',
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "MD060: false",
        )


def test_docs_lint_gate_requires_http_star_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace("http://*", "http://all")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "http://*",
        )


def test_docs_lint_gate_requires_md033_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(r'"MD033"\s*:\s*false', r'"MD033"\s*:\s*off')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "MD033: false",
        )


def test_docs_lint_gate_requires_md041_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(r'"MD041"\s*:\s*false', r'"MD041"\s*:\s*off')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "MD041: false",
        )


def test_docs_lint_gate_requires_md060_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(r'"MD060"\s*:\s*false', r'"MD060"\s*:\s*off')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "MD060: false",
        )


def test_docs_lint_gate_requires_line_length_re_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(r'"line_length"\s*:\s*200', r'"line_length"\s*:\s*100')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "line_length: 200",
        )


def test_docs_lint_gate_requires_siblings_re_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(r'"siblings_only"\s*:\s*true', r'"siblings_only"\s*:\s*yes')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "siblings_only: true",
        )


def test_lycheeignore_accepts_full_live_fixture_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(
            tmp_path / ".lycheeignore",
            "# modelcontextprotocol.io returns 308 — lychee false-positive\n"
            "https://modelcontextprotocol.io/\n"
            "# linuxfoundation.org returns 103 — lychee false-positive\n"
            "https://www.linuxfoundation.org/\n"
            "# img.shields.io badge CDN is flaky — license badge stays stewardship-enforced\n"
            "https://img\\.shields\\.io\n",
        )
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)



# --- TOKENMAXX docs-lint deepen after #100 (+36) ---
def test_docs_lint_gate_requires_wiki_spam_pin_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "not wiki / relative pin spam", "not wiki / relative pin noise"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "not wiki / relative pin spam",
        )


def test_docs_lint_gate_requires_308_redirect_pin_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace("308 redirect", "308 bounce")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "308 redirect",
        )


def test_docs_lint_gate_requires_103_early_pin_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace("103 early", "103 late")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "103 early",
        )


def test_docs_lint_gate_requires_lychee_assign_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'LYCHEEIGNORE = ROOT / ".lycheeignore"\nMARKDOWNLINT_CONFIG',
            'LYCHEEIGNORE = ROOT / ".lychee-ignore"\nMARKDOWNLINT_CONFIG',
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'LYCHEEIGNORE = ROOT / ".lycheeignore"',
        )


def test_docs_lint_gate_requires_md_assign_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'MARKDOWNLINT_CONFIG = ROOT / ".markdownlint.json"\nWORKFLOWS',
            'MARKDOWNLINT_CONFIG = ROOT / ".markdownlint.yml"\nWORKFLOWS',
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MARKDOWNLINT_CONFIG = ROOT / ".markdownlint.json"',
        )


def test_docs_lint_gate_requires_workflows_fn_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "def check_workflows_and_license(",
            "def check_workflows_and_licence(",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "check_workflows_and_license()",
        )


def test_docs_lint_gate_requires_missing_lychee_needle_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "Missing .lycheeignore (lychee link-check excludes)",
            "Missing lychee ignore file (lychee link-check excludes)",
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Missing .lycheeignore",
        )


def test_docs_lint_gate_requires_missing_md_needle_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "Missing .markdownlint.json (markdown-lint config)",
            "Missing markdownlint config (markdown-lint config)",
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Missing .markdownlint.json",
        )


def test_docs_lint_gate_requires_cdn_note_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "CDN exclude is not a missing License badge",
            "CDN exclude is not a missing license badge",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "CDN exclude is not a missing License badge",
        )


def test_docs_lint_gate_requires_workflows_call_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "check_workflows_and_license(errors)",
            "check_workflows_and_licence(errors)",
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "check_workflows_and_license(errors)",
        )


def test_lycheeignore_rejects_missing_308_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(
            tmp_path / ".lycheeignore",
            "# stewardship/license-badge enforcement\n"
            "https://modelcontextprotocol.io/\n"
            "# linuxfoundation.org 103 early hints\n"
            "https://www.linuxfoundation.org/\n"
            "https://img\\.shields\\.io\n",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "308",
        )


def test_lycheeignore_rejects_missing_103_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(
            tmp_path / ".lycheeignore",
            "# modelcontextprotocol.io 308 redirect\n"
            "https://modelcontextprotocol.io/\n"
            "# stewardship/license-badge enforcement\n"
            "https://www.linuxfoundation.org/\n"
            "https://img\\.shields\\.io\n",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "103",
        )


def test_docs_lint_gate_requires_wiki_spam_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "not wiki / relative pin spam", "wiki-ok relative pin spam"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "not wiki / relative pin spam",
        )


def test_docs_lint_gate_requires_308_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace("308 redirect", "308 status")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "308 redirect",
        )


def test_docs_lint_gate_requires_103_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace("103 early", "103 hint")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "103 early",
        )


def test_docs_lint_gate_requires_lychee_assign_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'LYCHEEIGNORE = ROOT / ".lycheeignore"\nMARKDOWNLINT_CONFIG',
            'LYCHEEIGNORE = ROOT / "lycheeignore"\nMARKDOWNLINT_CONFIG',
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'LYCHEEIGNORE = ROOT / ".lycheeignore"',
        )


def test_docs_lint_gate_requires_md_assign_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'MARKDOWNLINT_CONFIG = ROOT / ".markdownlint.json"\nWORKFLOWS',
            'MARKDOWNLINT_CONFIG = ROOT / "markdownlint.json"\nWORKFLOWS',
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MARKDOWNLINT_CONFIG = ROOT / ".markdownlint.json"',
        )


def test_docs_lint_gate_requires_workflows_fn_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "check_workflows_and_license", "check_workflows_and_docs", 1
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "check_workflows_and_license()",
        )


def test_docs_lint_gate_requires_cdn_note_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "CDN exclude is not a missing License badge",
            "CDN exclude is unrelated to License badge",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "CDN exclude is not a missing License badge",
        )


def test_docs_lint_gate_requires_workflows_call_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        # Replace only the main() call site by targeting trailing context
        text = path.read_text(encoding="utf-8")
        text = text.replace(
            "    check_workflows_and_license(errors)\n",
            "    check_workflows_and_licence(errors)\n",
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "check_workflows_and_license(errors)",
        )


def test_lycheeignore_rejects_missing_308_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(
            tmp_path / ".lycheeignore",
            "# stewardship note\n"
            "https://modelcontextprotocol.io/\n"
            "# 103 early hints\n"
            "https://www.linuxfoundation.org/\n"
            r"https://img\.shields\.io" + "\n",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "308",
        )


def test_lycheeignore_rejects_missing_103_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(
            tmp_path / ".lycheeignore",
            "# 308 redirect\n"
            "https://modelcontextprotocol.io/\n"
            "# stewardship note\n"
            "https://www.linuxfoundation.org/\n"
            r"https://img\.shields\.io" + "\n",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "103",
        )


def test_docs_lint_gate_requires_contract_call_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        # Break exact call needle without renaming the callable (avoid NameError).
        text = path.read_text(encoding="utf-8").replace(
            "    check_docs_lint_gate_contract(errors)\n",
            "    check_docs_lint_gate_contract (errors)\n",
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "check_docs_lint_gate_contract(errors)",
        )


def test_docs_lint_gate_requires_docs_slice_deepen_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace("docs-lint slice", "docs-lint area")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "docs-lint slice",
        )


def test_markdownlint_rejects_wrong_md013_spacing_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        text = path.read_text(encoding="utf-8").replace(
            '"MD013": { "line_length": 200 }',
            '"MD013": {"line_length": 200}',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD013": { "line_length": 200 }',
        )


def test_markdownlint_rejects_wrong_md024_spacing_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        text = path.read_text(encoding="utf-8").replace(
            '"MD024": { "siblings_only": true }',
            '"MD024": {"siblings_only": true}',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MD024": { "siblings_only": true }',
        )


def test_docs_lint_gate_requires_host_pin_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace("docs-lint host", "docs-lint guest")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "docs-lint host",
        )


def test_docs_lint_gate_requires_spam_pin_still_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "not CI workflow spam", "not CI workflow noise"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "not CI workflow spam",
        )


def test_lycheeignore_rejects_https_star_with_full_fixture_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(
            tmp_path / ".lycheeignore",
            "# modelcontextprotocol.io 308 redirect\n"
            "https://modelcontextprotocol.io/\n"
            "# linuxfoundation.org 103 early hints\n"
            "https://www.linuxfoundation.org/\n"
            "# stewardship/license-badge enforcement\n"
            "https://img\\.shields\\.io\n"
            "https://*\n",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "https://*",
        )


def test_lycheeignore_rejects_http_star_with_full_fixture_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        _write(
            tmp_path / ".lycheeignore",
            "# modelcontextprotocol.io 308 redirect\n"
            "https://modelcontextprotocol.io/\n"
            "# linuxfoundation.org 103 early hints\n"
            "https://www.linuxfoundation.org/\n"
            "# stewardship/license-badge enforcement\n"
            "https://img\\.shields\\.io\n"
            "http://*\n",
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "http://*",
        )


def test_docs_lint_gate_requires_live_doc_deepen_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "Live fail-closed pins after #100",
            "Live fail-closed pins after #99",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Live fail-closed pins after #100",
        )


def test_docs_lint_gate_requires_escaped_shields_deepen_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(r"img\.shields\.io", "img.shields.io")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "escaped",
        )


def test_docs_lint_gate_requires_mcp_deepen_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "modelcontextprotocol.io", "mcp.example.io"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "modelcontextprotocol.io",
        )


def test_docs_lint_gate_requires_lfs_deepen_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "linuxfoundation.org", "lf.example.org"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "linuxfoundation.org",
        )


def test_lycheeignore_accepts_live_tree_seed_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        # Seed fixture already matches live docs-lint posture after #100.
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)


def test_markdownlint_accepts_exact_objects_after_100() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".markdownlint.json"
        assert '"MD013": { "line_length": 200 }' in path.read_text(encoding="utf-8")
        assert '"MD024": { "siblings_only": true }' in path.read_text(encoding="utf-8")
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)




def test_badge_accepts_absolute_license_blob_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme().replace(
            "(LICENSE)",
            "(https://github.com/fuzzywigg/agents-governance/blob/main/LICENSE)",
            1,
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)


def test_badge_accepts_dot_slash_license_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme().replace("(LICENSE)", "(./LICENSE)", 1)
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)


def test_badge_gate_requires_abs_https_when_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'must use https:// when absolute',
            'must use https:// if absolute',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'must use https:// when absolute',
        )


def test_badge_gate_requires_abs_wf_url_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'absolute https:// workflow URL',
            'absolute https:// actions URL',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'absolute https:// workflow URL',
        )


def test_badge_gate_requires_badge_gate_eq_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'BADGE_GATE = Path(__file__).resolve()',
            'BADGE_GATE = Path(__file__).absolute()',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'BADGE_GATE = Path(__file__).resolve()',
        )


def test_badge_gate_requires_badge_line_exact_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            '(?P<label>[^\\]]+)',
            '(?P<label>[^]]+)',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'BADGE_LINE_RE exact',
        )


def test_badge_gate_requires_blob_lower_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'f"{label}|{img}|{link}".lower()',
            'f"{label}|{img}|{link}".casefold()',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'f"{label}|{img}|{link}".lower()',
        )


def test_badge_gate_requires_check_badges_call_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "check_badges(badges, errors)",
            "check_badges(badges, errs)",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "check_badges(badges, errors)",
        )


def test_badge_gate_requires_common_import_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'from stewardship_common import',
            'from stewardship_common  import',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'stewardship_common',
        )


def test_badge_gate_requires_contract_call_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        # Insert whitespace so the call still binds main's errors list, but the
        # exact contiguous pin check_badge_standard_gate_contract(errors) is gone.
        text = path.read_text(encoding="utf-8").replace(
            "    check_badge_standard_gate_contract(errors)",
            "    check_badge_standard_gate_contract( errors)",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "check_badge_standard_gate_contract(errors)",
        )


def test_badge_gate_requires_contract_fn_still_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'def check_badge_standard_gate_contract(',
            'def check_badge_standard_gate_verify(',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_badge_standard_gate_contract',
        )


def test_badge_gate_requires_expected_repo_lower_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'EXPECTED_REPO.lower()',
            'EXPECTED_REPO.casefold()',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'EXPECTED_REPO.lower()',
        )


def test_badge_gate_requires_expected_repo_still_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'EXPECTED_REPO = "fuzzywigg/agents-governance"',
            'EXPECTED_REPO = "fuzzywigg/other-repo"',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'EXPECTED_REPO = "fuzzywigg/agents-governance"',
        )


def test_badge_gate_requires_extract_call_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "extract_badge_row(text)",
            "extract_badge_row(readme)",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "extract_badge_row(text)",
        )


def test_badge_gate_requires_fail_readme_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'FAIL: README.md missing',
            'ERROR: README.md missing',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'FAIL: README.md missing',
        )


def test_badge_gate_requires_failed_banner_still_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'Badge standard check FAILED',
            'Badge standard check ERROR',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Badge standard check FAILED',
        )


def test_badge_gate_requires_found_lower_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'found.lower()',
            'found.casefold()',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'found.lower()',
        )


def test_badge_gate_requires_future_annotations_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'from __future__ import annotations',
            'from __future__ import generator_stop',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'from __future__ import annotations',
        )


def test_badge_gate_requires_github_re_exact_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'https://github\\.com/(?P<owner>[^/]+)/(?P<repo>[^/\\s?#]+)',
            'https://github\\.com/(?P<owner>[^/]+)/(?P<repo>[^/\\s]+)',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'REPO_FROM_GITHUB_RE exact',
        )


def test_badge_gate_requires_group_img_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'group("img")',
            'group("image")',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'group("img")',
        )


def test_badge_gate_requires_group_label_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'group("label")',
            'group("name")',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'group("label")',
        )


def test_badge_gate_requires_group_link_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'group("link")',
            'group("href")',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'group("link")',
        )


def test_badge_gate_requires_h1_startswith_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'startswith("# ")',
            'startswith("#\\t")',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'startswith("# ")',
        )


def test_badge_gate_requires_http_link_reject_still_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'link.startswith("http://")',
            'link.startswith("ftp://")',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'link.startswith(http://)',
        )


def test_badge_gate_requires_https_start_still_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'img.startswith("https://")',
            'img.startswith("http://")',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'img.startswith(https://)',
        )


def test_badge_gate_requires_ignorecase_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        text = text.replace("\n    re.IGNORECASE,\n)", "\n    re.ASCII,\n)", 2)
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "REPO_FROM_GITHUB_RE exact IGNORECASE",
        )


def test_badge_gate_requires_img_https_needle_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "Badge image for",
            "Badge image of",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Badge image for",
        )


def test_badge_gate_requires_lic_img_must_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'License image must use img.shields.io/github/license/',
            'License image must use img.shields.io/github/licence/',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'License image must use img.shields.io/github/license/',
        )


def test_badge_gate_requires_lic_point_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'License badge link must point at LICENSE',
            'License badge link must target LICENSE',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'License badge link must point at LICENSE',
        )


def test_badge_gate_requires_license_actions_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            '(?:license|actions)',
            '(?:license|status)',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'license|actions',
        )


def test_badge_gate_requires_link_img_must_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'Link Check image must use link-check.yml/badge.svg',
            'Link Check image must use link-check.yml/badge.png',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Link Check image must use link-check.yml/badge.svg',
        )


def test_badge_gate_requires_list_required_order_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'list(REQUIRED_ORDER)',
            'tuple(REQUIRED_ORDER)',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'list(REQUIRED_ORDER)',
        )


def test_badge_gate_requires_main_fn_still_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'def main() -> int:',
            'def entry() -> int:',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'main()',
        )


def test_badge_gate_requires_max_badges_eq_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'MAX_BADGES = 3',
            'MAX_BADGES = 4',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'MAX_BADGES = 3',
        )


def test_badge_gate_requires_md_img_must_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'Markdown Lint image must use markdown-lint.yml/badge.svg',
            'Markdown Lint image must use markdown-lint.yml/badge.png',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Markdown Lint image must use markdown-lint.yml/badge.svg',
        )


def test_badge_gate_requires_named_img_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            '(?P<img>',
            '(?P<image>',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '(?P<img>',
        )


def test_badge_gate_requires_named_label_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            '(?P<label>',
            '(?P<title>',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '(?P<label>',
        )


def test_badge_gate_requires_named_link_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            '(?P<link>',
            '(?P<href>',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '(?P<link>',
        )


def test_badge_gate_requires_ok_banner_still_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'OK: README badge row matches docs/badge-standard.md',
            'OK: README badge row matches badge-standard.md',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'OK: README badge row matches docs/badge-standard.md',
        )


def test_badge_gate_requires_only_allowed_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'only {list(REQUIRED_ORDER)} allowed',
            'only {list(REQUIRED_ORDER)} permitted',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'only {list(REQUIRED_ORDER)} allowed',
        )


def test_badge_gate_requires_required_order_still_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'REQUIRED_ORDER = ("Link Check", "Markdown Lint", "License")',
            'REQUIRED_ORDER = ("License", "Markdown Lint", "Link Check")',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'REQUIRED_ORDER = ("Link Check", "Markdown Lint", "License")',
        )


def test_badge_gate_requires_return_empty_errors_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'return [], errors',
            'return [], []',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'return [], errors',
        )


def test_badge_gate_requires_scripts_parent_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "_SCRIPTS = Path(__file__).resolve().parent",
            "_SCRIPTS = Path(__file__).absolute().parent",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "_SCRIPTS via Path(__file__).resolve().parent",
        )


def test_badge_gate_requires_shields_re_exact_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'r"https://img\\.shields\\.io/github/(?:license|actions)/"',
            'r"https://img\\.shields\\.io/github/(?:license|workflow)/"',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "REPO_FROM_SHIELDS_RE exact",
        )


def test_badge_gate_requires_stderr_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "file=sys.stderr",
            "file=sys.stdout",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "sys.stderr",
        )


def test_badge_gate_requires_strict_row_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'Strict row: no blank lines',
            'Loose row: blank lines ok',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Strict row',
        )


def test_badge_gate_requires_sys_exit_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        text = text.replace(
            'if __name__ == "__main__":\n    sys.exit(main())',
            'if __name__ == "__main__":\n    raise SystemExit(main())',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "sys.exit(main()) under __main__",
        )


def test_badge_gate_requires_third_pass_doc_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "Third-pass: BADGE_LINE_RE+REPO_FROM_* exact",
            "Third-pass: BADGE_LINE_RE exact only",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Third-pass BADGE_LINE_RE+REPO_FROM_* exact",
        )


def test_badge_gate_requires_unexpected_label_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'Unexpected badge label',
            'Unknown badge label',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Unexpected badge label',
        )


def test_badge_gate_requires_utf8_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'encoding="utf-8"',
            'encoding="latin-1"',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "utf-8",
        )


def test_badge_gate_requires_workflows_exact_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'REQUIRED_WORKFLOWS = (\n    "link-check.yml",\n    "markdown-lint.yml",\n    "stewardship-checks.yml",\n)',
            'REQUIRED_WORKFLOWS = (\n    "link-check.yml",\n    "markdown-lint.yml",\n    "stewardship.yml",\n)',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "REQUIRED_WORKFLOWS exact",
        )


def test_badge_gate_requires_workflows_path_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'ROOT / ".github" / "workflows"',
            'ROOT / ".github" / "Actions"',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'ROOT / ".github" / "workflows"',
        )


def test_badge_passes_good_fixture_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)


def test_badge_passes_live_contract_after_104() -> None:
    assert_pass_live("check_badge_standard.py")


def test_badge_rejects_codecov_hint_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme().replace(
            "link-check.yml/badge.svg",
            "link-check.yml/badge.svg#codecov",
            1,
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_downloads_hint_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme().replace(
            "markdown-lint.yml/badge.svg",
            "markdown-lint.yml/badge.svg#downloads",
            1,
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_four_badges_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n"
            "[![coverage](https://img.shields.io/badge/coverage-1-red)](https://example.com)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "exactly 3 badges",
        )


def test_badge_rejects_http_badge_link_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme().replace(
            "](https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)",
            "](http://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)",
            1,
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must use https:// when absolute",
        )


def test_badge_rejects_http_image_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme().replace(
            "https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml/badge.svg",
            "http://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml/badge.svg",
            1,
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "must be https://",
        )


def test_badge_rejects_invent_still_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n"
            "[![coverage](https://codecov.io/gh/x/y/badge.svg)](https://codecov.io)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "exactly 3 badges",
        )


def test_badge_rejects_missing_h1_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme().replace("# agents-governance", "agents-governance", 1)
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "missing H1",
        )


def test_badge_rejects_noncontiguous_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme().replace(
            "link-check.yml)\n[![Markdown Lint]",
            "link-check.yml)\n\n[![Markdown Lint]",
            1,
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "contiguous",
        )


def test_badge_rejects_secret_url_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?api_key=x)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Secret-like token",
        )


def test_badge_rejects_secret_url_still_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, (
            "# agents-governance\n\n"
            "[![Link Check](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/link-check.yml/badge.svg?api_key=x)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/link-check.yml)\n"
            "[![Markdown Lint](https://github.com/fuzzywigg/agents-governance/"
            "actions/workflows/markdown-lint.yml/badge.svg)]"
            "(https://github.com/fuzzywigg/agents-governance/actions/workflows/markdown-lint.yml)\n"
            "[![License](https://img.shields.io/github/license/fuzzywigg/agents-governance)]"
            "(LICENSE)\n\n"
            "See [docs/badge-standard.md](docs/badge-standard.md).\n"
            "Run `bash scripts/run_stewardship_checks.sh`.\n"
        ))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Secret-like token",
        )


def test_badge_rejects_stars_hint_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme().replace(
            "markdown-lint.yml/badge.svg",
            "markdown-lint.yml/badge.svg#stars",
            1,
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Forbidden invent-product",
        )


def test_badge_rejects_stewardship_product_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme(("Stewardship", "Markdown Lint", "License"))
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Badge labels must be in order",
        )


def test_badge_rejects_two_badges_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        lines = []
        for line in _good_readme().splitlines(True):
            if line.startswith("[![License]"):
                continue
            lines.append(line)
        scripts = _seed_badge_tree(tmp_path, "".join(lines))
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "exactly",
        )


def test_badge_rejects_wrong_license_link_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme().replace("(LICENSE)", "(COPYING)", 1)
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "License badge link must point",
        )


def test_badge_rejects_wrong_link_check_image_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme().replace(
            "link-check.yml/badge.svg",
            "other-check.yml/badge.svg",
            1,
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "link-check.yml/badge.svg",
        )


def test_badge_rejects_wrong_md_lint_image_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme().replace(
            "markdown-lint.yml/badge.svg",
            "other-lint.yml/badge.svg",
            1,
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "markdown-lint.yml/badge.svg",
        )


def test_badge_rejects_wrong_order_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(
            tmp_path,
            _good_readme(("Markdown Lint", "Link Check", "License")),
        )
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Badge labels must be in order",
        )


def test_badge_rejects_wrong_repo_after_104() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        readme = _good_readme().replace(
            "fuzzywigg/agents-governance",
            "fuzzywigg/other-repo",
        )
        scripts = _seed_badge_tree(tmp_path, readme)
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Badge URL repo slug must be",
        )



# --- TOKENMAXX deepen after #108: actionlint-style third-pass pins ---

def test_actionlint_third_gate_third_pass_lc_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'third-pass after #108' in text
        path.write_text(text.replace('third-pass after #108', 'third-pass after #00', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'third-pass after #108')


def test_actionlint_third_gate_third_pass_lc_still_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'third-pass after #108' in text
        path.write_text(text.replace('third-pass after #108', 'third-pass after #00', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'third-pass after #108')


def test_actionlint_third_gate_concurrency_if_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'if "concurrency:" not in text:' in text
        path.write_text(text.replace('if "concurrency:" not in text:', 'if "parallelism:" not in text:', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'concurrency:')


def test_actionlint_third_gate_concurrency_if_still_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'if "concurrency:" not in text:' in text
        path.write_text(text.replace('if "concurrency:" not in text:', 'if "parallelism:" not in text:', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'concurrency:')


def test_actionlint_third_gate_cancel_if_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'if "cancel-in-progress:" not in text:' in text
        path.write_text(text.replace('if "cancel-in-progress:" not in text:', 'if "cancel-in-flight:" not in text:', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'cancel-in-progress:')


def test_actionlint_third_gate_cancel_if_still_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'if "cancel-in-progress:" not in text:' in text
        path.write_text(text.replace('if "cancel-in-progress:" not in text:', 'if "cancel-in-flight:" not in text:', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'cancel-in-progress:')


def test_actionlint_third_gate_fail_actions_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'actions: write is forbidden on stewardship workflows' in text
        path.write_text(text.replace('actions: write is forbidden on stewardship workflows', 'actions: write is forbidden on stewardship jobs', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'actions: write')


def test_actionlint_third_gate_fail_actions_still_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'actions: write is forbidden on stewardship workflows' in text
        path.write_text(text.replace('actions: write is forbidden on stewardship workflows', 'actions: write is forbidden on stewardship jobs', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'actions: write')


def test_actionlint_third_gate_fail_packages_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'packages: write is forbidden on stewardship workflows' in text
        path.write_text(text.replace('packages: write is forbidden on stewardship workflows', 'packages: write is forbidden on stewardship jobs', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'packages: write')


def test_actionlint_third_gate_fail_packages_still_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'packages: write is forbidden on stewardship workflows' in text
        path.write_text(text.replace('packages: write is forbidden on stewardship workflows', 'packages: write is forbidden on stewardship jobs', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'packages: write')


def test_actionlint_third_gate_fail_prs_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'pull-requests: write is forbidden on stewardship workflows' in text
        path.write_text(text.replace('pull-requests: write is forbidden on stewardship workflows', 'pull-requests: write is forbidden on stewardship jobs', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'pull-requests: write')


def test_actionlint_third_gate_fail_prs_still_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'pull-requests: write is forbidden on stewardship workflows' in text
        path.write_text(text.replace('pull-requests: write is forbidden on stewardship workflows', 'pull-requests: write is forbidden on stewardship jobs', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'pull-requests: write')


def test_actionlint_third_gate_reversible_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'reversible CI concurrency' in text
        path.write_text(text.replace('reversible CI concurrency', 'reversible CI workflow', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'reversible CI concurrency')


def test_actionlint_third_gate_reversible_still_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'reversible CI concurrency' in text
        path.write_text(text.replace('reversible CI concurrency', 'reversible CI workflow', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'reversible CI concurrency')


def test_actionlint_third_gate_docker_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'startswith("docker://")' in text
        path.write_text(text.replace('startswith("docker://")', 'startswith("oci://")', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'docker://')


def test_actionlint_third_gate_docker_still_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'startswith("docker://")' in text
        path.write_text(text.replace('startswith("docker://")', 'startswith("oci://")', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'docker://')


def test_actionlint_third_gate_finditer_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 're.finditer(' in text
        path.write_text(text.replace('re.finditer(', 're.finditer_x(', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 're.finditer')


def test_actionlint_third_gate_finditer_still_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 're.finditer(' in text
        path.write_text(text.replace('re.finditer(', 're.finditer_x(', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 're.finditer')


def test_actionlint_third_gate_actions_re_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'r"(?m)^\\s*actions:\\s*write\\s*$"' in text
        path.write_text(text.replace('r"(?m)^\\s*actions:\\s*write\\s*$"', 'r"(?m)^\\s*actions:\\s*wrote\\s*$"', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'actions: write')


def test_actionlint_third_gate_actions_re_still_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'r"(?m)^\\s*actions:\\s*write\\s*$"' in text
        path.write_text(text.replace('r"(?m)^\\s*actions:\\s*write\\s*$"', 'r"(?m)^\\s*actions:\\s*wrote\\s*$"', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'actions: write')


def test_actionlint_third_gate_packages_re_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'r"(?m)^\\s*packages:\\s*write\\s*$"' in text
        path.write_text(text.replace('r"(?m)^\\s*packages:\\s*write\\s*$"', 'r"(?m)^\\s*packages:\\s*wrote\\s*$"', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'packages: write')


def test_actionlint_third_gate_packages_re_still_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'r"(?m)^\\s*packages:\\s*write\\s*$"' in text
        path.write_text(text.replace('r"(?m)^\\s*packages:\\s*write\\s*$"', 'r"(?m)^\\s*packages:\\s*wrote\\s*$"', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'packages: write')


def test_actionlint_third_gate_pr_re_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'r"(?m)^\\s*pull-requests:\\s*write\\s*$"' in text
        path.write_text(text.replace('r"(?m)^\\s*pull-requests:\\s*write\\s*$"', 'r"(?m)^\\s*pull-requests:\\s*wrote\\s*$"', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'pull-requests: write')


def test_actionlint_third_gate_pr_re_still_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'r"(?m)^\\s*pull-requests:\\s*write\\s*$"' in text
        path.write_text(text.replace('r"(?m)^\\s*pull-requests:\\s*write\\s*$"', 'r"(?m)^\\s*pull-requests:\\s*wrote\\s*$"', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'pull-requests: write')


def test_actionlint_third_gate_concurrency_mem_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert '"concurrency:" not in text' in text
        path.write_text(text.replace('"concurrency:" not in text', '"parallelism:" not in text', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'concurrency:')


def test_actionlint_third_gate_concurrency_mem_still_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert '"concurrency:" not in text' in text
        path.write_text(text.replace('"concurrency:" not in text', '"parallelism:" not in text', 1), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'concurrency:')


def test_actionlint_third_gate_rsplit_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'rsplit("@", 1)[-1]' in text
        path.write_text(text.replace('rsplit("@", 1)[-1]', 'rsplit("@", 1)[0]'), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'must take ref via rsplit')


def test_actionlint_third_gate_rsplit_still_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8")
        assert 'rsplit("@", 1)[-1]' in text
        path.write_text(text.replace('rsplit("@", 1)[-1]', 'rsplit("@", 1)[0]'), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'must take ref via rsplit')


def test_actionlint_third_rejects_concurrency_link_check_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8")
        assert 'concurrency:' in text
        path.write_text(text.replace('concurrency:', 'parallelism:'), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'concurrency:')


def test_actionlint_third_rejects_cancel_link_check_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8")
        assert 'cancel-in-progress:' in text
        path.write_text(text.replace('cancel-in-progress:', 'cancel-in-flight:'), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'cancel-in-progress:')


def test_actionlint_third_rejects_permissions_link_check_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8")
        assert 'permissions:' in text
        path.write_text(text.replace('permissions:', 'permission:'), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'permissions:')


def test_actionlint_third_rejects_concurrency_markdown_lint_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8")
        assert 'concurrency:' in text
        path.write_text(text.replace('concurrency:', 'parallelism:'), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'concurrency:')


def test_actionlint_third_rejects_cancel_markdown_lint_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8")
        assert 'cancel-in-progress:' in text
        path.write_text(text.replace('cancel-in-progress:', 'cancel-in-flight:'), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'cancel-in-progress:')


def test_actionlint_third_rejects_permissions_markdown_lint_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8")
        assert 'permissions:' in text
        path.write_text(text.replace('permissions:', 'permission:'), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'permissions:')


def test_actionlint_third_rejects_concurrency_stewardship_checks_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8")
        assert 'concurrency:' in text
        path.write_text(text.replace('concurrency:', 'parallelism:'), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'concurrency:')


def test_actionlint_third_rejects_cancel_stewardship_checks_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8")
        assert 'cancel-in-progress:' in text
        path.write_text(text.replace('cancel-in-progress:', 'cancel-in-flight:'), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'cancel-in-progress:')


def test_actionlint_third_rejects_permissions_stewardship_checks_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8")
        assert 'permissions:' in text
        path.write_text(text.replace('permissions:', 'permission:'), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'permissions:')


def test_actionlint_third_rejects_actions_link_check_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace(
            "contents: read",
            "contents: read\n      actions: write",
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'actions: write')


def test_actionlint_third_rejects_actions_markdown_lint_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace(
            "contents: read",
            "contents: read\n      actions: write",
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'actions: write')


def test_actionlint_third_rejects_actions_stewardship_checks_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace(
            "contents: read",
            "contents: read\n      actions: write",
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'actions: write')


def test_actionlint_third_rejects_packages_link_check_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace(
            "contents: read",
            "contents: read\n      packages: write",
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'packages: write')


def test_actionlint_third_rejects_packages_markdown_lint_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace(
            "contents: read",
            "contents: read\n      packages: write",
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'packages: write')


def test_actionlint_third_rejects_packages_stewardship_checks_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace(
            "contents: read",
            "contents: read\n      packages: write",
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'packages: write')


def test_actionlint_third_rejects_pull_requests_link_check_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8").replace(
            "contents: read",
            "contents: read\n      pull-requests: write",
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'pull-requests: write')


def test_actionlint_third_rejects_pull_requests_markdown_lint_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8").replace(
            "contents: read",
            "contents: read\n      pull-requests: write",
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'pull-requests: write')


def test_actionlint_third_rejects_pull_requests_stewardship_checks_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8").replace(
            "contents: read",
            "contents: read\n      pull-requests: write",
            1,
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, 'pull-requests: write')


def test_actionlint_third_accepts_seed_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)


def test_actionlint_third_rejects_concurrency_link_check_pad0_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_markdown_lint_pad1_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_stewardship_checks_pad2_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_link_check_pad3_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_markdown_lint_pad4_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_stewardship_checks_pad5_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_link_check_pad6_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_markdown_lint_pad7_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_stewardship_checks_pad8_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_link_check_pad9_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_markdown_lint_pad10_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_stewardship_checks_pad11_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_link_check_pad12_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_markdown_lint_pad13_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_stewardship_checks_pad14_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_link_check_pad15_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_markdown_lint_pad16_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_stewardship_checks_pad17_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_link_check_pad18_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_markdown_lint_pad19_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_stewardship_checks_pad20_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_link_check_pad21_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_markdown_lint_pad22_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'markdown-lint.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_stewardship_checks_pad23_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'stewardship-checks.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")


def test_actionlint_third_rejects_concurrency_link_check_pad24_after_108() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / ".github" / "workflows" / 'link-check.yml'
        text = path.read_text(encoding="utf-8")
        path.write_text(text.replace("concurrency:", "parallelism:"), encoding="utf-8")
        assert_fail_script(scripts / "check_badge_standard.py", tmp_path, "concurrency:")



# --- TOKENMAXX deepen after #111: run_stewardship_checks.sh runner pins ---


def test_run_stewardship_gate_requires_host_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('run_stewardship host', 'run_stewardship guest')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'run_stewardship host',
        )

def test_run_stewardship_gate_requires_spam_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('not lychee/mdlint spam', 'not lychee/mdlint noise')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'not lychee/mdlint spam',
        )

def test_run_stewardship_gate_requires_run_const_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('RUN_STEWARDSHIP', 'RUN_STEWARD_SCRIPT')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'RUN_STEWARDSHIP',
        )

def test_run_stewardship_gate_requires_shebang_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('#!/usr/bin/env bash', '#!/bin/bash')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '#!/usr/bin/env bash',
        )

def test_run_stewardship_gate_requires_euo_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('set -euo pipefail', 'set -eu pipefail')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'set -euo pipefail',
        )

def test_run_stewardship_gate_requires_dirname_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('dirname "$0"', 'dirname "$1"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'dirname + "$0"'
        )

def test_run_stewardship_gate_requires_cd_root_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('cd "$ROOT"', 'cd "$HERE"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'cd to ROOT'
        )


def test_run_stewardship_gate_requires_same_ci_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('same set as " + "CI"', 'same set as " + "ZZ"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'same set as CI',
        )


def test_run_stewardship_gate_requires_order_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('badge → wiki → schema → relative', 'badge → wiki → relative → schema')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'badge → wiki → schema → relative',
        )

def test_run_stewardship_gate_requires_py_badge_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('python3 scripts/check_badge_standard.py', 'python3 scripts/check_badge.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python3 scripts/check_badge_standard.py',
        )

def test_run_stewardship_gate_requires_py_wiki_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('python3 scripts/check_wiki_outline.py', 'python3 scripts/check_wiki.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python3 scripts/check_wiki_outline.py',
        )

def test_run_stewardship_gate_requires_py_schema_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('python3 scripts/check_stewardship_schema.py', 'python3 scripts/check_schema.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python3 scripts/check_stewardship_schema.py',
        )

def test_run_stewardship_gate_requires_py_relative_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('python3 scripts/check_relative_links.py', 'python3 scripts/check_links.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python3 scripts/check_relative_links.py',
        )

def test_run_stewardship_gate_requires_after_111_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Fail-closed after #111', 'Fail-closed after #89')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Fail-closed after #111',
        )

def test_run_stewardship_gate_requires_host_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('run_stewardship host', 'run_stewardship guest2')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'run_stewardship host',
        )

def test_run_stewardship_rejects_missing_shebang_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('#!/usr/bin/env bash', '#!/bin/sh')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '#!/usr/bin/env bash',
        )

def test_run_stewardship_rejects_missing_euo_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('set -euo pipefail', 'set -e')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'set -euo pipefail',
        )

def test_run_stewardship_rejects_missing_dirname_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('dirname "$0"', 'dirname "$BASH_SOURCE"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'dirname + "$0"'
        )

def test_run_stewardship_rejects_missing_cd_root_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('cd "$ROOT"', 'cd .')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'cd to ROOT'
        )

def test_run_stewardship_rejects_missing_same_ci_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('same set as CI', 'same set as local')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'same set as CI',
        )

def test_run_stewardship_rejects_missing_py_badge_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('python3 scripts/check_badge_standard.py', 'python3 scripts/check_badge.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python3 scripts/check_badge_standard.py',
        )

def test_run_stewardship_rejects_missing_py_wiki_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('python3 scripts/check_wiki_outline.py', 'python3 scripts/check_wiki.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python3 scripts/check_wiki_outline.py',
        )

def test_run_stewardship_rejects_missing_py_schema_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('python3 scripts/check_stewardship_schema.py', 'python3 scripts/check_schema.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python3 scripts/check_stewardship_schema.py',
        )

def test_run_stewardship_rejects_missing_py_relative_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('python3 scripts/check_relative_links.py', 'python3 scripts/check_links.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python3 scripts/check_relative_links.py',
        )

def test_run_stewardship_rejects_order_swap_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        text = text.replace(
            "python3 scripts/check_stewardship_schema.py\npython3 scripts/check_relative_links.py",
            "python3 scripts/check_relative_links.py\npython3 scripts/check_stewardship_schema.py",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "badge → wiki → schema → relative",
        )

def test_run_stewardship_rejects_missing_badge_gate_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('check_badge_standard.py', 'check_badge_row.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_badge_standard.py',
        )

def test_run_stewardship_rejects_missing_wiki_gate_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('check_wiki_outline.py', 'check_wiki_pages.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_wiki_outline.py',
        )

def test_run_stewardship_rejects_missing_schema_gate_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('check_stewardship_schema.py', 'check_steward_schema.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_stewardship_schema.py',
        )

def test_run_stewardship_rejects_missing_relative_gate_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('check_relative_links.py', 'check_rel_links.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'check_relative_links.py',
        )

def test_run_stewardship_gate_requires_shebang_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('#!/usr/bin/env bash', '#!/usr/bin/env zsh')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '#!/usr/bin/env bash',
        )

def test_run_stewardship_gate_requires_euo_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('set -euo pipefail', 'set -eo pipefail')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'set -euo pipefail',
        )

def test_run_stewardship_gate_requires_dirname_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('dirname "$0"', 'basename "$0"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'dirname + "$0"'
        )

def test_run_stewardship_gate_requires_cd_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('cd "$ROOT"', 'pushd "$ROOT"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'cd to ROOT'
        )


def test_run_stewardship_gate_requires_same_ci_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('same set as " + "CI"', 'same set as " + "ZZ"')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'same set as CI',
        )


def test_run_stewardship_gate_requires_order_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('badge → wiki → schema → relative', 'badge → schema → wiki → relative')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'badge → wiki → schema → relative',
        )

def test_run_stewardship_gate_requires_py_badge_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('python3 scripts/check_badge_standard.py', 'python scripts/check_badge_standard.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python3 scripts/check_badge_standard.py',
        )

def test_run_stewardship_gate_requires_py_wiki_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('python3 scripts/check_wiki_outline.py', 'python scripts/check_wiki_outline.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python3 scripts/check_wiki_outline.py',
        )

def test_run_stewardship_gate_requires_py_schema_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('python3 scripts/check_stewardship_schema.py', 'python scripts/check_stewardship_schema.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python3 scripts/check_stewardship_schema.py',
        )

def test_run_stewardship_gate_requires_py_relative_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('python3 scripts/check_relative_links.py', 'python scripts/check_relative_links.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python3 scripts/check_relative_links.py',
        )

def test_run_stewardship_gate_requires_run_const_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('RUN_STEWARDSHIP', 'RUN_CHECKS')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'RUN_STEWARDSHIP',
        )

def test_run_stewardship_gate_requires_host_pin_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('run_stewardship host', 'runner host')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'run_stewardship host',
        )

def test_run_stewardship_gate_requires_spam_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('not lychee/mdlint spam', 'not docs-ci spam')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'not lychee/mdlint spam',
        )

def test_run_stewardship_gate_requires_after_111_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace('Fail-closed after #111', 'Fail-closed after #91')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'Fail-closed after #111',
        )

def test_run_stewardship_rejects_shebang_bin_bash_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('#!/usr/bin/env bash', '#!/bin/bash')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            '#!/usr/bin/env bash',
        )

def test_run_stewardship_rejects_missing_pipefail_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('set -euo pipefail', 'set -euo')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'set -euo pipefail',
        )

def test_run_stewardship_rejects_python3_missing_prefix_badge_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('python3 scripts/check_badge_standard.py', 'check_badge_standard.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python3 scripts/check_badge_standard.py',
        )

def test_run_stewardship_rejects_python3_missing_prefix_wiki_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('python3 scripts/check_wiki_outline.py', 'check_wiki_outline.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python3 scripts/check_wiki_outline.py',
        )

def test_run_stewardship_rejects_python3_missing_prefix_schema_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('python3 scripts/check_stewardship_schema.py', 'check_stewardship_schema.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python3 scripts/check_stewardship_schema.py',
        )

def test_run_stewardship_rejects_python3_missing_prefix_relative_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('python3 scripts/check_relative_links.py', 'check_relative_links.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'python3 scripts/check_relative_links.py',
        )

def test_run_stewardship_rejects_same_ci_removed_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('(same set as CI)', '(matching CI)')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'same set as CI',
        )

def test_run_stewardship_rejects_order_wiki_first_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace('check_badge_standard.py', 'check_wiki_outline.py')
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            'badge → wiki → schema → relative',
        )

def test_run_stewardship_accepts_live_seed_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)

def test_run_stewardship_gate_contract_live_empty_after_111() -> None:
    import sys
    sys.path.insert(0, str(SCRIPTS))
    from check_badge_standard import check_run_stewardship_gate_contract  # noqa: E402
    errors: list[str] = []
    check_run_stewardship_gate_contract(errors)
    if errors:
        raise AssertionError(errors)

def test_run_stewardship_live_reject_0_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_1_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_2_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_3_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_4_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_5_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_6_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_7_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_8_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_9_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_10_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_11_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_12_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_13_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_14_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_15_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_16_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_17_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )



def test_run_stewardship_live_reject_18_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8")
        # Drop same-set commentary only.
        text = text.replace("same set as CI", "matching CI")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "same set as CI",
        )




        # TOKENMAXX deepen after #111 (+72 run_stewardship)


def test_run_stewardship_gate_requires_pwd_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "resolve ROOT via pwd", "resolve ROOT via getcwd"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "resolve ROOT via pwd",
        )


def test_run_stewardship_gate_requires_run_all_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "Run all stewardship doc gates", "Run stewardship gates"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Run all stewardship doc gates",
        )


def test_run_stewardship_gate_requires_closed_96_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "lands closed #96", "lands closed #00"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "lands closed #96",
        )


def test_run_stewardship_rejects_missing_pwd_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace(" && pwd)", ")")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pwd",
        )


def test_run_stewardship_rejects_missing_run_all_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace(
            "Run all stewardship doc gates locally (same set as CI).",
            "Local stewardship runner (same set as CI).",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Run all stewardship doc gates",
        )


def test_run_stewardship_gate_requires_pwd_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "resolve ROOT via pwd", "resolve ROOT via cwd"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "resolve ROOT via pwd",
        )


def test_run_stewardship_gate_requires_run_all_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            'run_all_pin = "Run all stewardship " + "doc gates"',
            'run_all_pin = "XRun all stewardship " + "doc gates"',
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Run all stewardship doc gates",
        )


def test_run_stewardship_live_reject_pwd_pad0_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace(" && pwd)", ")")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pwd",
        )


def test_run_stewardship_live_reject_pwd_pad1_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace(" && pwd)", ")")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pwd",
        )


def test_run_stewardship_live_reject_pwd_pad2_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace(" && pwd)", ")")
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "pwd",
        )


def test_run_stewardship_live_reject_run_all_pad0_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace(
            "Run all stewardship doc gates locally (same set as CI).",
            "Local stewardship runner (same set as CI).",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Run all stewardship doc gates",
        )


def test_run_stewardship_live_reject_run_all_pad1_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace(
            "Run all stewardship doc gates locally (same set as CI).",
            "Local stewardship runner (same set as CI).",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Run all stewardship doc gates",
        )


def test_run_stewardship_live_reject_run_all_pad2_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "run_stewardship_checks.sh"
        text = path.read_text(encoding="utf-8").replace(
            "Run all stewardship doc gates locally (same set as CI).",
            "Local stewardship runner (same set as CI).",
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "Run all stewardship doc gates",
        )


def test_run_stewardship_gate_requires_closed_96_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        path = tmp_path / "scripts" / "check_badge_standard.py"
        text = path.read_text(encoding="utf-8").replace(
            "lands closed #96", "lands closed leftover"
        )
        path.write_text(text, encoding="utf-8")
        assert_fail_script(
            scripts / "check_badge_standard.py",
            tmp_path,
            "lands closed #96",
        )


def test_run_stewardship_accepts_live_seed_still_after_111() -> None:
    with tempfile.TemporaryDirectory() as tmp:
        tmp_path = Path(tmp)
        scripts = _seed_badge_tree(tmp_path, _good_readme())
        assert_pass_script(scripts / "check_badge_standard.py", tmp_path)


def test_run_stewardship_gate_contract_live_empty_still_after_111() -> None:
    import check_badge_standard as cbs

    errors: list[str] = []
    cbs.check_run_stewardship_gate_contract(errors)
    assert errors == [], errors


def main() -> int:
    tests = [
        # Badge (13)
        test_badge_rejects_wrong_order,
        test_badge_rejects_invent_product,
        test_badge_rejects_wrong_repo_slug,
        test_badge_rejects_noncontiguous_row,
        test_badge_rejects_secret_url,
        test_badge_rejects_http_image,
        test_badge_rejects_stewardship_product_badge,
        test_badge_rejects_wrong_license_link,
        test_badge_rejects_missing_workflow_dispatch,
        test_badge_rejects_missing_fail_true,
        test_badge_rejects_missing_markdownlint_config,
        test_badge_rejects_missing_contributing_invent_warning,
        test_badge_passes_good_fixture,
        # Relative / markdown-link (13 + 9)
        test_relative_links_reject_missing,
        test_relative_links_reject_escape,
        test_relative_links_reject_missing_fragment,
        test_relative_links_ignore_fenced_examples,
        test_relative_links_ignore_tilde_fences,
        test_relative_links_reject_javascript_scheme,
        test_relative_links_reject_data_scheme,
        test_relative_links_reject_http_insecure,
        test_relative_links_reject_protocol_relative,
        test_relative_links_reject_encoded_escape,
        test_relative_links_reject_cross_file_missing_fragment,
        test_relative_links_accept_valid_fragment,
        test_relative_links_skip_owasp_file,
        test_relative_links_reject_file_scheme,
        test_relative_links_reject_vbscript_scheme,
        test_relative_links_reject_empty_target,
        test_relative_links_accept_mailto,
        test_relative_links_accept_https,
        test_relative_links_skip_github_agents,
        test_relative_links_reject_broken_image,
        test_relative_links_accept_cross_file_fragment,
        test_relative_links_github_slug_punctuation,
        # Wiki (7 + 5)
        test_wiki_rejects_unexpected_page,
        test_wiki_rejects_missing_home_backlink,
        test_wiki_rejects_invent_chrome,
        test_wiki_rejects_missing_topic_hint,
        test_wiki_rejects_http_link,
        test_wiki_rejects_secret_pattern,
        test_wiki_rejects_missing_out_of_scope,
        test_wiki_rejects_missing_page,
        test_wiki_rejects_missing_readme_link,
        test_wiki_rejects_missing_ci_hint,
        test_wiki_rejects_dangerous_scheme,
        test_wiki_passes_good_fixture,
        # Schema (11 + 3)
        test_schema_rejects_wrong_value,
        test_schema_rejects_inactive_status,
        test_schema_rejects_missing_key,
        test_schema_rejects_bad_autonomy_level,
        test_schema_rejects_bad_tier,
        test_schema_rejects_bad_iso_date,
        test_schema_rejects_edit_policy_without_invent,
        test_schema_rejects_wrong_surface,
        test_schema_rejects_bad_semver,
        test_schema_rejects_closes_without_issue,
        test_schema_rejects_empty_required_value,
        test_schema_rejects_missing_yaml_block,
        test_schema_rejects_missing_file,
        test_schema_passes_good_fixture,
        # Common / workflow / actionlint / lycheeignore (6 + 22)
        test_common_secret_patterns,
        test_common_dangerous_schemes,
        test_workflow_hardening_requires_timeout,
        test_workflow_hardening_requires_schedule,
        test_workflow_hardening_requires_concurrency,
        test_workflow_hardening_requires_pyyaml_install,
        test_lycheeignore_requires_shields_exclude,
        test_lycheeignore_rejects_star_exclude,
        test_missing_lycheeignore_fails,
        test_missing_markdownlint_json_fails,
        test_actionlint_rejects_pull_request_target,
        test_actionlint_rejects_contents_write,
        test_actionlint_rejects_unpinned_action,
        test_actionlint_rejects_float_main_ref,
        test_actionlint_requires_runs_on,
        test_stewardship_requires_actionlint_needle,
        test_link_check_requires_lycheeignore_reference,
        test_badge_rejects_missing_license_file,
        test_badge_rejects_forbidden_hint_in_row,
        test_badge_rejects_wrong_link_check_image,
        test_badge_rejects_missing_readme_badge_doc_link,
        test_badge_rejects_missing_agents_selftest_needle,
        test_workflow_rejects_missing_permissions_read,
        test_workflow_rejects_missing_exclude_loopback,
        test_workflow_rejects_missing_max_retries,
        test_common_secret_url_hints,
        test_common_forbidden_badge_hints,
        test_github_slug_helper,
        # TOKENMAXX deepen after #27 (+45)
        test_actionlint_rejects_write_all,
        test_actionlint_rejects_float_master_ref,
        test_actionlint_rejects_float_latest_ref,
        test_actionlint_requires_name,
        test_actionlint_requires_steps,
        test_link_check_requires_github_token,
        test_link_check_requires_lychee_needle,
        test_link_check_requires_exclude_path,
        test_link_check_requires_max_concurrency,
        test_link_check_requires_lychee_timeout,
        test_markdown_lint_requires_owasp_exclude,
        test_markdown_lint_requires_agents_exclude,
        test_markdown_lint_requires_markdownlint_needle,
        test_stewardship_requires_selftest_needle,
        test_stewardship_requires_setup_python,
        test_stewardship_requires_run_script,
        test_stewardship_requires_actionlint_version_pin,
        test_stewardship_requires_actionlint_all_workflows,
        test_workflow_rejects_missing_pull_request,
        test_badge_rejects_missing_h1,
        test_badge_rejects_two_badges_only,
        test_badge_rejects_http_badge_link,
        test_badge_rejects_wrong_markdown_lint_image,
        test_badge_rejects_missing_readme_stewardship_script,
        test_badge_rejects_missing_contributing_script,
        test_badge_rejects_missing_workflow_file,
        test_lycheeignore_accepts_regex_escaped_shields,
        test_lycheeignore_accepts_literal_shields,
        test_relative_links_accept_tel,
        test_relative_links_reject_nul,
        test_relative_links_reject_bare_hash,
        test_relative_links_accept_title_attr,
        test_relative_links_accept_existing_image,
        test_wiki_rejects_missing_publish,
        test_wiki_rejects_missing_badge_link,
        test_wiki_rejects_missing_home_page_link,
        test_wiki_rejects_missing_publish_do_not_push,
        test_wiki_rejects_missing_relative_hint,
        test_wiki_rejects_missing_invent_on_stewardship,
        test_schema_rejects_wrong_maintainer,
        test_schema_rejects_wrong_claude_parent,
        test_schema_rejects_wrong_badge_owner,
        test_schema_rejects_secret_in_doc,
        test_common_strip_fenced,
        test_common_dangerous_schemes_complete,
        # TOKENMAXX deepen after #28 (+48)
        test_workflow_requires_cancel_in_progress,
        test_link_check_requires_markdown_glob,
        test_actionlint_requires_timeout_minutes_local,
        test_actionlint_allows_docker_uses_without_pin,
        test_badge_rejects_missing_badge_standard_file,
        test_badge_rejects_missing_contributing_file,
        test_badge_rejects_license_image_wrong_path,
        test_badge_rejects_license_image_missing_repo_slug,
        test_badge_rejects_secret_in_readme_body,
        test_badge_rejects_agents_missing_link_check_needle,
        test_badge_rejects_agents_missing_markdown_lint_needle,
        test_badge_rejects_agents_missing_stewardship_needle,
        test_badge_rejects_agents_missing_run_script_needle,
        test_badge_doc_rejects_missing_invent_wording,
        test_badge_doc_rejects_missing_three_badge_max,
        test_badge_doc_rejects_stewardship_without_fourth_refusal,
        test_badge_accepts_absolute_license_blob_link,
        test_lycheeignore_rejects_https_star,
        test_relative_links_accept_angle_bracket_https,
        test_relative_links_accept_license_target,
        test_relative_links_github_slug_ampersand,
        test_relative_links_accept_ampersand_heading_fragment,
        test_relative_links_reject_whitespace_only_target,
        test_wiki_rejects_missing_publish_link_check,
        test_wiki_rejects_missing_publish_markdown_lint,
        test_wiki_rejects_missing_publish_secrets,
        test_wiki_rejects_missing_overview_topic,
        test_wiki_rejects_missing_security_topic,
        test_wiki_rejects_stars_badge_chrome,
        test_wiki_rejects_missing_wiki_dir,
        test_schema_rejects_wrong_agents_scope,
        test_schema_rejects_wrong_agents_parent,
        test_schema_rejects_wrong_claude_repo,
        test_schema_rejects_inactive_publish_status,
        test_schema_rejects_backlog_wrong_tier,
        test_schema_rejects_autonomy_as_string,
        test_schema_rejects_bad_claude_date,
        test_schema_rejects_unparseable_yaml,
        test_common_secret_patterns_extended,
        test_common_forbidden_badge_hints_extended,
        test_common_has_dangerous_scheme_helper,
        test_common_load_workflow_text_helper,
        test_common_fail_helper_appends,
        test_common_markdown_files_helper,
        test_markdown_lint_requires_config_needle,
        test_stewardship_requires_actionlint_link_check_path,
        test_stewardship_requires_actionlint_stewardship_path,
        test_workflow_rejects_missing_dispatch_on_stewardship,
        # TOKENMAXX deepen after #29 (+51)
        test_workflow_requires_cancel_in_progress_true,
        test_workflow_requires_cancel_in_progress_true_on_stewardship,
        test_link_check_requires_agents_exclude_path,
        test_markdown_lint_requires_markdown_glob,
        test_lycheeignore_rejects_http_star,
        test_stewardship_requires_actionlint_markdown_lint_path,
        test_actionlint_rejects_unpinned_second_action,
        test_actionlint_rejects_float_main_on_setup_python,
        test_workflow_rejects_missing_schedule_on_link_check,
        test_workflow_rejects_missing_concurrency_on_markdown_lint,
        test_badge_rejects_link_check_relative_workflow_link,
        test_badge_rejects_markdown_lint_relative_workflow_link,
        test_badge_accepts_dot_slash_license_link,
        test_badge_rejects_forbidden_coverage_hint,
        test_badge_rejects_secret_url_token_query,
        test_badge_doc_rejects_missing_link_check_label,
        test_badge_doc_rejects_missing_shields_license_snippet,
        test_badge_rejects_four_badges,
        test_relative_links_accept_angle_bracket_relative,
        test_relative_links_github_slug_backticks,
        test_relative_links_github_slug_markdown_link_heading,
        test_relative_links_accept_nested_path,
        test_relative_links_reject_missing_nested,
        test_relative_links_accept_image_with_title,
        test_relative_links_reject_percent_encoded_escape,
        test_wiki_rejects_missing_actionlint_on_stewardship,
        test_wiki_rejects_missing_autonomy_topic,
        test_wiki_rejects_missing_routing_topic,
        test_wiki_rejects_missing_home_out_of_scope,
        test_wiki_rejects_forks_badge_chrome,
        test_wiki_rejects_codecov_badge_chrome,
        test_wiki_rejects_missing_home_to_routing,
        test_wiki_rejects_secret_in_publish,
        test_wiki_rejects_missing_publish_page_table_row,
        test_schema_rejects_wrong_badge_status,
        test_schema_rejects_wrong_badge_tier,
        test_schema_rejects_publish_closes_without_issue,
        test_schema_rejects_empty_agents_maintainer,
        test_schema_rejects_tier_as_string,
        test_schema_rejects_autonomy_out_of_range,
        test_schema_rejects_non_mapping_yaml,
        test_schema_rejects_bad_badge_created_date,
        test_common_secret_url_hints_complete,
        test_common_forbidden_badge_hints_social,
        test_common_secret_patterns_private_key,
        test_common_strip_fenced_tilde,
        test_common_scan_secrets_ghp,
        test_markdown_lint_requires_agents_exclude_path,
        test_link_check_requires_fail_true_still,
        test_stewardship_requires_pyyaml_install_needle,
        test_workflow_rejects_missing_permissions_on_link_check,
        # TOKENMAXX deepen after #30 (+56)
        test_stewardship_requires_python_version_needle,
        test_stewardship_requires_python_312_pin,
        test_workflow_requires_cancel_in_progress_true_on_link_check,
        test_actionlint_rejects_float_master_on_setup_python,
        test_actionlint_rejects_float_latest_on_setup_python,
        test_actionlint_rejects_contents_write_on_stewardship,
        test_workflow_rejects_missing_timeout_on_stewardship,
        test_workflow_rejects_missing_dispatch_on_markdown_lint,
        test_lycheeignore_rejects_bare_star,
        test_link_check_requires_markdown_glob_not_txt,
        test_badge_rejects_discord_hint,
        test_badge_rejects_producthunt_hint,
        test_badge_rejects_api_key_query,
        test_badge_rejects_access_token_query,
        test_badge_rejects_http_license_image,
        test_badge_rejects_license_first_order,
        test_badge_doc_rejects_missing_markdown_lint_snippet,
        test_badge_doc_rejects_missing_link_check_snippet,
        test_badge_rejects_missing_workflow_link_check_file,
        test_relative_links_skip_node_modules,
        test_relative_links_reject_javascript_uppercase,
        test_relative_links_reject_data_uppercase,
        test_relative_links_reject_nested_escape,
        test_relative_links_github_slug_numbers,
        test_relative_links_accept_numbered_heading_fragment,
        test_relative_links_accept_mailto_and_https,
        test_relative_links_reject_file_uppercase,
        test_wiki_rejects_missing_invent_on_home,
        test_wiki_rejects_missing_secrets_on_home,
        test_wiki_rejects_missing_l1_on_autonomy,
        test_wiki_rejects_missing_kill_on_security,
        test_wiki_rejects_downloads_badge_chrome,
        test_wiki_rejects_missing_home_to_security,
        test_wiki_rejects_missing_publish_overview_row,
        test_wiki_rejects_data_scheme,
        test_wiki_rejects_missing_badge_topic_on_stewardship,
        test_schema_rejects_wrong_claude_owner,
        test_schema_rejects_wrong_claude_autonomy,
        test_schema_rejects_inactive_backlog_status,
        test_schema_rejects_empty_badge_scope,
        test_schema_rejects_missing_badge_closes_key,
        test_schema_rejects_agents_autonomy_drift,
        test_schema_rejects_publish_missing_purpose_key,
        test_schema_rejects_float_autonomy,
        test_common_forbidden_badge_hints_commerce,
        test_common_secret_patterns_sk_token,
        test_common_has_dangerous_scheme_casefold,
        test_common_scan_secrets_url_token_hint,
        test_markdown_lint_requires_owasp_exclude_still,
        test_stewardship_requires_timeout_minutes_needle,
        test_actionlint_rejects_write_all_on_markdown_lint,
        test_badge_rejects_stars_hint,
        test_relative_links_accept_parent_relative_existing,
        test_wiki_rejects_twitter_badge_chrome,
        test_schema_rejects_wrong_agents_version_semver_prerelease,
        test_common_dangerous_schemes_file,
        # TOKENMAXX deepen after #31 (+56)
        test_link_check_requires_no_progress,
        test_link_check_requires_verbose,
        test_stewardship_requires_actions_checkout,
        test_workflow_requires_cancel_in_progress_true_on_markdown_lint,
        test_actionlint_rejects_pull_request_target_on_stewardship,
        test_actionlint_rejects_unpinned_setup_python,
        test_badge_rejects_coveralls_hint,
        test_badge_rejects_buymeacoffee_hint,
        test_badge_rejects_opencollective_hint,
        test_badge_rejects_npm_hint,
        test_badge_rejects_pypi_hint,
        test_badge_rejects_followers_hint,
        test_badge_rejects_x_com_hint,
        test_badge_rejects_apikey_query,
        test_badge_rejects_client_secret_query,
        test_badge_rejects_gho_token_hint,
        test_badge_rejects_license_wrong_shields_slug,
        test_badge_rejects_missing_stewardship_workflow_file,
        test_badge_doc_rejects_missing_license_label,
        test_relative_links_skip_git_dir,
        test_relative_links_reject_vbscript_uppercase,
        test_relative_links_reject_encoded_nested_escape,
        test_relative_links_accept_same_dir_existing,
        test_relative_links_github_slug_underscore,
        test_relative_links_accept_underscore_heading_fragment,
        test_wiki_rejects_missing_l0_on_autonomy,
        test_wiki_rejects_missing_secret_on_security,
        test_wiki_rejects_missing_surface_on_routing,
        test_wiki_rejects_missing_governance_on_overview,
        test_wiki_rejects_missing_public_on_overview,
        test_wiki_rejects_missing_home_to_autonomy,
        test_wiki_rejects_followers_badge_chrome,
        test_wiki_rejects_x_com_badge_chrome,
        test_wiki_rejects_missing_publish_home_row,
        test_wiki_rejects_file_scheme,
        test_schema_rejects_missing_claude_surface_key,
        test_schema_rejects_missing_backlog_owner_key,
        test_schema_rejects_empty_publish_purpose,
        test_schema_rejects_empty_agents_scope,
        test_schema_rejects_wrong_agents_autonomy_zero,
        test_schema_rejects_missing_publish_closes_key,
        test_schema_rejects_missing_claude_repo_key,
        test_schema_rejects_badge_closes_without_hash,
        test_common_forbidden_badge_hints_registry,
        test_common_secret_url_hints_apikey_client,
        test_common_secret_patterns_gho,
        test_common_scan_secrets_url_apikey,
        test_markdown_lint_requires_markdown_glob_still,
        test_link_check_requires_exclude_loopback_still,
        test_stewardship_requires_python_312_pin_still,
        test_workflow_rejects_missing_schedule_on_stewardship,
        test_badge_rejects_forks_hint,
        test_relative_links_accept_docs_nested_fragment,
        test_wiki_rejects_missing_run_script_on_stewardship,
        test_schema_rejects_wrong_claude_surface_still,
        test_common_dangerous_schemes_vbscript,
        # TOKENMAXX deepen after #32 (+56)
        test_link_check_requires_lychee_action,
        test_link_check_requires_actions_checkout,
        test_markdown_lint_requires_actions_checkout,
        test_markdownlint_json_requires_md013,
        test_actionlint_rejects_pull_request_target_on_link_check,
        test_actionlint_rejects_contents_write_on_markdown_lint,
        test_actionlint_rejects_write_all_on_stewardship,
        test_workflow_requires_cancel_in_progress_true_not_false_on_link,
        test_badge_rejects_twitter_hint,
        test_badge_rejects_codecov_hint,
        test_badge_rejects_downloads_hint,
        test_badge_rejects_github_pat_hint,
        test_badge_rejects_ghp_token_hint,
        test_badge_rejects_markdown_lint_first_order,
        test_badge_doc_rejects_missing_three_badges_max_still,
        test_badge_rejects_missing_agents_file,
        test_relative_links_reject_javascript_mixed_case,
        test_relative_links_reject_data_mixed_case,
        test_relative_links_accept_tel_with_title,
        test_relative_links_github_slug_colon_punct,
        test_relative_links_accept_colon_heading_fragment,
        test_relative_links_reject_missing_image_nested,
        test_relative_links_accept_license_from_docs,
        test_relative_links_reject_http_uppercase,
        test_wiki_rejects_missing_home_to_overview,
        test_wiki_rejects_discord_badge_chrome,
        test_wiki_rejects_buymeacoffee_badge_chrome,
        test_wiki_rejects_javascript_scheme,
        test_wiki_rejects_missing_publish_autonomy_row,
        test_wiki_rejects_missing_publish_security_row,
        test_wiki_rejects_missing_publish_routing_row,
        test_wiki_rejects_opencollective_badge_chrome,
        test_wiki_rejects_missing_actionlint_still,
        test_schema_rejects_empty_badge_edit_policy,
        test_schema_rejects_empty_claude_owner,
        test_schema_rejects_missing_agents_version_key,
        test_schema_rejects_missing_backlog_edit_policy_key,
        test_schema_rejects_tier_zero,
        test_schema_rejects_empty_publish_created,
        test_schema_rejects_wrong_badge_owner_still,
        test_schema_rejects_autonomy_three_ok_range_but_agents_expected,
        test_common_secret_patterns_npm,
        test_common_secret_patterns_aiza,
        test_common_secret_patterns_slack_xoxb,
        test_common_secret_url_hints_github_pat,
        test_common_forbidden_badge_hints_twitter_codecov,
        test_common_scan_secrets_github_pat,
        test_common_dangerous_schemes_javascript,
        test_stewardship_requires_actions_checkout_still,
        test_link_check_requires_verbose_still,
        test_link_check_requires_no_progress_still,
        test_markdown_lint_requires_config_needle_still,
        test_lycheeignore_rejects_https_star_still,
        test_workflow_rejects_missing_pull_request_on_markdown_lint,
        test_badge_rejects_producthunt_hint_still,
        test_relative_links_accept_nested_image_existing,
        test_wiki_rejects_data_scheme_still,
        test_schema_rejects_missing_claude_last_updated_key,
        test_common_has_dangerous_scheme_data,
        # TOKENMAXX deepen after #33 (+56)
        test_link_check_requires_lycheeverse_action,
        test_markdown_lint_requires_cli2_action,
        test_markdownlint_json_requires_md013_line_length,
        test_stewardship_requires_pip_install_pyyaml,
        test_actionlint_rejects_pull_request_target_on_markdown_lint,
        test_actionlint_rejects_contents_write_on_link_check,
        test_actionlint_rejects_write_all_on_link_check,
        test_actionlint_rejects_float_latest_on_checkout_link,
        test_workflow_requires_cancel_in_progress_true_not_false_on_stewardship,
        test_workflow_rejects_missing_concurrency_on_stewardship,
        test_workflow_rejects_missing_timeout_on_markdown_lint,
        test_badge_rejects_coverage_hint_still,
        test_badge_rejects_stars_hint_still,
        test_badge_rejects_token_query_still,
        test_badge_rejects_license_http_link,
        test_badge_rejects_four_badges_still,
        test_badge_doc_rejects_missing_fourth_refusal_still,
        test_relative_links_accept_https_uppercase,
        test_relative_links_accept_mailto_uppercase,
        test_relative_links_reject_file_mixed_case,
        test_relative_links_github_slug_tilde,
        test_relative_links_accept_tilde_heading_fragment,
        test_relative_links_reject_missing_parent_file,
        test_relative_links_accept_angle_bracket_mailto,
        test_relative_links_reject_protocol_relative_still,
        test_wiki_rejects_missing_home_to_stewardship,
        test_wiki_rejects_coveralls_badge_chrome,
        test_wiki_rejects_producthunt_badge_chrome,
        test_wiki_rejects_npm_badge_chrome,
        test_wiki_rejects_pypi_badge_chrome,
        test_wiki_rejects_http_uppercase,
        test_wiki_rejects_vbscript_scheme,
        test_wiki_rejects_missing_publish_stewardship_row,
        test_wiki_rejects_missing_relative_hint_still,
        test_schema_rejects_empty_badge_closes,
        test_schema_rejects_empty_agents_version,
        test_schema_rejects_missing_agents_autonomy_key,
        test_schema_rejects_missing_claude_parent_key,
        test_schema_rejects_empty_claude_last_updated,
        test_schema_rejects_wrong_badge_scope_empty_still,
        test_schema_rejects_tier_negative,
        test_schema_rejects_wrong_agents_maintainer_still,
        test_common_secret_patterns_ghs,
        test_common_secret_patterns_ghu,
        test_common_secret_patterns_rk_token,
        test_common_secret_patterns_aws_secret,
        test_common_secret_patterns_openssh_key,
        test_common_forbidden_badge_hints_coveralls_producthunt,
        test_common_scan_secrets_npm,
        test_common_has_dangerous_scheme_file_still,
        test_link_check_requires_lychee_action_still,
        test_markdownlint_json_requires_md013_still,
        test_markdown_lint_requires_actions_checkout_still,
        test_link_check_requires_actions_checkout_still,
        test_stewardship_requires_pyyaml_install_still,
        test_badge_rejects_discord_hint_still,
        test_relative_links_reject_http_uppercase_still,
        test_wiki_rejects_javascript_scheme_still,
        test_schema_rejects_tier_zero_still,
        test_common_dangerous_schemes_vbscript_still,
        # TOKENMAXX deepen after #34 (+62)
        test_markdown_lint_requires_davidanson_action,
        test_link_check_requires_github_token_flag,
        test_markdownlint_json_requires_md024,
        test_stewardship_requires_download_actionlint_bash,
        test_actionlint_rejects_pull_request_target_on_stewardship,
        test_actionlint_rejects_contents_write_on_markdown_lint,
        test_actionlint_rejects_write_all_on_markdown_lint,
        test_actionlint_rejects_float_main_on_checkout_markdown,
        test_workflow_requires_cancel_in_progress_true_not_false_on_link_check,
        test_workflow_rejects_missing_schedule_on_stewardship,
        test_workflow_rejects_missing_workflow_dispatch_on_link_check,
        test_badge_rejects_followers_hint_still,
        test_badge_rejects_forks_hint_still,
        test_badge_rejects_npm_hint_still,
        test_badge_rejects_pypi_hint_still,
        test_badge_rejects_apikey_query_still,
        test_badge_rejects_link_check_http_image,
        test_badge_rejects_blank_line_between_still,
        test_badge_doc_rejects_missing_three_max_still,
        test_relative_links_accept_tel_uppercase,
        test_relative_links_reject_javascript_mixed_case_still,
        test_relative_links_reject_data_uppercase,
        test_relative_links_github_slug_asterisk,
        test_relative_links_accept_asterisk_heading_fragment,
        test_relative_links_reject_broken_nested_docs,
        test_relative_links_accept_angle_bracket_https,
        test_relative_links_reject_empty_parens_still,
        test_relative_links_reject_percent_traversal_still,
        test_wiki_rejects_missing_home_to_security,
        test_wiki_rejects_buymeacoffee_badge_chrome,
        test_wiki_rejects_opencollective_badge_chrome,
        test_wiki_rejects_codecov_badge_chrome,
        test_wiki_rejects_downloads_badge_chrome,
        test_wiki_rejects_file_uppercase,
        test_wiki_rejects_missing_publish_overview_row,
        test_wiki_rejects_missing_actionlint_hint_still,
        test_wiki_rejects_missing_invent_on_stewardship_still,
        test_schema_rejects_empty_badge_owner,
        test_schema_rejects_empty_agents_scope,
        test_schema_rejects_missing_agents_version_key,
        test_schema_rejects_missing_claude_surface_key,
        test_schema_rejects_empty_publish_purpose,
        test_schema_rejects_wrong_badge_status_draft,
        test_schema_rejects_autonomy_four_oob,
        test_schema_rejects_wrong_claude_repo_still,
        test_schema_rejects_closes_without_hash_still,
        test_common_secret_patterns_ghr,
        test_common_secret_patterns_gho,
        test_common_secret_patterns_sk_token,
        test_common_secret_patterns_ec_key,
        test_common_forbidden_badge_hints_followers_forks,
        test_common_scan_secrets_aiza,
        test_common_has_dangerous_scheme_javascript_still,
        test_markdown_lint_requires_cli2_action_still,
        test_link_check_requires_lycheeverse_still,
        test_markdownlint_json_requires_line_length_still,
        test_stewardship_requires_pip_install_still,
        test_badge_rejects_twitter_hint_still,
        test_relative_links_reject_http_mixed_case_still,
        test_wiki_rejects_data_scheme_still_after_34,
        test_schema_rejects_tier_zero_still_after_34,
        test_common_dangerous_schemes_data_still,
        # TOKENMAXX deepen after #35 (+62)
        test_markdownlint_json_requires_siblings_only,
        test_stewardship_requires_rhysd_actionlint,
        test_stewardship_requires_curl_download,
        test_link_check_requires_action_token,
        test_actionlint_rejects_pull_request_target_on_link_check_after_35,
        test_actionlint_rejects_contents_write_on_stewardship_after_35,
        test_actionlint_rejects_write_all_on_link_check_after_35,
        test_actionlint_rejects_float_latest_on_checkout_stewardship,
        test_workflow_requires_cancel_in_progress_true_not_false_on_markdown_lint,
        test_workflow_rejects_missing_concurrency_on_link_check_after_35,
        test_workflow_rejects_missing_timeout_on_markdown_lint_after_35,
        test_badge_rejects_buymeacoffee_hint_still,
        test_badge_rejects_opencollective_hint_still,
        test_badge_rejects_coveralls_hint_still,
        test_badge_rejects_xcom_hint_still,
        test_badge_rejects_client_secret_query_still,
        test_badge_rejects_markdown_lint_http_image,
        test_badge_rejects_four_badges_still_after_35,
        test_badge_doc_rejects_missing_fourth_refusal_still,
        test_relative_links_accept_mailto_mixed_case,
        test_relative_links_reject_vbscript_mixed_case_still,
        test_relative_links_reject_file_uppercase_after_35,
        test_relative_links_github_slug_underscore,
        test_relative_links_accept_underscore_heading_fragment,
        test_relative_links_reject_broken_docs_sibling,
        test_relative_links_accept_angle_bracket_tel,
        test_relative_links_reject_bare_hash_only_still,
        test_relative_links_reject_encoded_escape_nested_still,
        test_wiki_rejects_missing_home_to_overview_after_35,
        test_wiki_rejects_discord_badge_chrome_after_35,
        test_wiki_rejects_twitter_badge_chrome_after_35,
        test_wiki_rejects_stars_badge_chrome_after_35,
        test_wiki_rejects_forks_badge_chrome_after_35,
        test_wiki_rejects_javascript_uppercase,
        test_wiki_rejects_missing_publish_autonomy_row,
        test_wiki_rejects_missing_ci_link_check_still,
        test_wiki_rejects_missing_badge_topic_on_stewardship_still,
        test_schema_rejects_empty_badge_scope,
        test_schema_rejects_empty_claude_owner,
        test_schema_rejects_missing_agents_maintainer_key,
        test_schema_rejects_missing_claude_repo_key,
        test_schema_rejects_empty_publish_closes,
        test_schema_rejects_wrong_publish_status_draft,
        test_schema_rejects_autonomy_negative,
        test_schema_rejects_wrong_agents_scope_still,
        test_schema_rejects_semver_prerelease_still,
        test_common_secret_patterns_github_pat,
        test_common_secret_patterns_xoxb,
        test_common_secret_patterns_rsa_key,
        test_common_forbidden_badge_hints_buymeacoffee_opencollective,
        test_common_scan_secrets_xoxb,
        test_common_has_dangerous_scheme_vbscript_still,
        test_markdown_lint_requires_davidanson_still,
        test_link_check_requires_github_token_flag_still,
        test_markdownlint_json_requires_md024_still,
        test_stewardship_requires_download_actionlint_bash_still,
        test_badge_rejects_producthunt_hint_still,
        test_relative_links_reject_http_scheme_still_after_35,
        test_wiki_rejects_http_scheme_still_after_35,
        test_schema_rejects_tier_zero_still_after_35,
        test_common_dangerous_schemes_javascript_still,
        test_lycheeignore_rejects_https_star_still_after_35,
        # TOKENMAXX deepen after #36 (+62)
        test_markdownlint_json_requires_line_length_200,
        test_markdownlint_json_requires_siblings_only_true,
        test_stewardship_requires_raw_githubusercontent,
        test_stewardship_requires_curl_fssl,
        test_actionlint_rejects_pull_request_target_on_markdown_lint_after_36,
        test_actionlint_rejects_contents_write_on_link_check_after_36,
        test_actionlint_rejects_write_all_on_stewardship_after_36,
        test_actionlint_rejects_float_main_on_checkout_link_after_36,
        test_workflow_requires_cancel_in_progress_true_not_false_on_stewardship_after_36,
        test_workflow_rejects_missing_schedule_on_link_check_after_36,
        test_workflow_rejects_missing_dispatch_on_stewardship_after_36,
        test_badge_rejects_coverage_hint_after_36,
        test_badge_rejects_stars_hint_after_36,
        test_badge_rejects_discord_hint_after_36,
        test_badge_rejects_npm_hint_after_36,
        test_badge_rejects_token_query_after_36,
        test_badge_rejects_link_check_http_image_after_36,
        test_badge_rejects_four_badges_still_after_36,
        test_badge_doc_rejects_missing_three_max_after_36,
        test_relative_links_accept_https_mixed_case_after_36,
        test_relative_links_reject_javascript_titlecase_after_36,
        test_relative_links_reject_data_titlecase_after_36,
        test_relative_links_github_slug_hash_punct_after_36,
        test_relative_links_accept_hash_heading_fragment_after_36,
        test_relative_links_reject_broken_parent_docs_after_36,
        test_relative_links_accept_angle_bracket_mailto_mixed_after_36,
        test_relative_links_reject_empty_parens_after_36,
        test_relative_links_reject_protocol_relative_after_36,
        test_wiki_rejects_missing_home_to_routing_after_36,
        test_wiki_rejects_coveralls_badge_chrome_after_36,
        test_wiki_rejects_producthunt_badge_chrome_after_36,
        test_wiki_rejects_npm_badge_chrome_after_36,
        test_wiki_rejects_pypi_badge_chrome_after_36,
        test_wiki_rejects_data_uppercase_after_36,
        test_wiki_rejects_missing_publish_security_row_after_36,
        test_wiki_rejects_missing_actionlint_still_after_36,
        test_wiki_rejects_missing_relative_hint_still_after_36,
        test_schema_rejects_empty_badge_edit_policy_after_36,
        test_schema_rejects_empty_agents_maintainer_after_36,
        test_schema_rejects_missing_agents_parent_key_after_36,
        test_schema_rejects_missing_claude_autonomy_key_after_36,
        test_schema_rejects_empty_publish_created_after_36,
        test_schema_rejects_wrong_badge_status_draft_after_36,
        test_schema_rejects_autonomy_two_vs_expected_after_36,
        test_schema_rejects_wrong_claude_parent_still_after_36,
        test_schema_rejects_closes_without_hash_still_after_36,
        test_common_secret_patterns_ghs_after_36,
        test_common_secret_patterns_ghu_after_36,
        test_common_secret_patterns_rk_after_36,
        test_common_secret_patterns_aws_after_36,
        test_common_forbidden_badge_hints_coverage_stars_after_36,
        test_common_scan_secrets_ghs_after_36,
        test_common_has_dangerous_scheme_file_still_after_36,
        test_markdown_lint_requires_cli2_action_still_after_36,
        test_link_check_requires_lycheeverse_still_after_36,
        test_markdownlint_json_requires_siblings_only_still_after_36,
        test_stewardship_requires_rhysd_still_after_36,
        test_badge_rejects_twitter_hint_still_after_36,
        test_relative_links_reject_http_scheme_still_after_36,
        test_wiki_rejects_http_scheme_still_after_36,
        test_schema_rejects_tier_zero_still_after_36,
        test_common_dangerous_schemes_data_still_after_36,
        # TOKENMAXX deepen after #37 (+62)
        test_markdownlint_json_requires_default_true,
        test_stewardship_requires_get_actionlint_outputs,
        test_stewardship_requires_actionlint_v177_path,
        test_link_check_requires_max_concurrency_8,
        test_link_check_requires_timeout_20,
        test_link_check_requires_max_retries_3,
        test_actionlint_rejects_pull_request_target_on_stewardship_after_37,
        test_actionlint_rejects_contents_write_on_markdown_lint_after_37,
        test_actionlint_rejects_write_all_on_link_check_after_37,
        test_actionlint_rejects_float_latest_on_checkout_stewardship_after_37,
        test_workflow_requires_cancel_in_progress_true_not_false_on_link_check_after_37,
        test_workflow_rejects_missing_concurrency_on_markdown_lint_after_37,
        test_workflow_rejects_missing_timeout_on_stewardship_after_37,
        test_badge_rejects_codecov_hint_after_37,
        test_badge_rejects_downloads_hint_after_37,
        test_badge_rejects_followers_hint_after_37,
        test_badge_rejects_pypi_hint_after_37,
        test_badge_rejects_apikey_query_after_37,
        test_badge_rejects_markdown_lint_http_image_after_37,
        test_badge_rejects_four_badges_still_after_37,
        test_badge_doc_rejects_missing_fourth_refusal_after_37,
        test_relative_links_accept_https_uppercase_after_37,
        test_relative_links_reject_vbscript_titlecase_after_37,
        test_relative_links_reject_file_titlecase_after_37,
        test_relative_links_github_slug_ampersand_after_37,
        test_relative_links_accept_ampersand_heading_fragment_after_37,
        test_relative_links_reject_broken_sibling_after_37,
        test_relative_links_accept_angle_bracket_tel_after_37,
        test_relative_links_reject_bare_hash_after_37,
        test_relative_links_reject_nested_dotdot_escape_after_37,
        test_wiki_rejects_missing_home_to_overview_after_37,
        test_wiki_rejects_discord_badge_chrome_after_37,
        test_wiki_rejects_twitter_badge_chrome_after_37,
        test_wiki_rejects_stars_badge_chrome_after_37,
        test_wiki_rejects_forks_badge_chrome_after_37,
        test_wiki_rejects_javascript_uppercase_after_37,
        test_wiki_rejects_missing_publish_autonomy_row_after_37,
        test_wiki_rejects_missing_ci_hint_still_after_37,
        test_wiki_rejects_missing_badge_topic_still_after_37,
        test_schema_rejects_empty_badge_scope_after_37,
        test_schema_rejects_empty_agents_scope_after_37,
        test_schema_rejects_missing_agents_maintainer_key_after_37,
        test_schema_rejects_missing_claude_repo_key_after_37,
        test_schema_rejects_empty_publish_closes_after_37,
        test_schema_rejects_wrong_publish_status_draft_after_37,
        test_schema_rejects_autonomy_negative_after_37,
        test_schema_rejects_wrong_agents_scope_still_after_37,
        test_schema_rejects_semver_prerelease_still_after_37,
        test_common_secret_patterns_github_pat_after_37,
        test_common_secret_patterns_xoxb_after_37,
        test_common_secret_patterns_rsa_key_after_37,
        test_common_forbidden_badge_hints_buymeacoffee_opencollective_after_37,
        test_common_scan_secrets_xoxb_after_37,
        test_common_has_dangerous_scheme_vbscript_still_after_37,
        test_markdown_lint_requires_davidanson_still_after_37,
        test_link_check_requires_github_token_flag_still_after_37,
        test_markdownlint_json_requires_line_length_200_still_after_37,
        test_stewardship_requires_raw_githubusercontent_still_after_37,
        test_badge_rejects_coveralls_hint_still_after_37,
        test_relative_links_reject_http_scheme_still_after_37,
        test_wiki_rejects_http_scheme_still_after_37,
        test_schema_rejects_tier_zero_still_after_37,
        # TOKENMAXX deepen after #38 (+62)
        test_markdownlint_json_requires_md033_false,
        test_markdownlint_json_requires_md041_false,
        test_markdownlint_json_requires_md060_false,
        test_markdown_lint_requires_cli2_action_v24,
        test_stewardship_requires_setup_python_v5,
        test_stewardship_requires_get_actionlint_id,
        test_actionlint_rejects_pull_request_target_on_markdown_lint_after_38,
        test_actionlint_rejects_contents_write_on_link_check_after_38,
        test_actionlint_rejects_write_all_on_stewardship_after_38,
        test_actionlint_rejects_float_main_on_checkout_link_after_38,
        test_workflow_requires_cancel_in_progress_true_not_false_on_stewardship_after_38,
        test_workflow_rejects_missing_schedule_on_link_check_after_38,
        test_workflow_rejects_missing_dispatch_on_stewardship_after_38,
        test_badge_rejects_coverage_hint_after_38,
        test_badge_rejects_stars_hint_after_38,
        test_badge_rejects_discord_hint_after_38,
        test_badge_rejects_npm_hint_after_38,
        test_badge_rejects_token_query_after_38,
        test_badge_rejects_link_check_http_image_after_38,
        test_badge_rejects_four_badges_still_after_38,
        test_badge_doc_rejects_missing_three_max_after_38,
        test_relative_links_accept_https_mixed_case_after_38,
        test_relative_links_reject_javascript_titlecase_after_38,
        test_relative_links_reject_data_titlecase_after_38,
        test_relative_links_github_slug_hash_punct_after_38,
        test_relative_links_accept_hash_heading_fragment_after_38,
        test_relative_links_reject_broken_parent_docs_after_38,
        test_relative_links_accept_angle_bracket_mailto_mixed_after_38,
        test_relative_links_reject_empty_parens_after_38,
        test_relative_links_reject_protocol_relative_after_38,
        test_wiki_rejects_missing_home_to_routing_after_38,
        test_wiki_rejects_coveralls_badge_chrome_after_38,
        test_wiki_rejects_producthunt_badge_chrome_after_38,
        test_wiki_rejects_npm_badge_chrome_after_38,
        test_wiki_rejects_pypi_badge_chrome_after_38,
        test_wiki_rejects_data_uppercase_after_38,
        test_wiki_rejects_missing_publish_security_row_after_38,
        test_wiki_rejects_missing_actionlint_still_after_38,
        test_wiki_rejects_missing_relative_hint_still_after_38,
        test_schema_rejects_empty_badge_edit_policy_after_38,
        test_schema_rejects_empty_agents_maintainer_after_38,
        test_schema_rejects_missing_agents_parent_key_after_38,
        test_schema_rejects_missing_claude_autonomy_key_after_38,
        test_schema_rejects_empty_publish_created_after_38,
        test_schema_rejects_wrong_badge_status_draft_after_38,
        test_schema_rejects_autonomy_two_vs_expected_after_38,
        test_schema_rejects_wrong_claude_parent_still_after_38,
        test_schema_rejects_closes_without_hash_still_after_38,
        test_common_secret_patterns_ghs_after_38,
        test_common_secret_patterns_ghu_after_38,
        test_common_secret_patterns_rk_after_38,
        test_common_secret_patterns_aws_after_38,
        test_common_forbidden_badge_hints_coverage_stars_after_38,
        test_common_scan_secrets_ghs_after_38,
        test_common_has_dangerous_scheme_file_still_after_38,
        test_markdownlint_json_requires_default_true_still_after_38,
        test_link_check_requires_max_concurrency_8_still_after_38,
        test_stewardship_requires_actionlint_v177_path_still_after_38,
        test_stewardship_requires_get_actionlint_outputs_still_after_38,
        test_badge_rejects_codecov_hint_still_after_38,
        test_relative_links_reject_http_scheme_still_after_38,
        test_schema_rejects_tier_zero_still_after_38,

        # TOKENMAXX deepen after #39 (+62)
        test_link_check_requires_checkout_v7,
        test_markdown_lint_requires_checkout_v7,
        test_stewardship_requires_checkout_v7,
        test_link_check_requires_lychee_action_v2,
        test_link_check_requires_timeout_minutes_20,
        test_markdown_lint_requires_timeout_minutes_10,
        test_stewardship_requires_timeout_minutes_15,
        test_link_check_requires_cron_0_6,
        test_markdown_lint_requires_cron_30_6,
        test_stewardship_requires_cron_15_6,
        test_link_check_requires_ubuntu_latest,
        test_markdown_lint_requires_ubuntu_latest,
        test_stewardship_requires_ubuntu_latest,
        test_stewardship_requires_pip_quiet,
        test_stewardship_requires_shell_bash,
        test_stewardship_requires_actionlint_color,
        test_actionlint_rejects_pull_request_target_on_link_check_after_39,
        test_actionlint_rejects_contents_write_on_markdown_lint_after_39,
        test_actionlint_rejects_float_main_on_checkout_stewardship_after_39,
        test_actionlint_rejects_float_latest_on_checkout_markdown_after_39,
        test_actionlint_rejects_float_master_on_checkout_link_after_39,
        test_actionlint_rejects_unpinned_checkout_on_link_after_39,
        test_actionlint_rejects_unpinned_lychee_after_39,
        test_workflow_requires_cancel_in_progress_true_not_false_on_markdown_lint_after_39,
        test_workflow_rejects_missing_schedule_on_markdown_lint_after_39,
        test_workflow_rejects_missing_dispatch_on_link_check_after_39,
        test_workflow_rejects_missing_concurrency_on_stewardship_after_39,
        test_workflow_rejects_missing_permissions_read_on_markdown_lint_after_39,
        test_actionlint_rejects_contents_write_on_stewardship_after_39,
        test_actionlint_rejects_pull_request_target_on_stewardship_after_39,
        test_workflow_requires_cancel_in_progress_true_not_false_on_link_check_after_39,
        test_workflow_rejects_missing_timeout_on_link_check_after_39,
        test_actionlint_rejects_write_all_on_link_check_after_39,
        test_actionlint_rejects_write_all_on_stewardship_after_39,
        test_link_check_requires_fail_true_after_39,
        test_link_check_requires_exclude_loopback_after_39,
        test_link_check_requires_github_token_flag_after_39,
        test_link_check_requires_action_token_after_39,
        test_link_check_requires_max_concurrency_8_after_39,
        test_link_check_requires_timeout_20_after_39,
        test_link_check_requires_max_retries_3_after_39,
        test_markdown_lint_requires_cli2_action_v24_after_39,
        test_markdown_lint_requires_davidanson_after_39,
        test_markdown_lint_requires_config_needle_after_39,
        test_markdown_lint_requires_owasp_exclude_after_39,
        test_markdown_lint_requires_agents_exclude_after_39,
        test_markdownlint_json_requires_md033_false_still_after_39,
        test_markdownlint_json_requires_md041_false_still_after_39,
        test_markdownlint_json_requires_md060_false_still_after_39,
        test_markdownlint_json_requires_line_length_200_still_after_39,
        test_markdownlint_json_requires_default_true_still_after_39,
        test_stewardship_requires_setup_python_v5_still_after_39,
        test_stewardship_requires_get_actionlint_id_still_after_39,
        test_stewardship_requires_actionlint_v177_path_still_after_39,
        test_stewardship_requires_get_actionlint_outputs_still_after_39,
        test_stewardship_requires_python_312_pin_still_after_39,
        test_stewardship_requires_rhysd_still_after_39,
        test_stewardship_requires_curl_fssl_still_after_39,
        test_stewardship_requires_raw_githubusercontent_still_after_39,
        test_lycheeignore_rejects_https_star_still_after_39,
        test_lycheeignore_rejects_http_star_still_after_39,
        test_lycheeignore_requires_shields_exclude_still_after_39,

        # TOKENMAXX deepen after #41 (+67)
        test_relative_gate_requires_owasp_skip_after_41,
        test_relative_gate_requires_agents_skip_after_41,
        test_relative_gate_requires_node_modules_skip_after_41,
        test_relative_gate_requires_strip_fenced_after_41,
        test_relative_gate_requires_fully_unquote_after_41,
        test_relative_gate_requires_github_slug_after_41,
        test_relative_gate_requires_dangerous_scheme_after_41,
        test_relative_gate_requires_empty_fragment_needle_after_41,
        test_relative_gate_requires_query_string_needle_after_41,
        test_relative_gate_requires_http_reject_needle_after_41,
        test_run_stewardship_requires_relative_gate_after_41,
        test_run_stewardship_requires_badge_gate_after_41,
        test_run_stewardship_requires_wiki_gate_after_41,
        test_run_stewardship_requires_schema_gate_after_41,
        test_agents_requires_relative_mention_after_41,
        test_run_stewardship_requires_gate_order_after_41,
        test_relative_gate_file_must_exist_after_41,
        test_run_stewardship_file_must_exist_after_41,
        test_relative_gate_requires_atx_needle_after_41,
        test_relative_gate_requires_git_skip_after_41,
        test_relative_links_reject_empty_path_fragment_after_41,
        test_relative_links_reject_empty_path_fragment_nested_after_41,
        test_relative_links_reject_query_string_after_41,
        test_relative_links_reject_query_before_fragment_after_41,
        test_relative_links_reject_double_encoded_escape_after_41,
        test_relative_links_reject_abs_unix_escape_after_41,
        test_relative_links_accept_https_still_after_41,
        test_relative_links_accept_mailto_still_after_41,
        test_relative_links_accept_tel_still_after_41,
        test_relative_links_reject_protocol_relative_still_after_41,
        test_relative_links_reject_http_still_after_41,
        test_relative_links_reject_javascript_still_after_41,
        test_relative_links_reject_data_still_after_41,
        test_relative_links_reject_file_still_after_41,
        test_relative_links_reject_vbscript_still_after_41,
        test_relative_links_reject_empty_parens_still_after_41,
        test_relative_links_reject_bare_hash_still_after_41,
        test_relative_links_reject_broken_still_after_41,
        test_relative_links_reject_missing_fragment_still_after_41,
        test_relative_links_accept_valid_fragment_still_after_41,
        test_relative_links_ignore_fenced_still_after_41,
        test_relative_links_ignore_tilde_fence_still_after_41,
        test_relative_links_reject_image_query_after_41,
        test_relative_links_reject_image_empty_fragment_after_41,
        test_relative_links_accept_angle_https_still_after_41,
        test_relative_links_reject_encoded_escape_still_after_41,
        test_relative_links_reject_dotdot_escape_still_after_41,
        test_relative_links_github_slug_amp_still_after_41,
        test_relative_links_reject_nul_still_after_41,
        test_relative_links_accept_title_attr_still_after_41,
        test_relative_links_reject_http_mixed_still_after_41,
        test_relative_links_accept_https_mixed_still_after_41,
        test_relative_links_reject_whitespace_target_still_after_41,
        test_relative_links_reject_protocol_rel_image_after_41,
        test_relative_links_reject_javascript_image_after_41,
        test_relative_links_reject_empty_frag_angle_after_41,
        test_relative_links_skip_owasp_still_after_41,
        test_relative_links_skip_agents_still_after_41,
        test_relative_links_accept_cross_file_fragment_still_after_41,
        test_relative_links_accept_license_still_after_41,
        test_relative_links_accept_nested_docs_still_after_41,
        test_relative_links_reject_query_on_nested_after_41,
        test_relative_links_reject_empty_frag_on_nested_after_41,
        test_relative_links_skip_node_modules_still_after_41,
        test_relative_links_accept_image_existing_still_after_41,
        test_relative_links_reject_broken_image_still_after_41,
        test_relative_links_fully_unquote_helper_after_41,

        # TOKENMAXX deepen after #43 (+71)
        test_wiki_gate_file_must_exist_after_43,
        test_wiki_gate_requires_publishable_home_after_43,
        test_wiki_gate_requires_publishable_overview_after_43,
        test_wiki_gate_requires_publishable_autonomy_after_43,
        test_wiki_gate_requires_publishable_stewardship_after_43,
        test_wiki_gate_requires_publishable_routing_after_43,
        test_wiki_gate_requires_publishable_security_after_43,
        test_wiki_gate_requires_operator_publish_after_43,
        test_wiki_gate_requires_publishable_pages_const_after_43,
        test_wiki_gate_requires_topic_hints_const_after_43,
        test_wiki_gate_requires_l2_topic_after_43,
        test_wiki_gate_requires_l3_topic_after_43,
        test_wiki_gate_requires_credential_topic_after_43,
        test_wiki_gate_requires_copilot_topic_after_43,
        test_wiki_gate_requires_strip_fenced_after_43,
        test_wiki_gate_requires_dangerous_scheme_after_43,
        test_wiki_gate_requires_protocol_relative_needle_after_43,
        test_wiki_gate_requires_slash_slash_match_after_43,
        test_wiki_gate_requires_http_reject_after_43,
        test_wiki_gate_requires_forbidden_badge_hints_after_43,
        test_wiki_gate_requires_stewardship_ci_hints_after_43,
        test_wiki_gate_requires_markdown_lint_ci_after_43,
        test_wiki_gate_requires_link_check_ci_after_43,
        test_wiki_gate_requires_stewardship_checks_ci_after_43,
        test_wiki_gate_requires_actionlint_after_43,
        test_wiki_gate_requires_invent_pin_after_43,
        test_wiki_gate_requires_kill_switch_needle_after_43,
        test_wiki_gate_requires_scan_secrets_after_43,
        test_wiki_gate_requires_readme_link_hints_after_43,
        test_wiki_gate_requires_badge_standard_hints_after_43,
        test_wiki_gate_requires_out_of_scope_after_43,
        test_wiki_rejects_protocol_relative_after_43,
        test_wiki_rejects_protocol_relative_image_after_43,
        test_wiki_ignores_fenced_http_after_43,
        test_wiki_ignores_tilde_fenced_javascript_after_43,
        test_wiki_rejects_missing_l2_topic_after_43,
        test_wiki_rejects_missing_l3_topic_after_43,
        test_wiki_rejects_missing_credential_topic_after_43,
        test_wiki_rejects_missing_copilot_topic_after_43,
        test_wiki_rejects_missing_home_kill_after_43,
        test_wiki_rejects_http_still_after_43,
        test_wiki_rejects_javascript_still_after_43,
        test_wiki_rejects_data_still_after_43,
        test_wiki_rejects_vbscript_still_after_43,
        test_wiki_rejects_file_still_after_43,
        test_wiki_rejects_discord_chrome_still_after_43,
        test_wiki_rejects_stars_chrome_still_after_43,
        test_wiki_rejects_missing_actionlint_still_after_43,
        test_wiki_rejects_missing_relative_hint_still_after_43,
        test_wiki_rejects_missing_invent_home_still_after_43,
        test_wiki_rejects_missing_secret_home_still_after_43,
        test_wiki_rejects_missing_ci_hint_still_after_43,
        test_wiki_rejects_missing_publish_home_row_still_after_43,
        test_wiki_rejects_unexpected_page_still_after_43,
        test_wiki_rejects_missing_home_backlink_still_after_43,
        test_wiki_rejects_missing_home_to_security_still_after_43,
        test_wiki_rejects_missing_badge_topic_still_after_43,
        test_wiki_rejects_missing_l0_topic_still_after_43,
        test_wiki_rejects_missing_l1_topic_still_after_43,
        test_wiki_rejects_missing_kill_topic_still_after_43,
        test_wiki_rejects_missing_surface_topic_still_after_43,
        test_wiki_rejects_missing_governance_topic_still_after_43,
        test_wiki_passes_good_fixture_still_after_43,
        test_wiki_rejects_http_mixed_case_still_after_43,
        test_wiki_rejects_javascript_mixed_case_still_after_43,
        test_wiki_rejects_missing_do_not_push_still_after_43,
        test_wiki_rejects_npm_chrome_still_after_43,
        test_wiki_rejects_missing_out_of_scope_still_after_43,
        test_wiki_rejects_missing_run_script_still_after_43,
        test_wiki_rejects_missing_home_badge_link_still_after_43,
        test_wiki_rejects_secret_pattern_still_after_43,

        # TOKENMAXX deepen after #45 (+71)
        test_schema_gate_file_must_exist_after_45,
        test_schema_gate_requires_doc_schemas_const_after_45,
        test_schema_gate_requires_expected_values_const_after_45,
        test_schema_gate_requires_badge_standard_doc_after_45,
        test_schema_gate_requires_publish_doc_after_45,
        test_schema_gate_requires_backlog_doc_after_45,
        test_schema_gate_requires_agents_doc_after_45,
        test_schema_gate_requires_claude_doc_after_45,
        test_schema_gate_requires_edit_policy_key_after_45,
        test_schema_gate_requires_autonomy_key_after_45,
        test_schema_gate_requires_parent_governance_key_after_45,
        test_schema_gate_requires_version_key_after_45,
        test_schema_gate_requires_surface_key_after_45,
        test_schema_gate_requires_closes_key_after_45,
        test_schema_gate_requires_purpose_key_after_45,
        test_schema_gate_requires_maintainer_key_after_45,
        test_schema_gate_requires_semver_re_after_45,
        test_schema_gate_requires_iso_date_re_after_45,
        test_schema_gate_requires_issue_ref_re_after_45,
        test_schema_gate_requires_fenced_yaml_re_after_45,
        test_schema_gate_requires_yaml_fence_needle_after_45,
        test_schema_gate_requires_date_keys_after_45,
        test_schema_gate_requires_parse_simple_yaml_after_45,
        test_schema_gate_requires_load_yaml_after_45,
        test_schema_gate_requires_first_yaml_block_after_45,
        test_schema_gate_requires_safe_load_after_45,
        test_schema_gate_requires_mapping_needle_after_45,
        test_schema_gate_requires_scan_secrets_after_45,
        test_schema_gate_requires_invent_pin_after_45,
        test_schema_gate_requires_smtp_eth_after_45,
        test_schema_gate_requires_repository_specific_after_45,
        test_schema_gate_requires_copilot_pin_after_45,
        test_schema_gate_requires_active_status_after_45,
        test_schema_gate_requires_autonomy_range_after_45,
        test_schema_gate_requires_backlog_owner_copilot_after_45,
        test_schema_rejects_backlog_wrong_owner_after_45,
        test_schema_rejects_empty_badge_edit_policy_after_45,
        test_schema_rejects_empty_agents_maintainer_after_45,
        test_schema_rejects_missing_agents_parent_key_after_45,
        test_schema_rejects_missing_claude_autonomy_key_after_45,
        test_schema_rejects_empty_publish_created_after_45,
        test_schema_rejects_wrong_badge_status_draft_after_45,
        test_schema_rejects_autonomy_two_vs_expected_after_45,
        test_schema_rejects_wrong_claude_parent_still_after_45,
        test_schema_rejects_closes_without_hash_still_after_45,
        test_schema_rejects_tier_zero_still_after_45,
        test_schema_rejects_semver_prerelease_still_after_45,
        test_schema_rejects_wrong_agents_scope_still_after_45,
        test_schema_rejects_wrong_publish_status_draft_after_45,
        test_schema_rejects_autonomy_negative_after_45,
        test_schema_rejects_empty_badge_scope_after_45,
        test_schema_rejects_missing_agents_maintainer_key_after_45,
        test_schema_rejects_missing_claude_repo_key_after_45,
        test_schema_rejects_empty_publish_closes_after_45,
        test_schema_rejects_badge_missing_invent_wording_after_45,
        test_schema_rejects_wrong_badge_owner_after_45,
        test_schema_rejects_wrong_claude_surface_after_45,
        test_schema_rejects_bad_iso_date_after_45,
        test_schema_rejects_backlog_tier_zero_after_45,
        test_schema_rejects_backlog_draft_status_after_45,
        test_schema_rejects_publish_closes_without_hash_after_45,
        test_schema_rejects_missing_backlog_edit_policy_after_45,
        test_schema_rejects_agents_wrong_maintainer_after_45,
        test_schema_rejects_claude_wrong_repo_after_45,
        test_schema_rejects_secret_pattern_in_badge_after_45,
        test_schema_rejects_autonomy_four_after_45,
        test_schema_rejects_empty_agents_scope_after_45,
        test_schema_rejects_wrong_agents_parent_after_45,
        test_schema_rejects_missing_badge_closes_key_after_45,
        test_schema_rejects_non_mapping_yaml_after_45,
        test_schema_passes_good_fixture_still_after_45,

        # TOKENMAXX deepen after #46 (+69)
        test_common_gate_file_must_exist_after_46,
        test_common_gate_requires_secret_patterns_after_46,
        test_common_gate_requires_secret_url_hints_after_46,
        test_common_gate_requires_forbidden_badge_hints_after_46,
        test_common_gate_requires_dangerous_link_schemes_after_46,
        test_common_gate_requires_fenced_block_re_after_46,
        test_common_gate_requires_strip_fenced_code_after_46,
        test_common_gate_requires_has_dangerous_scheme_after_46,
        test_common_gate_requires_markdown_files_after_46,
        test_common_gate_requires_fail_helper_pin_after_46,
        test_common_gate_requires_load_workflow_pin_after_46,
        test_common_gate_requires_secret_ghp_after_46,
        test_common_gate_requires_secret_gho_after_46,
        test_common_gate_requires_secret_ghu_after_46,
        test_common_gate_requires_secret_ghs_after_46,
        test_common_gate_requires_secret_ghr_after_46,
        test_common_gate_requires_secret_github_pat_after_46,
        test_common_gate_requires_secret_private_key_after_46,
        test_common_gate_requires_secret_sk_after_46,
        test_common_gate_requires_secret_rk_after_46,
        test_common_gate_requires_secret_api_key_re_after_46,
        test_common_gate_requires_secret_aws_secret_after_46,
        test_common_gate_requires_secret_xox_after_46,
        test_common_gate_requires_secret_npm_after_46,
        test_common_gate_requires_secret_aiza_after_46,
        test_common_gate_requires_url_hint_token_eq_after_46,
        test_common_gate_requires_url_hint_access_token_after_46,
        test_common_gate_requires_url_hint_api_key_eq_after_46,
        test_common_gate_requires_url_hint_apikey_eq_after_46,
        test_common_gate_requires_url_hint_client_secret_after_46,
        test_common_gate_requires_forbidden_coverage_after_46,
        test_common_gate_requires_forbidden_codecov_after_46,
        test_common_gate_requires_forbidden_coveralls_after_46,
        test_common_gate_requires_forbidden_downloads_after_46,
        test_common_gate_requires_forbidden_discord_after_46,
        test_common_gate_requires_forbidden_twitter_after_46,
        test_common_gate_requires_forbidden_x_com_after_46,
        test_common_gate_requires_forbidden_stars_after_46,
        test_common_gate_requires_forbidden_forks_after_46,
        test_common_gate_requires_forbidden_followers_after_46,
        test_common_gate_requires_forbidden_npm__after_46,
        test_common_gate_requires_forbidden_pypi__after_46,
        test_common_gate_requires_forbidden_producthunt_after_46,
        test_common_gate_requires_forbidden_buymeacoffee_after_46,
        test_common_gate_requires_forbidden_opencollective_after_46,
        test_common_gate_requires_scheme_js_after_46,
        test_common_gate_requires_scheme_data_after_46,
        test_common_gate_requires_scheme_vbscript_after_46,
        test_common_gate_requires_scheme_file_after_46,
        test_common_gate_requires_tilde_fence_after_46,
        test_common_gate_requires_endswith_urlish_after_46,
        test_common_gate_requires_invent_wording_after_46,
        test_common_gate_requires_live_secret_scan_pin_after_46,
        test_common_secret_patterns_ghp_still_after_46,
        test_common_secret_patterns_github_pat_still_after_46,
        test_common_secret_patterns_npm_still_after_46,
        test_common_secret_patterns_aiza_still_after_46,
        test_common_dangerous_schemes_complete_still_after_46,
        test_common_has_dangerous_javascript_still_after_46,
        test_common_has_dangerous_data_still_after_46,
        test_common_forbidden_hints_coverage_still_after_46,
        test_common_strip_fenced_code_tilde_still_after_46,
        test_common_strip_fenced_code_backtick_still_after_46,
        test_common_scan_secrets_url_hint_still_after_46,
        test_common_scan_secrets_ghp_prefix_still_after_46,
        test_common_load_workflow_text_live_still_after_46,
        test_common_markdown_files_readme_still_after_46,
        test_common_fail_appends_still_after_46,
        test_common_secret_url_hints_complete_still_after_46,
        # TOKENMAXX deepen after #48 (+68 badge-standard gate pins)
        test_badge_gate_requires_required_order_after_48,
        test_badge_gate_requires_max_badges_after_48,
        test_badge_gate_requires_expected_repo_after_48,
        test_badge_gate_requires_required_workflows_after_48,
        test_badge_gate_requires_badge_line_re_after_48,
        test_badge_gate_requires_repo_from_github_re_after_48,
        test_badge_gate_requires_repo_from_shields_re_after_48,
        test_badge_gate_requires_label_link_check_after_48,
        test_badge_gate_requires_label_markdown_lint_after_48,
        test_badge_gate_requires_label_license_after_48,
        test_badge_gate_requires_max_badges_eq_3_after_48,
        test_badge_gate_requires_repo_slug_after_48,
        test_badge_gate_requires_wf_link_check_after_48,
        test_badge_gate_requires_wf_markdown_lint_after_48,
        test_badge_gate_requires_wf_stewardship_after_48,
        test_badge_gate_requires_fn_extract_badge_row_after_48,
        test_badge_gate_requires_fn_check_badges_after_48,
        test_badge_gate_requires_fn_check_badge_doc_after_48,
        test_badge_gate_requires_fn_readme_consistency_after_48,
        test_badge_gate_requires_fn_contributing_after_48,
        test_badge_gate_requires_fn_lycheeignore_after_48,
        test_badge_gate_requires_fn_actionlint_after_48,
        test_badge_gate_requires_fn_workflow_hardening_after_48,
        test_badge_gate_requires_fn_workflows_license_after_48,
        test_badge_gate_requires_link_check_badge_svg_after_48,
        test_badge_gate_requires_markdown_lint_badge_svg_after_48,
        test_badge_gate_requires_shields_license_after_48,
        test_badge_gate_requires_contiguous_pin_after_48,
        test_badge_gate_requires_fourth_refusal_after_48,
        test_badge_gate_requires_stewardship_product_reject_after_48,
        test_badge_gate_requires_live_badge_row_pin_after_48,
        test_badge_gate_requires_forbidden_hints_import_after_48,
        test_badge_gate_requires_secret_url_hints_import_after_48,
        test_badge_gate_requires_scan_secrets_after_48,
        test_badge_gate_requires_invent_wording_after_48,
        test_badge_gate_requires_https_images_after_48,
        test_badge_gate_required_order_still_after_48,
        test_badge_gate_max_badges_still_after_48,
        test_badge_gate_expected_repo_still_after_48,
        test_badge_gate_required_workflows_still_after_48,
        test_badge_gate_contract_fn_still_after_48,
        test_badge_gate_extract_row_still_after_48,
        test_badge_gate_check_badges_order_still_after_48,
        test_badge_gate_check_badges_https_still_after_48,
        test_badge_gate_doc_fourth_still_after_48,
        test_badge_gate_badge_line_re_still_after_48,
        test_badge_gate_repo_github_re_still_after_48,
        test_badge_gate_repo_shields_re_still_after_48,
        test_badge_gate_main_calls_contract_still_after_48,
        test_badge_gate_docstring_pin_still_after_48,
        test_badge_gate_three_max_still_after_48,
        test_badge_gate_contract_errors_empty_live_still_after_48,
        test_badge_gate_no_fourth_in_order_still_after_48,
        test_badge_gate_license_last_still_after_48,
        test_badge_gate_link_check_first_still_after_48,
        test_badge_gate_markdown_lint_middle_still_after_48,
        test_badge_gate_badge_gate_path_still_after_48,
        test_badge_gate_forbidden_hints_wired_still_after_48,
        test_badge_gate_secret_hints_wired_still_after_48,
        test_badge_gate_passes_live_contract_still_after_48,
        test_badge_gate_doc_mentions_three_still_after_48,
        test_badge_gate_readme_has_three_still_after_48,
        test_badge_gate_check_badges_live_still_after_48,
        test_badge_gate_contributing_invent_still_after_48,
        test_badge_gate_agents_selftest_still_after_48,
        test_badge_gate_lycheeignore_shields_still_after_48,
        test_badge_gate_source_has_contract_section_still_after_48,
        test_badge_gate_quiet_stewardship_doc_still_after_48,
        test_schema_gate_requires_string_keys_after_53,
        test_schema_gate_requires_reject_non_scalar_after_53,
        test_schema_gate_requires_front_door_scope_after_53,
        test_schema_gate_requires_reversible_purpose_after_53,
        test_schema_gate_requires_closes_16_after_53,
        test_schema_gate_requires_empty_yaml_needle_after_53,
        test_schema_gate_requires_bool_int_reject_after_53,
        test_schema_gate_requires_ecosystem_md_after_53,
        test_schema_gate_requires_parent_governance_pin_after_53,
        test_schema_gate_requires_fuzzywigg_owner_after_53,
        test_schema_gate_requires_agents_governance_repo_after_53,
        test_schema_rejects_bool_autonomy_after_53,
        test_schema_rejects_bool_tier_after_53,
        test_schema_rejects_nested_scope_after_53,
        test_schema_rejects_list_owner_after_53,
        test_schema_rejects_empty_yaml_block_after_53,
        test_schema_rejects_null_maintainer_after_53,
        test_schema_rejects_tilde_null_purpose_after_53,
        test_schema_rejects_int_scope_after_53,
        test_schema_rejects_wrong_badge_scope_after_53,
        test_schema_rejects_wrong_publish_purpose_after_53,
        test_schema_rejects_wrong_badge_closes_after_53,
        test_schema_rejects_wrong_publish_closes_after_53,
        test_schema_rejects_yes_autonomy_after_53,
        test_schema_rejects_nested_purpose_after_53,
        test_schema_rejects_list_closes_after_53,
        test_schema_rejects_bool_status_after_53,
        test_schema_rejects_wrong_value_still_after_53,
        test_schema_rejects_inactive_status_still_after_53,
        test_schema_rejects_missing_key_still_after_53,
        test_schema_rejects_bad_semver_still_after_53,
        test_schema_rejects_edit_policy_without_invent_still_after_53,
        test_schema_rejects_wrong_surface_still_after_53,
        test_schema_rejects_closes_without_issue_still_after_53,
        test_schema_rejects_bad_iso_date_still_after_53,
        test_schema_rejects_wrong_maintainer_still_after_53,
        test_schema_rejects_float_autonomy_still_after_53,
        test_schema_rejects_autonomy_as_string_still_after_53,
        test_schema_rejects_tier_as_string_still_after_53,
        test_schema_rejects_wrong_claude_repo_still_after_53,
        test_schema_rejects_empty_badge_edit_policy_still_after_53,
        test_schema_rejects_secret_in_doc_still_after_53,
        test_schema_rejects_missing_yaml_block_still_after_53,
        test_schema_rejects_missing_file_still_after_53,
        test_schema_rejects_non_mapping_yaml_still_after_53,
        test_schema_rejects_unparseable_yaml_still_after_53,
        test_schema_rejects_wrong_backlog_owner_after_53,
        test_schema_rejects_list_status_after_53,
        test_schema_rejects_nested_edit_policy_after_53,
        test_schema_rejects_bool_closes_after_53,
        test_schema_rejects_int_purpose_after_53,
        test_schema_rejects_bool_owner_after_53,
        test_schema_rejects_nested_parent_governance_after_53,
        test_schema_rejects_list_maintainer_after_53,
        test_schema_rejects_float_tier_after_53,
        test_schema_rejects_yes_tier_after_53,
        test_schema_rejects_null_closes_after_53,
        test_schema_rejects_int_surface_after_53,
        test_schema_rejects_nested_repo_after_53,
        test_schema_rejects_list_version_after_53,
        test_schema_rejects_bool_version_after_53,
        test_schema_rejects_wrong_badge_closes_hash_after_53,
        test_schema_rejects_wrong_publish_purpose_short_after_53,
        test_schema_rejects_wrong_front_door_scope_typo_after_53,
        test_schema_gate_requires_string_keys_const_still_after_53,
        test_schema_gate_requires_reject_non_scalar_still_after_53,
        test_schema_gate_requires_empty_yaml_still_after_53,
        test_schema_gate_requires_front_door_still_after_53,
        test_schema_gate_requires_reversible_still_after_53,
        test_schema_passes_good_fixture_still_after_53,
        test_schema_rejects_tier_zero_still_after_53,

        # TOKENMAXX deepen after #55 (+71 relative-link second-pass)
        test_relative_gate_requires_md_link_re_after_55,
        test_relative_gate_requires_skip_parts_after_55,
        test_relative_gate_requires_skip_prefixes_after_55,
        test_relative_gate_requires_skip_files_after_55,
        test_relative_gate_requires_max_unquote_passes_after_55,
        test_relative_gate_requires_max_unquote_eq_4_after_55,
        test_relative_gate_requires_should_skip_after_55,
        test_relative_gate_requires_iter_markdown_after_55,
        test_relative_gate_requires_headings_in_after_55,
        test_relative_gate_requires_check_file_after_55,
        test_relative_gate_requires_mailto_allow_after_55,
        test_relative_gate_requires_tel_allow_after_55,
        test_relative_gate_requires_nul_reject_after_55,
        test_relative_gate_requires_angle_bracket_strip_after_55,
        test_relative_gate_requires_image_link_re_after_55,
        test_relative_gate_requires_escapes_repo_needle_after_55,
        test_relative_gate_requires_broken_link_needle_after_55,
        test_relative_gate_requires_missing_heading_needle_after_55,
        test_relative_gate_requires_rglob_after_55,
        test_relative_gate_requires_no_md_fail_closed_after_55,
        test_relative_links_reject_triple_encoded_escape_after_55,
        test_relative_links_reject_query_with_amp_after_55,
        test_relative_links_reject_empty_frag_whitespace_after_55,
        test_relative_links_reject_image_http_after_55,
        test_relative_links_reject_image_data_after_55,
        test_relative_links_reject_image_file_scheme_after_55,
        test_relative_links_reject_image_vbscript_after_55,
        test_relative_links_accept_angle_mailto_after_55,
        test_relative_links_accept_angle_tel_after_55,
        test_relative_links_accept_title_on_https_after_55,
        test_relative_links_reject_abs_windows_style_after_55,
        test_relative_links_reject_query_on_image_path_after_55,
        test_relative_links_reject_empty_frag_on_image_after_55,
        test_relative_links_skip_git_dir_after_55,
        test_relative_links_accept_nested_cross_frag_after_55,
        test_relative_links_reject_nested_missing_frag_after_55,
        test_relative_links_accept_self_frag_slug_after_55,
        test_relative_links_reject_self_frag_missing_after_55,
        test_relative_links_reject_whitespace_only_target_after_55,
        test_relative_links_accept_https_image_after_55,
        test_relative_links_fully_unquote_cap_helper_after_55,
        test_relative_gate_requires_owasp_still_after_55,
        test_relative_gate_requires_fully_unquote_still_after_55,
        test_relative_gate_requires_empty_fragment_still_after_55,
        test_relative_gate_requires_query_string_still_after_55,
        test_relative_gate_requires_github_slug_still_after_55,
        test_relative_gate_requires_atx_still_after_55,
        test_relative_gate_requires_strip_fenced_still_after_55,
        test_relative_gate_requires_dangerous_still_after_55,
        test_run_stewardship_requires_gate_order_still_after_55,
        test_relative_links_reject_http_still_after_55,
        test_relative_links_reject_protocol_rel_still_after_55,
        test_relative_links_reject_javascript_still_after_55,
        test_relative_links_reject_data_still_after_55,
        test_relative_links_reject_file_still_after_55,
        test_relative_links_reject_vbscript_still_after_55,
        test_relative_links_reject_empty_parens_still_after_55,
        test_relative_links_reject_bare_hash_still_after_55,
        test_relative_links_reject_broken_still_after_55,
        test_relative_links_reject_empty_path_frag_still_after_55,
        test_relative_links_reject_query_still_after_55,
        test_relative_links_accept_https_still_after_55,
        test_relative_links_accept_mailto_still_after_55,
        test_relative_links_accept_tel_still_after_55,
        test_relative_links_ignore_fenced_still_after_55,
        test_relative_links_ignore_tilde_still_after_55,
        test_relative_links_skip_owasp_still_after_55,
        test_relative_links_skip_agents_still_after_55,
        test_relative_links_skip_node_modules_still_after_55,
        test_relative_links_accept_valid_fragment_still_after_55,
        test_relative_links_reject_nul_still_after_55,

        # TOKENMAXX deepen after #59 (+72 wiki-outline second-pass)
        test_wiki_gate_requires_operator_only_after_59,
        test_wiki_gate_requires_operator_only_eq_publish_after_59,
        test_wiki_gate_requires_docs_path_after_59,
        test_wiki_gate_requires_wiki_path_after_59,
        test_wiki_gate_requires_reject_invent_helper_after_59,
        test_wiki_gate_requires_autonomy_topic_after_59,
        test_wiki_gate_requires_governance_topic_after_59,
        test_wiki_gate_requires_public_topic_after_59,
        test_wiki_gate_requires_kill_topic_after_59,
        test_wiki_gate_requires_secret_topic_after_59,
        test_wiki_gate_requires_surface_topic_after_59,
        test_wiki_gate_requires_routing_topic_after_59,
        test_wiki_gate_requires_run_script_topic_after_59,
        test_wiki_gate_requires_badge_topic_after_59,
        test_wiki_gate_requires_home_backlink_needle_after_59,
        test_wiki_gate_requires_unexpected_md_needle_after_59,
        test_wiki_gate_requires_missing_wiki_needle_after_59,
        test_wiki_gate_requires_failed_banner_after_59,
        test_wiki_gate_requires_ok_match_needle_after_59,
        test_wiki_gate_requires_angle_bracket_strip_after_59,
        test_wiki_gate_requires_image_link_re_after_59,
        test_wiki_gate_requires_do_not_push_after_59,
        test_wiki_gate_requires_readme_blob_after_59,
        test_wiki_gate_requires_badge_hint_path_after_59,
        test_wiki_gate_requires_stars_special_case_after_59,
        test_wiki_gate_requires_forks_special_case_after_59,
        test_wiki_gate_requires_followers_special_case_after_59,
        test_wiki_gate_requires_invent_chrome_needle_after_59,
        test_wiki_gate_requires_home_table_row_after_59,
        test_wiki_rejects_missing_public_topic_after_59,
        test_wiki_rejects_missing_autonomy_topic_after_59,
        test_wiki_rejects_missing_routing_topic_after_59,
        test_wiki_rejects_missing_governance_topic_after_59,
        test_wiki_rejects_followers_chrome_after_59,
        test_wiki_rejects_twitter_chrome_after_59,
        test_wiki_rejects_xcom_chrome_after_59,
        test_wiki_rejects_coveralls_chrome_after_59,
        test_wiki_rejects_image_http_after_59,
        test_wiki_rejects_image_javascript_after_59,
        test_wiki_rejects_image_data_after_59,
        test_wiki_rejects_image_file_scheme_after_59,
        test_wiki_accepts_angle_https_after_59,
        test_wiki_accepts_title_attr_after_59,
        test_wiki_accepts_mailto_after_59,
        test_wiki_accepts_tel_after_59,
        test_wiki_rejects_missing_home_to_overview_after_59,
        test_wiki_rejects_missing_home_to_autonomy_after_59,
        test_wiki_rejects_missing_overview_home_backlink_after_59,
        test_wiki_rejects_missing_publish_secrets_after_59,
        test_wiki_rejects_missing_publish_link_check_after_59,
        test_wiki_rejects_secret_in_publish_after_59,
        test_wiki_rejects_downloads_chrome_after_59,
        test_wiki_rejects_opencollective_chrome_after_59,
        test_wiki_ignores_fenced_protocol_rel_after_59,
        test_wiki_rejects_http_title_attr_after_59,
        test_wiki_rejects_protocol_rel_title_attr_after_59,
        test_wiki_gate_requires_publishable_pages_still_after_59,
        test_wiki_gate_requires_topic_hints_still_after_59,
        test_wiki_gate_requires_l2_still_after_59,
        test_wiki_gate_requires_credential_still_after_59,
        test_wiki_gate_requires_copilot_still_after_59,
        test_wiki_gate_requires_strip_fenced_still_after_59,
        test_wiki_gate_requires_dangerous_still_after_59,
        test_wiki_gate_requires_protocol_rel_still_after_59,
        test_wiki_gate_requires_http_reject_still_after_59,
        test_wiki_gate_requires_forbidden_hints_still_after_59,
        test_wiki_gate_requires_stewardship_ci_still_after_59,
        test_wiki_gate_requires_actionlint_still_after_59,
        test_wiki_gate_requires_invent_still_after_59,
        test_wiki_gate_requires_kill_switch_still_after_59,
        test_wiki_gate_requires_scan_secrets_still_after_59,
        test_wiki_passes_good_fixture_still_after_59,

        # TOKENMAXX deepen after #61 (+72 badge-standard second-pass)
        test_badge_gate_requires_required_order_eq_after_61,
        test_badge_gate_requires_expected_repo_eq_after_61,
        test_badge_gate_requires_readme_path_after_61,
        test_badge_gate_requires_license_path_after_61,
        test_badge_gate_requires_badge_standard_path_after_61,
        test_badge_gate_requires_contributing_path_after_61,
        test_badge_gate_requires_agents_path_after_61,
        test_badge_gate_requires_lycheeignore_path_after_61,
        test_badge_gate_requires_markdownlint_path_after_61,
        test_badge_gate_requires_contract_fn_after_61,
        test_badge_gate_requires_main_fn_after_61,
        test_badge_gate_requires_full_link_check_svg_after_61,
        test_badge_gate_requires_full_md_lint_svg_after_61,
        test_badge_gate_requires_exactly_needle_after_61,
        test_badge_gate_requires_order_needle_after_61,
        test_badge_gate_requires_contiguous_needle_after_61,
        test_badge_gate_requires_missing_h1_needle_after_61,
        test_badge_gate_requires_unexpected_label_needle_after_61,
        test_badge_gate_requires_forbidden_hint_needle_after_61,
        test_badge_gate_requires_secret_token_needle_after_61,
        test_badge_gate_requires_failed_banner_after_61,
        test_badge_gate_requires_ok_match_needle_after_61,
        test_badge_gate_requires_endswith_license_after_61,
        test_badge_gate_requires_dot_slash_license_after_61,
        test_badge_gate_requires_load_workflow_text_after_61,
        test_badge_gate_requires_invent_product_doc_pin_after_61,
        test_badge_gate_requires_intentionally_pin_after_61,
        test_badge_gate_requires_quiet_stewardship_after_61,
        test_badge_gate_requires_selftest_agents_pin_after_61,
        test_badge_gate_requires_relative_agents_pin_after_61,
        test_badge_gate_requires_img_startswith_https_after_61,
        test_badge_gate_requires_link_http_reject_after_61,
        test_badge_rejects_wrong_order_after_61,
        test_badge_rejects_four_badges_after_61,
        test_badge_rejects_http_image_after_61,
        test_badge_rejects_stewardship_product_after_61,
        test_badge_rejects_wrong_repo_after_61,
        test_badge_rejects_noncontiguous_after_61,
        test_badge_rejects_secret_url_after_61,
        test_badge_rejects_forbidden_coverage_after_61,
        test_badge_rejects_two_badges_after_61,
        test_badge_rejects_missing_h1_after_61,
        test_badge_rejects_wrong_license_link_after_61,
        test_badge_rejects_http_badge_link_after_61,
        test_badge_accepts_dot_slash_license_after_61,
        test_badge_accepts_absolute_license_blob_after_61,
        test_badge_passes_good_fixture_after_61,
        test_badge_rejects_codecov_hint_after_61,
        test_badge_rejects_downloads_hint_after_61,
        test_badge_rejects_stars_hint_after_61,
        test_badge_rejects_wrong_link_check_image_after_61,
        test_badge_rejects_wrong_md_lint_image_after_61,
        test_badge_gate_requires_required_order_still_after_61,
        test_badge_gate_requires_max_badges_still_after_61,
        test_badge_gate_requires_expected_repo_still_after_61,
        test_badge_gate_requires_required_workflows_still_after_61,
        test_badge_gate_requires_badge_line_re_still_after_61,
        test_badge_gate_requires_max_eq_3_still_after_61,
        test_badge_gate_requires_link_svg_still_after_61,
        test_badge_gate_requires_md_svg_still_after_61,
        test_badge_gate_requires_shields_still_after_61,
        test_badge_gate_requires_contiguous_still_after_61,
        test_badge_gate_requires_fourth_still_after_61,
        test_badge_gate_requires_stew_reject_still_after_61,
        test_badge_gate_requires_live_row_still_after_61,
        test_badge_gate_requires_forbidden_import_still_after_61,
        test_badge_gate_requires_scan_secrets_still_after_61,
        test_badge_passes_live_contract_still_after_61,
        test_badge_rejects_wrong_order_still_after_61,
        test_badge_passes_good_fixture_still_after_61,
        test_badge_rejects_invent_still_after_61,
        test_badge_rejects_secret_url_still_after_61,

        # TOKENMAXX deepen after #65 (+72 stewardship_common second-pass)
        test_common_gate_requires_root_parents_after_65,
        test_common_gate_requires_fence_dotall_after_65,
        test_common_gate_requires_fence_pattern_after_65,
        test_common_gate_requires_strip_doc_after_65,
        test_common_gate_requires_danger_doc_after_65,
        test_common_gate_requires_scan_doc_after_65,
        test_common_gate_requires_md_doc_after_65,
        test_common_gate_requires_pattern_needle_after_65,
        test_common_gate_requires_url_hint_needle_after_65,
        test_common_gate_requires_token_hint_needle_after_65,
        test_common_gate_requires_relative_to_after_65,
        test_common_gate_requires_strip_lower_after_65,
        test_common_gate_requires_startswith_after_65,
        test_common_gate_requires_errors_append_after_65,
        test_common_gate_requires_passwd_token_after_65,
        test_common_gate_requires_openssh_after_65,
        test_common_gate_requires_openssh_ec_after_65,
        test_common_gate_requires_public_docs_after_65,
        test_common_gate_requires_invent_surface_after_65,
        test_common_gate_requires_social_chrome_after_65,
        test_common_gate_requires_link_schemes_after_65,
        test_common_gate_requires_is_file_after_65,
        test_common_gate_requires_sorted_after_65,
        test_common_gate_requires_workflows_after_65,
        test_common_gate_requires_return_none_after_65,
        test_common_gate_requires_url_hints_head_after_65,
        test_common_gate_requires_schemes_js_first_after_65,
        test_common_gate_requires_contract_fn_after_65,
        test_common_gate_requires_contract_call_after_65,
        test_common_secret_patterns_ghp_still_after_65,
        test_common_secret_patterns_github_pat_still_after_65,
        test_common_secret_patterns_npm_still_after_65,
        test_common_secret_patterns_aiza_still_after_65,
        test_common_secret_patterns_passwd_still_after_65,
        test_common_secret_patterns_openssh_still_after_65,
        test_common_dangerous_schemes_complete_still_after_65,
        test_common_has_dangerous_javascript_still_after_65,
        test_common_has_dangerous_data_still_after_65,
        test_common_has_dangerous_none_https_after_65,
        test_common_forbidden_hints_coverage_still_after_65,
        test_common_strip_fenced_tilde_still_after_65,
        test_common_strip_fenced_backtick_still_after_65,
        test_common_scan_secrets_url_hint_still_after_65,
        test_common_scan_secrets_ghp_prefix_still_after_65,
        test_common_load_workflow_text_live_still_after_65,
        test_common_load_workflow_text_missing_none_after_65,
        test_common_markdown_files_readme_still_after_65,
        test_common_fail_appends_still_after_65,
        test_common_secret_url_hints_complete_still_after_65,
        test_common_root_parents_live_after_65,
        test_common_gate_requires_secret_patterns_still_after_65,
        test_common_gate_requires_secret_url_hints_still_after_65,
        test_common_gate_requires_forbidden_badge_hints_still_after_65,
        test_common_gate_requires_dangerous_link_schemes_still_after_65,
        test_common_gate_requires_fenced_block_re_still_after_65,
        test_common_gate_requires_strip_fenced_code_still_after_65,
        test_common_gate_requires_has_dangerous_scheme_still_after_65,
        test_common_gate_requires_markdown_files_still_after_65,
        test_common_gate_requires_fail_helper_pin_still_after_65,
        test_common_gate_requires_load_workflow_pin_still_after_65,
        test_common_gate_requires_secret_ghp_still_after_65,
        test_common_gate_requires_secret_github_pat_still_after_65,
        test_common_gate_requires_url_hint_token_eq_still_after_65,
        test_common_gate_requires_forbidden_coverage_still_after_65,
        test_common_gate_requires_scheme_js_still_after_65,
        test_common_gate_requires_endswith_urlish_still_after_65,
        test_common_gate_requires_invent_wording_still_after_65,
        test_common_gate_requires_live_secret_scan_pin_still_after_65,
        test_common_secret_patterns_sk_still_after_65,
        test_common_secret_patterns_xox_still_after_65,
        test_common_has_dangerous_vbscript_still_after_65,
        test_common_has_dangerous_file_still_after_65,

        # TOKENMAXX deepen after #72 (+72 CI workflow second-pass)
        test_link_check_requires_name_link_check_after_72,
        test_markdown_lint_requires_name_after_72,
        test_stewardship_requires_name_after_72,
        test_link_check_requires_branches_glob_after_72,
        test_markdown_lint_requires_branches_glob_after_72,
        test_stewardship_requires_branches_glob_after_72,
        test_link_check_requires_concurrency_prefix_after_72,
        test_markdown_lint_requires_concurrency_prefix_after_72,
        test_stewardship_requires_concurrency_prefix_after_72,
        test_link_check_requires_github_workflow_after_72,
        test_link_check_requires_github_ref_after_72,
        test_markdown_lint_requires_github_workflow_after_72,
        test_stewardship_requires_github_ref_after_72,
        test_link_check_requires_job_id_after_72,
        test_markdown_lint_requires_job_id_lint_after_72,
        test_stewardship_requires_job_id_after_72,
        test_link_check_requires_self_path_filter_after_72,
        test_markdown_lint_requires_self_path_filter_after_72,
        test_stewardship_requires_scripts_path_after_72,
        test_stewardship_requires_docs_path_after_72,
        test_stewardship_requires_readme_path_after_72,
        test_link_check_requires_secrets_github_token_after_72,
        test_stewardship_requires_python_312_exact_after_72,
        test_stewardship_requires_pip_quiet_pyyaml_after_72,
        test_stewardship_requires_bash_run_script_after_72,
        test_stewardship_requires_python3_selftest_after_72,
        test_markdown_lint_requires_config_key_after_72,
        test_markdown_lint_requires_agents_glob_bang_after_72,
        test_markdown_lint_requires_owasp_bang_after_72,
        test_stewardship_actionlint_lists_link_check_path_after_72,
        test_stewardship_actionlint_lists_markdown_path_after_72,
        test_stewardship_download_passes_177_after_72,
        test_link_check_requires_checkout_v7_still_after_72,
        test_markdown_lint_requires_checkout_v7_still_after_72,
        test_stewardship_requires_checkout_v7_still_after_72,
        test_link_check_requires_lychee_v2_still_after_72,
        test_link_check_requires_timeout_20_still_after_72,
        test_markdown_lint_requires_timeout_10_still_after_72,
        test_stewardship_requires_timeout_15_still_after_72,
        test_link_check_requires_cron_still_after_72,
        test_markdown_lint_requires_cron_still_after_72,
        test_stewardship_requires_cron_still_after_72,
        test_link_check_requires_ubuntu_still_after_72,
        test_markdown_lint_requires_ubuntu_still_after_72,
        test_stewardship_requires_ubuntu_still_after_72,
        test_stewardship_requires_setup_python_v5_still_after_72,
        test_stewardship_requires_actionlint_color_still_after_72,
        test_stewardship_requires_shell_bash_still_after_72,
        test_link_check_requires_fail_true_still_after_72,
        test_link_check_requires_verbose_still_after_72,
        test_link_check_requires_no_progress_still_after_72,
        test_markdown_lint_requires_cli2_v24_still_after_72,
        test_stewardship_requires_get_actionlint_id_still_after_72,
        test_stewardship_requires_pyyaml_still_after_72,
        test_link_check_requires_exclude_loopback_still_after_72,
        test_link_check_requires_max_concurrency_8_still_after_72,
        test_stewardship_requires_actionlint_177_still_after_72,
        test_stewardship_rejects_cancel_in_progress_false_after_72,
        test_link_check_rejects_missing_pull_request_after_72,
        test_markdown_lint_rejects_missing_workflow_dispatch_after_72,
        test_stewardship_rejects_missing_contents_read_after_72,
        test_link_check_rejects_missing_schedule_after_72,
        test_stewardship_rejects_missing_concurrency_after_72,
        test_markdown_lint_rejects_missing_owasp_exclude_still_after_72,
        test_link_check_requires_lycheeignore_path_after_72,
        test_markdown_lint_requires_markdownlint_json_path_after_72,
        test_link_check_requires_max_retries_3_still_after_72,
        test_link_check_requires_timeout_arg_20_still_after_72,

        # TOKENMAXX actionlint-style gate pins after #75 (+71)
        test_actionlint_style_gate_requires_fn_after_75,
        test_actionlint_style_gate_requires_static_doc_after_75,
        test_actionlint_style_gate_requires_live_pins_doc_after_75,
        test_actionlint_style_gate_requires_load_workflow_text_after_75,
        test_actionlint_style_gate_requires_top_level_name_after_75,
        test_actionlint_style_gate_requires_jobs_runs_on_after_75,
        test_actionlint_style_gate_requires_jobs_steps_after_75,
        test_actionlint_style_gate_requires_timeout_minutes_after_75,
        test_actionlint_style_gate_requires_prt_reject_after_75,
        test_actionlint_style_gate_requires_harden_wording_after_75,
        test_actionlint_style_gate_requires_write_all_reject_after_75,
        test_actionlint_style_gate_requires_contents_write_reject_after_75,
        test_actionlint_style_gate_requires_id_token_write_reject_after_75,
        test_actionlint_style_gate_requires_docker_skip_after_75,
        test_actionlint_style_gate_requires_unpinned_reject_after_75,
        test_actionlint_style_gate_requires_float_main_after_75,
        test_actionlint_style_gate_requires_float_master_after_75,
        test_actionlint_style_gate_requires_float_latest_after_75,
        test_actionlint_style_gate_requires_float_set_after_75,
        test_actionlint_style_gate_requires_main_call_after_75,
        test_actionlint_style_gate_requires_fn_still_after_75,
        test_actionlint_style_gate_requires_id_token_still_after_75,
        test_actionlint_style_gate_requires_write_all_still_after_75,
        test_actionlint_style_gate_requires_prt_still_after_75,
        test_actionlint_style_gate_requires_docker_still_after_75,
        test_actionlint_style_gate_requires_unpinned_still_after_75,
        test_actionlint_style_gate_requires_timeout_still_after_75,
        test_actionlint_style_gate_requires_runs_on_still_after_75,
        test_actionlint_style_gate_requires_steps_still_after_75,
        test_actionlint_style_gate_requires_live_doc_still_after_75,
        test_actionlint_rejects_id_token_write_on_stewardship_after_75,
        test_actionlint_rejects_id_token_write_on_link_check_after_75,
        test_actionlint_rejects_id_token_write_on_markdown_lint_after_75,
        test_actionlint_rejects_write_all_on_stewardship_after_75,
        test_actionlint_rejects_write_all_on_link_check_after_75,
        test_actionlint_rejects_contents_write_on_stewardship_after_75,
        test_actionlint_rejects_contents_write_on_link_check_after_75,
        test_actionlint_rejects_prt_on_stewardship_after_75,
        test_actionlint_rejects_prt_on_link_check_after_75,
        test_actionlint_rejects_float_main_checkout_after_75,
        test_actionlint_rejects_float_master_checkout_after_75,
        test_actionlint_rejects_float_latest_checkout_after_75,
        test_actionlint_rejects_unpinned_checkout_after_75,
        test_actionlint_rejects_float_main_setup_python_after_75,
        test_actionlint_rejects_missing_timeout_on_stewardship_after_75,
        test_actionlint_rejects_missing_runs_on_after_75,
        test_actionlint_rejects_missing_steps_after_75,
        test_actionlint_rejects_id_token_write_stewardship_still_after_75,
        test_actionlint_rejects_id_token_write_link_still_after_75,
        test_actionlint_rejects_write_all_stewardship_still_after_75,
        test_actionlint_rejects_contents_write_stewardship_still_after_75,
        test_actionlint_rejects_prt_stewardship_still_after_75,
        test_actionlint_rejects_float_main_still_after_75,
        test_actionlint_rejects_float_latest_still_after_75,
        test_actionlint_rejects_unpinned_still_after_75,
        test_actionlint_rejects_float_master_setup_python_still_after_75,
        test_actionlint_rejects_missing_name_after_75,
        test_actionlint_rejects_prt_markdown_lint_after_75,
        test_actionlint_rejects_write_all_markdown_lint_after_75,
        test_actionlint_rejects_contents_write_markdown_lint_after_75,
        test_actionlint_rejects_float_main_markdown_lint_after_75,
        test_actionlint_rejects_unpinned_markdown_lint_after_75,
        test_actionlint_rejects_missing_timeout_link_after_75,
        test_actionlint_rejects_missing_timeout_markdown_after_75,
        test_actionlint_rejects_id_token_write_markdown_still_after_75,
        test_actionlint_rejects_float_latest_setup_python_after_75,
        test_actionlint_rejects_unpinned_setup_python_after_75,
        test_actionlint_rejects_contents_write_link_still_after_75,
        test_actionlint_rejects_write_all_link_still_after_75,
        test_actionlint_rejects_prt_link_still_after_75,
        test_actionlint_rejects_float_master_link_after_75,

        test_stewardship_requires_raw_githubusercontent_still_after_72,
        test_stewardship_requires_curl_fssl_still_after_72,
        test_markdown_lint_requires_davidanson_still_after_72,
        test_link_check_requires_lycheeverse_still_after_72,


        # TOKENMAXX deepen after #72/#75 (+72 stewardship-schema second-pass)
        test_schema_gate_requires_fenced_yaml_exact_after_72,
        test_schema_gate_requires_iso_date_exact_after_72,
        test_schema_gate_requires_semver_exact_after_72,
        test_schema_gate_requires_issue_ref_exact_after_72,
        test_schema_gate_requires_date_keys_exact_after_72,
        test_schema_gate_requires_tiny_yaml_doc_after_72,
        test_schema_gate_requires_scalar_doc_after_72,
        test_schema_gate_requires_no_fence_needle_after_72,
        test_schema_gate_requires_unsupported_yaml_after_72,
        test_schema_gate_requires_empty_key_after_72,
        test_schema_gate_requires_scalar_needle_after_72,
        test_schema_gate_requires_nonempty_needle_after_72,
        test_schema_gate_requires_string_needle_after_72,
        test_schema_gate_requires_active_needle_after_72,
        test_schema_gate_requires_tier_needle_after_72,
        test_schema_gate_requires_autonomy_needle_after_72,
        test_schema_gate_requires_iso_needle_after_72,
        test_schema_gate_requires_invent_needle_after_72,
        test_schema_gate_requires_semver_needle_after_72,
        test_schema_gate_requires_closes_needle_after_72,
        test_schema_gate_requires_failed_banner_after_72,
        test_schema_gate_requires_ok_banner_after_72,
        test_schema_gate_requires_stdlib_subset_after_72,
        test_schema_gate_requires_pyyaml_label_after_72,
        test_schema_gate_requires_bool_subclass_after_72,
        test_schema_gate_requires_group_one_after_72,
        test_schema_gate_requires_missing_keys_after_72,
        test_schema_gate_requires_utf8_encoding_after_72,
        test_schema_gate_requires_string_keys_status_after_72,
        test_schema_gate_requires_string_keys_edit_policy_after_72,
        test_schema_gate_requires_string_keys_closes_after_72,
        test_schema_gate_requires_string_keys_purpose_after_72,
        test_schema_gate_requires_string_keys_version_after_72,
        test_schema_gate_requires_string_keys_maintainer_after_72,
        test_schema_gate_requires_string_keys_parent_after_72,
        test_schema_gate_requires_string_keys_repo_after_72,
        test_schema_gate_requires_string_keys_surface_after_72,
        test_schema_gate_requires_string_keys_last_updated_after_72,
        test_schema_gate_requires_second_pass_doc_after_72,
        test_schema_gate_requires_contract_fn_def_after_72,
        test_schema_rejects_bool_autonomy_after_72,
        test_schema_rejects_bool_tier_after_72,
        test_schema_rejects_nested_list_value_after_72,
        test_schema_rejects_empty_yaml_block_after_72,
        test_schema_rejects_non_string_owner_after_72,
        test_schema_rejects_bad_semver_after_72,
        test_schema_rejects_closes_without_hash_after_72,
        test_schema_rejects_inactive_status_after_72,
        test_schema_rejects_bad_iso_date_after_72,
        test_schema_passes_good_fixture_after_72,
        test_schema_gate_requires_doc_schemas_still_after_72,
        test_schema_gate_requires_expected_values_still_after_72,
        test_schema_gate_requires_string_keys_still_after_72,
        test_schema_gate_requires_reject_non_scalar_still_after_72,
        test_schema_gate_requires_front_door_still_after_72,
        test_schema_gate_requires_closes_16_still_after_72,
        test_schema_gate_requires_copilot_still_after_72,
        test_schema_gate_requires_scan_secrets_still_after_72,
        test_schema_gate_requires_safe_load_still_after_72,
        test_schema_gate_requires_mapping_still_after_72,
        test_schema_gate_requires_empty_yaml_still_after_72,
        test_schema_gate_requires_smtp_eth_still_after_72,
        test_schema_gate_requires_agents_governance_repo_still_after_72,
        test_schema_gate_requires_reversible_still_after_72,
        test_schema_gate_requires_contract_call_after_72,
        test_schema_rejects_missing_key_after_72,
        test_schema_rejects_wrong_maintainer_after_72,
        test_schema_rejects_edit_policy_without_invent_after_72,
        test_schema_rejects_autonomy_out_of_range_after_72,
        test_schema_rejects_tier_zero_after_72,
        test_schema_rejects_missing_yaml_fence_after_72,
        test_schema_gate_requires_bool_int_reject_still_after_72,
        # TOKENMAXX actionlint-style second-pass pins after #83/#86
        test_actionlint_style_gate_requires_name_re_after_86,
        test_actionlint_style_gate_requires_uses_re_after_86,
        test_actionlint_style_gate_requires_write_all_re_after_86,
        test_actionlint_style_gate_requires_contents_write_re_after_86,
        test_actionlint_style_gate_requires_id_token_re_after_86,
        test_actionlint_style_gate_requires_docker_startswith_after_86,
        test_actionlint_style_gate_requires_at_not_in_uses_after_86,
        test_actionlint_style_gate_requires_rsplit_after_86,
        test_actionlint_style_gate_requires_group_strip_after_86,
        test_actionlint_style_gate_requires_runs_membership_after_86,
        test_actionlint_style_gate_requires_steps_membership_after_86,
        test_actionlint_style_gate_requires_prt_membership_after_86,
        test_actionlint_style_gate_requires_timeout_membership_after_86,
        test_actionlint_style_gate_requires_none_continue_after_86,
        test_actionlint_style_gate_requires_required_workflows_loop_after_86,
        test_actionlint_style_gate_requires_least_privilege_after_86,
        test_actionlint_style_gate_requires_oidc_comment_after_86,
        test_actionlint_style_gate_requires_majors_comment_after_86,
        test_actionlint_style_gate_requires_timeout_comment_after_86,
        test_actionlint_style_gate_requires_fail_top_name_after_86,
        test_actionlint_style_gate_requires_fail_runs_after_86,
        test_actionlint_style_gate_requires_fail_steps_after_86,
        test_actionlint_style_gate_requires_fail_prt_after_86,
        test_actionlint_style_gate_requires_fail_write_all_after_86,
        test_actionlint_style_gate_requires_fail_contents_after_86,
        test_actionlint_style_gate_requires_fail_id_token_after_86,
        test_actionlint_style_gate_requires_fail_unpinned_after_86,
        test_actionlint_style_gate_requires_fail_float_after_86,
        test_actionlint_style_gate_requires_fail_timeout_after_86,
        test_actionlint_style_gate_requires_second_pass_doc_after_86,
        test_actionlint_style_gate_requires_module_second_pass_after_86,
        test_actionlint_style_gate_requires_fn_still_after_86,
        test_actionlint_style_gate_requires_static_doc_still_after_86,
        test_actionlint_style_gate_requires_live_pins_still_after_86,
        test_actionlint_style_gate_requires_load_workflow_still_after_86,
        test_actionlint_style_gate_requires_float_set_still_after_86,
        test_actionlint_style_gate_requires_main_call_still_after_86,
        test_actionlint_style_gate_requires_docker_still_after_86,
        test_actionlint_style_gate_requires_id_token_still_after_86,
        test_actionlint_style_gate_requires_write_all_still_after_86,
        test_actionlint_style_gate_requires_prt_still_after_86,
        test_actionlint_rejects_float_main_lychee_after_86,
        test_actionlint_rejects_float_master_lychee_after_86,
        test_actionlint_rejects_float_latest_lychee_after_86,
        test_actionlint_rejects_unpinned_lychee_after_86,
        test_actionlint_rejects_float_main_markdownlint_after_86,
        test_actionlint_rejects_float_master_markdownlint_after_86,
        test_actionlint_rejects_float_latest_markdownlint_after_86,
        test_actionlint_rejects_unpinned_markdownlint_after_86,
        test_actionlint_rejects_missing_name_link_after_86,
        test_actionlint_rejects_missing_name_markdown_after_86,
        test_actionlint_rejects_missing_runs_on_link_after_86,
        test_actionlint_rejects_missing_runs_on_markdown_after_86,
        test_actionlint_rejects_missing_steps_link_after_86,
        test_actionlint_rejects_missing_steps_markdown_after_86,
        test_actionlint_rejects_id_token_write_link_still_after_86,
        test_actionlint_rejects_id_token_write_markdown_still_after_86,
        test_actionlint_rejects_write_all_link_still_after_86,
        test_actionlint_rejects_write_all_markdown_still_after_86,
        test_actionlint_rejects_contents_write_link_still_after_86,
        test_actionlint_rejects_contents_write_markdown_still_after_86,
        test_actionlint_rejects_prt_link_still_after_86,
        test_actionlint_rejects_prt_markdown_still_after_86,
        test_actionlint_rejects_float_main_checkout_link_after_86,
        test_actionlint_rejects_float_latest_checkout_markdown_after_86,
        test_actionlint_rejects_unpinned_checkout_stewardship_after_86,
        test_actionlint_rejects_missing_timeout_stewardship_still_after_86,
        test_actionlint_rejects_missing_timeout_link_still_after_86,
        test_actionlint_rejects_missing_timeout_markdown_still_after_86,
        test_actionlint_rejects_float_master_setup_python_after_86,
        test_actionlint_rejects_float_latest_setup_python_still_after_86,
        test_actionlint_rejects_unpinned_setup_python_still_after_86,

        # TOKENMAXX deepen after #90 (+72 relative-link third-pass)
        test_relative_gate_requires_md_link_exact_after_90,
        test_relative_gate_requires_atx_exact_after_90,
        test_relative_gate_requires_skip_parts_exact_after_90,
        test_relative_gate_requires_skip_files_exact_after_90,
        test_relative_gate_requires_skip_prefix_path_after_90,
        test_relative_gate_requires_ok_banner_after_90,
        test_relative_gate_requires_failed_banner_after_90,
        test_relative_gate_requires_empty_target_needle_after_90,
        test_relative_gate_requires_http_needle_after_90,
        test_relative_gate_requires_proto_needle_after_90,
        test_relative_gate_requires_dangerous_needle_after_90,
        test_relative_gate_requires_utf8_after_90,
        test_relative_gate_requires_as_posix_after_90,
        test_relative_gate_requires_md_suffix_after_90,
        test_relative_gate_requires_value_error_after_90,
        test_relative_gate_requires_sorted_after_90,
        test_relative_gate_requires_unicode_after_90,
        test_relative_gate_requires_space_dash_after_90,
        test_relative_gate_requires_percent_stable_after_90,
        test_relative_gate_requires_cap_nested_after_90,
        test_relative_gate_requires_empty_parens_pin_after_90,
        test_relative_gate_requires_sys_exit_after_90,
        test_relative_gate_requires_urllib_unquote_after_90,
        test_relative_gate_requires_group2_after_90,
        test_relative_gate_requires_startswith_hash_after_90,
        test_relative_gate_requires_split_hash_after_90,
        test_relative_gate_requires_files_scanned_after_90,
        test_relative_gate_requires_common_import_after_90,
        test_relative_gate_requires_title_attr_after_90,
        test_relative_gate_requires_atx_hash_range_after_90,
        test_relative_gate_requires_slug_punct_after_90,
        test_relative_gate_requires_offline_doc_after_90,
        test_relative_gate_requires_lychee_doc_after_90,
        test_relative_gate_requires_path_parent_after_90,
        test_relative_gate_requires_fully_unquote_call_after_90,
        test_relative_gate_requires_dangerous_call_after_90,
        test_relative_gate_requires_third_pass_doc_after_90,
        test_relative_gate_requires_def_main_after_90,
        test_relative_gate_requires_relative_to_root_after_90,
        test_relative_gate_requires_contract_fn_def_after_90,
        test_relative_links_reject_image_protocol_rel_after_90,
        test_relative_links_reject_image_empty_after_90,
        test_relative_links_reject_image_bare_hash_after_90,
        test_relative_links_reject_encoded_nul_after_90,
        test_relative_links_reject_http_uppercase_after_90,
        test_relative_links_accept_https_with_frag_after_90,
        test_relative_links_accept_mailto_with_subject_after_90,
        test_relative_links_reject_query_on_nested_after_90,
        test_relative_links_reject_empty_frag_nested_after_90,
        test_relative_links_accept_angle_https_title_after_90,
        test_relative_links_reject_js_uppercase_after_90,
        test_relative_links_github_slug_underscore_heading_after_90,
        test_relative_links_reject_dotdot_escape_still_after_90,
        test_relative_links_accept_self_dot_path_after_90,
        test_relative_links_reject_missing_image_nested_after_90,
        test_relative_gate_requires_md_link_re_still_after_90,
        test_relative_gate_requires_max_unquote_4_still_after_90,
        test_relative_gate_requires_should_skip_still_after_90,
        test_relative_gate_requires_iter_markdown_still_after_90,
        test_relative_gate_requires_headings_in_still_after_90,
        test_relative_gate_requires_check_file_still_after_90,
        test_relative_gate_requires_mailto_still_after_90,
        test_relative_gate_requires_tel_still_after_90,
        test_relative_gate_requires_nul_still_after_90,
        test_relative_gate_requires_angle_strip_still_after_90,
        test_relative_gate_requires_escapes_still_after_90,
        test_relative_gate_requires_broken_still_after_90,
        test_relative_gate_requires_missing_heading_still_after_90,
        test_relative_gate_requires_rglob_still_after_90,
        test_relative_gate_requires_no_md_fail_still_after_90,
        test_relative_links_reject_http_still_after_90,
        test_relative_links_reject_protocol_still_after_90,
        # TOKENMAXX deepen after #90/#94 (+72 wiki-outline third-pass)
        test_wiki_gate_requires_wiki_exact_after_90,
        test_wiki_gate_requires_removesuffix_after_90,
        test_wiki_gate_requires_glob_md_after_90,
        test_wiki_gate_requires_sorted_unexpected_after_90,
        test_wiki_gate_requires_link_check_accept_after_90,
        test_wiki_gate_requires_markdown_lint_accept_after_90,
        test_wiki_gate_requires_no_secrets_accept_after_90,
        test_wiki_gate_requires_invent_home_needle_after_90,
        test_wiki_gate_requires_secrets_home_needle_after_90,
        test_wiki_gate_requires_kill_home_needle_after_90,
        test_wiki_gate_requires_out_of_scope_section_after_90,
        test_wiki_gate_requires_relative_steward_needle_after_90,
        test_wiki_gate_requires_invent_steward_needle_after_90,
        test_wiki_gate_requires_actionlint_steward_needle_after_90,
        test_wiki_gate_requires_run_script_needle_after_90,
        test_wiki_gate_requires_startswith_slash_after_90,
        test_wiki_gate_requires_startswith_http_after_90,
        test_wiki_gate_requires_group2_after_90,
        test_wiki_gate_requires_utf8_after_90,
        test_wiki_gate_requires_sys_exit_after_90,
        test_wiki_gate_requires_common_import_after_90,
        test_wiki_gate_requires_downloads_special_after_90,
        test_wiki_gate_requires_discord_special_after_90,
        test_wiki_gate_requires_twitter_special_after_90,
        test_wiki_gate_requires_xcom_special_after_90,
        test_wiki_gate_requires_shields_io_after_90,
        test_wiki_gate_requires_md_badge_open_after_90,
        test_wiki_gate_requires_badge_in_lowered_after_90,
        test_wiki_gate_requires_readme_up_up_after_90,
        test_wiki_gate_requires_readme_up_after_90,
        test_wiki_gate_requires_badge_docs_path_after_90,
        test_wiki_gate_requires_badge_blob_after_90,
        test_wiki_gate_requires_pages_operator_ok_after_90,
        test_wiki_gate_requires_intentional_pin_after_90,
        test_wiki_gate_requires_topic_get_after_90,
        test_wiki_gate_requires_strip_fenced_call_after_90,
        test_wiki_gate_requires_dangerous_call_after_90,
        test_wiki_gate_requires_scan_path_after_90,
        test_wiki_gate_requires_scan_publish_after_90,
        test_wiki_gate_requires_third_pass_doc_after_90,
        test_wiki_gate_requires_contract_fn_def_after_90,
        test_wiki_rejects_missing_link_check_publish_after_90,
        test_wiki_rejects_missing_markdown_lint_publish_after_90,
        test_wiki_rejects_missing_home_invent_after_90,
        test_wiki_rejects_missing_home_secret_after_90,
        test_wiki_rejects_missing_home_kill_after_90,
        test_wiki_rejects_missing_steward_relative_after_90,
        test_wiki_rejects_missing_steward_actionlint_after_90,
        test_wiki_rejects_protocol_rel_after_90,
        test_wiki_rejects_http_link_after_90,
        test_wiki_rejects_javascript_link_after_90,
        test_wiki_rejects_discord_chrome_after_90,
        test_wiki_rejects_unexpected_page_after_90,
        test_wiki_accepts_angle_https_after_90,
        test_wiki_accepts_mailto_after_90,
        test_wiki_ignores_fenced_http_after_90,
        test_wiki_rejects_missing_run_script_steward_after_90,
        test_wiki_gate_requires_def_main_after_90,
        test_wiki_gate_requires_text_lower_after_90,
        test_wiki_gate_requires_present_set_after_90,
        test_wiki_gate_requires_wiki_is_dir_after_90,
        test_wiki_gate_requires_third_pass_after_90_pin,
        test_wiki_gate_requires_operator_only_still_after_90,
        test_wiki_gate_requires_reject_invent_helper_still_after_90,
        test_wiki_gate_requires_home_backlink_still_after_90,
        test_wiki_gate_requires_failed_banner_still_after_90,
        test_wiki_gate_requires_ok_match_still_after_90,
        test_wiki_gate_requires_publishable_pages_still_after_90,
        test_wiki_gate_requires_topic_hints_still_after_90,
        test_wiki_gate_requires_stars_still_after_90,
        test_wiki_gate_requires_len_pages_after_90,
        test_wiki_gate_requires_home_is_file_after_90,
        # TOKENMAXX deepen after #100 (+72 docs-lint)
        test_docs_lint_gate_requires_lycheeignore_const_after_100,
        test_docs_lint_gate_requires_markdownlint_const_after_100,
        test_docs_lint_gate_requires_lycheeignore_path_after_100,
        test_docs_lint_gate_requires_markdownlint_path_after_100,
        test_docs_lint_gate_requires_fn_lycheeignore_after_100,
        test_docs_lint_gate_requires_live_doc_pin_after_100,
        test_docs_lint_gate_requires_escaped_shields_pin_after_100,
        test_docs_lint_gate_requires_mcp_pin_after_100,
        test_docs_lint_gate_requires_lfs_pin_after_100,
        test_docs_lint_gate_requires_stewardship_note_after_100,
        test_docs_lint_gate_requires_https_star_reject_after_100,
        test_docs_lint_gate_requires_http_star_reject_after_100,
        test_docs_lint_gate_requires_md013_obj_after_100,
        test_docs_lint_gate_requires_md024_obj_after_100,
        test_docs_lint_gate_requires_md033_false_re_after_100,
        test_docs_lint_gate_requires_md041_false_re_after_100,
        test_docs_lint_gate_requires_md060_false_re_after_100,
        test_docs_lint_gate_requires_default_true_re_after_100,
        test_docs_lint_gate_requires_line_length_re_after_100,
        test_docs_lint_gate_requires_siblings_re_after_100,
        test_docs_lint_gate_requires_main_call_lychee_after_100,
        test_docs_lint_gate_requires_docs_slice_wording_after_100,
        test_docs_lint_gate_requires_host_pin_after_100,
        test_docs_lint_gate_requires_contract_spam_pin_after_100,
        test_lycheeignore_rejects_missing_escaped_shields_after_100,
        test_lycheeignore_rejects_missing_mcp_after_100,
        test_lycheeignore_rejects_missing_lfs_after_100,
        test_lycheeignore_rejects_missing_stewardship_note_after_100,
        test_lycheeignore_rejects_https_star_after_100,
        test_lycheeignore_rejects_http_star_after_100,
        test_lycheeignore_rejects_bare_star_after_100,
        test_lycheeignore_rejects_missing_shields_any_after_100,
        test_markdownlint_rejects_wrong_md013_obj_after_100,
        test_markdownlint_rejects_wrong_md024_obj_after_100,
        test_markdownlint_rejects_md033_true_after_100,
        test_markdownlint_rejects_md041_true_after_100,
        test_markdownlint_rejects_md060_true_after_100,
        test_markdownlint_rejects_default_false_after_100,
        test_markdownlint_rejects_missing_md013_after_100,
        test_markdownlint_rejects_missing_md024_after_100,
        test_docs_lint_gate_requires_lycheeignore_const_still_after_100,
        test_docs_lint_gate_requires_markdownlint_const_still_after_100,
        test_docs_lint_gate_requires_escaped_shields_still_after_100,
        test_docs_lint_gate_requires_mcp_still_after_100,
        test_docs_lint_gate_requires_lfs_still_after_100,
        test_docs_lint_gate_requires_md013_obj_still_after_100,
        test_docs_lint_gate_requires_md024_obj_still_after_100,
        test_docs_lint_gate_requires_fn_lycheeignore_still_after_100,
        test_docs_lint_gate_requires_live_doc_still_after_100,
        test_docs_lint_gate_requires_https_star_still_after_100,
        test_docs_lint_gate_requires_stewardship_note_still_after_100,
        test_docs_lint_gate_requires_default_true_still_after_100,
        test_docs_lint_gate_requires_docs_slice_still_after_100,
        test_lycheeignore_rejects_missing_mcp_still_after_100,
        test_lycheeignore_rejects_missing_lfs_still_after_100,
        test_lycheeignore_rejects_missing_escaped_still_after_100,
        test_markdownlint_rejects_line_length_120_still_after_100,
        test_markdownlint_rejects_siblings_false_still_after_100,
        test_markdownlint_rejects_missing_default_still_after_100,
        test_docs_lint_gate_requires_contract_fn_after_100,
        test_docs_lint_gate_requires_spam_wording_after_100,
        test_lycheeignore_rejects_missing_note_still_after_100,
        test_markdownlint_rejects_md033_true_still_after_100,
        test_markdownlint_rejects_md041_true_still_after_100,
        test_markdownlint_rejects_md060_true_still_after_100,
        test_docs_lint_gate_requires_http_star_still_after_100,
        test_docs_lint_gate_requires_md033_still_after_100,
        test_docs_lint_gate_requires_md041_still_after_100,
        test_docs_lint_gate_requires_md060_still_after_100,
        test_docs_lint_gate_requires_line_length_re_still_after_100,
        test_docs_lint_gate_requires_siblings_re_still_after_100,
        test_lycheeignore_accepts_full_live_fixture_after_100,
        # TOKENMAXX deepen after #100 (+36 docs-lint)
        test_docs_lint_gate_requires_wiki_spam_pin_after_100,
        test_docs_lint_gate_requires_308_redirect_pin_after_100,
        test_docs_lint_gate_requires_103_early_pin_after_100,
        test_docs_lint_gate_requires_lychee_assign_after_100,
        test_docs_lint_gate_requires_md_assign_after_100,
        test_docs_lint_gate_requires_workflows_fn_after_100,
        test_docs_lint_gate_requires_missing_lychee_needle_after_100,
        test_docs_lint_gate_requires_missing_md_needle_after_100,
        test_docs_lint_gate_requires_cdn_note_after_100,
        test_docs_lint_gate_requires_workflows_call_after_100,
        test_lycheeignore_rejects_missing_308_after_100,
        test_lycheeignore_rejects_missing_103_after_100,
        test_docs_lint_gate_requires_wiki_spam_still_after_100,
        test_docs_lint_gate_requires_308_still_after_100,
        test_docs_lint_gate_requires_103_still_after_100,
        test_docs_lint_gate_requires_lychee_assign_still_after_100,
        test_docs_lint_gate_requires_md_assign_still_after_100,
        test_docs_lint_gate_requires_workflows_fn_still_after_100,
        test_docs_lint_gate_requires_cdn_note_still_after_100,
        test_docs_lint_gate_requires_workflows_call_still_after_100,
        test_lycheeignore_rejects_missing_308_still_after_100,
        test_lycheeignore_rejects_missing_103_still_after_100,
        test_docs_lint_gate_requires_contract_call_still_after_100,
        test_docs_lint_gate_requires_docs_slice_deepen_after_100,
        test_markdownlint_rejects_wrong_md013_spacing_after_100,
        test_markdownlint_rejects_wrong_md024_spacing_after_100,
        test_docs_lint_gate_requires_host_pin_still_after_100,
        test_docs_lint_gate_requires_spam_pin_still_after_100,
        test_lycheeignore_rejects_https_star_with_full_fixture_after_100,
        test_lycheeignore_rejects_http_star_with_full_fixture_after_100,
        test_docs_lint_gate_requires_live_doc_deepen_after_100,
        test_docs_lint_gate_requires_escaped_shields_deepen_after_100,
        test_docs_lint_gate_requires_mcp_deepen_after_100,
        test_docs_lint_gate_requires_lfs_deepen_after_100,
        test_lycheeignore_accepts_live_tree_seed_after_100,
        test_markdownlint_accepts_exact_objects_after_100,
    test_badge_accepts_absolute_license_blob_after_104,
    test_badge_accepts_dot_slash_license_after_104,
    test_badge_gate_requires_abs_https_when_after_104,
    test_badge_gate_requires_abs_wf_url_after_104,
    test_badge_gate_requires_badge_gate_eq_after_104,
    test_badge_gate_requires_badge_line_exact_after_104,
    test_badge_gate_requires_blob_lower_after_104,
    test_badge_gate_requires_check_badges_call_after_104,
    test_badge_gate_requires_common_import_after_104,
    test_badge_gate_requires_contract_call_after_104,
    test_badge_gate_requires_contract_fn_still_after_104,
    test_badge_gate_requires_expected_repo_lower_after_104,
    test_badge_gate_requires_expected_repo_still_after_104,
    test_badge_gate_requires_extract_call_after_104,
    test_badge_gate_requires_fail_readme_after_104,
    test_badge_gate_requires_failed_banner_still_after_104,
    test_badge_gate_requires_found_lower_after_104,
    test_badge_gate_requires_future_annotations_after_104,
    test_badge_gate_requires_github_re_exact_after_104,
    test_badge_gate_requires_group_img_after_104,
    test_badge_gate_requires_group_label_after_104,
    test_badge_gate_requires_group_link_after_104,
    test_badge_gate_requires_h1_startswith_after_104,
    test_badge_gate_requires_http_link_reject_still_after_104,
    test_badge_gate_requires_https_start_still_after_104,
    test_badge_gate_requires_ignorecase_after_104,
    test_badge_gate_requires_img_https_needle_after_104,
    test_badge_gate_requires_lic_img_must_after_104,
    test_badge_gate_requires_lic_point_after_104,
    test_badge_gate_requires_license_actions_after_104,
    test_badge_gate_requires_link_img_must_after_104,
    test_badge_gate_requires_list_required_order_after_104,
    test_badge_gate_requires_main_fn_still_after_104,
    test_badge_gate_requires_max_badges_eq_after_104,
    test_badge_gate_requires_md_img_must_after_104,
    test_badge_gate_requires_named_img_after_104,
    test_badge_gate_requires_named_label_after_104,
    test_badge_gate_requires_named_link_after_104,
    test_badge_gate_requires_ok_banner_still_after_104,
    test_badge_gate_requires_only_allowed_after_104,
    test_badge_gate_requires_required_order_still_after_104,
    test_badge_gate_requires_return_empty_errors_after_104,
    test_badge_gate_requires_scripts_parent_after_104,
    test_badge_gate_requires_shields_re_exact_after_104,
    test_badge_gate_requires_stderr_after_104,
    test_badge_gate_requires_strict_row_after_104,
    test_badge_gate_requires_sys_exit_after_104,
    test_badge_gate_requires_third_pass_doc_after_104,
    test_badge_gate_requires_unexpected_label_after_104,
    test_badge_gate_requires_utf8_after_104,
    test_badge_gate_requires_workflows_exact_after_104,
    test_badge_gate_requires_workflows_path_after_104,
    test_badge_passes_good_fixture_after_104,
    test_badge_passes_live_contract_after_104,
    test_badge_rejects_codecov_hint_after_104,
    test_badge_rejects_downloads_hint_after_104,
    test_badge_rejects_four_badges_after_104,
    test_badge_rejects_http_badge_link_after_104,
    test_badge_rejects_http_image_after_104,
    test_badge_rejects_invent_still_after_104,
    test_badge_rejects_missing_h1_after_104,
    test_badge_rejects_noncontiguous_after_104,
    test_badge_rejects_secret_url_after_104,
    test_badge_rejects_secret_url_still_after_104,
    test_badge_rejects_stars_hint_after_104,
    test_badge_rejects_stewardship_product_after_104,
    test_badge_rejects_two_badges_after_104,
    test_badge_rejects_wrong_license_link_after_104,
    test_badge_rejects_wrong_link_check_image_after_104,
    test_badge_rejects_wrong_md_lint_image_after_104,
    test_badge_rejects_wrong_order_after_104,
    test_badge_rejects_wrong_repo_after_104,
    test_actionlint_third_gate_third_pass_lc_after_108,

    test_actionlint_third_gate_third_pass_lc_still_after_108,

    test_actionlint_third_gate_concurrency_if_after_108,

    test_actionlint_third_gate_concurrency_if_still_after_108,

    test_actionlint_third_gate_cancel_if_after_108,

    test_actionlint_third_gate_cancel_if_still_after_108,

    test_actionlint_third_gate_fail_actions_after_108,

    test_actionlint_third_gate_fail_actions_still_after_108,

    test_actionlint_third_gate_fail_packages_after_108,

    test_actionlint_third_gate_fail_packages_still_after_108,

    test_actionlint_third_gate_fail_prs_after_108,

    test_actionlint_third_gate_fail_prs_still_after_108,

    test_actionlint_third_gate_reversible_after_108,

    test_actionlint_third_gate_reversible_still_after_108,

    test_actionlint_third_gate_docker_after_108,

    test_actionlint_third_gate_docker_still_after_108,

    test_actionlint_third_gate_finditer_after_108,

    test_actionlint_third_gate_finditer_still_after_108,

    test_actionlint_third_gate_actions_re_after_108,

    test_actionlint_third_gate_actions_re_still_after_108,

    test_actionlint_third_gate_packages_re_after_108,

    test_actionlint_third_gate_packages_re_still_after_108,

    test_actionlint_third_gate_pr_re_after_108,

    test_actionlint_third_gate_pr_re_still_after_108,

    test_actionlint_third_gate_concurrency_mem_after_108,

    test_actionlint_third_gate_concurrency_mem_still_after_108,

    test_actionlint_third_gate_rsplit_after_108,

    test_actionlint_third_gate_rsplit_still_after_108,

    test_actionlint_third_rejects_concurrency_link_check_after_108,

    test_actionlint_third_rejects_cancel_link_check_after_108,

    test_actionlint_third_rejects_permissions_link_check_after_108,

    test_actionlint_third_rejects_concurrency_markdown_lint_after_108,

    test_actionlint_third_rejects_cancel_markdown_lint_after_108,

    test_actionlint_third_rejects_permissions_markdown_lint_after_108,

    test_actionlint_third_rejects_concurrency_stewardship_checks_after_108,

    test_actionlint_third_rejects_cancel_stewardship_checks_after_108,

    test_actionlint_third_rejects_permissions_stewardship_checks_after_108,

    test_actionlint_third_rejects_actions_link_check_after_108,

    test_actionlint_third_rejects_actions_markdown_lint_after_108,

    test_actionlint_third_rejects_actions_stewardship_checks_after_108,

    test_actionlint_third_rejects_packages_link_check_after_108,

    test_actionlint_third_rejects_packages_markdown_lint_after_108,

    test_actionlint_third_rejects_packages_stewardship_checks_after_108,

    test_actionlint_third_rejects_pull_requests_link_check_after_108,

    test_actionlint_third_rejects_pull_requests_markdown_lint_after_108,

    test_actionlint_third_rejects_pull_requests_stewardship_checks_after_108,

    test_actionlint_third_accepts_seed_after_108,

    test_actionlint_third_rejects_concurrency_link_check_pad0_after_108,

    test_actionlint_third_rejects_concurrency_markdown_lint_pad1_after_108,

    test_actionlint_third_rejects_concurrency_stewardship_checks_pad2_after_108,

    test_actionlint_third_rejects_concurrency_link_check_pad3_after_108,

    test_actionlint_third_rejects_concurrency_markdown_lint_pad4_after_108,

    test_actionlint_third_rejects_concurrency_stewardship_checks_pad5_after_108,

    test_actionlint_third_rejects_concurrency_link_check_pad6_after_108,

    test_actionlint_third_rejects_concurrency_markdown_lint_pad7_after_108,

    test_actionlint_third_rejects_concurrency_stewardship_checks_pad8_after_108,

    test_actionlint_third_rejects_concurrency_link_check_pad9_after_108,

    test_actionlint_third_rejects_concurrency_markdown_lint_pad10_after_108,

    test_actionlint_third_rejects_concurrency_stewardship_checks_pad11_after_108,

    test_actionlint_third_rejects_concurrency_link_check_pad12_after_108,

    test_actionlint_third_rejects_concurrency_markdown_lint_pad13_after_108,

    test_actionlint_third_rejects_concurrency_stewardship_checks_pad14_after_108,

    test_actionlint_third_rejects_concurrency_link_check_pad15_after_108,

    test_actionlint_third_rejects_concurrency_markdown_lint_pad16_after_108,

    test_actionlint_third_rejects_concurrency_stewardship_checks_pad17_after_108,

    test_actionlint_third_rejects_concurrency_link_check_pad18_after_108,

    test_actionlint_third_rejects_concurrency_markdown_lint_pad19_after_108,

    test_actionlint_third_rejects_concurrency_stewardship_checks_pad20_after_108,

    test_actionlint_third_rejects_concurrency_link_check_pad21_after_108,

    test_actionlint_third_rejects_concurrency_markdown_lint_pad22_after_108,

    test_actionlint_third_rejects_concurrency_stewardship_checks_pad23_after_108,

    test_actionlint_third_rejects_concurrency_link_check_pad24_after_108,
    test_run_stewardship_gate_requires_host_after_111,
    test_run_stewardship_gate_requires_spam_after_111,
    test_run_stewardship_gate_requires_run_const_after_111,
    test_run_stewardship_gate_requires_shebang_after_111,
    test_run_stewardship_gate_requires_euo_after_111,
    test_run_stewardship_gate_requires_dirname_after_111,
    test_run_stewardship_gate_requires_cd_root_after_111,
    test_run_stewardship_gate_requires_same_ci_after_111,
    test_run_stewardship_gate_requires_order_after_111,
    test_run_stewardship_gate_requires_py_badge_after_111,
    test_run_stewardship_gate_requires_py_wiki_after_111,
    test_run_stewardship_gate_requires_py_schema_after_111,
    test_run_stewardship_gate_requires_py_relative_after_111,
    test_run_stewardship_gate_requires_after_111_after_111,
    test_run_stewardship_gate_requires_host_still_after_111,
    test_run_stewardship_rejects_missing_shebang_after_111,
    test_run_stewardship_rejects_missing_euo_after_111,
    test_run_stewardship_rejects_missing_dirname_after_111,
    test_run_stewardship_rejects_missing_cd_root_after_111,
    test_run_stewardship_rejects_missing_same_ci_after_111,
    test_run_stewardship_rejects_missing_py_badge_after_111,
    test_run_stewardship_rejects_missing_py_wiki_after_111,
    test_run_stewardship_rejects_missing_py_schema_after_111,
    test_run_stewardship_rejects_missing_py_relative_after_111,
    test_run_stewardship_rejects_order_swap_after_111,
    test_run_stewardship_rejects_missing_badge_gate_after_111,
    test_run_stewardship_rejects_missing_wiki_gate_after_111,
    test_run_stewardship_rejects_missing_schema_gate_after_111,
    test_run_stewardship_rejects_missing_relative_gate_after_111,
    test_run_stewardship_gate_requires_shebang_still_after_111,
    test_run_stewardship_gate_requires_euo_still_after_111,
    test_run_stewardship_gate_requires_dirname_still_after_111,
    test_run_stewardship_gate_requires_cd_still_after_111,
    test_run_stewardship_gate_requires_same_ci_still_after_111,
    test_run_stewardship_gate_requires_order_still_after_111,
    test_run_stewardship_gate_requires_py_badge_still_after_111,
    test_run_stewardship_gate_requires_py_wiki_still_after_111,
    test_run_stewardship_gate_requires_py_schema_still_after_111,
    test_run_stewardship_gate_requires_py_relative_still_after_111,
    test_run_stewardship_gate_requires_run_const_still_after_111,
    test_run_stewardship_gate_requires_host_pin_still_after_111,
    test_run_stewardship_gate_requires_spam_still_after_111,
    test_run_stewardship_gate_requires_after_111_still_after_111,
    test_run_stewardship_rejects_shebang_bin_bash_after_111,
    test_run_stewardship_rejects_missing_pipefail_after_111,
    test_run_stewardship_rejects_python3_missing_prefix_badge_after_111,
    test_run_stewardship_rejects_python3_missing_prefix_wiki_after_111,
    test_run_stewardship_rejects_python3_missing_prefix_schema_after_111,
    test_run_stewardship_rejects_python3_missing_prefix_relative_after_111,
    test_run_stewardship_rejects_same_ci_removed_after_111,
    test_run_stewardship_rejects_order_wiki_first_after_111,
    test_run_stewardship_accepts_live_seed_after_111,
    test_run_stewardship_gate_contract_live_empty_after_111,
    test_run_stewardship_live_reject_0_after_111,
    test_run_stewardship_live_reject_1_after_111,
    test_run_stewardship_live_reject_2_after_111,
    test_run_stewardship_live_reject_3_after_111,
    test_run_stewardship_live_reject_4_after_111,
    test_run_stewardship_live_reject_5_after_111,
    test_run_stewardship_live_reject_6_after_111,
    test_run_stewardship_live_reject_7_after_111,
    test_run_stewardship_live_reject_8_after_111,
    test_run_stewardship_live_reject_9_after_111,
    test_run_stewardship_live_reject_10_after_111,
    test_run_stewardship_live_reject_11_after_111,
    test_run_stewardship_live_reject_12_after_111,
    test_run_stewardship_live_reject_13_after_111,
    test_run_stewardship_live_reject_14_after_111,
    test_run_stewardship_live_reject_15_after_111,
    test_run_stewardship_live_reject_16_after_111,
    test_run_stewardship_live_reject_17_after_111,
    test_run_stewardship_live_reject_18_after_111,
    test_run_stewardship_gate_requires_pwd_after_111,
    test_run_stewardship_gate_requires_run_all_after_111,
    test_run_stewardship_gate_requires_closed_96_after_111,
    test_run_stewardship_rejects_missing_pwd_after_111,
    test_run_stewardship_rejects_missing_run_all_after_111,
    test_run_stewardship_gate_requires_pwd_still_after_111,
    test_run_stewardship_gate_requires_run_all_still_after_111,
    test_run_stewardship_live_reject_pwd_pad0_after_111,
    test_run_stewardship_live_reject_pwd_pad1_after_111,
    test_run_stewardship_live_reject_pwd_pad2_after_111,
    test_run_stewardship_live_reject_run_all_pad0_after_111,
    test_run_stewardship_live_reject_run_all_pad1_after_111,
    test_run_stewardship_live_reject_run_all_pad2_after_111,
    test_run_stewardship_gate_requires_closed_96_still_after_111,
    test_run_stewardship_accepts_live_seed_still_after_111,
    test_run_stewardship_gate_contract_live_empty_still_after_111,
    ]
    try:
        for script in GATE_SCRIPTS:
            assert_pass_live(script)
        for test in tests:
            test()
    except AssertionError as exc:
        print(f"Stewardship self-test FAILED: {exc}", file=sys.stderr)
        return 1
    print(f"OK: stewardship gate self-tests passed ({len(tests)} cases + live tree)")
    return 0


if __name__ == "__main__":
    sys.exit(main())
